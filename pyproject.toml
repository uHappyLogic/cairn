[project]
name = "cairn-tooling"
version = "1.3.0"
description = "Development tooling for the cairn plugin (Antigravity transpilation)"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0.2",
]

[tool.uv]
package = false
