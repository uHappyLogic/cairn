<open-questions>
  <open-question id="Milestone resolution per question">
    <question>The shared answer-with-recommendation procedure resolves the current milestone as its own step 1 and takes only SHORT TITLE as input. When the inline sweep follows it once per question in one conversation, does it re-run that milestone lookup every time, or does the procedure gain an optional pre-resolved MILESTONE_DIR input that the sweep (and the single-question skill) may pass so the lookup runs once per run?</question>
  </open-question>
  <open-question id="Whole-set read form">
    <question>The sweep reads the question set once for context before the loop. Is that a direct read of open_questions.xml and requirements.md with the file-reading tool, sanctioned for reasoning only, or does it rely on the per-question locate prints alone and read no document whole, given that the goal forbids judging picks and the answer core&apos;s own locate call is the fresh read after each cascade?</question>
  </open-question>
  <open-question id="Skipped-question report source">
    <question>A question skipped because its lift call failed mid-run has lost its recommendation to an earlier answer&apos;s cascade, so the end-of-run walk cannot print it. Should the sweep list those skipped Short Titles alongside its terse status line as the still-skipped advisory, sourced from its own record of failed lift calls during the loop, or keep today&apos;s rule of enumerating nothing and let the next recommendation pass surface them?</question>
  </open-question>
  <open-question id="Mid-question failure handling">
    <question>Today a FAILED agent return leaves the tree untouched and stops the loop. Inline, a remove call can fail after the decision was already folded into requirements.md, leaving the harmless superset uncommitted. On such a failure, does the sweep stop the whole run at once with the tool&apos;s Error line and the fold left in the working tree, roll the fold back before stopping, or skip that question and continue to the next?</question>
    <alternative id="Stop in place">
      On any tool Error line after the fold, the sweep stops the whole run straight away. It reports the question&apos;s Short Title with the Error line quoted verbatim and leaves this question&apos;s uncommitted edits in the working tree as they are. Every earlier answer is already committed, so the fold-before-remove superset (decision folded, block still standing) is the only thing left dirty.
      <advantage>This handles the failure the same way the answer core&apos;s fold-before-remove invariant was designed for: an interruption leaves the harmless superset, and nothing is ever lost. It needs no rollback machinery and never writes open_questions.xml outside the tool. It also keeps the agent-era report-and-stop semantics and the complete-task precedent of leaving a resumable tree rather than an untouched one. The failure should be rare anyway, because the sweep passes the lifted option id verbatim and so cannot hit the &quot;names none of the alternatives&quot; refusal.</advantage>
      <drawback>The run no longer promises what today&apos;s FAILED return does, a tree left untouched. The sweep has no clean-tree precondition and the stranded block still carries its recommendation, so a plain re-run walks that block again and folds the same decision a second time. To avoid the duplicate, the user has to discard or finish the fold by hand first. If the failing call was a cascade remove after the answered block&apos;s remove succeeded, the left-over state is a recorded answer plus a partial cascade, not the pure superset.</drawback>
    </alternative>
    <alternative id="Roll back, then stop">
      The sweep undoes this question&apos;s uncommitted edits back to the state they had before the question started, then stops the run with the Short Title and the verbatim Error line. Every earlier answer stays committed.
      <advantage>It keeps today&apos;s contract that a failed question leaves nothing behind. A re-run after the cause is fixed is clean and can&apos;t double-fold, because the tree matches the last per-answer commit for the two milestone files.</advantage>
      <drawback>The rollback can&apos;t be a plain restore to HEAD. The run has no clean-tree precondition, so that would discard any edits the user already had uncommitted in requirements.md. It needs a snapshot of the files taken before each question, like capture restoring its own snapshot rather than HEAD. It also runs into the rule that the tool is the only writer of open_questions.xml. A failed remove leaves that document byte-for-byte unchanged, but when a cascade remove fails after the answered block&apos;s remove succeeded, the tool has already written the document. Restoring it from a snapshot would then be exactly the kind of hand edit the invariants forbid, so this option only works cleanly when the answered block&apos;s own remove is the call that failed.</drawback>
    </alternative>
    <alternative id="Roll back, skip, continue">
      The sweep undoes this question&apos;s uncommitted edits the same way, records the Short Title as failed, and moves on to the next question in the walk order. It reports the failed questions with their Error lines once, next to the status line at the end of the run.
      <advantage>It gets the most out of an unattended run: a problem with one question doesn&apos;t cost the rest of the set. This matches how the sweep already treats a failed lift call, which is a skip rather than a stop.</advantage>
      <drawback>It has every rollback cost of stopping after a rollback: a snapshot per question, and no safe undo once a tool write has already cascaded. The rollback also can&apos;t be dropped. If this question&apos;s fold stayed behind, the commit procedure (which stages both files by path) would sweep it into the next question&apos;s commit under the wrong Recommendation-answer subject. That breaks one-commit-per-answer, the capture provenance, and revert-then-re-answer. Carrying on also means later questions whose depends-on tags name the failed question are recorded while that question stays open. The run needs an end-of-run failure report too; how that fits with the still-skipped advisory depends on how the skipped-question report is settled.</drawback>
    </alternative>
  </open-question>
  <open-question id="Retired-agent rationale home">
    <question>CLAUDE.md records retired patterns that a later editor must not restore, such as the per-question recommendation commit and the sequential recommend-agent sweep. When the mutation-in-agent inversion invariant is deleted, should the rewritten answer-sweep invariant carry a sentence that the per-question recording agent is retired and must not be restored, with its reason, or is the deletion left unrecorded in the invariants?</question>
    <alternative id="Retirement sentence with reason">
      The rewritten answer-sweep invariant gains one sentence saying the per-question recording agent is retired and must not be restored, with its reason: the pick is already computed, so the recording needs no isolation, and each dispatch re-read CLAUDE.md, the answer procedures and the growing requirements.md, which cost about 100 seconds per answer across milestone 30&apos;s 13 answers.
      <advantage>It follows the pattern CLAUDE.md already uses for retired designs, such as &quot;The retired per-question recommendation commit, the sequential recommend-agent sweep ... must not be restored&quot; and &quot;Milestone 10 reversed the earlier agent-commits inversion&quot;. Keeping the reason next to the ban lets a later editor weigh the ban instead of guessing at it. That reason matters because CLAUDE.md still describes the complete-task skill-and-agent pair and the &quot;inline or isolated&quot; claim, and both make an agent for the sweep look like the natural shape to restore.</advantage>
      <drawback>It adds words to a file of about 2,950 words that every dispatched agent and inline run reads, and that file already carries several retirement clauses. The cost figures are tied to this milestone&apos;s measurement and can go out of date as models and file sizes change.</drawback>
    </alternative>
    <alternative id="Bare retirement clause">
      The rewritten answer-sweep invariant carries a short clause, with no reason given, saying the sweep dispatches no agent and the per-question recording agent is retired. This matches the terse style of &quot;The old role-based commit split is retired.&quot;
      <advantage>A later editor still gets the do-not-restore signal, for only a handful of words in the file every run reads. It also avoids tying CLAUDE.md to cost figures that can go out of date.</advantage>
      <drawback>CLAUDE.md&apos;s own rule is that the rationale is what lives in CLAUDE.md, and this option drops it. An editor who meets a bare ban cannot tell whether it still holds when conditions change, for example cheaper dispatch or a need for isolation. The reason survives only in the git history and the milestone record.</drawback>
    </alternative>
    <alternative id="Deletion left unrecorded">
      The mutation-in-agent inversion invariant is deleted and the answer-sweep invariant is rewritten to describe only the inline shape, with no mention of the retired agent. The history stays in CHANGELOG.md, milestones/README.md and the milestone&apos;s commits.
      <advantage>CLAUDE.md stays the smallest possible description of the current design. The inline shape the invariant describes (&quot;dispatches no agent&quot;, recording inline per answer) already rules out the agent, much as the recommendation pass&apos;s inline description does, so a second statement of the ban would be redundant.</advantage>
      <drawback>It breaks the file&apos;s established practice of naming retired designs so they are not &quot;fixed&quot; back. That practice exists because the per-question recommendation agent was a design worth restoring until a retirement note said otherwise. Nothing in the invariants would then stop a later editor, reading the surviving complete-task pair and design claim 19, from adding an agent for the sweep again.</drawback>
    </alternative>
  </open-question>
  <open-question id="Headless chain sweep model">
    <question>The four headless chain lines run the answer sweep on opus at effort high or xhigh, chosen when the reasoning happened inside dispatched agents. With the sweep now one inline run over the whole set like the recommend line, which runs on fable at effort high, do those lines keep opus and their current effort, or move to the same model and effort as the other inline reasoning line?</question>
    <alternative id="Keep opus at current effort">
      Leave all four answer-sweep lines as they are: `--model &quot;opus&quot;` at `--effort high` in the mixed-agent review and executing-tasks chains, and `--effort xhigh` on both lines of the stuck-milestone chain. The milestone changes only the skill behind those lines.
      <advantage>The goal says the sweep records every surviving pick as given and never judges picks, so what it does is still recording, not the whole-set judgment that milestone 30 gave fable for. Keeping opus also leaves `docs/ways-of-using-cairn.md` and its settings sentences unchanged, keeps every line at values that have actually been run, and needs no new run to confirm them first, which milestone 30 required before new values go on the page.</advantage>
      <drawback>The question says the current values were picked when the reasoning ran inside a fresh agent for each question. They were never re-examined for one long inline run that holds the whole question set and the growing `requirements.md` in one context. The page would also keep the stuck-milestone chain&apos;s xhigh on a step that the goal now describes as mechanical, so the extra effort buys nothing for it to judge.</drawback>
    </alternative>
    <alternative id="Match recommend line fable high">
      Change all four answer-sweep lines to `--model &quot;fable&quot; --effort high`, the same settings as `/cairn:recommend-all-open-questions`, the other skill that reasons inline over the whole question set in the main conversation. Rewrite each affected way&apos;s settings sentence to say so.
      <advantage>The two inline whole-set runs would use the same settings. The model that holds the whole set, the growing `requirements.md`, and a fresh `locate` after every cascade gets the most capacity to keep them straight. It also gets the best chance of noticing the contradictions the goal asks the sweep to report beside its status line.</advantage>
      <drawback>Milestone 30 gave fable to the recommend line because that is the one whole-set judgment made once per run. The sweep is barred from judging picks, so this spends the strongest model on the work that most resembles recording, once per answer across a loop that can run to a dozen or more questions. The settings have also never run on this line. Under milestone 30&apos;s rule they must be confirmed by a run on this repository before the page records them.</drawback>
    </alternative>
    <alternative id="Opus at uniform high">
      Keep `--model &quot;opus&quot;` on all four lines, but set `--effort high` on each one. This lowers the stuck-milestone chain&apos;s two `xhigh` answer lines and rewrites that way&apos;s settings sentence so it no longer calls out a separate effort for the answer lines.
      <advantage>Opus high is already verified on two of the lines. The stuck-milestone xhigh existed to add intelligence to reasoning inside dispatched agents. That reasoning is now just recording what was picked, so one opus high setting across the sweep costs less and makes the page simpler without moving to an untested model.</advantage>
      <drawback>It weakens the one chain whose stated purpose is putting more intelligence into a milestone that has not converged. Any benefit xhigh gave in noticing contradictions or folding decisions cleanly into a long `requirements.md` is given up without a run showing that nothing is lost. The stuck-milestone line at high has also not been run in that chain, so milestone 30&apos;s run-before-record rule still applies there.</drawback>
    </alternative>
  </open-question>
  <open-question id="Sweep new-question advisory">
    <question>The single-question skill reports, beside its terse line, any new open questions the recorded decision may have introduced, never adding them itself. The sweep today reports none. Now that the sweep records inline with the whole set in context, should it carry that same new-question advisory once for the run alongside the contradiction advisory, or remain limited to the contradiction advisory the goal names?</question>
    <alternative id="Contradiction advisory only">
      The sweep keeps to what the goal names. It prints its terse status line, the contradiction advisory when it noticed one, and the still-skipped advisory. It reports no new open questions a recorded decision may have raised, and leaves those for the next `/review-milestone-requirements` pass.
      <advantage>It matches the goal&apos;s wording and today&apos;s sweep report exactly, so no new reporting rule is added. It also fits how the sweep is actually run: all four headless chain lines in docs/ways-of-using-cairn.md run it unattended, and in the review loop the next line is a review pass, which is the sole author of question blocks and surfaces new gaps anyway.</advantage>
      <drawback>The sweep and the single-question skill run the same answer core but report differently. The core&apos;s analyse step already notices sub-questions it cannot settle and is told to &quot;surface the rest rather than guessing&quot;. In the sweep, those observations are dropped. In the &quot;Executing tasks&quot; chain the next line is derive-tasks rather than a review pass, so a gap a decision opened is never mentioned before the task list is written.</drawback>
    </alternative>
    <alternative id="Run-level new-question advisory">
      The sweep collects the new open questions each recorded decision may have introduced as it loops. After the terse status line it prints them once for the whole run, next to the contradiction advisory. It never adds any of them to open_questions.xml, just as the single-question skill surfaces them without adding them.
      <advantage>The same answer core then reports the same way whichever runner records it, so the sweep matches answer-open-question, answer-open-question-with-alternative, and answer-open-question-with-recommendation. It also puts to use the analysis the answer core&apos;s analyse step already does in context. Before, that analysis was lost inside the dispatched agent.</advantage>
      <drawback>It adds a second decision-critical advisory to a run the goal describes with one advisory, and it relaxes the sweep&apos;s current &quot;enumerate nothing&quot; reporting rule. The reporting step, the CLAUDE.md terse-reporting invariant, and the skill reference would all have to name it. In a long unattended headless run, few people will read the listed items, and they overlap with what the next review pass surfaces.</drawback>
    </alternative>
    <alternative id="Fixed review-pointer line">
      When at least one recorded decision seemed to raise a new open question, the sweep prints one fixed line with no identifiers pointing at `/review-milestone-requirements`. It does not list the questions, so the review pass stays their only author and enumerator.
      <advantage>It keeps the useful signal, that a gap may have opened, without the sweep writing its own list. That keeps enumeration inside the review pass, the one runner that can reconcile a gap against recorded decisions and add a block. It also keeps the console output as terse as the one-line-per-run rule asks.</advantage>
      <drawback>It is a third reporting form, matching neither the single-question skill (which lists questions) nor the goal&apos;s contradiction-only report. The reader learns only that something may be open, not what, and has to run a review to find out. In chains where a review pass already follows the sweep, the line adds nothing.</drawback>
    </alternative>
  </open-question>
</open-questions>
