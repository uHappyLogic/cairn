# TASKS TODO

## Sync README.md To Answer-With-Recommendation Machinery

Update the repository's public `README.md` (Cairn's authoritative, adoption-focused workflow documentation) to reflect every new and changed answer/recommend artifact this milestone ships, across its two answer/recommend-documenting surfaces: the **Skill reference** section (per-skill descriptions) and the **per-phase Mermaid workflow diagrams** (notably the *Iterating milestone requirements* phase) plus the surrounding flow prose. This is the `README.md` half of the "Documentation surfaces to sync" scope in `requirements.md`; the `CLAUDE.md` half is the separate "Sync CLAUDE.md To Answer-With-Recommendation Machinery" task and must not be touched here.

**Notes:**
- Ground every edit in `requirements.md`'s Goal + Decisions — those are authoritative. The relevant Decisions to encode are **Standalone skill commit** (single-question skill commits inline under `Recommendation-answer:`), **Sweep commit ownership** + **Sweep cascade ordering** (the batch orchestrator dispatches the file-editing agent strictly sequentially, one commit per answer, gather-once-most-significant-first + per-dispatch re-read/skip), **Recommendation-answer commit subject** (`Recommendation-answer: <Short Title>`, matching neither `^Manual-answer:` nor `Principle-based-answer:`), and **answer-open-question redirect after extraction** (the retained redirect guard). Keep README's public, adoption-focused voice — distill, don't paste CLAUDE.md's terse invariant phrasing.
- **Skill reference — add entries** for: the new single-question skill `answer-open-question-with-recommendation` (lifts a question's embedded `> **Recommendation:**` anchor and records+commits it inline under `Recommendation-answer:`) and the new batch orchestrator `answer-all-open-questions-with-recommendation` (sweeps every open/deferred question carrying an embedded recommendation and dispatches the file-editing agent strictly sequentially, one commit per answer). Mention the file-editing agent twin at README's established level of detail — it already gives sibling subagents (`try-answer-question-by-principle`, `recommend-open-question`) their own `### … (subagent)` entries, so an analogous entry for the file-editing `answer-open-question-with-recommendation` agent fits (note the divergence: it *mutates* `requirements.md` and *commits its own* answer, unlike the read-only subagents).
- **Skill reference — narrow the existing `answer-open-question` entry** (~lines 244–248) to literal-only: remove the second paragraph describing **record-recommendation mode** (the sentinel lift of the embedded `> **Recommendation:**` line), and note instead the retained **redirect guard** that recognizes the old `record the recommendation` sentinel and cleanly stops, pointing the user at `/answer-open-question-with-recommendation`. Leave the first paragraph's literal-answer + `Manual-answer: <Short Title>` commit prose intact — that path is unchanged.
- **Skill reference — repoint the recommend-sweep consumer prose** in the `recommend-all-open-questions` entry (~line 268): its closing "the eventual … consumes one" clause currently names `/answer-open-question`'s `record the recommendation` mode and a `Manual-answer:` commit — repoint it to name the new consumer (`/answer-open-question-with-recommendation` and/or the `/answer-all-open-questions-with-recommendation` sweep) and correct the commit subject to `Recommendation-answer:` in the **same** clause, or it self-contradicts.
- **Skill reference — also fix the `recommend-open-question` (subagent) entry** (~line 272): the anchor-line rationale "its `> **Recommendation:** …` anchor is what `answer-open-question` later lifts" is live README prose naming the wrong consumer. This site is **not** covered by the "Repoint Recommend-Sweep Consumer Pointers" task (that one is scoped to the skill/agent source files only), so it must be repointed here to the new skill/sweep — otherwise a stale consumer pointer survives in README.
- **Flow prose + Mermaid diagram — *Iterating milestone requirements* phase** (~lines 104–137). Update the flow-prose sentence (~line 104, "which `/answer-open-question` can then record directly via its `record the recommendation` mode") to name the new consumer. In the diagram, the edge `ITM5 -.->|record the recommendation| ITM4` is stale on **both** ends: retarget it away from `ITM4` (`/answer-open-question`) to a new node for the recommendation-answer consumer (`/answer-open-question-with-recommendation` single, and/or the `/answer-all-open-questions-with-recommendation` batch sweep), **and** change the edge *label* — the text `record the recommendation` is itself the retired sentinel, so a repoint that keeps that label still presents the dead mode. Keep `ITM4` (`/answer-open-question`) in the diagram: literal answers still flow through it to `D2`, so only the recommendation-consumer edge repoints. Preserve the diagram's existing style, node-naming/`classDef` conventions, `optional` dashed styling for sweeps, and the `D1`/`D2` state-seam nodes.
- Exact node IDs, label wording, and topology are completion-time detail — decide them fresh against the live diagram; do not treat this as an open strategic choice. Match sibling-phase diagrams for consistency.

**Success:**
- The `## Skill reference` section lists the new single-question skill `answer-open-question-with-recommendation` and the new batch orchestrator `answer-all-open-questions-with-recommendation`, and mentions the file-editing agent twin at README's established level of detail.
- The `answer-open-question` skill-reference entry is narrowed to literal-only: no live description of record-recommendation mode as a current feature, and it notes the retained redirect guard pointing at `/answer-open-question-with-recommendation`; its literal-answer + `Manual-answer:` commit prose is intact.
- No live README prose or Mermaid edge (including the flow prose ~line 104, the `recommend-all-open-questions` entry ~line 268, and the `recommend-open-question` subagent entry ~line 272) presents `answer-open-question`'s `record the recommendation` mode as the recommendation consumer — the recommend sweep's consumer prose now names `/answer-open-question-with-recommendation` and/or `/answer-all-open-questions-with-recommendation`, with the `Recommendation-answer:` commit subject where a commit is named.
- In the *Iterating milestone requirements* Mermaid diagram, the former `record the recommendation` edge is repointed off `ITM4` to the new recommendation-answer consumer and its stale-sentinel label text is replaced; `ITM4` (`/answer-open-question`) remains for literal answers.
- The Mermaid diagram still renders (valid `flowchart TD` syntax) and stays consistent with the sibling per-phase diagrams' style, `classDef`s, dashed-optional convention, and `D1`/`D2` seam nodes.
- `CLAUDE.md` and all skill/agent/shared-procedure files are unmodified — this is a `README.md`-only edit.

---
