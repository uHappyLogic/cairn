# Milestone 24: Repo Hygiene Surface

## Goal

Give the cairn repository the hygiene surface a first-time visitor scans for. Add a GitHub Actions workflow that runs `uv run scripts/build_hosts.py --check` on every push and pull request, with a CI badge beside the release badge; a root `CONTRIBUTING.md` (edit `core/`, rebuild, invariants live in `CLAUDE.md`), `SECURITY.md`, `CODE_OF_CONDUCT.md`, bug and feature issue templates, and a pull-request template; a `CONTRIBUTING.md` template in each host definition that renders into the host tree and points contributors at the root repository; an in-repo `CHANGELOG.md` backfilled from every existing GitHub Release and thereafter maintained by `/release-plugin`, which prepends each release's composed notes and stages the file in the `Release: <VERSION>` commit so the changelog, the monorepo release, and the distribution releases carry identical notes; and Discussions enabled on the root repository. `FUNDING.yml` and release cadence are out of scope.

## Relevant starting state

### GitHub repository surface

`.github/` exists but holds only `assets/readme/cairn-banner.png` — no `workflows/`, no `ISSUE_TEMPLATE/`, no `PULL_REQUEST_TEMPLATE.md`. GitHub's community-profile API for `uHappyLogic/cairn` scores the repository 42%: `license` and `readme` present; `code_of_conduct`, `contributing`, `issue_template`, and `pull_request_template` missing. Repository settings: issues enabled, wiki enabled (unused), Discussions disabled; both distribution repositories (`uHappyLogic/cairn-claude`, `uHappyLogic/cairn-antigravity`) have issues disabled by design. `README.md` opens with the banner and a centered `<p>` holding exactly two shields.io badges — the release badge (`github/v/release/uHappyLogic/cairn`, tag display) and a static MIT license badge — and has no Contributing, Security, or Changelog section.

### Continuous integration

No CI exists. The repository's only automated check is `uv run scripts/build_hosts.py --check` — the drift gate that renders every host from `core/`, runs the full validation set, compares byte-for-byte against the committed `hosts/<host>/` trees, writes nothing, and exits non-zero listing differing paths — and today it runs only by hand or as pre-flight gate 2e of `/release-plugin`. Tooling is `uv` with Python `>=3.11` (`.python-version` pins `3.13`), one dependency (`pyyaml`) pinned in `uv.lock`, and `pyproject.toml` marked `package = false`; the drift gate also asserts the root `.claude-plugin/marketplace.json` entry equals `VERSION`.

### Host definitions and template rendering

Each host is a definition directory `scripts/hosts/<host>/` whose every file other than `settings.toml` is a template rendered to the same relative path in `hosts/<host>/`, with `{{VERSION}}` filled from the root `VERSION` and `{{NAME}}` from `plugin_name` (`scripts/build_hosts.py`, `render_host`). Claude's templates are `README.md`, `.claude-plugin/plugin.json`, and `.claude-plugin/marketplace.json`; Antigravity's are `README.md` and `plugin.json`. `LICENSE` is not a template — the build copies the root file by a dedicated code path — so a new text template with no slots would render unchanged and pass the gate's `unfilled-placeholder` check. Both distribution `README.md` templates already carry a "Generated — do not edit" paragraph directing issues and pull requests to `uHappyLogic/cairn`, and a `## Source` section linking the release tag and noting that the release page carries the notes.

### Release skill and release notes

`.claude/skills/release-plugin/SKILL.md` is the maintainer-only release procedure. Step 5 composes `<RELEASE_BODY>` in context and mutates nothing: one `## <Title> (milestone <N>)` section per `Milestone-finish:` commit in `<LAST_TAG>..HEAD` (highest number first, bullets condensed from that milestone's `milestones/README.md` history entry), closed by a `**Full Changelog**: …/compare/<LAST_TAG>...<VERSION>` line; an empty range instead builds a `## Changes since <LAST_TAG>` section behind a maintainer confirmation. Step 6 runs `set_version.py`, rebuilds, stages exactly `VERSION`, `pyproject.toml`, `uv.lock`, `.claude-plugin/marketplace.json`, and `hosts/` path-scoped, records the one `Release: <VERSION>` commit, and verifies `git status --porcelain --untracked-files=no` is empty; step 6 is skipped whole on a resumption (HEAD subject already `Release: <VERSION>`). The skill (step 6b) and `CLAUDE.md`'s Development section both state that the release commit "changes version slots and nothing else, by construction", resting on pre-flight 2a (clean tracked tree) and 2e (drift gate). The notes exist only in context until step 8c writes them to a temp file for `gh release create --notes-file`; each distribution repository's release carries the identical body, and its `Release:` commit body links to the monorepo release page for notes. Step 7 shows `<VERSION>` and the full `<RELEASE_BODY>` as the run's single pause, after the commit.

### Existing release history

Sixteen GitHub releases exist, tagged bare `0.9.0` through `1.4.0`, published 2026-06-12 through 2026-09-15, each titled by its tag. The eight bodies `0.9.0`–`0.9.7` carry only the `**Full Changelog**` compare link (`0.9.0`'s points at `/commits/0.9.0`); the eight from `0.9.8` on carry milestone sections in the step-5 shape (`0.9.9` has two). Publish dates are available via `gh release list --json tagName,publishedAt`. No `CHANGELOG.md` exists anywhere in the repository.

### Contribution-relevant documentation

`README.md`'s `## Development` section already documents the core-once-rebuild model, the three `build_hosts.py` invocations, the `VERSION`/`set_version.py` rule, the distribution repositories, and the directory-marketplace developer install; `## Self-dogfooding` notes that `milestones/` holds the project's own live workflow artifacts. The design invariants a contributor must preserve live in `CLAUDE.md` (~100 KB, also reachable as the `AGENTS.md` symlink) under "Invariants to preserve when editing skills". Commits follow function-derived `<Marker>: <descriptor>` subjects. `LICENSE` is MIT; no security-contact or conduct text exists in any file.

## Decisions

### Contributor workflow

An outside contribution is proposal-first, then a contributor-run milestone on a maintainer-reserved slot. A substantive change is proposed as an issue or Discussion; acceptance is the maintainer running `/define-milestone-goal` on `main` with the accepted proposal — only while their own current-milestone pointer is `none`, and never activating it there — which reserves the milestone number. The contributor branches from that define commit, runs `/goto-next-milestone` in the fork as the first step, and runs the full requirements-and-task pipeline through `/finish-current-milestone` there, so the branch's net change to the pointer line is nil and `main`'s pointer stays the maintainer's to hold; an abandoned contribution leaves only its defined directory on `main`, reverted or left as a `goto-next-milestone` candidate; the pull request is merged with a merge commit — squash merges are disabled on the repository — so the milestone commits survive for release notes and for `/capture-milestone-principle-updates`, which the maintainer runs after the merge. There is no plain-pull-request tier: small fixes are filed as issues for the maintainer to make.

### Continuous integration

The drift-gate workflow triggers on `push` and `pull_request` with no branch filter and no `paths` filter, so the gate runs on every commit reaching any branch of the root repository, on every pull request, and on each release tag push. Filtering is deliberately not done: the gate finishes in 0.2 seconds inside a half-minute free job, so filtering buys nothing measurable, while a `paths` list would be a hand-maintained second copy of the build's input set — the very drift the gate exists to catch — and a path-skipped run would leave a pull request with no status. Every push and pull request is the goal's literal wording, and the two-event trigger has nothing to curate.

### Code of conduct

`CODE_OF_CONDUCT.md` is Contributor Covenant 2.1 verbatim, with its single `[INSERT CONTACT METHOD]` slot filled with the git author address every existing commit already carries, `kosiak.lukasz@gmail.com`. 2.1 is the current 2.x text and the one GitHub's content detection labels Contributor Covenant on the Community Standards page (a 3.0 file resolves to Other), and it has one slot to fill instead of 3.0's two authored notes. The contact is an email because GitHub offers no private conduct channel — issues and Discussions are public, and private vulnerability reporting is for security — and the author address is the only one that verifiably exists and is read, so naming it exposes a rendered file to crawler indexing but publishes no new information. A dedicated conduct address would be chosen only if a monitored mailbox on a maintainer-controlled domain already existed; none does, and creating one for a repository with no community yet would be an inbox nobody checks.

### Security policy

`SECURITY.md` directs vulnerability reports to GitHub private vulnerability reporting only — the root repository's Report a vulnerability form — and the milestone enables private vulnerability reporting on `uHappyLogic/cairn` with one `gh api --method PUT` toggle of the same kind as the Discussions enable. Private reporting puts each report where the fix happens, as a draft advisory with a private thread and a publish step, instead of in a gmail inbox. No email address appears in the file: the maintainer publishes none today, and a fallback address is a one-line addition should one ever be wanted, so nothing is lost by leaving it out. The supported-versions table names the latest release only and states that every fix ships as a new release to both distribution repositories — the one honest value, because the sixteen releases are strictly linear, the release skill publishes only `main`'s HEAD, and both patch releases (1.0.1, 1.1.1) landed on the newest minor.

### Issue templates

The bug and feature issue templates are YAML issue forms, not Markdown templates. Host, cairn version, and skill invoked are the three facts a cairn bug cannot be reproduced without and exactly what free text loses, and required form fields are the only mechanism that guarantees them on a repository with no issues yet and no triage history to fall back on. The host is a two-value dropdown mirroring `scripts/hosts/`, and the version and skill are free-text inputs, so no field enumerates the 21 skills a list would have to track. The one cost is the `gh` CLI gap — it does not detect YAML forms — which a reporter crosses with `gh issue create --web`; a Markdown template would only paper over that gap, since `gh` pre-fills sections it does not enforce, so Markdown would be chosen only if terminal-filed issues were expected to be the main channel.

### Changelog

`CHANGELOG.md` puts a plain version-date heading over each release's body verbatim: a title, a one-line note that each entry is that release's notes verbatim, newest first, and per release a heading of the form `## <VERSION> — <YYYY-MM-DD>` (the em-dash style of the `milestones/README.md` history headings) directly over the release body, whose milestone sections sit at `###` so they nest under it. There is no Unreleased section and no reference-link list; the body's `**Full Changelog**` line is the compare link. The goal fixes the entry body as the release's composed notes, identical across the changelog and the three release pages, which rules out the categorised body that is Keep a Changelog's substance and leaves only its skeleton to borrow — and that skeleton costs more than it reads: an Unreleased section nothing fills between releases, a reference-link list duplicating the body's own Full Changelog line, and three edits per release in place of one prepend. The plain heading keeps the release write to one prepend below the title with no other slot in the file, and the backfill to a loop over `gh release view --json body` with the `publishedAt` date as each heading's date. The one shape change is that `/release-plugin` step 5 composes the milestone sections at `###` from this release on, and the eight existing sectioned bodies (0.9.8 through 1.4.0) are demoted once in the backfill, so the entry stays byte-identical to the body — which is also what a later reread from the file needs should the release body ever be read back from `CHANGELOG.md`.

The backfill carries the eight link-only releases, 0.9.0 through 0.9.7, verbatim: each entry is the plain version-date heading over the body exactly as `gh release view` returns it, however thin — a lone `**Full Changelog**` compare link, or nothing at all where the published body is empty — with no notes reconstructed from the milestone history or commit ranges and no release page edited. The goal names the existing releases as the backfill's source and requires the changelog and the release pages to carry identical notes, and under the plain heading over the verbatim body that is a single `gh release view` loop that holds for all sixteen entries with no exception. Reconstructing would turn eight entries into a September rewrite a reader cannot distinguish from notes that shipped, and the only way to reconstruct without breaking identity would be to rewrite eight published release pages — the one thing that would change the choice, taken only if the maintainer wants those pages rewritten. The compare link still gives each early entry its commit range, and `milestones/README.md` already holds the condensed per-milestone story.

Once the `Release: <VERSION>` commit exists, the release body is the committed changelog entry: the entry for `<VERSION>` extracted from `HEAD:CHANGELOG.md` — the lines under its `## <VERSION> — <YYYY-MM-DD>` heading up to the next `##` heading, blank lines trimmed — so `/release-plugin` step 7 shows and step 8 publishes that text on a fresh run and a resumption alike, and the changelog, the monorepo release, and the two distribution releases stay identical even when a run fails between creating them. Step 5 composes the body only on a fresh run; a resumption skips it whole and reads the entry back rather than recomposing a condensed rewrite that could hand a distribution release notes differing from a monorepo release already created. Under the plain heading form the extraction is a fixed line range and the body is byte-identical to the entry. A pre-mutation gate beside the tag-existence check requires that no entry for `<VERSION>` exists on a fresh run and that exactly one exists at `HEAD` on a resumption — one grep that flips on resumption — so a stale unpublished Release commit buried below `HEAD`, which would otherwise gain a duplicate entry in a later commit that succeeds, stops in pre-flight instead. The accepted cost is that the skill must state the extraction rule exactly, and that a resumption's skipped step 5 leaves its cross-check and empty-range confirmation un-rerun, resting on the committed entry as the evidence they passed.

## Out of Scope

## Open questions

<open-question id="Contributing vs Development overlap">
  <question>Does the root CONTRIBUTING.md absorb the README Development section (with the README linking out to it), or summarize the contributor path and link to the README section that stays authoritative?</question>
  <alternative id="Absorb into CONTRIBUTING.md">
    The README ## Development section moves whole — the core/-once model, the three build_hosts.py invocations and their validation gate, the hosts/-never-hand-edited rule, VERSION and set_version.py, the distribution repositories, and the directory-marketplace developer install — into the root CONTRIBUTING.md as the section after the proposal-first contributor workflow, with its one in-section link re-pointed to README.md#installation, and the README keeps a short pointer in its place (a sentence naming the model plus a link to CONTRIBUTING.md), whose exact form the README-links sibling settles.
    <advantage>The contributor gets one self-sufficient surface: the file GitHub itself links from the new-issue and new-pull-request pages and the community profile carries the whole path — propose, edit core/, rebuild, --check, preserve the CLAUDE.md invariants, merge commit — with no hop, and the human-facing build description keeps exactly one hand-held copy beside CLAUDE.md&apos;s agent-facing one instead of gaining a third; nothing in the repository links to README&apos;s #development anchor, so the move dangles nothing.</advantage>
    <drawback>The README loses its self-contained account of why hosts/ is generated and where releases go, so a curious visitor who is not contributing takes one hop for it; CONTRIBUTING.md becomes a mixed contributor-and-maintainer document whose VERSION and distribution-publishing paragraphs no contributor runs; and it forces at least a link-out in the README, which closes the stay-untouched branch of the still-open README-links sibling before that question is answered.</drawback>
  </alternative>
  <alternative id="Summarize and link to README">
    CONTRIBUTING.md states the contributor path in a few lines — edit core/, run uv run scripts/build_hosts.py and its --check form, keep the CLAUDE.md invariants, then the proposal-first workflow — and links to README ## Development, which stays unchanged and authoritative for the model, the validation gate, VERSION, the distribution repositories, and the developer install.
    <advantage>The README stays the complete, self-contained description of the repository for every reader and no existing text moves; CONTRIBUTING.md stays as short and scannable as SECURITY.md and CODE_OF_CONDUCT.md; and the goal&apos;s own parenthetical — edit core/, rebuild, invariants live in CLAUDE.md — reads as exactly this summary.</advantage>
    <drawback>The summary is a third hand-held statement of the build loop beside README&apos;s and CLAUDE.md&apos;s, aligned by hand with nothing gating it — the drift the repository&apos;s single-source habit exists to avoid — and the contributor GitHub sends to CONTRIBUTING.md hops to README for the commands and to CLAUDE.md for the rules: three files for one path.</drawback>
  </alternative>
  <alternative id="Verbatim in both">
    CONTRIBUTING.md carries the Development section word for word and README keeps its own, the two aligned by hand the way milestone 23 aligned the README Installation subsections with the distribution README templates.
    <advantage>Neither reader hops and no existing text moves — the self-sufficient-surface shape the Installation decision already chose, applied again.</advantage>
    <drawback>Five hundred words duplicated inside one checkout for a README reader who does not need them — Installation was duplicated across two repositories for two readers who each did — so the alignment cost is paid for nobody, a contributor reading README then CONTRIBUTING.md meets the same section twice, and unlike the host trees no --check gates the two copies against drift.</drawback>
  </alternative>
  <recommendation option="Absorb into CONTRIBUTING.md">The Development section has had one reader since milestone 13 wrote it so contributors would know how to build the plugin, and this milestone hands that reader a file of their own that GitHub links from every new issue and pull request, so by the repository&apos;s own rule that each surface be self-sufficient for its reader (milestone 23&apos;s Installation decision) the build loop belongs in CONTRIBUTING.md whole, while the README&apos;s remaining reader — an adopter, per its milestone-1 rewrite as an adoption-focused landing page — came to install, which Installation already covers; absorbing keeps the human-facing build description at one hand-held copy beside CLAUDE.md&apos;s agent-facing one where a summary would add an ungated third, nothing links into the README anchor so the move breaks nothing, and it sets the self-contained pattern the still-open CONTRIBUTING.md-handoff-wording sibling can follow; the summary would win only if the README were meant to remain a complete repository manual for non-contributors, which its landing-page rewrite decided against.</recommendation>
</open-question>
<open-question id="README links to hygiene files">
  <question>Beyond the CI badge, should README.md gain a Contributing section or links to CONTRIBUTING.md, SECURITY.md, CHANGELOG.md, and Discussions, or stay untouched?</question>
  <alternative id="Contributing section as intake map">
    A ## Contributing section takes the slot ## Development leaves when its body moves into CONTRIBUTING.md: the one-sentence core/-once-rebuild pointer with the link to CONTRIBUTING.md for the full contributor path, then one line each routing bug reports and proposals to the issue forms, questions to the Discussions tab, vulnerability reports to SECURITY.md, and release notes to CHANGELOG.md — five or six lines between the skill reference and Self-dogfooding, in the shape of the one-line ## License section, with CODE_OF_CONDUCT.md left to GitHub&apos;s About sidebar.
    <advantage>The README is the one page every first-time visitor scans — the goal&apos;s own framing — and a handful of lines makes it the map to the whole hygiene surface; it is the only in-repo pointer CHANGELOG.md can get, since GitHub surfaces that file nowhere (the release badge reaches only the latest release and the Releases sidebar only the release pages); it keeps the bug-reports-and-pull-requests-belong-here routing sentence that otherwise leaves with Development&apos;s body; and it gives the residual pointer the Absorb move leaves a heading that names what it now is.</advantage>
    <drawback>Four of its five routes duplicate what GitHub already surfaces — CONTRIBUTING.md on the new-issue and pull-request pages, SECURITY.md on the Security tab and About sidebar, the forms in the issue chooser, Discussions as a tab — so the section is a hand-held link list re-pointed by hand if a file or route ever moves, and it puts a contributor-facing section on the page milestone 1 rewrote for an adopter.</drawback>
  </alternative>
  <alternative id="Pointer to CONTRIBUTING.md only">
    ## Development shrinks to exactly the pointer the Absorb option leaves — one sentence naming the core/-once-rebuild model and a link to CONTRIBUTING.md — and the README gains nothing else: SECURITY.md, CODE_OF_CONDUCT.md, the issue forms, and Discussions are left to GitHub&apos;s own surfacing (Security tab, About sidebar, issue chooser, Discussions tab), and release notes to the Releases page the badge already links, whose bodies the changelog decision makes identical to CHANGELOG.md.
    <advantage>The smallest README change the Absorb move already requires — no new section and no link list to keep aligned — and the README stays the adoption-focused landing page milestone 1 made it, with every route it omits reachable through the GitHub chrome built for exactly that.</advantage>
    <drawback>CHANGELOG.md, the file the changelog decision makes the source of truth for every release body, is reachable only by browsing the tree, since GitHub surfaces it nowhere and the badge reaches only the latest release; and the visitor who scans the README for where to ask a question or report a vulnerability — the visitor the goal names — finds nothing and must already know GitHub&apos;s tabs, while the page&apos;s one routing sentence today leaves with Development&apos;s body.</drawback>
  </alternative>
  <alternative id="Untouched beyond the badge">
    README.md gains the CI badge and nothing else: ## Development stays whole and authoritative, and no section or link names CONTRIBUTING.md, SECURITY.md, CHANGELOG.md, or Discussions.
    <advantage>Zero churn on the landing page and nothing new to keep aligned, and GitHub surfaces every file the question lists except CHANGELOG.md by itself.</advantage>
    <drawback>It stands only if the overlap sibling settles against Absorb — under the Absorb option it recommends, Development&apos;s body leaves and a pointer to CONTRIBUTING.md must take its place by construction, so untouched is not on the table — and even then CHANGELOG.md, which GitHub surfaces nowhere, is reachable only by browsing the tree.</drawback>
  </alternative>
  <depends-on question="Contributing vs Development overlap" option="Absorb into CONTRIBUTING.md"/>
  <recommendation option="Contributing section as intake map">The goal is the hygiene surface a first-time visitor scans for, and the README is what that visitor scans — GitHub&apos;s Security tab, About sidebar, issue chooser, and Discussions tab reach only the visitor who already knows where to look, and none of them reaches the CHANGELOG.md this milestone adds — while the Absorb move already forces a pointer into Development&apos;s slot, so a ## Contributing heading over that sentence plus four one-line routes costs the handful of lines the ## License section already spends on one file, keeps the page&apos;s only routing sentence that would otherwise leave with Development&apos;s body, and gives the residual pointer a heading that says what it is; the pointer-only variant would win only if the hygiene files were meant to be found through GitHub&apos;s chrome alone, which leaves the changelog with no pointer at all, and untouched is closed by the Absorb recommendation.</recommendation>
</open-question>
<open-question id="Blank issues and contact links">
  <question>Should an ISSUE_TEMPLATE config.yml disable blank issues and route questions to a Discussions category once Discussions is enabled?</question>
  <alternative id="Disable blank issues and route questions">
    A .github/ISSUE_TEMPLATE/config.yml beside the forms that sets blank_issues_enabled: false and carries one contact link — name Ask a question, url https://github.com/uHappyLogic/cairn/discussions/new?category=q-a, about usage and how-to questions — and no security link, because the chooser gains GitHub&apos;s own Report a security vulnerability line by itself once private vulnerability reporting is enabled.
    <advantage>The chooser becomes the complete intake map — bug, feature, and proposal forms, a question door into Discussions, and the automatic security line — so the required-field guarantee the Issue templates decision chose the forms for stops being opt-out for readers, while the maintainer (write access) still sees a Blank issue option labelled Maintainers only for their own filing; questions never enter the tracker as unstructured issues, and it is one versioned four-line file with no repository setting or hand click.</advantage>
    <drawback>A report that fits no form — the small fix the contributor-workflow decision says to file as an issue, say a README typo — goes through the feature form or the bug form&apos;s required host, version, and skill fields with n/a in them; the disable is a web-chooser nudge only, since gh issue create, the API, and a hand-edited URL still create blank issues; and the link is dead until the Discussions toggle runs, so the settings task must land before or with the templates.</drawback>
  </alternative>
  <alternative id="Route questions, keep blank issues">
    The same config.yml with the Q&amp;A contact link but blank_issues_enabled left at its default of true, so the chooser lists the forms, the question door, and the Open a blank issue link at its foot.
    <advantage>The question route is added without closing any door: a report that fits neither form still has a blank issue to land in, on a repository with no issues yet and no triage history to say what those reports will look like.</advantage>
    <drawback>The blank link stays in front of every visitor, so the forms&apos; required fields — chosen because they are the only mechanism that guarantees host, version, and skill on a repository with no history — are optional for exactly the reporter most likely to skip them, and the file then exists only to add one link that README.md or CONTRIBUTING.md could carry as prose.</drawback>
  </alternative>
  <alternative id="No chooser config">
    No chooser configuration at all: the forms plus GitHub&apos;s default blank-issue link, with Discussions reached only through its tab or a prose pointer in README.md or CONTRIBUTING.md.
    <advantage>Nothing to write and nothing that names a Discussions category before the toggle has run; Discussions stays an uncommitted surface the maintainer can point at later, once real questions show where they want to live.</advantage>
    <drawback>The goal enables Discussions and nothing routes anything to it — the chooser is the one surface every issue-filing visitor passes, so a question arrives as a blank issue beside the bugs and the enable is a tab nobody is sent to — while the blank link stays for every visitor exactly as in the keep-blank variant.</drawback>
  </alternative>
  <depends-on question="Proposal intake route" option="Proposal issue form"/>
  <depends-on question="Discussions categories" option="Keep GitHub defaults"/>
  <recommendation option="Disable blank issues and route questions">The Issue templates decision chose YAML forms because required fields are the only mechanism that guarantees host, version, and skill on a repository with no issues yet, and the blank-issue link at the foot of the chooser is that guarantee&apos;s opt-out — closed for readers by one line while the maintainer keeps a Maintainers-only blank option; with proposals on their own form, the security line added by GitHub itself once private reporting is enabled, and the goal enabling Discussions whose default Q&amp;A category is the answerable home a question needs, the one visitor a closed chooser would strand is the one with a question, and a single contact link at the fixed slug q-a is that door — so one four-line file makes the chooser the whole intake map, the small-fix report is a feature-form entry or a question rather than a reason to reopen the forms, and the gh and API bypasses are the gap the forms decision already accepted rather than a new one; keeping blank issues would be right only if reports fitting no form were expected to be common, which two forms plus a question door on a repository with no history gives no reason to expect.</recommendation>
</open-question>
<open-question id="Unused wiki tab">
  <question>Should the unused wiki be disabled on the root repository so a visitor sees no empty Wiki tab, or be left enabled?</question>
  <alternative id="Disable in the settings task">
    The repository-settings brief that enables Discussions and private vulnerability reporting on uHappyLogic/cairn adds --enable-wiki=false (the flag milestone 23 ran on both distribution repositories) to its gh repo edit call, reads has_wiki back as false, and records that read-back among its Verified bullets, so the root repository joins cairn-claude and cairn-antigravity in switching off a feature nothing uses.
    <advantage>One flag on a command the task already runs, reversible with the same flag, and it turns the repository&apos;s in-tree documentation rule into a setting: README.md is the authoritative workflow documentation and skill reference, CLAUDE.md holds the invariants, the distribution READMEs are rendered by the build, and this milestone puts the changelog in the tree, so the one surface where a page could start documentation outside the commit conventions and the drift gate is closed rather than left to habit — and the wiki git repository does not exist, so nothing is hidden or lost.</advantage>
    <drawback>It changes nothing for the visitor the question names: GitHub shows an empty wiki only to accounts with write access — an anonymous fetch of the repository page today lists Code, Issues, Pull requests, Actions, Projects, Security, and Insights with no Wiki tab, and /wiki redirects to the repository home — so the empty tab is the maintainer&apos;s own view alone; the community profile does not count the wiki either way; and the record is point-in-time, a later hand re-enable being invisible to the tree.</drawback>
  </alternative>
  <alternative id="Leave enabled">
    The wiki stays enabled as it is, no task or flag touches it, and the setting is left for the maintainer to revisit if a wiki page is ever wanted.
    <advantage>Nothing to do and nothing to undo: no visitor sees a Wiki tab while the wiki has no pages, so the status quo already delivers the no-empty-tab outcome the question asks for, and the enable is the very state the maintainer would flip back to should long-form documentation ever want a home outside the README.</advantage>
    <drawback>The setting keeps open a documentation surface the repository has no plan for — its documentation is in-tree by design — so the maintainer&apos;s own view carries a Create the first page invitation whose first page would put documentation outside the commit conventions and the drift gate and make the Wiki tab appear to every visitor; the root stays inconsistent with the distribution repositories, where the wiki is disabled; and the starting state&apos;s wiki enabled (unused) remains a fact the milestone noticed and left standing.</drawback>
  </alternative>
  <depends-on question="Squash merge setting" option="Repository-settings task"/>
  <recommendation option="Disable in the settings task">The question&apos;s visitor-facing motive is already met — GitHub hides an empty wiki from everyone without write access, so no visitor sees the tab today — but the flag still earns its place because it costs nothing on a gh repo edit call the settings task already runs with a read-back, it makes the in-tree documentation rule (README.md authoritative, CLAUDE.md invariants, the changelog this milestone adds) a setting instead of a habit by closing the one surface where a page could start documentation outside the commit conventions and the drift gate, and it matches the distribution repositories milestone 23 already configured; leaving it enabled would be right only if a wiki page were foreseen, and none is, while re-enabling stays one flag away; the empty tab a first-time visitor does see is Projects (Open 0, Closed 0), which the question does not name and the same flag line can carry if the maintainer wants that tab gone too.</recommendation>
</open-question>
<open-question id="Squash merge setting">
  <question>Is disabling squash merges on the root repository, which the contributor-workflow decision requires so a contributor milestone&apos;s Milestone-finish: and answer commit subjects survive the merge, a task of this milestone alongside enabling Discussions, or a repository setting the maintainer flips by hand outside any task?</question>
  <alternative id="Repository-settings task">
    A brief of this milestone, naturally the same one that enables Discussions and private vulnerability reporting on uHappyLogic/cairn: it runs gh repo edit uHappyLogic/cairn --enable-squash-merge=false (or the equivalent PATCH field) beside the Discussions enable, verifies by reading allow_squash_merge back as false, and lands in TASKS_DONE.md with that read-back among its Verified bullets.
    <advantage>It follows the repository&apos;s own precedent for repository settings — milestone 13&apos;s gh repo edit --description task, milestone 23&apos;s distribution-repository gh repo edit recorded &quot;as the once-only maintainer act this milestone records&quot;, and this milestone&apos;s Discussions and private-vulnerability-reporting toggles — and the TASKS_DONE entry plus the milestone&apos;s finish summary become the only in-repo record of a setting no git diff captures, so the contributor-workflow decision&apos;s present-tense &quot;squash merges are disabled&quot; (false today: allow_squash_merge is true, with no branch protection or ruleset either) is made true and verified inside the milestone at the marginal cost of one flag.</advantage>
    <drawback>A task whose whole work is one flag beside the Discussions enable, and its record is point-in-time: a later hand re-enable of squash merges is invisible to the tree, so the entry records that the setting was flipped, not that it stays so.</drawback>
  </alternative>
  <alternative id="Maintainer hand flip">
    The maintainer runs the same gh repo edit command by hand — now, during requirements, or whenever — outside any task, on the view that a repository setting is not a repository deliverable; this question&apos;s answer, restating the contributor-workflow decision as a maintainer act, is the only record.
    <advantage>No task for a ten-second setting change that produces no file, and it can be done immediately rather than when the milestone&apos;s tasks run; the maintainer is the only account that can flip it anyway.</advantage>
    <drawback>It breaks the precedent every earlier repository-setting change followed and splits this milestone&apos;s three toggles across two mechanisms for no gain; no brief carries it, so no Verified read-back dates or confirms the flip and finish-current-milestone has nothing to summarize — the record is a sentence of intent in Decisions — and a setting whose entire purpose is to make the merge-commit contract enforced rather than remembered is itself left to memory, with allow_squash_merge staying true while the decision says otherwise until someone remembers.</drawback>
  </alternative>
  <recommendation option="Repository-settings task">A repository setting that a decision states as fact and that git never records is exactly what this repository has always turned into a task with a read-back — milestone 13&apos;s description edit, milestone 23&apos;s distribution-repository edits, and this milestone&apos;s own Discussions and private-vulnerability-reporting toggles — and here the toggle is one extra flag beside the Discussions enable, so the task costs nothing while its TASKS_DONE entry is the only place the flip and its verification will ever be written down; the hand flip would be the choice only if the setting were needed before the milestone&apos;s tasks run, and nothing needs it before CONTRIBUTING.md publishes the merge-commit contract that depends on it.</recommendation>
</open-question>
<open-question id="Proposal intake route">
  <question>Is the proposal that precedes a contributor-run milestone filed through a dedicated proposal issue form beside the bug and feature templates, through a Discussions category once Discussions is enabled, or through the feature template as it stands?</question>
  <alternative id="Proposal issue form">
    A third YAML issue form beside the bug and feature forms whose required fields are what acceptance consumes: a goal statement in the shape a milestone ## Goal takes, the motivation, what is in and out of scope, and a checkbox committing the proposer to run the milestone in a fork through /finish-current-milestone.
    <advantage>The filing itself yields the /define-milestone-goal input and the contributor commitment, on the object GitHub treats as a work item — labels, an assignee, and Closes #N from the contributor&apos;s pull request — so the proposal&apos;s lifecycle (open, accepted, milestone reserved, merged) closes itself, and CONTRIBUTING.md and the pull-request template can name &quot;the proposal issue&quot; concretely.</advantage>
    <drawback>One more template to maintain and a third chooser card on a repository with no issues yet, and a small ask filed as a proposal (or the reverse) lands on the wrong form and needs a relabel.</drawback>
  </alternative>
  <alternative id="Discussions proposals category">
    A curated Proposals category in Discussions once the milestone enables it, with CONTRIBUTING.md directing proposals there; on acceptance the maintainer runs /define-milestone-goal and closes the thread as resolved or creates a tracking issue from it.
    <advantage>Keeps open-ended design talk out of the issue tracker: threaded replies and upvotes suit a proposal that may go nowhere, and stalled or declined proposals never clutter the work list.</advantage>
    <drawback>A discussion has no work-item lifecycle — no pull request can close it, so every accepted proposal ends as a hand-closed thread or a second, tracking issue — the category cannot be created by API (the GraphQL schema offers createDiscussion and closeDiscussion but no category mutation), so the route rests on a hand-made web-UI category, and it commits the still-open Discussions categories question to curating one.</drawback>
  </alternative>
  <alternative id="Feature form as it stands">
    The feature request form doubles as the proposal route: the contributor files a feature request, states in its body that they would run it as a milestone, and CONTRIBUTING.md says to do exactly that.
    <advantage>No new surface at all — the two forms the goal names are the whole intake, and a proposal is a feature request whose reporter volunteers.</advantage>
    <drawback>Nothing at filing separates &quot;please add this&quot; from &quot;I propose to run this as a milestone&quot; or asks for the goal statement, the scope, or the commitment, so the maintainer extracts all of it by conversation and labels by hand — the free-text loss the Issue templates decision chose forms to prevent — and a Feature request heading mislabels a design or refactor proposal.</drawback>
  </alternative>
  <alternative id="Feature form with proposal fields">
    The feature form gains an optional &quot;I would run this as a contributor milestone&quot; checkbox and an optional scope field, so one form carries both intents.
    <advantage>One form, with the intent captured by a tick instead of prose the maintainer has to read for.</advantage>
    <drawback>The two intents need different required sets — a small ask needs no goal statement or scope while a proposal needs both — so the shared form either leaves the proposal fields optional, losing the guarantee required fields exist for, or drags every small ask through proposal fields.</drawback>
  </alternative>
  <recommendation option="Proposal issue form">A proposal is a work item in waiting whose acceptance produces a milestone and whose pull request should close it, which is what an issue is and a discussion is not; its required fields are exactly the /define-milestone-goal input and the run-it-yourself commitment the contributor-workflow decision hinges on, which neither feature-form variant can require without burdening small asks; and it leaves Discussions free for the questions a blank-issues config would route there, with no hand-made category to curate.</recommendation>
</open-question>
<open-question id="CONTRIBUTING.md handoff wording">
  <question>Does the root CONTRIBUTING.md spell out the proposal-then-reserved-milestone handoff step by step (propose, maintainer defines and activates the milestone on main, branch from that commit, run the pipeline through /finish-current-milestone, merge commit), or state the contract in a sentence and point at the README workflow documentation for the steps?</question>
</open-question>
<open-question id="Pull-request template milestone field">
  <question>Does the pull-request template ask for the reserved milestone id and the proposal it was accepted in, so a reviewer can check the branch carries that milestone&apos;s artifacts, or stay a generic checklist (rebuilt hosts/ trees, --check green, CLAUDE.md invariants preserved)?</question>
  <alternative id="Milestone fields and artifact checks">
    A single .github/PULL_REQUEST_TEMPLATE.md that opens with two fill-in lines — the reserved milestone id (the milestone_&lt;N&gt;_&lt;slug&gt; directory name the maintainer&apos;s Milestone-definition: commit created on main) and Closes #&lt;proposal issue&gt; — followed by a reviewer checklist of what a finished contributor milestone leaves in the tree: milestones/&lt;id&gt;/requirements.md with no &lt;open-question&gt; block, TASKS_TODO.md with no task section, TASKS_DONE.md carrying every task with its Verified bullets, milestones/README.md with the milestone&apos;s history entry and the pointer at none, one Milestone-finish: &lt;id&gt; commit on the branch, CLAUDE.md touched only for lasting changes, and hosts/ rebuilt (which the drift-gate status check confirms).
    <advantage>It describes the only pull request the contributor-workflow decision allows and names the two inputs the maintainer&apos;s post-merge steps consume — the milestone id is the argument /capture-milestone-principle-updates takes verbatim, and the Milestone-finish: commit it asks the reviewer to find is what /release-plugin step 5 reads for the release notes, the reason the merge commit is required — so the reviewer checks the branch against a list fixed by the pipeline rather than by the change, and the Closes line closes the proposal issue on merge by itself.</advantage>
    <drawback>Pull-request templates are Markdown only, with no form or required-field equivalent, so every line is a prompt the contributor can delete and GitHub enforces nothing — unlike the YAML issue forms — and the checklist is a hand-held restatement of what derive-tasks and finish-current-milestone already enforce, so any change to a finished milestone&apos;s artifact set must be mirrored here by hand.</drawback>
  </alternative>
  <alternative id="Generic checklist">
    The conventional template: a summary field and a checklist of rebuilt hosts/ trees, --check green, and CLAUDE.md invariants preserved, naming no milestone and no proposal.
    <advantage>Familiar to any GitHub contributor, true of every conceivable pull request including a maintainer&apos;s own branch, and nothing in it needs rewriting if the contributor workflow ever changes.</advantage>
    <drawback>Two of its three items are the drift gate the CI decision already runs as a status check on every pull request, leaving CLAUDE.md invariants as its only human-judged line, and it describes the plain-fix pull request the contributor-workflow decision says will not exist — small fixes are filed as issues — so the reviewer of the one pull-request kind the repository accepts gets no prompt for the milestone id or the proposal and reconstructs both from the branch&apos;s commits.</drawback>
  </alternative>
  <alternative id="Generic checklist with optional milestone line">
    The generic checklist with one optional line above it — Milestone and proposal issue, if any — so a milestone pull request and any other branch share one template.
    <advantage>One line serves both a contributor milestone and a maintainer-opened branch without committing the template to either.</advantage>
    <drawback>If any implies the plain-pull-request tier the contributor-workflow decision ruled out and invites the small fix it routes to an issue, the optional field is skippable by exactly the contributor whose branch the reviewer most needs to check, and it carries none of the artifact checks that make the id worth asking for.</drawback>
  </alternative>
  <depends-on question="Proposal intake route" option="Proposal issue form"/>
  <recommendation option="Milestone fields and artifact checks">Every pull request this repository will receive is a contributor-run milestone — the contributor-workflow decision leaves no plain tier, the maintainer commits straight to main and has opened no pull request in the repository&apos;s history, and the drift gate already machine-checks the generic items on every pull request — so the template&apos;s only job is the check CI cannot make, that the branch carries the reserved milestone&apos;s finished artifacts, and asking for the milestone id and the proposal issue names exactly the two inputs the post-merge steps consume (the id feeds /capture-milestone-principle-updates, the Milestone-finish: commit feeds the release notes, and Closes #N closes the proposal form&apos;s issue) over an artifact list the pipeline fixes; the generic checklist would win only if plain-fix pull requests were expected, which the decision rules out.</recommendation>
</open-question>
<open-question id="CI action pinning">
  <question>Should the drift-gate workflow pin its checkout and uv setup actions to full commit SHAs or to major-version tags?</question>
  <alternative id="Full SHAs with version comments">
    Both steps name a full 40-character commit SHA followed by a comment naming the release it resolves — as of 2026-09-16, uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1 and uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4 # v10.1.0, the form every example in setup-uv&apos;s own README takes — bumped by hand whenever the maintainer wants a newer release.
    <advantage>A commit SHA is the one reference neither action&apos;s maintainers nor an attacker can re-point: checkout&apos;s releases are not immutable (immutable=false on every one) and GitHub moved its floating major tags across every supported line on 2026-07-20, while setup-uv stopped publishing major and minor tags at v8.0.0 in March 2026 citing the tj-actions tag-retargeting attack and names the git hash as its first choice — so the workflow runs exactly the code the maintainer read, in the same posture as uv.lock&apos;s hash-pinned pyyaml, with the comment keeping the line as readable as a tag and GitHub&apos;s sha_pinning_required repository policy (off today) available to enforce the form.</advantage>
    <drawback>Two opaque refs the maintainer bumps by hand with nothing prompting it, so the pins fall behind releases silently — the July 2026 checkout safety backport reached floating-tag workflows automatically and SHA-pinned ones not at all — and the comment is only as honest as the last bump; the payoff is also modest here, because the job holds no secrets and the repository&apos;s default GITHUB_TOKEN is read-only, so a compromised action could falsify a status or burn minutes and nothing more.</drawback>
  </alternative>
  <alternative id="Major-version tags">
    Both steps name a floating major tag — uses: actions/checkout@v7 and uses: astral-sh/setup-uv@v7 — so each run resolves the tag afresh and gets whatever release the action&apos;s maintainers have pointed it at.
    <advantage>The form GitHub&apos;s documentation and checkout&apos;s README use, readable at a glance, and for checkout it delivers fixes within a major by itself: on 2026-07-20 GitHub backported its pull_request_target safety enforcement to every supported major (v2 through v7) and moved the floating tags to those releases, reaching tag-pinned workflows automatically where SHA pins had to be bumped.</advantage>
    <drawback>For setup-uv the option no longer exists — since v8.0.0 (2026-03-29) the action publishes no major or minor tags, so @v8, @v9, and @v10 resolve to nothing and @v7 is the last floating tag, frozen at v7.6.0 from 2026-03-16, six months and three majors behind, in exactly the mutable-tag shape its authors withdrew as a supply-chain risk — and checkout&apos;s tag is a movable pointer on non-immutable releases, one compromised account away from running code nobody here read, which the July backport demonstrated by shipping a change its own notes mark BREAKING through the v4 tag with no commit to any consuming repository.</drawback>
  </alternative>
  <alternative id="Full-version tags">
    Both steps name an exact release tag — uses: actions/checkout@v7.0.1 and uses: astral-sh/setup-uv@v10.1.0 — readable without a comment and, per setup-uv&apos;s v8.0.0 release notes, the acceptable second choice after the git hash.
    <advantage>Readable pins that cannot contradict themselves the way a stale comment can, and for setup-uv the tag is as fixed as a SHA, since every release from v8.0.0 on is an immutable GitHub release whose tag cannot be moved or deleted.</advantage>
    <drawback>Half the pin is a promise rather than a guarantee — checkout&apos;s releases are not immutable, so v7.0.1 is a tag any account with write access to actions/checkout can re-point — and it needs the same hand bump on every release as a SHA while giving up the one property that makes the bump worth making; GitHub&apos;s sha_pinning_required policy and the OpenSSF Scorecard pinned-dependencies check both count it as unpinned.</drawback>
  </alternative>
  <alternative id="SHAs kept fresh by Dependabot">
    The full-SHA form plus a .github/dependabot.yml declaring the github-actions package ecosystem, so Dependabot opens a pull request bumping each SHA and its version comment whenever an action releases — the upgrade path GitHub&apos;s July changelog names for SHA-pinned workflows.
    <advantage>Immutable pins that never go stale unnoticed: every action release arrives as a reviewable pull request the drift gate runs on, with the comment updated in the same edit.</advantage>
    <drawback>A fourth .github surface the goal does not name, and its pull requests are exactly the plain pull requests the contributor-workflow decision says the repository will not receive — no proposal, no milestone, merged as merge commits carrying Bump subjects outside the Marker: descriptor convention — all to keep two lines fresh in a half-minute job whose only other pinned dependency, pyyaml in uv.lock, the maintainer already bumps by hand.</drawback>
  </alternative>
  <recommendation option="Full SHAs with version comments">The major-tag half of the question is no longer on offer for one of the two actions — setup-uv stopped publishing major and minor tags at v8.0.0 in March 2026, citing the tj-actions tag-retargeting attack, so @v10 does not resolve and the only floating tag left is a v7 frozen six months and three majors back — and for the other, checkout, the floating tag is a movable pointer on non-immutable releases that GitHub itself re-pointed across every supported major in July 2026 to ship a change marked BREAKING; a full SHA is the one reference neither action&apos;s maintainers nor an attacker can move, it is the form setup-uv&apos;s README uses in every example, and a # vX.Y.Z comment keeps the line as readable as a tag, so the workflow runs exactly the code the maintainer read at the cost of two hand-bumped lines, the same posture as uv.lock&apos;s hash-pinned pyyaml; neither form fixes the uv version anyway — setup-uv installs the latest uv unless pyproject.toml names a required-version — so the pin buys action provenance while the lockfile already supplies the gate&apos;s reproducibility; full-version tags would win only if checkout&apos;s releases were immutable, and Dependabot only if the repository accepted plain pull requests, which the contributor-workflow decision rules out.</recommendation>
</open-question>
<open-question id="CI badge source">
  <question>Is the CI badge a shields.io workflow-status badge matching the two existing badges or GitHub&apos;s native badge.svg, and does it report the main branch only or the latest run on any branch?</question>
  <alternative id="Shields badge pinned to main pushes">
    A third shields.io badge in the existing centred &lt;p&gt; row, https://img.shields.io/github/actions/workflow/status/uHappyLogic/cairn/&lt;workflow-file&gt;?branch=main&amp;event=push&amp;style=flat&amp;label=ci, wrapped in a link to the workflow&apos;s runs page filtered to main (actions/workflows/&lt;workflow-file&gt;?query=branch%3Amain), where &lt;workflow-file&gt; is the drift-gate workflow&apos;s file name the CI task fixes.
    <advantage>It matches the two badges it sits beside exactly — same renderer, style=flat, the #555 label panel, 20-pixel geometry, font, and a README-owned lowercase label like label=release — while reporting precisely what GitHub reports, because shields.io&apos;s workflow-status service fetches GitHub&apos;s own badge.svg with branch and event passed straight through and re-renders the status it extracts; branch=main&amp;event=push makes the badge mean the drift gate on main&apos;s pushed HEAD and nothing else, so a contributor&apos;s failing pull-request run or a fork working on its own main can never flip it, and shields re-fetches every 60 seconds where GitHub serves its own badge at 300.</advantage>
    <drawback>The status text takes one more hop through a third party — a shields.io outage, or a change in GitHub&apos;s badge SVG markup that shields&apos; regex no longer matches, blanks the badge to an error — the pinned main needs a hand edit if the default branch is ever renamed, where the parameterless form would follow it, and the passing and failing colours are shields&apos; own green and red rather than the row&apos;s 22c55e.</drawback>
  </alternative>
  <alternative id="Native badge pinned to main pushes">
    GitHub&apos;s own badge, https://github.com/uHappyLogic/cairn/actions/workflows/&lt;workflow-file&gt;/badge.svg?branch=main&amp;event=push — the URL the Actions page&apos;s Create status badge dialog emits with branch main and event push selected — as the third &lt;img&gt; in the row, linked the same way.
    <advantage>No third party at all: GitHub serves it from the run data itself, the very badge.svg shields.io scrapes, so the README carries nothing shields could break, and the label is the workflow&apos;s name: field, set once in the workflow file.</advantage>
    <drawback>It is visibly a second family beside the two shields badges — GitHub&apos;s dark #24292E gradient label panel, its Actions icon, and its own green — so the row the goal says the badge sits in reads as two styles, and it removes no dependency, because the release badge next to it is still shields.io backed by GitHub&apos;s API; its label lives in the workflow file rather than the README, and it is served with a 300-second cache.</drawback>
  </alternative>
  <alternative id="Default-branch rule, no params">
    Either renderer with no branch or event parameter, so GitHub&apos;s documented rule applies — the badge displays the default branch, and only while the default branch has no run at all does it display the most recent run across all branches — which is the only form the latest-run-on-any-branch reading exists in; no parameter selects any-branch outright.
    <advantage>The shortest URL, it follows a default-branch rename by itself, and today it is indistinguishable from the pinned form — main is the only branch, no pull request has ever been opened, and once the workflow&apos;s first push lands on main the any-branch fallback never fires again.</advantage>
    <drawback>What the badge means is a rule the reader has to know rather than a URL they can read, and the CI decision admits pull_request runs from contributor forks into the root repository&apos;s run list, where the default form keys on the head-branch name — a fork that worked on its own main shares it — and takes any event, so a contributor&apos;s failed pull-request run can turn the README badge red for a drift that is not on main, exactly what event=push excludes.</drawback>
  </alternative>
  <recommendation option="Shields badge pinned to main pushes">Both sources report the same runs — shields.io&apos;s workflow-status badge fetches GitHub&apos;s own badge.svg with branch and event passed through and re-renders the status it extracts — so the source is only a question of the row and the scope only a question of the URL: the goal puts the badge beside a release badge that is shields flat, a native badge&apos;s dark gradient panel with its Actions icon is visibly a second family while the shields form matches panel, geometry, font, and README-owned lowercase label exactly, and the third-party exposure a native badge would avoid is already carried by the release badge next to it, so avoiding it buys nothing; pinning branch=main&amp;event=push makes the badge say the one thing a README visitor should read from it — the drift gate on main&apos;s pushed HEAD — where the parameterless rule is the same today, with main the only branch and no pull request ever opened, but leaves the contributor pull-request runs the CI decision admits to a head-branch filter the reader cannot see; the native badge would win only if the row dropped shields.io altogether, and the parameterless form only if the default branch were expected to be renamed.</recommendation>
</open-question>
<open-question id="Distribution tree hygiene files">
  <question>Beyond the CONTRIBUTING.md template, should SECURITY.md and CODE_OF_CONDUCT.md also be rendered into each host tree so a visitor to a distribution repository finds them, or stay root-only?</question>
  <alternative id="Root-only via existing pointers">
    SECURITY.md and CODE_OF_CONDUCT.md exist only at the monorepo root, and each host tree carries exactly the three non-plugin files it already has or the goal already gives it — the README.md template, the copied LICENSE, and the CONTRIBUTING.md pointer template — with the README&apos;s generated-do-not-edit paragraph and that pointer as a distribution visitor&apos;s route to the root, the CONTRIBUTING.md template naming the root&apos;s private vulnerability reporting form for security reports beside the issue route.
    <advantage>Both files stay where the surfaces they govern are — the issue tracker, Discussions, and the private vulnerability reporting form the security decision enables on uHappyLogic/cairn alone, while both distribution repositories have issues, wiki, projects, and Discussions disabled by design and private reporting off — so no copy ever has to say &quot;not here&quot;, nothing new is rendered or hand-aligned, and nothing more ships inside the plugin tree itself, which the Antigravity install extracts whole into every consumer&apos;s .agents/plugins/cairn/ and Claude Code installs into every plugin cache.</advantage>
    <drawback>A distribution repository&apos;s Security tab shows no policy and its About sidebar no code-of-conduct link, so a visitor to cairn-claude — the documented install source — who clicks Security meets a dead end and reaches the reporting form only through the README paragraph or the CONTRIBUTING.md pointer, two hops instead of one, and each distribution repository&apos;s community profile stays without code_of_conduct.</drawback>
  </alternative>
  <alternative id="Pointer templates per host">
    Each host definition gains a SECURITY.md template of a few lines — report vulnerabilities through the root repository&apos;s private vulnerability reporting form, never as an issue or pull request here — and a CODE_OF_CONDUCT.md template stating that the root&apos;s Contributor Covenant applies and linking it, rendered into each host tree exactly as the CONTRIBUTING.md template is, so GitHub&apos;s Security tab and About sidebar on each distribution repository show them.
    <advantage>The pattern the goal already chose for CONTRIBUTING.md, applied to the other two files: the Security tab on cairn-claude and cairn-antigravity shows a policy that sends a reporter straight to the root&apos;s private form in one hop, and the templates carry no slot and no host-specific text, so they render unchanged release after release.</advantage>
    <drawback>Four more hand-aligned template copies — the build has no shared-template mechanism, every template belongs to one host definition — whose entire content is &quot;go to the root&quot;; a pointer CODE_OF_CONDUCT.md is a file GitHub&apos;s detection cannot label Contributor Covenant (the labeling the conduct decision valued) governing a repository with no space to govern; and every pointer file ships inside the plugin tree, extracted into each Antigravity workspace and installed into each Claude Code plugin cache.</drawback>
  </alternative>
  <alternative id="Verbatim copies through the build">
    The build&apos;s dedicated LICENSE copy path becomes a list of root files copied verbatim into every host tree — LICENSE, SECURITY.md, CODE_OF_CONDUCT.md — so each distribution repository carries the full policy and the full covenant byte-identical to the root under the same drift gate.
    <advantage>One authored copy at the root with identity enforced by the gate rather than by hand, the covenant labeled Contributor Covenant and the full policy shown on every distribution repository, and code_of_conduct present in each distribution repository&apos;s community profile.</advantage>
    <drawback>A build change that couples policy text to the plugin build — a root policy edit made without a rebuild fails the CI drift gate and needs a sync commit — carrying text that reads wrong in place: the policy&apos;s instruction to use this repository&apos;s Report a vulnerability form names a form that exists only on the root while the copy sits on a repository where private reporting is off, and the covenant&apos;s contact email is published in three repositories instead of one; and both files travel into every consumer&apos;s workspace and cache.</drawback>
  </alternative>
  <alternative id="Account default files">
    A public uHappyLogic/.github repository carrying SECURITY.md and CODE_OF_CONDUCT.md, which GitHub displays as the default for every repository the account owns that lacks its own file of that type, so both distribution repositories — and any future cairn-&lt;host&gt; — show the policy and the covenant with nothing rendered into any host tree.
    <advantage>GitHub&apos;s own mechanism for a family of repositories sharing one policy: zero files in the plugin trees, zero build changes, and a future host&apos;s distribution repository is covered the day it is created.</advantage>
    <drawback>A fourth repository outside the monorepo, its build, and its commit conventions, holding a second copy of each file beside the root&apos;s own (the goal fixes the root files, and a repository&apos;s own file overrides the default) with nothing gating the two against drift; and the defaults apply to every repository the personal account owns regardless of visibility — forty-five public ones today, forty-two of them forks and unrelated projects — so a cairn policy pointing at cairn&apos;s reporting form would be displayed on repositories it does not describe.</drawback>
  </alternative>
  <recommendation option="Root-only via existing pointers">The two files govern interaction surfaces that exist only on the root — the issue tracker, Discussions, and the private vulnerability reporting form the security decision enables on uHappyLogic/cairn alone — while the distribution repositories are plugin trees published verbatim with issues, wiki, projects, and Discussions disabled by design, so the goal&apos;s asymmetry is principled: CONTRIBUTING.md is rendered because a pull request is the one interaction a mirror cannot disable and a pointer there prevents work the next release would overwrite, whereas no report and no conversation can happen on a mirror, so a SECURITY.md or CODE_OF_CONDUCT.md there could only repeat what the README&apos;s generated-do-not-edit paragraph and the CONTRIBUTING.md pointer already say; every file added to a host tree also ships inside the plugin to every consumer, the root&apos;s issue chooser gains GitHub&apos;s own Report a security vulnerability line the moment private reporting is enabled, and milestone 23 recorded that search lands on the monorepo and a distribution repository is only what a user installs from; the pointer templates would win only if the distribution repositories were where security reports were expected to originate, and the account default only if every public repository of the account should carry cairn&apos;s policy, which it should not.</recommendation>
</open-question>
<open-question id="Backfilled release page headings">
  <question>Since the backfill demotes the milestone sections of the eight sectioned releases (0.9.8 through 1.4.0) to ###, are those eight release pages edited to match so the changelog and release bodies stay identical, or is the heading-level divergence for backfilled entries accepted?</question>
  <alternative id="Edit the eight pages to match">
    The backfill task first demotes the nine ## milestone-section lines on the eight sectioned monorepo release pages, 0.9.8 through 1.4.0, to ### with one gh release edit --notes-file loop whose PATCH carries the body alone and changes no other line (the distribution repositories, created 2026-09-16, hold no release, so those eight pages are the whole set), then runs the uniform sixteen-tag gh release view loop that writes CHANGELOG.md, and records among its Verified bullets that every entry read back from the file equals its page body byte for byte.
    <advantage>Identity holds for all sixteen entries by construction instead of for the eight thin ones and the releases still to come: the changelog&apos;s one-line verbatim note is literally true, the backfill loop is a pure copy with no transformation, the verification is one sixteen-way diff, and the release series renders at one heading level — the eight pages match the ### that step 5 composes from this release on rather than the split the heading-form decision priced in (new pages one level smaller than 0.9.8 through 1.4.0); the edit moves nine heading markers and no words, is reversible with the same command, leaves no reader-visible trace (an edited release page shows no edited marker, only its publish date, as the 0.9.7 page edited on 2026-09-07 shows today), and the repository has already hand-edited published releases once, when the legacy tags were renamed on 2026-09-07 and their releases repointed, a date the release skill records.</advantage>
    <drawback>It rewrites eight published pages by hand outside any release run — the skill&apos;s own step 8c forbids editing a published release to make a run pass, and the link-only decision chose no page edited — so the act is recorded only in the task&apos;s Verified bullets and the API&apos;s updated_at (the Atom feed&apos;s updated stamp moves too, as it did for nine entries on 2026-09-07), and on those eight pages the body&apos;s top heading becomes a ### one size smaller than readers saw at publish.</drawback>
  </alternative>
  <alternative id="Accept the divergence">
    The eight release pages keep their ## sections untouched; the backfill loop applies the demotion itself — a ^## to ### substitution that is a no-op on the eight link-only bodies, so the loop stays one uniform pass over all sixteen tags — and the changelog&apos;s one-line note states that entries backfilled before this milestone carry their milestone headings one level deeper than the release page.
    <advantage>No published artifact is touched, the stance the link-only decision took for the thin releases and the outcome the heading-form recommendation already accepted when it noted that pages from here on render one level smaller than 0.9.8 through 1.4.0; the goal attaches its identical-notes clause to the releases /release-plugin prepends from now on, which the release-body decision makes byte-identical by construction, and nothing the skill relies on ever reads a backfilled entry back, so the divergence is a heading marker on eight historical entries that no consumer parses.</advantage>
    <drawback>The identity the goal names carries an eight-entry asterisk for as long as the file exists: the verbatim note needs a qualifier, the backfill loop carries a transformation the verification must repeat, and the release series stays split at two heading levels — ## on 0.9.8 through 1.4.0, ### from the next release on — an inconsistency nine heading markers would remove and that grows more visible with every release published at ###.</drawback>
  </alternative>
  <recommendation option="Edit the eight pages to match">Both paths leave the same words on every page and differ only in whether nine heading markers on eight monorepo pages are moved once, and moving them is what makes the goal&apos;s identity hold for all sixteen entries by construction — the backfill becomes a pure sixteen-tag copy with a byte-for-byte read-back as its Verified bullet, the changelog&apos;s verbatim note needs no qualifier, and the release series renders at one heading level instead of splitting at 1.4.0 for good — at the cost of a lossless, reversible gh release edit loop over pages the repository already hand-edited on 2026-09-07 when the legacy tags were renamed, with no distribution release yet to touch and no reader-visible trace left on a page; the link-only decision&apos;s no-page-edited stance guarded against rewriting content a reader saw ship, which a heading marker is not, so accepting the divergence would be right only if published release bodies were held immutable, and the 2026-09-07 repoint shows they are not.</recommendation>
</open-question>
<open-question id="Notes edit at release pause">
  <question>Now that the release body is the committed changelog entry, can the maintainer revise the notes at the step-7 pause, and if so does the skill amend the unpushed Release commit or must the run be aborted and restarted?</question>
  <alternative id="In-run amend at the pause">
    Step 7 takes a revision as a third answer beside publish and stop: while the Release commit is still unpushed (after a fetch, git merge-base --is-ancestor HEAD origin/main fails), the skill applies the maintainer&apos;s stated revision to the &lt;VERSION&gt; entry in the working CHANGELOG.md (an edit the maintainer already made by hand in that file counts the same), stages that one path with git add -- CHANGELOG.md, amends with git commit --amend --no-edit so the Release: &lt;VERSION&gt; subject the resumption keys on is untouched, re-extracts the entry from HEAD:CHANGELOG.md, shows it again in full, and asks again; once origin/main carries the commit — a resumption past step 8a — a revision request is a stop naming that reason, and step 6d&apos;s one-commit sentence names this as the run&apos;s only amend.
    <advantage>The notes, the file, and the commit are all the skill&apos;s own artifacts and the pause is its own review point, so revising them there keeps the single pause, the fresh run&apos;s full step-5 checks, and the identity rule intact at once — the amend is path-scoped and subject-preserving by construction, step 8 publishes the re-extracted entry exactly as the source-of-truth decision requires, the pushed-or-not condition is the skill&apos;s check rather than the maintainer&apos;s memory, and an unpushed amend is precisely the local work step 7 already says a git reset undoes.</advantage>
    <drawback>The run gains its one history rewrite: step 6d&apos;s never-amend-a-later-step-into-it sentence needs a stated exception, the binary pause becomes a loop, and a later editor meets an amend in a skill that otherwise never rewrites anything; and nothing here helps once main is pushed — the notes are then frozen for the run, and a fix is a follow-up commit plus gh release edit on three pages outside the skill.</drawback>
  </alternative>
  <alternative id="Stop, hand-amend, resume">
    Step 7 keeps its binary answer and an edit request stays a stop, but the stop message names the edit path the source-of-truth decision already opened: edit the &lt;VERSION&gt; entry in CHANGELOG.md, run git add -- CHANGELOG.md &amp;&amp; git commit --amend --no-edit, and re-run /release-plugin &lt;VERSION&gt;, which the unchanged HEAD subject makes a resumption that skips step 5, passes the exactly-one-entry gate, and shows the read-back entry at step 7 for a fresh veto — stated as valid only while the commit is unpushed.
    <advantage>No new mechanism at all: the resumption read-back publishes whatever entry HEAD carries, so the edit path is the existing recovery story plus one message, in the stop-fix-rerun shape the step-2e drift remedy already uses; the skill never rewrites history, the maintainer edits in an editor with no size limit, and every re-run re-verifies the amended commit through gates that already exist — 2a demands the edit be committed, 2c stops an amend of a commit already on origin, and the resumption gate demands exactly one entry.</advantage>
    <drawback>It invites a hand amend of a release commit — the one hand git operation the release asks for, done outside any procedure — so a mistaken follow-up commit (HEAD subject no longer Release:, a fresh run, the duplicate-entry gate) or a post-push amend is caught only after the fact by the re-run&apos;s gates and left to the maintainer to untangle; every wording tweak costs a full pre-flight re-run; and every edited release then travels the resumption path whose skipped step-5 cross-check and empty-range confirmation the source-of-truth decision accepted as a recovery-only cost.</drawback>
  </alternative>
  <alternative id="Review before the commit">
    The review moves ahead of the write: step 5 ends by showing the composed body and taking revisions in context until the maintainer approves, step 6 writes the approved text into CHANGELOG.md and commits once, and step 7 stays a veto-only publish gate over the read-back entry — an edit wanted there is abort-and-restart, git reset --hard HEAD~1 on the unpushed commit and a fresh run that revises at step 5.
    <advantage>No amend anywhere, in the run or by hand: the Release commit is written once with approved notes, the one-commit shape step 6 describes, and the step-7 abort is the plain git reset step 7 already says undoes everything before it.</advantage>
    <drawback>Every fresh run pauses twice — once on in-context text and again on the committed entry — where the skill today pauses once, at step 7, on a non-empty range; a resumption skips step 5 and so has no edit window at all, leaving an unpushed resumption to a hand amend anyway; and the abort throws away a correct version bump and rebuild to redo them for a wording change, then re-runs the whole pre-flight.</drawback>
  </alternative>
  <recommendation option="In-run amend at the pause">The source-of-truth decision is what makes a revision possible at all — today an edit request stops the run and a re-run recomposes the same condensed rewrite at step 5, so the veto is the only lever — and once the notes are a file the run wrote inside a commit the run made, revising them at the run&apos;s own review point is the skill&apos;s job rather than a remedy owed by the maintainer (the drift stop sends the maintainer away because the fix is to content the skill does not author; the notes are its own output); an amend of an unpushed commit is the local work step 7 already says a git reset undoes, it stays path-scoped to CHANGELOG.md and keeps the Release: subject the resumption keys on, step 8 still publishes what HEAD records, and checking origin/main before amending states the frozen-after-push case up front instead of leaving pre-flight 2c to catch a hand amend that has already gone wrong; the hand-amend path would win only if the skill were held never to rewrite even an unpushed commit, and the pre-commit review only if the review were meant to move ahead of the write, which the decision that step 7 shows what git records argues against.</recommendation>
</open-question>
<open-question id="Discussions categories">
  <question>Does enabling Discussions keep GitHub&apos;s default category set, or does the milestone curate categories (for example a proposals category) for the intake routes the templates point at?</question>
  <alternative id="Keep GitHub defaults">
    Enable Discussions with the one gh api --method PATCH repos/uHappyLogic/cairn -F has_discussions=true toggle and leave the six categories GitHub creates (Announcements, General, Ideas, Polls, Q&amp;A, Show and tell) exactly as they come; any intake route that points at Discussions names the default Q&amp;A category by its fixed slug q-a (discussions/new?category=q-a), and nothing else is pointed at.
    <advantage>The Discussions enable stays the single API toggle the security-policy decision already pairs it with, read back as has_discussions like every earlier repository-setting task, and the default set already carries the one category a route needs — an answerable Q&amp;A whose slug GitHub fixes — so a config.yml contact link can be written before the toggle runs and points at a category that exists by construction, with no hand-made category anywhere.</advantage>
    <drawback>Six categories on a repository with no discussions, two of which (Polls, Show and tell) have no foreseeable use here, and the default Ideas category stands beside the proposal issue form as an unrouted second place a proposal can land, which the maintainer resolves by pointing at the form or creating an issue from the thread.</drawback>
  </alternative>
  <alternative id="Prune defaults to routed set">
    Enable via the same toggle, then delete in the web UI (the pencil beside Categories, the trash icon, Delete &amp; Move) the defaults no template or CONTRIBUTING.md route names — Polls, Show and tell, and Ideas — leaving Announcements, General, and Q&amp;A, with the task&apos;s Verified bullet reading discussionCategories back through GraphQL.
    <advantage>A visitor opens Discussions and sees only categories with a documented purpose, and no Ideas category competes with the proposal issue form for proposals.</advantage>
    <drawback>Categories have no create, update, or delete API — the GraphQL schema offers createDiscussion, closeDiscussion, and the other discussion mutations but nothing for a category, and the docs describe only the pencil and trash icons — so the one-toggle task gains hand-clicked steps no command records, taken on a guess about which defaults a community that does not exist yet will use, and a deleted category is restored only by hand.</drawback>
  </alternative>
  <alternative id="Curated Proposals category">
    Add a Proposals category in the web UI on top of the defaults, optionally backed by a versioned .github/DISCUSSION_TEMPLATE/proposals.yml category form whose required fields ask for the goal statement and scope, and point CONTRIBUTING.md&apos;s proposal route at it.
    <advantage>Proposals get a threaded, upvoteable home where a design that goes nowhere never clutters the issue list, and the category form keeps the required-fields guarantee in a file the repository versions.</advantage>
    <drawback>It opens a second front door for the intent the Proposal intake route sibling settles on the issue form, the category itself can only be hand-made in the web UI (the form file applies only to a slug that already exists), and a discussion still has no work-item lifecycle for the accepting pull request to close.</drawback>
  </alternative>
  <depends-on question="Proposal intake route" option="Proposal issue form"/>
  <recommendation option="Keep GitHub defaults">With proposals on an issue form, the only intake route that can point at Discussions is the questions route the still-open blank-issues sibling may choose, and GitHub&apos;s default set already carries an answerable Q&amp;A category with the fixed slug q-a — exactly the target a config.yml contact link would name — so the milestone&apos;s Discussions work stays the one API toggle the security-policy decision pairs it with, read back like every earlier repository-setting task, whereas any curation is web-UI-only hand work (no category mutation exists) done on a guess about a community with no discussions yet; pruning earns its clicks only once real threads show which defaults go unused, and a Proposals category only if the proposal route ever moves off the issue form.</recommendation>
</open-question>
<open-question id="Mid-milestone sync with main">
  <question>May a contributor merge or rebase main into their milestone branch before running /finish-current-milestone, given that a sync taken while the maintainer&apos;s own milestone is live makes the branch&apos;s finish read as a change from that live pointer to none at merge time, or does the contributor never sync and the maintainer resolve every conflict when merging the pull request?</question>
</open-question>
