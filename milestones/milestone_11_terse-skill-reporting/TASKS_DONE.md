# TASKS DONE

## Terse Reporting: Milestone-Lifecycle Skills

Cut the success-path reporting of the four milestone-lifecycle skills — `define-milestone-goal`, `specify-milestone-starting-state`, `goto-next-milestone`, and `modify-milestone-goal` — to a single fixed terse status line, removing each one's prose summary and next-step handoff pointer. Each also gains a distinct one-line no-op message for when its commit's dirty-own-path guard fires. This applies the milestone's terse-reporting decisions to this group of `SKILL.md` files.

**Notes:**
- Terse line shape (per the "Terse status line shape" decision): a fixed sentence carrying no identifier — no milestone id, count, or commit subject. Concrete choices to use: `define-milestone-goal` step 6 → "Milestone defined."; `specify-milestone-starting-state` step 7 → "Starting state recorded."; `goto-next-milestone` step 5 → "Milestone activated."; `modify-milestone-goal` step 6 → "Goal revised." Delete the prose-summary bullets and the "Suggest the next step" / "Recommended next step" handoff pointer from each of these success steps.
- No-op case (per the "No-op pass reporting" decision): when the shared commit procedure's dirty-own-path guard fires (the pass changed no files, so nothing was committed), the step must instead print a concise one-line message stating that nothing changed and briefly why — distinct from the terse success line, because git holds no durable record of a no-op.
- Boundary — leave every failure/clean-stop message untouched: `define-milestone-goal` step 3's conflict stop; `specify-milestone-starting-state` step 1's not-found stop and step 3's `/init` suggestion; `goto-next-milestone` steps 1–2's prerequisite / "none" / candidate-selection stops. Only the final success-report step of each skill changes.
- `modify-milestone-goal` step 6 currently also surfaces a before→after and a downstream-impact analysis; it collapses fully to the terse line too. This skill is deliberately not among the ones the "Non-redundant advisory output" decision keeps advisory output for (those are `review-milestone-requirements`, `derive-tasks`, and the answer skills — all outside this task). Its "surface, never cascade" analysis body (steps 3–5) and its Rules stay unchanged; only the step-6 output collapses.
- No file-writing or commit behavior changes; edit each `SKILL.md` inline (no shared reporting-convention file is introduced).

**Success:**
- In each of the four `SKILL.md` files, the final `## Workflow` step's success path instructs printing exactly one fixed terse status line with no identifier and no next-step/handoff pointer.
- Each of those four final steps also instructs a distinct one-line no-op message, triggered when the dirty-own-path guard fires, that states nothing changed and briefly why.
- The strings "Suggest the next step" and "Recommended next step", and the per-item prose-summary bullets, no longer appear in those four success steps.
- `define-milestone-goal` step 3, `specify-milestone-starting-state` steps 1 and 3, and `goto-next-milestone` steps 1–2 retain their existing failure/clean-stop wording unchanged; every file-creation/edit and commit step across the four skills is unchanged.
- No new file is created under `shared/`.

---

## Terse Reporting: Answer Skills

Cut the success-path reporting of the three answer skills — `answer-open-question`, `answer-open-question-with-recommendation`, and `answer-open-question-with-alternative` — to a single fixed terse status line, removing the prose re-narration of which question resolved, how the document changed, and cascade details. Per the milestone's "Non-redundant advisory output" decision, keep the one piece of genuinely git-absent advisory output — the note about newly-exposed open questions — alongside the terse line. Each step also gains a distinct one-line no-op message for when its commit's dirty-own-path guard fires. This applies the milestone's terse-reporting decisions to the answer-skill group of `SKILL.md` files.

**Notes:**
- The final reporting step in each skill: `answer-open-question` step 5 "Report findings", `answer-open-question-with-recommendation` step 3 "Report findings", `answer-open-question-with-alternative` step 6 "Report findings". Only these steps change.
- Terse line shape (per the "Terse status line shape" decision): a fixed sentence carrying no identifier — no Short Title, no commit subject. A concrete choice to use across all three: "Answer recorded." Delete the "which question was resolved and how the document changed (resolved block, decision folded into `## Decisions`, cascading resolutions)" re-narration bullet — and, for `answer-open-question-with-alternative`, also the "which alternative you recorded (id + how it read)" clause — since the committed diff and git log are the durable record of that.
- Non-redundant advisory (per the "Non-redundant advisory output" decision): retain each step's "Any new open questions the recorded decision may have introduced — surface these but do **not** add them to the document without user confirmation" note beside the terse line. This is decision-critical information the commit never captured, so it stays. The two `with-…` skills' trailing "Then stay available…" follow-up sentence also stays (it is not diff re-narration).
- No-op case (per the "No-op pass reporting" decision): when the shared commit procedure's dirty-own-path guard fires (the pass changed no files, so nothing was committed), the step must instead print a concise one-line message stating that nothing changed and briefly why — distinct from the terse success line, because git holds no durable record of a no-op. For these skills the guard fires on the clean-stop cases each already documents (Short-Title mismatch, missing `<recommendation>`/`<alternative>` element, the retired-sentinel redirect, a parse error).
- Boundary — leave every failure/clean-stop message untouched: `answer-open-question`'s step 1 parse-error message, its step 2 retired-sentinel redirect guard pointing at `/answer-open-question-with-recommendation`, and its step 3 Short-Title-mismatch relay; `answer-open-question-with-recommendation`'s step 1 no-`<recommendation>`-element / missing-block clean-stop-and-point; `answer-open-question-with-alternative`'s step 2 guard (no matching question id, no `<alternative>` elements, no matching alternative id, each listing the available ids). Every skill's `## Rules` section is also unchanged.
- No file-writing or commit behavior changes; edit each `SKILL.md` inline (no shared reporting-convention file is introduced).

**Success:**
- In each of the three `SKILL.md` files, the final reporting step's success path instructs printing exactly one fixed terse status line with no identifier, followed only by the newly-exposed-open-questions advisory note.
- The re-narration of the resolved block, the decision folded into `## Decisions`, and the cascading resolutions no longer appears in those three success steps; `answer-open-question-with-alternative`'s "which alternative you recorded" clause is likewise gone.
- Each of those three steps also instructs a distinct one-line no-op message, triggered when the dirty-own-path guard fires, that states nothing changed and briefly why.
- `answer-open-question` step 1's parse-error message, step 2's retired-sentinel redirect guard (still pointing at `/answer-open-question-with-recommendation`), and step 3's Short-Title-mismatch relay are unchanged; the missing-element and Short-Title clean stops in the other two skills are unchanged; every record/commit step and every `## Rules` section across the three skills is unchanged.
- No new file is created under `shared/`.

---

## Terse Reporting: Review-Milestone-Requirements

Cut the success-path reporting of `review-milestone-requirements` step 5 "Report convergence" to a single fixed terse status line, removing the "What changed this pass" prose re-narration and the handoff pointers to `/discuss-open-question` and `/answer-open-question`. Per the milestone's "Non-redundant advisory output" decision, keep the genuinely git-absent, decision-critical output — the convergence verdict (whether the document is ready for `/derive-tasks`) and the list of still-open questions — alongside the terse line. The step also gains a distinct one-line no-op message for when the step-4 commit's dirty-own-path guard fires. This applies the milestone's terse-reporting decisions to `review-milestone-requirements`, this skill's flagship no-op-pass case.

**Notes:**
- Only step 5 "Report convergence" changes. Terse line shape (per the "Terse status line shape" decision): a fixed sentence carrying no identifier — no milestone id, count, or commit subject; a concrete choice to use is "Requirements reviewed."
- Delete the "What changed this pass" bullet (blocks pruned with covering decision cited, repeats merged, new questions raised, "possibly resolved — confirm" flags) — that reshaping is exactly the committed diff the terse cut targets — and remove the "one-line nudge toward `/discuss-open-question` or `/answer-open-question`" handoff pointer from the "What's still open" bullet.
- Non-redundant advisory (per the "Non-redundant advisory output" decision): keep the still-open list (remaining `status="open"` blocks by Short Title / `id`) and keep the full **Convergence** verdict — the `no status="open"` blocks remaining → ready-for-`/derive-tasks` rule, and the deferred-blocks-may-carry-forward note. This is decision-critical state the commit never captures, so it stays beside the terse line.
- No-op case (per the "No-op pass reporting" decision): step 4 already documents this as "the milestone's flagship no-op-pass case" — when the step-4 dirty-own-path guard fires (a pass that reconciled, pruned, and surfaced nothing, so `requirements.md` is unchanged and nothing was committed), step 5 must instead print a concise one-line message stating that nothing changed and briefly why — distinct from the terse success line, because git holds no durable record of a no-op.
- Boundary — every document-editing step is untouched: step 2 (reconcile/prune/dedup), step 3 (surface new gaps / author `<open-question>` blocks), and step 4 (the commit and its shared-procedure reference) are unchanged, as is the `## Rules` section. Only step 5's console output changes.
- No file-writing or commit behavior changes; edit `skills/review-milestone-requirements/SKILL.md` inline (no shared reporting-convention file is introduced).

**Success:**
- `review-milestone-requirements` step 5's success path instructs printing exactly one fixed terse status line with no identifier, followed only by the still-open list and the convergence verdict; the "What changed this pass" bullet and the `/discuss-open-question` / `/answer-open-question` handoff pointers no longer appear in step 5.
- Step 5 also instructs a distinct one-line no-op message, triggered when the step-4 dirty-own-path guard fires, that states nothing changed and briefly why.
- Steps 0–4 and the `## Rules` section of `skills/review-milestone-requirements/SKILL.md` are unchanged — every reconcile, surface, prune, authoring, and commit instruction is byte-for-byte intact.
- No new file is created under `shared/`.

---

## Terse Reporting: Task And Principle-Capture Skills

Cut the success-path reporting of the three task-level and principle-capture skills — `submit-task`, `complete-task`, and `capture-milestone-principle-updates` — to a single fixed terse status line, removing the prose summary of what was authored/completed/distilled and every next-step/follow-up pointer. Each step also gains (or, for `capture`, keeps) a distinct one-line no-op message for when its commit's dirty-own-path guard fires. This applies the milestone's terse-reporting decisions to the task-level/capture group of `SKILL.md` files; the same-named dispatched agents under `agents/` (which return structured `DONE`/`FAILED` to an orchestrator, not console output) are out of scope.

**Notes:**
- The final reporting step in each skill: `submit-task` step 6 "Confirm", `complete-task` step 3 "Hand back", `capture-milestone-principle-updates` step 6 "Report". Only these steps change.
- Terse line shape (per the "Terse status line shape" decision): a fixed sentence carrying no identifier — no task title, no principle Short Title, no commit subject. Concrete choices to use: `submit-task` → "Task submitted."; `complete-task` → "Task completed."; `capture-milestone-principle-updates` → "Principles captured." Delete the prose summaries: `submit-task`'s task-title-and-insert-position line; `complete-task`'s "summarize what you completed, how you verified it, and the commit you made"; `capture`'s per-principle Short-Title + add/revision recap. Delete the follow-up pointers too: `capture`'s "now available to the recommendation advisor (`/discuss-open-question` and `/recommend-all-open-questions`)" pointer.
- These three skills are **not** among the ones the "Non-redundant advisory output" decision keeps advisory output for (those are `review-milestone-requirements`, `derive-tasks`, and the answer skills — all outside this task), so their success paths collapse fully to the terse line. In particular: `submit-task`'s success-path "if the atomic-scope rule left a second buildable piece unauthored, mention it and offer to queue it" clause and its "stay available…" invitation, and `complete-task`'s "stay available…" invitation, are part of the success narration that collapses — none is a preserved advisory. (The context-stays-for-follow-up behavior is unchanged; only the printed sentence goes.)
- No-op case (per the "No-op pass reporting" decision): when the shared commit procedure's dirty-own-path guard fires (the pass changed no files, so nothing was committed), the step must print a concise one-line message stating that nothing changed and briefly why — distinct from the terse success line, because git holds no durable record of a no-op. For `capture` this is its **existing** single-line "nothing to distill" report (the empty commit range / none-generalize / user-declined-every-candidate convergence at step 6); keep it as the distinct one-line no-op message rather than collapsing it into "Principles captured." For `submit-task` and `complete-task` the guard fires on the defensive no-file-change case each already documents (a completion abandoned before it touched any file; an insert that authored nothing).
- Interactive mid-workflow conversation is not success reporting and is untouched: `capture`'s step 4 phase-2 per-candidate revise-vs-add confirmation dialogue with the user (including any optional up-front pool preview) stays exactly as is — only the final step-6 report goes terse.
- Boundary — leave every failure/clean-stop message untouched: `submit-task` step 2's duplicate stop and out-of-scope redirect, and step 6's "could not author a concrete task — suggest `/discuss-new-task`" fallback; `complete-task` step 1's no-matching-task stop that lists the available `##` headings; `capture` step 1's pointer-resolution, step 2's empty-range exit, and step 3's none-generalize exit as preconditions. Every skill's `## Rules` section is also unchanged.
- No file-writing or commit behavior changes; edit each `SKILL.md` inline (no shared reporting-convention file is introduced).

**Success:**
- In each of the three `SKILL.md` files, the final reporting step's success path instructs printing exactly one fixed terse status line with no identifier and no next-step/follow-up pointer.
- The per-item prose summaries no longer appear on those success paths: `submit-task`'s task-title-and-position line and its atomic-scope-leftover offer; `complete-task`'s what/how/commit summary; `capture`'s per-principle add/revision recap and its recommendation-advisor pointer.
- Each of the three steps instructs a distinct one-line no-op message, triggered when the dirty-own-path guard fires, that states nothing changed and briefly why; for `capture` this is its existing single-line "nothing to distill" report, kept distinct and not collapsed into the terse success line.
- Failure/clean-stop wording is unchanged: `submit-task` step 2's triage stops and step 6's "could not author / suggest `/discuss-new-task`" fallback; `complete-task` step 1's no-matching-task stop; `capture` steps 1–3's preconditions; and `capture` step 4's interactive revise-vs-add dialogue.
- Every file-creation/edit and commit step across the three skills is unchanged, and no new file is created under `shared/`.

---

## Terse Reporting: Finish-Current-Milestone

Cut the success-path reporting of `finish-current-milestone` step 9 "Confirm" to a single fixed terse status line, removing the prose summary (milestone name, tasks-completed count, the README bullet list, the pointer-clear note) and — the distinctive piece of this task — retiring the runtime "Suggest the next steps" block entirely, including its first item recommending `/capture-milestone-principle-updates` and its second item pointing at `/define-milestone-goal`. The step also gains a distinct one-line no-op message for when the step-8 commit's dirty-own-path guard fires. This applies the milestone's terse-reporting decisions to `finish-current-milestone`; the finish→capture handoff stays documented in `CLAUDE.md` and `README.md` (a separate documentation task updates those) but is no longer printed at runtime.

**Notes:**
- Only step 9 "Confirm" changes. Terse line shape (per the "Terse status line shape" decision): a fixed sentence carrying no identifier — no milestone id/name, task count, or commit subject; a concrete choice to use is "Milestone finished."
- Delete both prose-summary and handoff elements from step 9: the report of milestone name / tasks-completed count / README accomplishment bullets / pointer-clear / CLAUDE.md-sections-touched / single-commit note, **and** the entire ordered "Suggest the next steps" block (both its `/capture-milestone-principle-updates` item and its `/define-milestone-goal` item). `finish-current-milestone` is **not** among the skills the "Non-redundant advisory output" decision keeps advisory output for, so its success path collapses fully to the terse line.
- No-op case (per the "No-op pass reporting" decision): when the step-8 shared-commit dirty-own-path guard fires (the pass changed no files, so nothing was committed), step 9 must instead print a concise one-line message stating that nothing changed and briefly why — distinct from the terse success line, because git holds no durable record of a no-op.
- The `## Rules` bullet that currently reads "`/finish-current-milestone` only *suggests* `/capture-milestone-principle-updates` — it never invokes it. Step 9 adds the recommendation as suggestion text only." describes the now-retired runtime suggestion; reconcile it so it no longer asserts step 9 prints that recommendation (the handoff living on only in `CLAUDE.md`/`README.md`). This is the one Rules-section edit this task makes; leaving it would contradict the changed step 9.
- Boundary — leave every failure/clean-stop message untouched: step 1's no-active-milestone stop (pointer is `none`) and step 3's pending-tasks stop ("`TASKS_TODO.md` still has `##` sections — complete them first"). Every file-writing/commit step is unchanged: step 4–5's README history + `## Completed Milestones` row, step 6's `Current milestone: none` pointer clear, step 7's conditional `CLAUDE.md` update, and step 8's `Milestone-finish: milestone_<NN>_<slug>` commit all stay exactly as they are.
- No file-writing or commit behavior changes; edit `skills/finish-current-milestone/SKILL.md` inline (no shared reporting-convention file is introduced).

**Success:**
- `finish-current-milestone` step 9's success path instructs printing exactly one fixed terse status line with no identifier and no next-step/handoff pointer.
- The "Suggest the next steps" block is gone from step 9's runtime output — neither the `/capture-milestone-principle-updates` recommendation nor the `/define-milestone-goal` pointer appears in the skill's printed success path — and the milestone-name / task-count / README-bullet-list / pointer-clear prose summary no longer appears.
- Step 9 also instructs a distinct one-line no-op message, triggered when the step-8 dirty-own-path guard fires, that states nothing changed and briefly why.
- The stale `## Rules` bullet no longer asserts that step 9 prints the `/capture-milestone-principle-updates` recommendation.
- Steps 1–8 of `skills/finish-current-milestone/SKILL.md` are unchanged — the no-active-milestone stop, the pending-tasks stop, the README history/table writes, the pointer clear, the conditional `CLAUDE.md` update, and the `Milestone-finish:` commit are byte-for-byte intact.
- No new file is created under `shared/`.

---

## Terse Reporting: Orchestrator Skills

Cut the success-path reporting of the four orchestrator skills — `complete-all-tasks`, `recommend-all-open-questions`, `answer-all-open-questions-with-recommendation`, and `derive-tasks` — to a single fixed terse status line for the whole run, removing all per-item output produced during the dispatch loop (the list of completed tasks, the annotated-vs-skipped question breakdown, the per-answer outcomes, the ordered task titles) and every consumer/next-step pointer. Per the milestone's "Non-redundant advisory output" decision, `derive-tasks` keeps exactly one git-absent advisory alongside its terse line: the flag for any requirement it could not trace to a task. Each orchestrator also gains a distinct one-line no-op message for when its run's dirty-own-path guard fires (a run that committed nothing). This applies the milestone's terse-reporting decisions to the orchestrator group of `SKILL.md` files.

**Notes:**
- The run-level success-report step in each skill: `complete-all-tasks` step 3 "Report completion", `recommend-all-open-questions` step 6 "Report", `answer-all-open-questions-with-recommendation` step 3 "Report", `derive-tasks` step 8 "Verify coverage and report". Only these steps change.
- Terse line shape (per the "Terse status line shape" decision): one fixed sentence for the whole run, carrying no identifier — no task heading, question Short Title, count, or commit subject. Concrete choices to use: `complete-all-tasks` → "All tasks completed."; `recommend-all-open-questions` → "Recommendations embedded."; `answer-all-open-questions-with-recommendation` → "Recommendations recorded."; `derive-tasks` → "Tasks derived."
- Delete the per-item run narration and pointers on each success path: `complete-all-tasks`'s "list each task that was completed"; `recommend-all-open-questions`'s "which questions were annotated" / "which questions were skipped" bullets and its whole consumer pointer to `/answer-open-question-with-recommendation` and `/answer-all-open-questions-with-recommendation`; `answer-all-open-questions-with-recommendation`'s "how many recommendations were recorded and direct the user to review the commits" re-narration; `derive-tasks`'s "ordered task titles" summary.
- Non-redundant advisory (per the "Non-redundant advisory output" decision): `derive-tasks` step 8 **keeps** the flag for any requirement it could not trace to a task ("flag as a gap — never silently omit") beside the terse line — a coverage gap is decision-critical and the commit never captures it. The other three orchestrators keep no advisory (they list only re-narration), so their success paths collapse fully to the terse line.
- No-op case (per the "No-op pass reporting" decision): when a run's dirty-own-path guard fires (the run changed none of its own paths, so nothing was committed — e.g. a recommend sweep that annotated nothing because every block already carried a `<recommendation>`, or a `derive-tasks` run that derived nothing), the step must instead print a concise one-line message stating that nothing changed and briefly why — distinct from the terse success line, because git holds no durable record of a no-op. For `answer-all-open-questions-with-recommendation` this is its **existing** "If the sweep recorded nothing … say so in one sentence" report; keep it as the distinct one-line no-op message rather than collapsing it into the terse line.
- Boundary — leave every failure/partial path untouched: `complete-all-tasks`'s stop-the-loop-and-report-which-task-failed (step 2b and its `## Rules` bullet); `derive-tasks`'s step 2 unresolved-open-questions stop and step 7 stop-on-`FAILED`-brief; `answer-all-open-questions-with-recommendation`'s step 2 per-question `FAILED`/skip handling; `recommend-all-open-questions`'s step 1 "no open/deferred questions at all — say so and stop" clean stop and its per-question dispatch. Every skill's `## Rules` section is also unchanged.
- No dispatch, commit-granularity, or file-writing behavior changes; edit each `SKILL.md` inline (no shared reporting-convention file is introduced). Commit granularity stays as-is: `complete-all-tasks` per task, `answer-all-open-questions-with-recommendation` per answer, `recommend-all-open-questions` and `derive-tasks` once at end.

**Success:**
- In each of the four `SKILL.md` files, the run-level report step's success path instructs printing exactly one fixed terse status line for the whole run, with no identifier, no per-item listing, and no consumer/next-step pointer.
- `recommend-all-open-questions` step 6 no longer instructs the annotated-vs-skipped breakdown or the consumer pointer to the `/answer-…-with-recommendation` skills; `complete-all-tasks` step 3 no longer lists completed tasks; `answer-all-open-questions-with-recommendation` step 3 no longer narrates per-answer outcomes; `derive-tasks` step 8 no longer lists the ordered task titles.
- `derive-tasks` step 8 still instructs flagging any requirement it could not trace to a task as a gap, alongside the terse line.
- Each of the four steps instructs a distinct one-line no-op message, triggered when the run's dirty-own-path guard fires, that states nothing changed and briefly why; for `answer-all-open-questions-with-recommendation` this is its existing "recorded nothing" sentence, kept distinct and not collapsed into the terse success line.
- Failure/partial-path wording is unchanged: `complete-all-tasks`'s stop-the-loop-report-which-task-failed handling, `derive-tasks`'s unresolved-open-questions stop and stop-on-`FAILED`-brief, `answer-all-open-questions-with-recommendation`'s per-question `FAILED`/skip handling, and `recommend-all-open-questions`'s no-questions clean stop; every `## Rules` section across the four skills is unchanged.
- No new file is created under `shared/`.

---

## Terse Reporting: CLAUDE.md Documentation And Invariant

Update the plugin repository's own `CLAUDE.md` to track the terse-reporting change this milestone applies to the skills. Two kinds of edit: (1) reconcile the existing assertions that `finish-current-milestone` "recommends (never auto-runs)" `capture-milestone-principle-updates` at runtime so they no longer claim a runtime recommendation, while keeping the finish→capture handoff documented as the optional finish-time follow-up a user runs after finishing; and (2) add a new standalone invariant to the "Invariants to preserve when editing skills" section codifying the cross-cutting terse-success-reporting convention, per the milestone's "Codifying the terse-reporting convention" decision.

**Notes:**
- The runtime-recommendation assertions to reconcile are the two that repeat the "**recommends (never auto-runs)**" claim: the `finish-current-milestone` skill-layout line in the repository-layout skill list (its trailing "recommends (never auto-runs) capture-milestone-principle-updates" clause), and the ordering invariant near the end of the invariants list ("`finish-current-milestone` also **recommends (never auto-runs)** `capture-milestone-principle-updates` as an optional post-finish follow-up"). Both must stop asserting a *runtime* recommendation while still documenting the handoff: `capture-milestone-principle-updates` remains the optional finish-time follow-up a user runs after finishing (this is also how the `capture-milestone-principle-updates` layout line already frames it — "optional finish-time follow-up recommended by finish-current-milestone" — reconcile that phrasing too if it now reads as a runtime print).
- Also reconcile any other `CLAUDE.md` text that asserts a skill prints a success-path prose summary or next-step pointer at runtime, so no stale claim survives; the invariants describing *commit* behavior, file-writing behavior, and the retained git-absent advisories (convergence verdict, coverage-gap flag, newly-exposed-questions note) are unchanged — only success-path *console reporting* claims are in scope.
- The new standalone invariant (its sole durable authoritative home — there is deliberately no shared reporting-convention file to carry it, mirroring how the equally cross-cutting commit convention earns its own invariant) must codify: every file-mutating committing skill's success path prints exactly one fixed terse status line carrying no identifier (no milestone id, task heading, Short Title, or commit subject) and no next-step/handoff pointer; orchestrators print one such line for the whole run with no per-item output during the loop; the genuinely git-absent decision-critical advisories are preserved alongside the terse line (name them: `review-milestone-requirements`' convergence verdict and still-open list, `derive-tasks`' untraceable-requirement flag, and the answer skills' newly-exposed-open-questions note); a no-op pass whose dirty-own-path guard fired prints a distinct one-line explanation instead (git holds no durable record of a no-op); failure and clean-stop paths keep their full explanatory messages; the conversational skills (`discuss-milestone-goal`, `discuss-open-question`, `discuss-new-task`, `ask-in-milestone-context`) and the two non-committing exempt skills (`init-milestone-base-workflow`, `migrate-workspace`) are untouched; and the convention lives in each `SKILL.md` directly.
- This task edits only `CLAUDE.md`. The parallel `README.md` documentation reconciliation (the *Ending a milestone* prose and the learning-loop/skill-reference sections that mention the runtime recommendation and the per-skill "suggest next step" behavior) is a separate concern and is out of scope here.

**Success:**
- No text in `CLAUDE.md` asserts that `finish-current-milestone` recommends `capture-milestone-principle-updates` (or prints any next-step pointer) at runtime; the strings claiming a runtime "recommends (never auto-runs)" recommendation are gone from both the layout line and the ordering invariant.
- The finish→capture handoff is still documented in `CLAUDE.md`: `capture-milestone-principle-updates` remains described as the optional finish-time follow-up a user runs after `finish-current-milestone`.
- A new standalone invariant is present in the "Invariants to preserve when editing skills" section codifying the terse-success-reporting convention, and it is consistent with the milestone's four decisions (terse status line shape, non-redundant advisory output, no-op pass reporting, and codifying the convention) — including the one-terse-line success rule, the orchestrator one-line-per-run rule, the named preserved advisories, the distinct no-op-pass line, the untouched failure/clean-stop paths, the untouched conversational and exempt skills, and the "no shared reporting-convention file; each SKILL.md carries it" point.
- No other `CLAUDE.md` invariant is altered beyond the finish→capture reconciliation and the new invariant; the commit-convention, file-writing, and retained-advisory invariants are byte-for-byte intact except where a stale runtime success-reporting claim required reconciliation.

---

## Terse Reporting: README Documentation

Update the plugin repository's own `README.md` to track the terse-reporting change this milestone applies to the skills. Reconcile every passage that describes a skill's success path as printing a prose summary or a runtime next-step/handoff suggestion — notably `finish-current-milestone`'s runtime recommendation of `/capture-milestone-principle-updates` — so it no longer reads as runtime console output, while keeping the same handoffs and the finish→capture follow-up documented as workflow guidance. This is the `README.md` counterpart to the separate `CLAUDE.md` documentation task.

**Notes:**
- The specific runtime-recommendation phrasing to reconcile is in two places: the *Ending a milestone* prose (currently "`/finish-current-milestone` … It then **recommends** the optional `/capture-milestone-principle-updates`, which distills …") and the `capture-milestone-principle-updates` skill-reference entry (currently "Run it as the optional follow-up `/finish-current-milestone` **recommends**"). Both must stop framing the recommendation as something the skill prints at runtime, while still documenting `capture-milestone-principle-updates` as the optional finish-time follow-up a user runs after finishing. The *Ending a milestone* Mermaid diagram's `optional: distill principles` edge (D5 ⇢ EM2) is already framed as documented workflow guidance and stays as-is.
- The *answer-principle-learning loop* section's Manual-teaching-flow bullet mentions capture distilling `Manual-answer:` commits "at milestone finish" — that is workflow guidance and stays; just ensure nothing in that section (or anywhere else) asserts `finish-current-milestone` *prints/recommends* capture at runtime.
- Reconcile any remaining skill-reference passage that reads as a printed success-path prose summary or a printed "suggest the next step" handoff pointer, per the milestone's terse-reporting decisions — e.g. `modify-milestone-goal`'s "**points you at** `/review-milestone-requirements` to reconcile" — so the documented handoff survives as workflow guidance (after modifying the goal, reconcile via `/review-milestone-requirements`) rather than a runtime print. Check the other handoff descriptions the goal names (define → specify, specify/goto → review, review → the answer/discuss skills plus the `/derive-tasks`-readiness verdict, `recommend-all-open-questions` → its `answer-…-with-recommendation` consumers); most already read as workflow guidance and need no change — do not manufacture edits where a passage already describes a skill's function or a preserved advisory rather than a runtime print.
- Keep `README.md` accurate about what still surfaces on the console under terse reporting where it touches on it: the preserved git-absent advisories — `review-milestone-requirements`' convergence verdict and still-open list (already described in its skill-reference entry — keep it accurate), `derive-tasks`' coverage-gap flag, and the answer skills' newly-exposed-open-questions note — plus the distinct one-line no-op message a pass prints when it changes nothing. Do not add a claim that a skill prints a full prose success summary.
- Boundary: the conversational skills' descriptions (`discuss-milestone-goal`, `discuss-open-question`, `discuss-new-task`, `ask-in-milestone-context`) are untouched, as is the *How skills commit* section (commit behavior, not reporting). This task edits `README.md` only — the `CLAUDE.md` reconciliation and the new terse-reporting invariant are the separate "Terse Reporting: CLAUDE.md Documentation And Invariant" task.

**Success:**
- No passage in `README.md` describes `finish-current-milestone` recommending `/capture-milestone-principle-updates` at runtime; both the *Ending a milestone* prose and the `capture-milestone-principle-updates` skill-reference entry are reworded so the finish→capture handoff reads as the optional finish-time follow-up a user runs, documented as workflow guidance rather than a printed runtime recommendation.
- No remaining `README.md` passage describes a skill's success path as printing a prose summary of what it did or a runtime next-step/handoff suggestion; the handoffs the goal names remain documented as workflow guidance (including the modify-goal → review reconciliation path).
- The described console behavior in `README.md` is consistent with the terse-reporting decisions: the preserved advisories (`review-milestone-requirements`' convergence verdict + still-open list, `derive-tasks`' coverage-gap flag, the answer skills' newly-exposed-questions note) and the distinct no-op message remain accurately described, and no text claims a full prose success summary is printed.
- The conversational skills' descriptions and the *How skills commit* section are unchanged; no `CLAUDE.md` edit is made by this task.

---

