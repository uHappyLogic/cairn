# TASKS TODO

## Neutralize CLAUDE.md Invariant Set

Sweep the plugin's own `CLAUDE.md` — the invariant set Claude acts on when editing this repo — so it becomes fully work-type-agnostic and is reconciled against the swept behavior files. Rewrite its environment-context invariant to the milestone's neutral enumeration, neutralize the skill/agent descriptions and invariants phrased in code terms (deliverable-as-code/codebase, "live code", "insertion points", "assertion wording", "exact code"), and update any invariant whose wording a sibling sweep task changed in the behavior files so no CLAUDE.md passage contradicts them. The invariant set's structure and all workflow, commit, and reporting rules stay intact.

**Notes:**
- The environment-context invariant is the primary target: the line beginning "`specify-milestone-starting-state`, `derive-tasks`, and the shared completion procedure … read the project's environment context (tech stack, build/test commands, MCP tools, conventions) from `CLAUDE.md`. Run `/init` once…". Per the "Environment-context replacement wording" decision, replace the SE enumeration item-for-item with "the project's domain context, working conventions, available tools, and how work is verified as done", keeping the enumerated shape; retain the generalized `/init` pointer (`/init` documents any project regardless of work type). Reconcile against the exact enumeration phrasing the sibling "Neutralize Complete-Task Procedure Language" task provides.
- Code-term passages to neutralize (deliverable-as-software framing under the zero-findings bar): the skill-map line and the invariant for `ask-in-milestone-context` ("the live code those tasks produced" / "grounds answers … in the **live code**") reframe to grounding in the live deliverables/artifacts the tasks produced; the task-altitude invariant ("reconstructed from the goal + live code", "the **detailed design** (exact code, insertion points, assertion wording, decided fresh against the live codebase)", "line-by-line build detail") reframe to work-type-neutral terms (what can be re-derived at completion time against the live project/deliverable, the detailed design decided fresh); and the two lighter "live code" occurrences in the `recommend`-cluster invariants (the `recommend-open-question` subagent grounding and `shared/recommend-procedure.md`'s "grounding discipline (live code over memory)") reframe to grounding in the live project.
- Cairn's own operating mechanics are **exempt** under the "SE-specific finding boundary" decision — do **not** touch them: git commits and commit-subject markers, the `git log`/`git show`/`git log --grep` references, path-scoped staging, `git add -A` prohibitions, the `CLAUDE.md`/`README.md`/`milestones/README.md` file names, and the consuming project being a git repo. The `git log`/`git show` supporting-lens mention inside the `ask-in-milestone-context` invariant stays intact; only the *deliverable* vocabulary around it changes.
- Reconcile — do not contradict — the swept behavior files: the completion procedure's verification reframe (CLAUDE.md must not reassert a build/test verification mechanism), the canonical written-guide example, and the neutral agent personas. CLAUDE.md carries no `Creep`/`RailCameraSnapper` worked example, so no example replacement is needed here — only ensure no passage re-implies a software deliverable.
- This is a language + reconciliation sweep only. The invariant set's structure, every workflow/commit/reporting rule, all commit-subject markers, and every skill/agent behavioral contract must survive unchanged in meaning — only work-type framing changes.

**Success:**
- No software-engineer-specific phrasing that assumes the user's deliverable is software remains in `CLAUDE.md` — the environment-context "tech stack, build/test commands, MCP tools, conventions" enumeration, "the live code those tasks produced", "the **live code**", "exact code, insertion points, assertion wording, decided fresh against the live codebase", "line-by-line build detail", and the "live code over memory" phrasings are gone or neutralized, verifiable by reading the file.
- The environment-context invariant enumerates "the project's domain context, working conventions, available tools, and how work is verified as done" (preserving the enumerated shape), with the generalized `/init` pointer retained.
- Cairn's own operating mechanics are left intact — git commit/commit-subject/`git log`/path-scoped-staging references, `git add -A` prohibitions, the `CLAUDE.md`/`README.md`/`milestones/README.md` file names, and git-repo references are unchanged, verifiable by reading the file.
- No CLAUDE.md passage contradicts the swept behavior files — nothing reasserts a build/test verification mechanism, and no invariant re-implies a software deliverable.
- The invariant set's structure and all workflow, commit-subject, reporting, and skill/agent behavioral rules are present and unchanged in meaning, verifiable by reading the file.

---

## Run Independent Zero-Findings Re-Audit

Run the milestone's final independent re-audit of the whole plugin — the success proof. Dispatch a single fresh-context subagent given only (a) the decided finding criteria and (b) the full file set to audit, with no access to this sweep's reasoning, and collect its structured output. This is the milestone's success-proof mechanism per the "Re-audit execution form" and "Re-audit task success wording" decisions; it audits, it does not fix.

**Notes:**
- The subagent must be given only two inputs and nothing about how the sweep was done (independence is the whole point). Input (a) — the finding criteria, taken verbatim from the "SE-specific finding boundary" decision: a finding is any text assuming the user's own deliverable or domain is software engineering (personas, "the code/codebase," insertion points, build/test verification, code examples, and the like), applied as the generalizable "does this assume the user's deliverable is software?" test to cases the initial audit did not foresee; with Cairn's own operating mechanics exempt as domain-uniform plugin infrastructure — git commits and git-log greps, path-scoped staging, the `CLAUDE.md`/`README.md`/`milestones/README.md` file names, and the consuming project being a git repository. Input (b) — the full file set: everything under `skills/`, `agents/`, `shared/`, plus root `README.md` and `CLAUDE.md`.
- The subagent returns a structured per-finding list — each finding giving the file path, the location, the offending phrase, and which criterion/layer it violates — or an explicit zero-findings verdict when it finds nothing. Report that structured output.
- Success is that the audit ran and its structured output was delivered — the per-finding list or the explicit zero-findings verdict — **not** that the verdict is clean. Do not fix findings within this task and do not author the next round here: on findings, follow-up fix tasks plus a fresh re-audit task are appended after this one (that queuing is separate work), iterating until a run comes back clean; that final clean verdict is the milestone's zero-findings proof.

**Success:**
- A single fresh-context subagent was dispatched with only the two inputs — the decided finding criteria (the "SE-specific finding boundary" test and its exemptions) and the full file set (`skills/`, `agents/`, `shared/`, `README.md`, `CLAUDE.md`) — and no access to the sweep's reasoning.
- The subagent's structured output was produced and reported: either a per-finding list (each entry carrying file path, location, offending phrase, and the criterion/layer violated) or an explicit zero-findings verdict.
- No findings were fixed and no follow-up tasks were authored within this task — its deliverable is solely the delivered audit output, whatever the verdict.

---
