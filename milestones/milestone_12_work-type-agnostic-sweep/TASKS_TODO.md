# TASKS TODO

## Neutralize Ask-In-Milestone-Context Skill Language

Sweep `skills/ask-in-milestone-context/SKILL.md` — the read-only Q&A skill that answers questions grounded in the current milestone — so it becomes fully work-type-agnostic. Reframe its code-grounding language ("the code those tasks shipped", "the actual code those tasks produced", "the truth is in the code", "Live code is primary", "live source", "the actual source", "the repository … the code wins", the step-2 heading "what actually shipped") as grounding answers in the live deliverables/artifacts the tasks produced, whatever the work type. The skill's contracts stay intact.

**Notes:**
- This skill is not one of the milestone's seven environment-context reader files, so the "Environment-context replacement wording" decision does not govern it — do not force the enumerated phrase in. Its `CLAUDE.md` read (step 1, "the project's stack, conventions, and real names for systems and files") still carries light SE flavor; neutralize it to a work-type-neutral equivalent (e.g. the project's domain context, conventions, and real names for its systems and files) in the same pass without adopting the seven-file enumeration.
- The best-effort task→commit mapping (step 2 and the frontmatter/prose) — `TASKS_DONE.md` intent plus `git log`/`git show` as a *supporting lens* — is a contract to preserve, not neutralize: git commits and greps are exempt plugin infrastructure under the "SE-specific finding boundary" decision. Keep the mapping and its "best-effort / not guaranteed / fall back to reading the live files" hedge intact; only the *deliverable* vocabulary around it changes (e.g. "reading the live files" stays, "reading the live source/code" is reframed to reading the live deliverable/artifact).
- The illustrative example questions that name software work ("how did the auth task end up handling token refresh?", "where did we put the retry logic?") are SE-specific illustrations under the zero-findings bar; neutralize them to work-type-neutral examples in the same pass. This skill is not where the milestone's one canonical worked example lives, so match the surrounding illustration style rather than importing the "Draft the Getting Started section" example.
- This is a language sweep only — carry no verification-mechanism reframe (this skill verifies nothing) and no canonical-example import. The frontmatter `description` field carries the "the code those tasks shipped" framing and is in scope; neutralize it too.

**Success:**
- No software-engineer-specific phrasing remains in the file — the code-grounding terms it currently carries ("the code those tasks shipped", "the actual code those tasks produced", "the truth is in the code", "Live code is primary", "live source", "the actual source the task touched", "the code wins", "reading the live source", the step-2 heading "Ground the answer in what actually shipped") are gone or neutralized to deliverable/artifact framing, verifiable by reading the file including the frontmatter `description`.
- The read-only contract is intact: the skill still never edits any file, and step 4 still only *offers* the owning skill for a surfaced next action while performing none of its work (the `## Rules` read-only and asking/acting bullets unchanged in meaning).
- The best-effort task→commit mapping — `TASKS_DONE.md` entries as intent plus `git log`/`git show` as a supporting-lens, with its not-guaranteed hedge and live-file fallback — is present and unchanged in function.
- The skill's structure survives: the workflow steps (0–4) and their flow, and the `## Rules` section, are all present and unchanged in function.

---

## Neutralize Question-Skill Examples And Phrasing

Sweep the three question-handling skills that carry software/game illustrative examples — `skills/answer-open-question/SKILL.md`, `skills/discuss-open-question/SKILL.md`, and `skills/review-milestone-requirements/SKILL.md` — so their illustrative content is work-type-agnostic. Replace every software/game example (the Cinemachine virtual cameras, `RailCameraSnapper`, and `ForceCameraPosition` in the answer skill's example command, and the recurring example-question titles "Arc drive technique" and "Player input during swing" across all three) with the milestone's canonical work-type-neutral example, reused consistently across the three skills, and in the same pass neutralize `discuss-open-question`'s residual "live code / actual code / source files, scripts" phrasing. Each skill's workflow, contracts, and structure stay unchanged — only illustrative content and residual SE phrasing change.

**Notes:**
- Two recorded decisions pin the replacement — pull them directly rather than inventing. Per "Canonical example content", the recurring example-question title becomes a document-design question titled **"Getting-started section order"**, and where a worked example is illustrated it is the written-guide task "Draft the Getting Started section of the user guide". Per "Replacement example strategy", the *same* canonical example is reused consistently across all three skills — do not introduce a second neutral example.
- Concrete example locations to replace: in `skills/answer-open-question/SKILL.md`, the illustrative `/answer-open-question` command whose answer names two Cinemachine virtual cameras, `RailCameraSnapper`, and `ForceCameraPosition` (rewrite it as a document-design decision on the "Getting-started section order" question — keep the *shape* of the illustration, a concrete answer text following the `<Short Title>. <answer>` first-`.` split, but name no software/game specifics), and the `record the recommendation` redirect-guard example command (retitle its question to "Getting-started section order"). In `skills/discuss-open-question/SKILL.md`, the `/discuss-open-question Arc drive technique` example invocation (retitle to "Getting-started section order"). In `skills/review-milestone-requirements/SKILL.md`, the Short-Title illustration `e.g. "Arc drive technique", "Player input during swing"` (replace with two neutral document-design Short Titles, e.g. "Getting-started section order", "Glossary term scope").
- `discuss-open-question`'s residual SE phrasing to neutralize (grounding step): "read any source files, scripts, or design documents", "Prefer reading the actual code over reasoning from memory", and "taking in the surrounding document and code" — reframe to reading the project's real artifacts/deliverables and grounding in real project state, keeping the reason-across-whole-documents-vs-CLI-locate distinction intact. This skill is not one of the milestone's seven environment-context reader files, so do not force the enumerated environment phrase in.
- The `record the recommendation` sentinel in the answer skill's redirect-guard example is the retired exact-whole-string match — retitle only the illustrative question, and leave the sentinel phrase `record the recommendation` itself byte-for-byte intact (it is the redirect trigger, not an SE example).
- Each skill carries no verification mechanism, so the "Verification fallback" decision does not apply here; this is an examples + residual-phrasing sweep only. Do not touch these skills' step flows, contracts (the answer skill's first-`.` split and `Manual-answer:` commit; `discuss-open-question`'s recommendation/WWCYM structure and on-decision offers; `review-milestone-requirements`'s reconcile/surface/report engine and its case-insensitive-unique Short-Title rule), or their `## Rules` sections.

**Success:**
- No software/game example content remains in the three files — the Cinemachine virtual cameras, `RailCameraSnapper`, and `ForceCameraPosition` are gone from `skills/answer-open-question/SKILL.md`, and the titles "Arc drive technique" and "Player input during swing" no longer appear in any of the three skills, verifiable by reading the files.
- The replacement examples are consistent with the milestone's canonical example: the recurring example-question title reads "Getting-started section order" and any worked illustration is the written-guide ("Draft the Getting Started section of the user guide") example, with no second neutral example introduced and no concrete domain named.
- `skills/discuss-open-question/SKILL.md`'s grounding step no longer carries "actual code" / "source files, scripts" / "surrounding document and code" phrasing — it is reframed to real project artifacts — while the reason-across-whole-documents-vs-CLI-locate distinction is preserved.
- Each skill's workflow, contracts, and `## Rules` are unchanged in function: the answer skill's first-`.` split and the intact `record the recommendation` redirect sentinel, `discuss-open-question`'s alternatives/recommendation/WWCYM structure and on-decision offers, and `review-milestone-requirements`'s reconcile/surface/report engine with its case-insensitive-unique Short-Title rule all survive, verifiable by reading the files.

---

## Neutralize Recommendation And Clarification Phrasing

Sweep the four files in the recommendation-and-clarification cluster that carry lighter software-engineering phrasing — `agents/recommend-open-question.md`, `shared/recommend-procedure.md`, `skills/recommend-all-open-questions/SKILL.md`, and `skills/discuss-new-task/SKILL.md` — so their residual SE wording becomes work-type-agnostic. Neutralize the "live code", "source files", "scripts" (as a deliverable type), "bug", "during development", and "build X" phrasing to work-type-neutral equivalents (grounding in the live project and its artifacts; an issue surfaced mid-flight). Each file's contracts and boundaries stay intact — only deliverable-as-code framing changes.

**Notes:**
- This is a language sweep only — these four files carry neither a verification mechanism nor the milestone's canonical worked example, so the "Verification fallback" and "Canonical example content" decisions do not apply. None of the four is one of the milestone's seven environment-context reader files either, so the "Environment-context replacement wording" enumerated phrase must **not** be forced in — reframe to match each file's surrounding style.
- `skills/discuss-open-question/SKILL.md` appears alongside these four in the starting state's "lighter phrasing" list but is **out of scope here** — its residual "live code / source files, scripts" phrasing and its game examples are already owned by the sibling "Neutralize Question-Skill Examples And Phrasing" task. Do not touch it in this pass.
- Concrete SE phrasing to neutralize, by file: `agents/recommend-open-question.md` — "read the project's `requirements.md` and source files" (step framing), "the actual source files the question turns on", and the two "the live code" occurrences in its grounding step; `shared/recommend-procedure.md` — "the actual source files, scripts, or design documents the question turns on", "Prefer reading the live code over reasoning from memory", and "live code over memory" in the Rules grounding bullet; `skills/recommend-all-open-questions/SKILL.md` — "it reads `requirements.md` and the live code to ground its" recommendation; `skills/discuss-new-task/SKILL.md` — the frontmatter `description`'s "discovered during development", "reports a bug", and "verify the fix" wording, the `<issue description>` input line's "discovered during development" / "a single bug or a larger 'we need to build X'", the "relevant source files" grounding line, and the light "the stack and conventions" `CLAUDE.md`-read line.
- Do **not** over-neutralize the recommendation-independence phrasing "a recommendation … decides nothing, so nothing may build on it/one" (in the agent and the orchestrator) — "build on" here is generic English about not feeding one recommendation into another, not an SE reference; leave it intact.
- Preserve every contract and boundary exactly: `shared/recommend-procedure.md` stays execution-neutral and principle-aware with its alternatives (2–4 genuine options) + single-recommendation contract and its in-place read of `milestones/answer_decision_principles.md`; `agents/recommend-open-question.md` stays read-only (mutates nothing) with its `<alternative>`/`<applied-principle>`/`<recommendation>` XML sub-element rendering; `skills/recommend-all-open-questions/SKILL.md` keeps its orchestration (sole mutator, whole-block-replacement embedding), idempotency (skip blocks already carrying a `<recommendation>`), and commit behavior (`Recommendation-annotation: <milestone_id>`); `skills/discuss-new-task/SKILL.md` keeps its clarify-then-hand-off-to-`/submit-task` flow.

**Success:**
- No software-engineer-specific phrasing remains across the four files — "live code", "the actual source files"/"source files"/"source file", "scripts" (as a deliverable type), "bug", "during development", and "build X" are gone or neutralized to work-type-neutral equivalents (grounding in the live project/artifacts; an issue surfaced mid-flight), verifiable by reading all four files including `discuss-new-task`'s frontmatter `description`.
- `skills/discuss-open-question/SKILL.md` is untouched by this task (it is owned by the sibling question-skill task), and the recommendation-independence "nothing may build on it/one" phrasing is left intact in the agent and the orchestrator.
- Each file's contracts and boundaries are unchanged in function: `recommend-procedure.md` remains execution-neutral and principle-aware with its alternatives+recommendation contract; `recommend-open-question.md` remains read-only with its XML sub-element rendering; `recommend-all-open-questions` keeps its orchestration, idempotency, and `Recommendation-annotation:` commit behavior; `discuss-new-task` keeps its clarify-then-hand-off flow — all verifiable by reading the files.

---

## Neutralize Lifecycle-Skill Environment Context

Sweep the three remaining environment-context reader skills not covered by other sweep tasks — `skills/init-milestone-base-workflow/SKILL.md`, `skills/discuss-milestone-goal/SKILL.md`, and `skills/finish-current-milestone/SKILL.md` — so their environment-context wording becomes work-type-agnostic. Reword each file's "tech stack, build/test commands, MCP tools, conventions"-style environment-context phrasing per the milestone's decisions, neutralize `finish-current-milestone`'s `CLAUDE.md`-update framing (its "tech-stack or structural changes", `## Tech Stack` / `## Repository Layout`, and "game/codebase" references) to work-type-agnostic terms for lasting project changes, and replace `discuss-milestone-goal`'s game-flavored `## Usage` illustration with a work-type-neutral example. Each skill's workflow and contracts stay unchanged.

**Notes:**
- The environment-context read follows the "Environment-context replacement wording" decision: replace the SE enumeration item-for-item with the neutral enumeration "the project's domain context, working conventions, available tools, and how work is verified as done", keeping the enumerated shape. Reconcile against the exact phrasing the sibling "Neutralize Complete-Task Procedure Language" task provides. Retain the generalized `/init` pointer wherever it appears (`/init` documents any project regardless of work type).
- Concrete SE-flavored spots, by file. `init-milestone-base-workflow`: the step-4 `/init`-enrichment suggestion ("full codebase documentation — tech stack, build/test commands, file organization, and conventions… read this environment context"), the step-5 next-step `/init` line ("the project's tech stack, tooling, and conventions"), and the `## Rules` bullet "Do not document the tech stack or conventions" — the `/init` pointer must survive, generalized. `discuss-milestone-goal`: step 1's "Read `CLAUDE.md` to understand the current milestone, tech stack, and project overview." `finish-current-milestone`: the frontmatter `description`'s "lasting tech-stack or structural changes", step 4's "what now exists in the game/codebase", and step 7's CLAUDE.md-update framing ("new packages added to the tech stack, new MCP servers integrated… structural changes to the repository layout", the `## Tech Stack` / `## Repository Layout` example section names).
- `discuss-milestone-goal`'s `## Usage` illustration (`/discuss-milestone-goal add a shooting mechanic where the player can fire at enemies`) is a game-flavored SE example; replace it with a work-type-neutral goal consistent with the milestone's canonical written-guide example per the "Canonical example content" / "Replacement example strategy" decisions (e.g. a goal about drafting a user guide), naming no software/game specifics and introducing no second neutral example. These three skills carry no verification mechanism, so the "Verification fallback" decision does not apply here.
- Structural elements that must survive unchanged: `init-milestone-base-workflow`'s additive/idempotent bootstrap (detect-existing-state, the exact `milestones/README.md` scaffold, the `## Milestone Workflow` ensure logic, the non-committing "leave staging to the user" rule); `discuss-milestone-goal`'s no-file, purely-conversational role and its five-step flow; and `finish-current-milestone`'s completion-recording + pointer-clearing flow (steps 1–9, the step-8 commit `Milestone-finish: milestone_<NN>_<slug>` via `${CLAUDE_PLUGIN_ROOT}/shared/commit-procedure.md`, the step-9 terse "Milestone finished." line and its no-op variant, and the conditional-`CLAUDE.md`-in-PATHS logic).

**Success:**
- No software-engineer-specific environment-context phrasing remains in the three files — the "tech stack / build/test commands / MCP tools / file organization / conventions" enumeration wording is neutralized to "the project's domain context, working conventions, available tools, and how work is verified as done" (preserving the enumerated shape) wherever it appears, verifiable by reading the files.
- The generalized `/init` pointer is retained (not dropped) everywhere it appeared in `init-milestone-base-workflow`, reworded so it reads for any work type.
- `finish-current-milestone`'s CLAUDE.md-update framing is neutralized to work-type-agnostic lasting-change terms — the frontmatter `description`, the step-4 "game/codebase" phrase, and the step-7 `## Tech Stack` / `## Repository Layout` example section names are gone or generalized, verifiable by reading the file.
- No software/game-flavored illustration remains in the three files — `discuss-milestone-goal`'s `## Usage` "shooting mechanic" example is replaced by a work-type-neutral one (e.g. a user-guide drafting goal) consistent with the milestone's canonical example, verifiable by reading the file.
- Each skill's workflow and contracts are unchanged in function: `init-milestone-base-workflow`'s additive/idempotent bootstrap and non-committing rule, `discuss-milestone-goal`'s no-file conversational five-step flow, and `finish-current-milestone`'s steps 1–9 (verify-done, history/table append, pointer-clear to `none`, the `Milestone-finish:` commit, and the terse-reporting step with its no-op variant) all survive, verifiable by reading the files.

---

## Neutralize Public README Documentation

Sweep the plugin's public `README.md` — the adoption-focused landing page and authoritative workflow documentation — so it becomes fully work-type-agnostic. Neutralize the software-engineer-specific framing in its pitch (`## Why Cairn?`), its install/setup prose, its per-phase diagram narration, and its `## Skill reference` entries: the deliverable-as-code/codebase framing, the build/test verification wording, the "tech stack, build/test commands, MCP tools"-style environment enumeration, and any software/game illustrative content. The README's structure — the six per-phase Mermaid diagrams, the `## Skill reference` as the single home for full skill descriptions, and the install/marketplace instructions — stays intact.

**Notes:**
- Cairn's own operating mechanics are **exempt** under the milestone's "SE-specific finding boundary" decision — do **not** neutralize them: git commits and commit-subject markers, path-scoped staging, the `CLAUDE.md`/`README.md`/`milestones/README.md` file names, and the consuming project being a git repo. In particular the entire `## How skills commit` section is exempt plugin infrastructure and must be left intact, and the diagram seam node labels that name `CLAUDE.md` (e.g. D0's "milestones/ + CLAUDE.md documented") stay as-is.
- The environment-context enumeration is reworded to the milestone's neutral enumeration per the "Environment-context replacement wording" decision — "the project's domain context, working conventions, available tools, and how work is verified as done" — reconciled against the exact phrasing the sibling "Neutralize Complete-Task Procedure Language" task provides. The README's SE enumerations appear in `## Why Cairn?` ("It works with any tech stack. Skills read your project's environment (tooling, conventions, build commands) from `CLAUDE.md`"), the `## Installation` `/init` line ("document your project's tech stack and tooling"), and the "One-time setup" diagram narration ("`/init` records the tech stack and tooling"). The generalized `/init` pointer survives, reworded (`/init` documents any project regardless of work type).
- Concrete SE-framing spots to neutralize beyond the environment enumeration: the `## Why Cairn?` pitch ("Large-scale software projects fail…", "before coding starts", "moving an idea from rough goal to shipped code") and the tagline "Milestone-driven development for any stack."; the "Iterating milestone requirements" narration "fills the starting state from the codebase"; the `specify-milestone-starting-state` skill-reference entry ("explores the project…", "writes a concise technical summary"); the `ask-in-milestone-context` entry ("the actual code those tasks produced… grounding the answer in the live files and source"); and the `finish-current-milestone` entry / "Ending a milestone" prose ("lasting tech-stack or structural changes"). Reframe each to work-type-neutral deliverable/artifact terms.
- The README carries no software/game *worked* example (no Creep/RailCameraSnapper), so the canonical written-guide example is imported only if the sweep introduces or needs an illustration — do not force it in where the existing prose needs none; per "Replacement example strategy" introduce no second neutral example.
- This is a documentation sweep, not a behavioral change: every `/skill` name, the six `mermaid` code blocks and their node/edge structure, the `[Skill reference](#skill-reference)` and `[How skills commit](#how-skills-commit)` anchors, the `/plugin marketplace add uHappyLogic/cairn` install command, and the `## Self-dogfooding` and `## License` sections must survive unchanged in function.

**Success:**
- No software-engineer-specific phrasing that assumes the user's deliverable is software remains in `README.md` — "shipped code", "before coding starts", "for any stack"/"any tech stack", "tech stack and tooling", "from the codebase", "technical summary", "the actual code those tasks produced"/"live … source", and "tech-stack or structural changes" are gone or neutralized to work-type-neutral deliverable/artifact terms, verifiable by reading the file.
- The environment-context enumeration wherever it appears (the `## Why Cairn?` pitch, the `## Installation` `/init` line, and the "One-time setup" narration) is reworded to "the project's domain context, working conventions, available tools, and how work is verified as done", preserving the enumerated shape, with the generalized `/init` pointer retained.
- Cairn's own operating mechanics are left intact — the `## How skills commit` section, the commit-subject markers, path-scoped staging, the `CLAUDE.md`/`README.md`/`milestones/README.md` file names, and git-repo references are unchanged, verifiable by reading the file.
- The README's structure is intact: the six per-phase Mermaid diagrams and their node/edge structure are present and unchanged, the `## Skill reference` remains the single home for full skill descriptions, and the install/marketplace instructions (`/plugin marketplace add uHappyLogic/cairn`) are present and unchanged.

---

## Neutralize CLAUDE.md Invariant Set

Sweep the plugin's own `CLAUDE.md` — the invariant set Claude acts on when editing this repo — so it becomes fully work-type-agnostic and is reconciled against the swept behavior files. Rewrite its environment-context invariant to the milestone's neutral enumeration, neutralize the skill/agent descriptions and invariants phrased in code terms (deliverable-as-code/codebase, "live code", "insertion points", "assertion wording", "exact code"), and update any invariant whose wording a sibling sweep task changed in the behavior files so no CLAUDE.md passage contradicts them. The invariant set's structure and all workflow, commit, and reporting rules stay intact.

**Notes:**
- The environment-context invariant is the primary target: the line beginning "`specify-milestone-starting-state`, `derive-tasks`, and the shared completion procedure … read the project's environment context (tech stack, build/test commands, MCP tools, conventions) from `CLAUDE.md`. Run `/init` once…". Per the "Environment-context replacement wording" decision, replace the SE enumeration item-for-item with "the project's domain context, working conventions, available tools, and how work is verified as done", keeping the enumerated shape; retain the generalized `/init` pointer (`/init` documents any project regardless of work type). Reconcile against the exact enumeration phrasing the sibling "Neutralize Complete-Task Procedure Language" task provides.
- Code-term passages to neutralize (deliverable-as-software framing under the zero-findings bar): the skill-map line and the invariant for `ask-in-milestone-context` ("the live code those tasks produced" / "grounds answers … in the **live code**") reframe to grounding in the live deliverables/artifacts the tasks produced; the task-altitude invariant ("reconstructed from the goal + live code", "the **detailed design** (exact code, insertion points, assertion wording, decided fresh against the live codebase)", "line-by-line build detail") reframe to work-type-neutral terms (what can be re-derived at completion time against the live project/deliverable, the detailed design decided fresh); and the two lighter "live code" occurrences in the `recommend`-cluster invariants (the `recommend-open-question` subagent grounding and `shared/recommend-procedure.md`'s "grounding discipline (live code over memory)") reframe to grounding in the live project.
- Cairn's own operating mechanics are **exempt** under the "SE-specific finding boundary" decision — do **not** touch them: git commits and commit-subject markers, the `git log`/`git show`/`git log --grep` references, path-scoped staging, `git add -A` prohibitions, the `CLAUDE.md`/`README.md`/`milestones/README.md` file names, and the consuming project being a git repo. The `git log`/`git show` supporting-lens mention inside the `ask-in-milestone-context` invariant stays intact; only the *deliverable* vocabulary around it changes.
- Reconcile — do not contradict — the swept behavior files: the completion procedure's verification reframe (CLAUDE.md must not reassert a build/test verification mechanism), the canonical written-guide example, and the neutral agent personas. CLAUDE.md carries no `Creep`/`RailCameraSnapper` worked example, so no example replacement is needed here — only ensure no passage re-implies a software deliverable.
- This is a language + reconciliation sweep only. The invariant set's structure, every workflow/commit/reporting rule, all commit-subject markers, and every skill/agent behavioral contract must survive unchanged in meaning — only work-type framing changes.

**Success:**
- No software-engineer-specific phrasing that assumes the user's deliverable is software remains in `CLAUDE.md` — the environment-context "tech stack, build/test commands, MCP tools, conventions" enumeration, "the live code those tasks produced", "the **live code**", "exact code, insertion points, assertion wording, decided fresh against the live codebase", "line-by-line build detail", and the "live code over memory" phrasings are gone or neutralized, verifiable by reading the file.
- The environment-context invariant enumerates "the project's domain context, working conventions, available tools, and how work is verified as done" (preserving the enumerated shape), with the generalized `/init` pointer retained.
- Cairn's own operating mechanics are left intact — git commit/commit-subject/`git log`/path-scoped-staging references, `git add -A` prohibitions, the `CLAUDE.md`/`README.md`/`milestones/README.md` file names, and git-repo references are unchanged, verifiable by reading the file.
- No CLAUDE.md passage contradicts the swept behavior files — nothing reasserts a build/test verification mechanism, and no invariant re-implies a software deliverable.
- The invariant set's structure and all workflow, commit-subject, reporting, and skill/agent behavioral rules are present and unchanged in meaning, verifiable by reading the file.

---

## Run Independent Zero-Findings Re-Audit

Run the milestone's final independent re-audit of the whole plugin — the success proof. Dispatch a single fresh-context subagent given only (a) the decided finding criteria and (b) the full file set to audit, with no access to this sweep's reasoning, and collect its structured output. This is the milestone's success-proof mechanism per the "Re-audit execution form" and "Re-audit task success wording" decisions; it audits, it does not fix.

**Notes:**
- The subagent must be given only two inputs and nothing about how the sweep was done (independence is the whole point). Input (a) — the finding criteria, taken verbatim from the "SE-specific finding boundary" decision: a finding is any text assuming the user's own deliverable or domain is software engineering (personas, "the code/codebase," insertion points, build/test verification, code examples, and the like), applied as the generalizable "does this assume the user's deliverable is software?" test to cases the initial audit did not foresee; with Cairn's own operating mechanics exempt as domain-uniform plugin infrastructure — git commits and git-log greps, path-scoped staging, the `CLAUDE.md`/`README.md`/`milestones/README.md` file names, and the consuming project being a git repository. Input (b) — the full file set: everything under `skills/`, `agents/`, `shared/`, plus root `README.md` and `CLAUDE.md`.
- The subagent returns a structured per-finding list — each finding giving the file path, the location, the offending phrase, and which criterion/layer it violates — or an explicit zero-findings verdict when it finds nothing. Report that structured output.
- Success is that the audit ran and its structured output was delivered — the per-finding list or the explicit zero-findings verdict — **not** that the verdict is clean. Do not fix findings within this task and do not author the next round here: on findings, follow-up fix tasks plus a fresh re-audit task are appended after this one (that queuing is separate work), iterating until a run comes back clean; that final clean verdict is the milestone's zero-findings proof.

**Success:**
- A single fresh-context subagent was dispatched with only the two inputs — the decided finding criteria (the "SE-specific finding boundary" test and its exemptions) and the full file set (`skills/`, `agents/`, `shared/`, `README.md`, `CLAUDE.md`) — and no access to the sweep's reasoning.
- The subagent's structured output was produced and reported: either a per-finding list (each entry carrying file path, location, offending phrase, and the criterion/layer violated) or an explicit zero-findings verdict.
- No findings were fixed and no follow-up tasks were authored within this task — its deliverable is solely the delivered audit output, whatever the verdict.

---
