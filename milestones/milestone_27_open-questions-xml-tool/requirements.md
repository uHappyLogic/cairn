# Milestone 27: Open Questions XML Tool

## Goal

Split the `## Open questions` section out of `requirements.md` into a sibling `<MILESTONE_DIR>/open_questions.xml` — a well-formed XML document under one root element, created empty by `define-milestone-goal`, whose "no `<open-question>` child remains" state is the convergence verdict and the `derive-tasks` precondition — and ship one stdlib-only Python tool inside the plugin (rendered into every host tree, invoked via `{{PLUGIN_ROOT}}`, and the file's sole writer) that owns every deterministic operation over that document: list, locate, add, lift, remove, embed with fragment validation replacing the seven-test acceptance gate, `<depends-on>` reconciliation in exact-id mode with transitive strip, dependent-strip on prune, and the answer sweep's dependency-graph walk. Rewire every skill, agent, and shared procedure that touches open questions from the `awk`/`sed`/`grep` boundary-line idiom onto tool invocations so that runtime prose keeps only judgment (authoring text, significance ranking, judgment-mode reconciliation for literal answers) and the entity-escaping, indentation, and whole-block-replacement contracts become the tool's internals — the aim being maximal token efficiency — while `capture-milestone-principle-updates` keeps its diff-line reconstruction, repointed at the new file. Give the tool a pytest suite run under `uv` in CI beside the drift gate, document Python 3.9+ as a prerequisite checked once by `init-milestone-base-workflow`, sync `CLAUDE.md`, `README.md`, and `docs/`, and provide no migration of or compatibility with pre-split milestones.

## Relevant starting state

### The `<open-question>` block and where it lives

Open questions are raw XML blocks under a single `## Open questions` section of `<MILESTONE_DIR>/requirements.md`, beside the four prose sections. `define-milestone-goal`'s template seeds `## Goal`, `## Relevant starting state`, `## Decisions`, and `## Out of Scope` only; `review-milestone-requirements` creates the section the first time it authors a block, so on disk it sits last, after `## Out of Scope`, and its empty heading survives once every block is answered. Every one of the 27 milestone directories holds exactly `requirements.md`, `TASKS_TODO.md`, and `TASKS_DONE.md`; 24 of the `requirements.md` files carry the heading and none carries a live block, so the annotated shape exists only in git history (for example the removed lines of `1cdddec`, `Recommendation-answer: Distribution badge form`). An authored block is three lines — a single-line `<open-question id="Short Title">` opening tag at the base column, one `<question>` child at a 2-space indent, `</open-question>` — and the recommend sweep grows it in a fixed child order: `<alternative id="…">` (what-it-is text, then `<advantage>` and `<drawback>`), `<applied-principle>`, self-closing `<depends-on question="…" option="…"/>`, and one `<recommendation option="…">`. Attribute values and element text are escaped with the five predefined entities, ids are compared case-folded after un-escaping, and no file declares a root element, an XML prolog, or a schema.

### The boundary-line CLI idiom and its runtime restatements

No runtime file invokes an executable today: `core/` holds Markdown only (`find core -type f ! -name '*.md'` returns nothing). Every deterministic operation over the question set is instead prose telling the runner to drive `awk`/`sed`/`grep` over the `<open-question …>`/`</open-question>` boundary lines and never `xmllint`, restated in each consumer: `core/shared/answer-procedure.md` (locate by `id="([^"]*)"`, re-query after the fold, line-range removal, dependent reconciliation with transitive strip), `core/shared/answer-with-recommendation-procedure.md` (locate and lift `<recommendation>`), `answer-open-question-with-alternative` (locate and lift an `<alternative>`), `discuss-open-question` (locate, then read the whole document), `recommend-all-open-questions` (gather once, the seven-test acceptance gate over the returned region with its eight reason strings and one-slot repair template, embed by whole-block-replacement `Edit` at 2-space indents), `answer-all-open-questions-with-recommendation` (gather in document order, drop unresolvable edges, depth walk with origin promotion, per-question re-check that also lifts the commit body), `review-milestone-requirements` (author, prune, dedup, transitive dependent strip), and `derive-tasks` (its precondition is a scan for any `<open-question` line, and its stop message lists the Short Titles). The five-entity un-escaping rule (`&amp;` last) is spelled out in six of those files. The `recommend-open-question` agent renders the child elements itself, indents them, escapes them, and self-checks the two boundary shape tests; the `answer-open-question-with-recommendation` agent stages `<MILESTONE_DIR>/requirements.md` path-scoped and returns bare `DONE`/`FAILED`.

### Indirect readers of the question set

`core/shared/recommend-procedure.md` reads `requirements.md` for the sibling blocks' embedded recommendations and the principle store `milestones/answer_decision_principles.md` in place. `ask-in-milestone-context` lists "any remaining open questions" among what it reads from `requirements.md`, `modify-milestone-goal` reports which open questions a goal change settles, opens, or moots, and `submit-task` and `discuss-new-task` only point at `/discuss-open-question`. `finish-current-milestone` and `goto-next-milestone` never read a question, and `goto-next-milestone` detects a defined milestone by its directory alone.

### Commit and provenance contracts on the question path

`core/shared/commit-procedure.md` takes an explicit PATHS set (guarded by `git status --porcelain -- <PATHS>`, staged by name, never `git add -A`), and every question skill passes `<MILESTONE_DIR>/requirements.md` as its sole path, under `Requirements-review:`, `Recommendation-annotation:`, `Manual-answer:`, `Recommendation-answer:`, and `Alternative-answer:` subjects. `capture-milestone-principle-updates` walks `git log -E --grep='^(Manual-answer|Alternative-answer|Recommendation-answer): ' -- milestones/<milestone_id>/requirements.md`, reads each commit with `git show <hash> --format='%s%n%n%b' -- milestones/<milestone_id>/requirements.md`, and reconstructs the answered block from the diff's removed lines between `-<open-question id="…"` and `-</open-question>` while reading the recorded decision from the added lines; it also treats pre-block-form blockquote answers (`> **Deferred — …:**`) as the no-recommendation case.

### The host build and what a host tree can carry

`scripts/build_hosts.py` walks every file under `core/`, applies `{{PLUGIN_ROOT}}` substitution and `prose_drop_patterns` to every UTF-8 text file whatever its extension, and lands each top-level `core/` directory where the host's `[layout]` maps it — both definitions map exactly `skills`, `agents`, `shared`, a file at the top of `core/` is a `BuildError`, and a layout that does not name every top-level directory fails validation. Its checks run over every rendered file: `unfilled-placeholder` fails on any `{{` not preceded by `$` (so a Python f-string's `{{` would fail), `host-name-in-core` fails on `claude` or `antigravity` as a whole word anywhere in `core/`, `dangling-plugin-root` requires every `<plugin_root>/<path>` (path characters `[A-Za-z0-9_./-]`) to name a file in the rendered tree, and the frontmatter and description checks apply to `skills/<name>/SKILL.md` and `agents/<name>.md` only. The plugin-root literals are `${CLAUDE_PLUGIN_ROOT}` and `.agents/plugins/cairn`, rendered into runtime prose as `${CLAUDE_PLUGIN_ROOT}/shared/answer-procedure.md` and `.agents/plugins/cairn/shared/answer-procedure.md`. `hosts/claude/` (37 files) and `hosts/antigravity/` (36 files) pass `--check` at version 1.5.1; the release skill stages `hosts/` whole, and `CLAUDE.md` enumerates the tree's contents as skills, agents, shared, plus exactly four non-plugin files.

### Python tooling, CI, and the test surface

`pyproject.toml` declares `cairn-tooling` with `requires-python = ">=3.11"` (the build imports `tomllib`), `pyyaml` as its one dependency, and `[tool.uv] package = false`; `.python-version` pins 3.13 and `uv.lock` holds only `cairn-tooling` and `pyyaml`. No `tests/` directory, no `pytest` reference, and no test runner exist anywhere in the repository — the build's validation gate is the only automated check. `.github/workflows/drift-gate.yml` is one job on unfiltered `push` and `pull_request` with `permissions: contents: read`, `actions/checkout@3d3c42e…` (v7.0.1) and `astral-sh/setup-uv@bec219d…` (v10.1.0) pinned by SHA, and the single step `uv run scripts/build_hosts.py --check`; `CONTRIBUTING.md`'s Development section documents those `uv run` commands as the whole build loop. On this machine `uv run` resolves to CPython 3.13.5 under uv 0.11.14; no Python 3.9 interpreter is installed, though uv lists `cpython-3.9.25` and `3.11.15` as downloadable.

### Prerequisites and the bootstrap skill

No document names a runtime prerequisite for the installed plugin: `README.md`'s `## Installation` gives the Claude Code marketplace commands, the Antigravity `curl … | tar -xz` extraction into `.agents/plugins/cairn`, and the bootstrap step, and both distribution README templates under `scripts/hosts/<host>/README.md` repeat the same sections; `uv` appears only in maintainer text. `init-milestone-base-workflow` probes just three things — `milestones/`, `milestones/README.md` with a `Current milestone:` line, and a `## Milestone Workflow` section in `CLAUDE.md` — creates what is missing, stops when all three exist, commits nothing, and runs no environment check; its `milestones/README.md` template describes `requirements.md` as "goal, relevant starting state, decisions, open questions", the line this repository's own `milestones/README.md` carries.

### Documentation surfaces that state the current design

`CLAUDE.md` (mirrored by the `AGENTS.md` symlink) mentions `open-question` 33 times and `boundary-line` 7 times: the "Milestone file structure" section says each milestone contains exactly three files, and the review, answer-procedure, recommend-sweep, answer-sweep, alternative-answer, capture, and dispatched-agent-return invariants each spell out the CLI idiom, the seven-test gate, or the whole-block-replacement embed. `README.md` repeats the three-file list under `## How it works` and, under `## Design principles`, claim 11's line that blocks are "found by their boundary lines, so every answer, cascade, and prune is a deterministic line-range edit"; `docs/design-claims.md` claim 11 gives that as its **Design** with a golden-file-diff **Metric**; `docs/workflow.md`'s "Iterating milestone requirements" paragraph states the query-where-it-pays convention (`awk`/`sed`/`grep`, never `xmllint`); and `docs/skill-reference.md` carries the entries for the eight question skills and agents that describe the same mechanics.

## Decisions

### `open_questions.xml` document shape

- The root element of `open_questions.xml` is a bare `<open-questions>` — no XML declaration and no attributes. The empty file `define-milestone-goal` creates is that one empty root, and every `<open-question>` block sits one level inside it exactly as it stood under the old heading. An undeclared document is UTF-8 by the XML spec and stdlib ElementTree reads and writes it that way by default, the directory already names the milestone, and a declaration or root attribute stays available later as an additive change no reader breaks on.

### Tool placement in the plugin tree

- The Python tool lives in a new top-level `core/tools/` directory, and both host definitions add `tools = "tools"` to their `[layout]` so it renders to `{{PLUGIN_ROOT}}/tools/` on every host. A program the runner executes is a different artifact kind from a procedure it follows, and the plugin keeps one top-level directory per kind, so this fourth directory keeps the `core/shared/*.md` procedure rules exact, gives the tool and any test layout beside it a home with rules of its own, and costs one build-validated line in each host `[layout]`. The name is `tools` rather than `bin` or `scripts` because it names what the runner does with the contents, reads as plain English, and overloads no directory the repository already reserves for maintainer build tooling.

### Tool interpreter command

- Every invocation site runs the tool as `python3 {{PLUGIN_ROOT}}/<tool path> …` — one fixed, deterministic command with nothing restated per site — and the once-per-project bootstrap check in `init-milestone-base-workflow` executes `python3` to read its version, probing `python` only when that fails so its message can say the interpreter exists under the other name and must be exposed as `python3`. `python3` is the name PEP 394 guarantees and the one the stock macOS and Debian-family shells the agent usually runs in actually have; the platforms where only `python` exists are exactly what the bootstrap check is for, and probing the second name there — asking the user to expose it as `python3` — buys a per-site probe's coverage at no per-site cost. No invocation site carries a fallback expression, and the runtime prose never consults `python`.

### Tool file resolution

- Every tool invocation takes the already-resolved `<MILESTONE_DIR>` as an argument, and the tool joins it with the one file name it owns, `open_questions.xml`; the caller resolves the milestone exactly as it does today through `get-current-milestone.md`, and the tool never reads `milestones/README.md`. The tool sits below the skills exactly where the shared procedures do, and that layer takes resolved inputs from its caller — every question skill already holds `<MILESTONE_DIR>`, `define-milestone-goal` must write a milestone the pointer does not name, and a pytest case then needs only a `tmp_path` — so passing the directory keeps one pointer parser in the plugin and one file name inside the tool. The argument is the directory rather than the document path because the goal already binds the tool to `<MILESTONE_DIR>/open_questions.xml` and its creation, so the shorter argument the prose already carries wins.

### Tool output and error contract

- Every read prints only the un-escaped value the runner needs, one item per line: set queries (list, the dependency walk) print bare ids in document or walk order, locate prints the block verbatim as the file holds it, and lift prints the recombined answer text (`<option> — <rationale>` or `<id> — <what-it-is>`). Every mutator (create, add, embed, remove, reconcile) prints nothing on success. Every failure is exactly one `Error: <reason>` line on stderr with exit status 1 (a malformed invocation keeps argparse's native usage message and exit 2), a non-zero exit guarantees the document is byte-for-byte unchanged, no traceback ever reaches the runner, and an empty set is empty stdout with exit 0 — emptiness is data, never a failure. The runner is an LLM reading a console, so the contract that costs it the fewest tokens per call and the least decoding is bare un-escaped values on stdout and nothing at all from a mutator, with failure carried by the two signals every process already has; that reason line fills the repair template's slot verbatim, a failing lift is the answer sweep's re-check and skip in one call, an empty set staying exit 0 means no consumer ever mistakes a converged document for an error, and which checks and mutations the tool exposes may still change without changing the form of what is printed — which is what rules out a verdict token that would only duplicate the exit status a process already has.

### Tool text input channel

- `add` and `embed` keep their short identifiers (the Short Title among them) as ordinary arguments and read their one free-text body — the question text, the returned fragment — from standard input, which every runtime invocation site supplies as a quoted-delimiter heredoc (`<<'EOF' … EOF`); the tool reads `sys.stdin.buffer` and decodes it as UTF-8 itself, refuses a terminal stdin with an error so a call that forgot its heredoc fails instead of blocking, and reads exactly one body per call. There is no path argument and no `--from` flag: `< file` redirection and `subprocess.run(input=…)` give a saved file and the pytest suite the same entry at no code cost, so a second channel would be code without a consumer. The body is the one input that is prose or XML rather than an identifier, so it travels by the channel that carries bytes untouched — a quoted heredoc passes a multi-line fragment full of backticks, `&apos;`, and `${…}` verbatim with nothing expanded, in one shell call and with no per-call escaping by the runner, the same idiom the coding agent's shell tool already relies on for multi-line content — and the channel is indifferent to whether `embed` receives the agent's whole message or only the extracted region, because both travel the same way.

### Clearing a stale recommendation

- The tool gains a `strip` subcommand that takes one or more Short Titles beside the milestone directory every other subcommand takes and, for each named block, deletes every child between its `<question>` element and its `</open-question>` line — the `<alternative>`, `<applied-principle>`, `<depends-on>`, and `<recommendation>` children — leaving the wrapper and `<question>` intact; it touches no dependent and no `<depends-on>` tag that names the block, rejects an unknown id with the document unchanged, and does nothing on a block already bare. Clearing a stale recommendation is that one call followed by a re-run of `/recommend-all-open-questions`, whose skip rule regenerates the now recommendation-less block. There is no hand-edit exception — the on-disk document is always the tool's own output, with no caveat on the sole-writer clause anywhere it is stated — and no skill wraps the subcommand: a skill around it is an additive step to take only if the hatch, unexercised in every commit to date, ever becomes routine. The strip is the same per-block primitive the tool already performs internally for the transitive strip in `<depends-on>` reconciliation and the dependent-strip on prune — one behaviour serving the reconciliation walk transitively and the user one block at a time — so exposing it costs an argparse entry and a test, and in return the user gets an id-validated one-line call in place of an eye-located multi-kilobyte edit on the one file whose escaping and indentation are tool-internal, and a half-deleted block the tool would refuse at every later call cannot arise.

### Reconciliation on removal

- `remove` takes an optional `--option <recorded option>` and, after deleting the named block, reconciles that block's dependents in the same write: with the option, a surviving block whose `<depends-on>` names the removed id with an equal un-escaped, case-folded option loses that one tag and every other dependent of the removed id is stripped; without it — a mooted sibling, a review prune or dedup — every dependent is stripped; both transitively over the dependents of stripped blocks, before the single write. There is no separate `reconcile` subcommand. Every removal the plugin performs is followed by reconciliation and none ever skips it, and a removal with no recorded option is exactly the strip-every-dependent case under strip-on-doubt, so the one optional flag covers the answered block, a mooted sibling, a prune, and a dedup with one call and one write each and makes "no removal leaves a dangling tag" hold by construction; reconciling one removed block per call in any order reproduces the set-at-once outcome of the reconciliation step because a strip is monotone and subsumes the tag drop, whereas a separate `reconcile` would break the tie on its second call buying nothing any caller wants while adding a forgettable step whose omission is silent and lets the answer sweep record a dependent's stale recommendation. The literal-answer path's judged-agreeing dependents are the one case left to route, on top of this strip-all default.
- The literal-answer path routes that case through the same flag and the tool gains nothing for it: the recording core's judgment mode collapses to one verdict per answer — whether ANSWER plainly settles on one of the answered block's own `<alternative id>` values — and the procedure passes that id as `remove … --option`, so the exact-id engine untags the dependents that assumed it and strips the rest; an answer that settles on no listed alternative (or a hand-cleared target with none) passes nothing, which is the bare `remove` and its strip-all default. The tool rejects an `--option` naming none of the removed block's alternatives, so a mistyped judgment fails loudly with the document unchanged rather than stripping silently, and the core's judgment-mode contract is restated as judging one settled id from the block's alternatives — a judgment made in prose against the closed set the runner already holds, never a parse of ANSWER, a line an editor must keep drawn. A dependent's tag records an option, not a claim about that dependent, so every per-dependent verdict the literal path could form is the one verdict of whether the answer settles on that option — exactly the comparison `remove --option` already makes — which gives one engine and one write for every answer path, makes a literal answer reconcile exactly as a lifted one would on the same document, and needs no dependent enumeration, no new flag, and no multi-call window; the one thing per-dependent verdicts could add — keeping a dependent whose assumed option the answer only arguably preserves — is precisely the doubt case strip-on-doubt says to strip, which is what rules out a repeatable `--agree` flag on `remove` and per-dependent `untag`/`strip` primitives applied around the removal alike. The accepted cost is that an answer agreeing in substance but matching no listed id — a refinement, a partial answer, or a target regenerated with renamed alternatives after the tags were embedded — strips every dependent, a re-dispatch each.

### Capture's per-commit read

- `capture-milestone-principle-updates` keeps its log filter on `open_questions.xml` alone and reads each answer commit with one `git show` that names both files — `-- <MILESTONE_DIR>/open_questions.xml <MILESTONE_DIR>/requirements.md` — so the removed lines of the `open_questions.xml` hunk feed the block reconstruction and the added lines of the `requirements.md` hunk fill `recorded_decision` exactly as today; the per-commit record, the `Manual-answer:` agreement test with its "the body or the added `## Decisions` entry" fallback, and the fresh-option best guess stay unchanged for one added pathspec on one command. The recording core writes the decision into `## Decisions` by contract and the one-commit-=-one-answer invariant carries that fold beside the block removal in the same commit, so the `requirements.md` hunk is the only source that still holds the decision itself on the deliberated `Manual-answer:` path, whose body is reasoning by contract and whose `agrees`/`overrides` class sets the reinforcement shield. A body-alone read would weigh a wrongly shielded or unshielded store entry against about a fifth of a read the removed block dominates anyway, and a provenance-conditional second pathspec would save a paragraph per commit at the cost of the first per-provenance rule in a diff read that has kept every rule it carries uniform, so one uniform show wins.

### CI Python versions for the tool's test suite

- CI runs the tool's pytest suite twice in one job and never as a matrix: a pinned run, `uv run pytest` in project mode under the interpreter `.python-version` names (3.13 today) against the pytest the locked `dev` group serves, and a floor run, `uv run --no-project --python 3.9 --with pytest pytest`, which bypasses the build tooling's `requires-python = ">=3.11"` (the stdlib-only tool needs nothing from that project) and resolves the last pytest that supports 3.9. `pyproject.toml`, `uv.lock`, and the build scripts stay untouched; the pinned run carries no version literal, so a pin bump is one file, and the floor run's literal `3.9` is the floor's third home beside the README prerequisite and the bootstrap check. The 3.9 floor is a promise the README makes and the bootstrap check enforces, so CI must run the suite under it or the promise is checked by nothing — and the tool is written on the pinned 3.13, exactly where a `match` or an `X | None` slips in unnoticed and first fails at a user's stock 3.9.6 after bootstrap has passed them. Because stdlib availability is monotone across minors, the two bracketing runs catch every too-new-for-the-floor and removed-by-the-pin slip, whether the suite imports the tool or drives it through `sys.executable`, which is what rules out a five-leg matrix whose interior runners could fail only on a version-specific stdlib bug; lowering the project floor falls out because it rewrites build tooling for an interpreter it never runs under. Two pytest majors are in play (8.4 under 3.9, 9 under 3.13 today), so the suite stays on the API both share.

### `embed` extraction ownership

- `embed` accepts the recommend agent's whole final message on stdin and extracts the fragment itself — slicing from the first `<alternative` line through the last `</recommendation>` line before it parses and validates, discarding the text outside that region, a slice that is the identity on a clean return — and the orchestrator keeps only the last-line verdict: a return whose last non-whitespace line begins `FAILED:` is skipped unrepaired, as at every dispatch site, and every other return is piped to `embed` whole. Extraction is a deterministic line-range operation keyed on the fragment's own boundary tokens and, on the milestone-19 evidence, the common case rather than the exception, so it belongs in the tool — a short slice before the parse `embed` performs anyway, the identity on a clean return so it needs no mode, pytest-covered, and the source of every repair reason, the two extraction misses included, as one `Error:` line from one place — while the last-line `FAILED:` verdict stays with the orchestrator because it is agent protocol shared with the two tool-less dispatch sites and its outcome (skip, never repair) is a control decision only the orchestrator takes, which is what rules out handing the tool the whole judging pipeline. The surrounding prose the orchestrator re-emits is a paragraph against a multi-kilobyte fragment, far cheaper than a line-range rule kept in prose.

### Canonical serialization format

- The tool owns a small fixed-shape serializer and re-renders the whole document from its parsed tree on every write, so the same tree always writes the same bytes whatever the input looked like — the recommend agent's fragment, a hand-edit, or an older write. The layout is one element per line at exactly 2 spaces per depth: the bare root at column 0, each `<open-question>` at 2 with no column-0 exception for the blocks (every raw-line reader keys on the tag, not the column), its `<question>`, `<alternative>`, `<applied-principle>`, `<depends-on …/>`, and `<recommendation>` children at 4, and inside an alternative the what-it-is text on its own line above `<advantage>` and `<drawback>` at 6; every text value is folded to one line (whitespace runs and newlines collapsed to a single space, ends trimmed), so no text value can ever carry a line break; self-closing tags are written `/>` with no space; and all five predefined entities (`&amp;`, `&lt;`, `&gt;`, `&quot;`, `&apos;`) are substituted in element text and attribute values alike. The guarantee the format question asks for exists only if the writer normalizes, and the stdlib pretty-printer cannot render the mixed-content `<alternative>` one element per line, so the tool must own the serializer in any case; owning it keeps every child one greppable line for git diffs and capture's removed-line reconstruction, makes the agent's own indentation and escaping stop being load-bearing because `embed` parses the fragment and re-renders it, and gives `write(read(x)) == x` on a golden file as the whole format test. The five-entity rule wins over minimal escaping because it is one rule for text and attributes, the convention every consumer already reverses (`&amp;` last), and the raw file's readers — the tool, the agent, and `git diff` — pay nothing for it; the accepted cost is that every apostrophe in prose is stored as `&apos;`.

### Test suite location

- The pytest suite lives in a repository-root `tests/` directory beside `scripts/` and `pyproject.toml` — one `tests/test_*.py` module per concern plus its golden `open_questions.xml` fixtures — and imports the tool from `core/tools/` through a `pythonpath = ["core/tools"]` line in `[tool.pytest.ini_options]`, with `testpaths = ["tests"]` beside it and pytest in a `dev` dependency group, so `uv run pytest` at the root, locally and as the CI step beside the drift gate, runs it with no path argument. `core/` is by contract the runtime layer and nothing else, and every rule the build enforces over it — the host-name scan that reads excluded files too, the layout that must map each top-level directory, the render checks — is written for runtime prose, so a test corpus under it would inherit those rules for nothing, while adjacency buys no import simplicity (a `tests/` subdirectory beside the tool needs the same `pythonpath` line as a root one); the suite's configuration already lives at the root `pyproject.toml`, making a root `tests/` the conventional pair and the one home a later build-script test could join, and the tie against an excluded in-core suite breaks on golden fixtures: 43 of this repository's 197 answer commits remove block lines that name a host, and only outside `core/` may such a block copied from history serve as a fixture as it stands. The location is indifferent to the CI interpreter arrangement, since `uv run` reads the root project from the working directory whichever directory holds the tests.

### Empty document creation

- `define-milestone-goal` writes its three Markdown files as today and creates `open_questions.xml` through one invocation of the tool's `create` subcommand with the new milestone directory — a silent mutator that writes the serializer's own empty document (the one empty root element), refuses an existing file with the document untouched, and whose output joins the skill's path-scoped commit as a fourth path; the skill runs the tool call first, so a missing interpreter leaves nothing behind. The goal makes the tool the file's sole writer and the owner of its on-disk format, so the empty document is the serializer's to define and to write: one `create` call keeps that form in exactly one place, pinned by a test, with a converged document byte-identical to a fresh one and the sole-writer guarantee exception-free from the first byte, as the `strip` subcommand already keeps it at the other end of a block's life. The skill pays one invocation and one ordering rule, while the interpreter dependency a fixed template would spare it is only deferred to the next skill in the pipeline — which is what settles it against a template that is smaller to write but a second, untested copy of the format; scaffolding the whole milestone through the tool falls out on scope, since the goal binds the tool to this one document.

### Read access to `open_questions.xml`

- Read-only consumers that need the whole question set — the recommend agent's grounding, `discuss-open-question`'s context step, `ask-in-milestone-context`, and `modify-milestone-goal`'s impact analysis — read `<MILESTONE_DIR>/open_questions.xml` directly with the file-reading tool, exactly as they read `requirements.md` beside it, while every deterministic operation (list, locate, lift, the dependency walk) and every write stays a tool invocation: the tool is the file's sole writer, not its sole reader. A whole read that feeds judgment is neither a deterministic operation nor a write, so it stays in prose as one file-read call with a bare path, and because every write is the tool's canonical re-render, the file's bytes are exactly what a tool read-back would print — routing the read through the tool could add only a process and a longer command line. The consumers that need less than the whole set use the tool's `list`, and each read site carries the one rule that a whole read is for reasoning only while every locate, list, or lift goes through the tool, which is what settles it against a digest subcommand the two readers needing embedded alternative ids could not use.

### Prerequisite check failure mode

- `init-milestone-base-workflow` runs its interpreter check first, ahead of the state detection, and when no Python 3.9+ interpreter answers it stops before any write with a full explanatory message — what it looked for, what it found, the install or expose-as-`python3` remedy, and that a re-run completes the bootstrap — having created nothing, so the scaffold is written only once the check passes; a re-run on an already-bootstrapped project gets the same check. The scaffold exists only to enable the skills that follow, and with the tool creating the empty document the very next one, `define-milestone-goal`, invokes it, so a scaffold written without an interpreter is unusable from its first step and the goal's checked-once clause holds only if the one check enforces — a warning would move the failure to a raw shell error inside a skill kept check-free by design; the stop costs one re-run of an idempotent, non-committing skill and nothing else, its message carries the remedy the probe identified, and it is the clean-stop-and-point idiom every unmet precondition in the plugin already uses, which is what settles it against a notice-and-confirm whose proceed could only yield a scaffold nothing can use.

## Out of Scope

## Open questions

<open-question id="Tool module name">
  <question>What is the file name of the Python tool under `core/tools/` that every `python3` invocation site and the pytest suite&apos;s import name?</question>
  <alternative id="open_questions.py">
    Name the module after the document it owns — `core/tools/open_questions.py`, run as `python3 {{PLUGIN_ROOT}}/tools/open_questions.py &lt;subcommand&gt; &lt;MILESTONE_DIR&gt; …` at every site and imported by the suite as `import open_questions`.
    <advantage>One spelled-out name then runs through the whole family — the `open_questions.xml` document, the `&lt;open-question&gt;` element, the `*-open-question` skills, the tool, and the `tests/test_*.py` modules that import it — so every invocation site and every test reads as plain English with nothing to decode, and the stem is both a valid Python identifier and inside the build&apos;s `[A-Za-z0-9_./-]` plugin-root path class, so `import open_questions` and the `dangling-plugin-root` check over every site work as they stand.</advantage>
    <drawback>The tool shares its stem with the data file it is sole writer of, so prose naming either must carry the extension to say which is meant, and every call spends about one token more than a two-letter abbreviation would.</drawback>
  </alternative>
  <alternative id="oq.py">
    Abbreviate to `core/tools/oq.py` — `python3 {{PLUGIN_ROOT}}/tools/oq.py …` at every site and `import oq` in the suite.
    <advantage>The shortest command line and import the goal&apos;s token-efficiency aim could buy, saving a token or two on each invocation site and on every call the runner makes.</advantage>
    <drawback>The plugin has no established abbreviation for open questions, so `oq` is a legend the runner, a skill reader, and a `test_oq.py` reader must each carry or be told; the saving is one token per call against multi-kilobyte skill files, while the goal&apos;s efficiency comes from removing restated procedure from the prose, not from shaving a file name.</drawback>
  </alternative>
  <alternative id="questions.py">
    Drop the qualifier and name the module `core/tools/questions.py`, imported as `import questions`.
    <advantage>Shorter than the full stem while staying plain English, and unambiguous by directory since `core/tools/` holds nothing else.</advantage>
    <drawback>Bare &quot;questions&quot; is the wrong noun in this vocabulary: the `&lt;question&gt;` element is the text inside a block, the tool&apos;s own contract is phrased against it (`strip` deletes every child between the `&lt;question&gt;` element and the closing line), and every other artifact keeps the qualifier — `open_questions.xml`, `&lt;open-question&gt;`, `answer-open-question` — so `questions.strip(…)` reads as an operation on question text.</drawback>
  </alternative>
  <alternative id="manage_open_questions.py">
    Follow the verb_object form of the repository&apos;s two existing Python files (`build_hosts.py`, `set_version.py`) by putting a verb in front of the stem — `core/tools/manage_open_questions.py` or a synonym.
    <advantage>Matches the only Python file-naming precedent in the repository, so a maintainer browsing `scripts/` and `core/tools/` sees one convention.</advantage>
    <drawback>The tool has no one verb — it creates, lists, locates, adds, lifts, embeds, strips, removes, and walks — so any verb chosen is generic filler that names nothing the reader could not get from the directory, and it lengthens every invocation site and import for it; the `scripts/` precedent is one-action maintainer scripts of a different artifact kind, which the placement decision deliberately keeps apart.</drawback>
  </alternative>
  <applied-principle>Name by distinctive function</applied-principle>
  <applied-principle>Drop-vs-replace by ambiguity</applied-principle>
  <recommendation option="open_questions.py">The tool&apos;s distinctive responsibility is being the sole writer of one document, `open_questions.xml`, so its name is that document&apos;s stem: `open_questions.py` keeps the one spelled-out name the whole family already uses (`open_questions.xml`, `&lt;open-question&gt;`, `*-open-question`), is a valid identifier the suite imports directly, sits inside the build&apos;s plugin-root path class so the `dangling-plugin-root` check verifies every site, and reads as plain English at every invocation site and in every test with no legend; the verb form adds generic filler for a tool that creates, lists, locates, adds, lifts, embeds, strips, removes, and walks under no one verb, the bare noun drops the qualifier that separates the block from its `&lt;question&gt;` text, and the abbreviation&apos;s one-token saving per call is what breaks the tie against it — the goal&apos;s efficiency comes from removing restated procedure from the prose, not from a file name nobody can read back; the name is indifferent to how the still-open Build handling of tool files question settles, since a file name is a path the check resolves, not content the build renders.</recommendation>
</open-question>
<open-question id="Build handling of tool files">
  <question>Does the host build render the Python files under `core/tools/` through the same placeholder substitution, prose-drop patterns, unfilled-placeholder check, and host-name scan it applies to Markdown — so the tool source must avoid `{{` and host names — or does it copy and check them under different rules?</question>
  <alternative id="Uniform render and check">
    The build treats `core/tools/*.py` exactly as it treats every other `core/` file today — `{{PLUGIN_ROOT}}` substitution, `prose_drop_patterns`, the frontmatter strip (a no-op on a file that opens with no `---`), every rendered-tree check (`unfilled-placeholder`, `foreign-plugin-root`, `dangling-plugin-root`), and the `host-name-in-core` scan — so the tool source carries no bare `{{` and no host name as a word, and nothing in `scripts/build_hosts.py` or either `settings.toml` changes beyond the `tools = &quot;tools&quot;` layout line the placement decision already costed.
    <advantage>Zero build code and no new definition key: the render already substitutes and checks every UTF-8 file under `core/` whatever its extension, the gate proves the tool host-neutral at build time (a stray host name or `{{` fails with file and line rather than at a user&apos;s shell), and the two constraints cost this tool nothing — an XML serializer emits no literal brace, the only ordinary Python use of `{{` being an f-string or `format` template producing one, and a plugin tool names no host by design.</advantage>
    <drawback>Python source is authored under two prose rules it never needed — no literal `{{`, so no f-string or `format` literal brace, and no host name as a word even in a comment or docstring — and the render path rewrites code: a no-op while the tool carries no slot and both `prose_drop_patterns` lists are empty, but a future prose pattern would also run over code that only the un-rendered `core/tools/` copy is tested against, so a mangled rendered tool would pass the drift gate unnoticed.</drawback>
  </alternative>
  <alternative id="Copy untouched, check fully">
    `render_host` gains a file-kind branch — Markdown is rendered as today while every other `core/` file is copied byte-for-byte with no substitution and no prose-drop — but every check still runs over the copied file, so the tool source must still avoid `{{` and host names and can never carry a `{{PLUGIN_ROOT}}` slot.
    <advantage>The shipped tool is byte-identical to the source the suite imports from `core/tools/` by construction, and no prose-drop regex can ever touch code.</advantage>
    <drawback>A kind branch in a render that CLAUDE.md documents as applying to every UTF-8 text file whatever its extension, plus the docstring and invariant rewording, buys a guarantee the constraints keep anyway — the source still avoids `{{` and host names because the checks still run — against a hazard both empty pattern lists leave hypothetical.</drawback>
  </alternative>
  <alternative id="Copy untouched, checks exempted">
    Copy `core/tools/` byte-for-byte and exempt it from the text checks — the `unfilled-placeholder` and `dangling-plugin-root` line greps and the `host-name-in-core` scan — through a definition key both hosts carry or a directory the build hard-codes, so the tool is ordinary Python free to use `{{` and to name a host.
    <advantage>The tool source is unconstrained Python, with no authoring rule an f-string, a format template, or a comment could trip.</advantage>
    <drawback>The first path-scoped exemption in a gate the placeholder-gate decision kept exemption-free by rejecting exactly a path-scoped exemption, an escape sequence, and an exempt key; it needs a second exemption in the host-name scan, which reads `core/` directly and ignores the render path; it hard-codes a directory or adds a key both hosts must carry; and it opens a hole where a stray `{{PLUGIN_ROOT}}` in the tool ships un-rendered and unchecked — all for freedoms an XML tool never uses.</drawback>
  </alternative>
  <recommendation option="Uniform render and check">The build already renders and checks every UTF-8 file under `core/` whatever its extension, so the `tools` layout line the placement decision costed is the whole build change and the tool source simply carries no bare `{{` and no host name — constraints that are free for an XML serializer emitting no literal brace and a plugin tool that is host-neutral by design, and that no copy path lifts anyway because the host-name scan reads `core/` directly; the gate&apos;s uniformity is a property this repository defended when the workflow template needed `{{`, choosing the one-character lookbehind over a path exemption, an escape sequence, and an exempt key, and the test-suite location decision already set the pattern that what cannot live under the uniform rules of `core/` (fixtures naming hosts) lives outside it while the tool, which can, lives inside them; the tool carries no `{{PLUGIN_ROOT}}` slot, so the rendered file is byte-identical to the source the suite imports, and the accepted residual — a future prose-drop pattern also running over code — stays hypothetical while both lists are empty, with the kind branch of the copy option as the one-line remedy to take then rather than now.</recommendation>
</open-question>
<open-question id="Sweep list detail">
  <question>How does the recommend orchestrator obtain each block&apos;s `&lt;question&gt;` text for its ranking and learn which blocks already carry a `&lt;recommendation&gt;` to skip, given that `list` prints bare ids — a `list` flag or extra column, or a whole-file read?</question>
  <alternative id="List filter flag plus locate">
    `list` gains one filter flag, `--unannotated`, that prints in document order the bare ids of every block carrying no `&lt;recommendation&gt;` element; the orchestrator runs the bare `list` first (an empty document stops the run, an empty filtered list is the nothing-to-annotate case), then the filtered one, then `locate` over the surviving ids, whose verbatim print of a bare block is the three lines — wrapper, `&lt;question&gt;`, closing tag — that carry the question text for the ranking and are the block the dispatch prompt already carries.
    <advantage>The skip test is the one predicate the tool already evaluates for the answer sweep&apos;s walk (gather the blocks that contain a `&lt;recommendation&gt;`), so the flag is its complement for an argparse entry and a test, `list` keeps exactly the bare-ids form the output contract fixed for set queries, and the question text arrives inside the very block the dispatch prompt carries — read once, spent twice, nothing rebuilt or re-escaped by the runner — at about 300 bytes per bare block against the roughly 5 KB an annotated block weighs in this file.</advantage>
    <drawback>Two `list` calls plus `locate` calls for the survivors — one per id unless `locate`, like `strip`, takes several — so the gather is several invocations where the other two options need one, and the orchestrator feeds `list`&apos;s output back into `locate` itself.</drawback>
  </alternative>
  <alternative id="Long listing with columns">
    `list` gains a long form (`--long` or the like) that prints one tab-separated line per block in document order — the id, a marker saying whether the block carries a `&lt;recommendation&gt;`, and the folded question text — so a single call tells the orchestrator that questions exist, which to skip, and what each asks.
    <advantage>One tool call and no `locate` round trips: the ranking input, the skip set, and the empty-document check arrive together, and the orchestrator holds no XML at all.</advantage>
    <drawback>A second, columnar output shape for a set query — columns to split and a marker to decode — against the recorded contract of bare un-escaped values one per line; it prints the question text of every annotated block the sweep will skip; and it hands the orchestrator un-escaped, blockless text it must re-wrap and re-escape into an `&lt;open-question&gt;` block for the dispatch prompt (or the agent&apos;s prompt contract changes to take plain text), which is exactly the escaping contract the milestone moves into the tool.</drawback>
  </alternative>
  <alternative id="Whole-file read">
    The orchestrator reads `&lt;MILESTONE_DIR&gt;/open_questions.xml` whole with the file-reading tool, as the recommend agent&apos;s grounding does, and by eye takes each block&apos;s id and `&lt;question&gt;` line and notes which blocks already hold a `&lt;recommendation&gt;` element, invoking the tool only to embed.
    <advantage>No new tool surface at all — one file-read call of the kind the plugin&apos;s whole-set readers already make, with every block, bare or annotated, in front of the orchestrator as it ranks.</advantage>
    <drawback>The read is dominated by what the sweep skips — an annotated block is about 5 KB against 300 bytes for a bare one, and on the common re-run (a review pass surfacing a few new blocks among many annotated ones) that multi-kilobyte XML sits in the orchestrator&apos;s context across every dispatch of the run; the skip test becomes a membership check done by eye over raw XML, a deterministic operation the goal moves into the tool; and it contradicts the Read access decision, which names the whole-set readers and puts every consumer needing less on `list`, and the sweep&apos;s own rule that the subagent, not the orchestrator, grounds in the document.</drawback>
  </alternative>
  <recommendation option="List filter flag plus locate">The sweep needs a membership test and the block text, and the tool already holds both — the answer sweep&apos;s walk evaluates exactly the carries-a-`&lt;recommendation&gt;` predicate and `locate` prints a block verbatim — so `list --unannotated` exposes that predicate&apos;s complement for an argparse entry and a test while `list` keeps the bare-ids form the output contract fixed for set queries, and `locate` over the surviving ids yields each bare three-line block that is at once the ranking input and the dispatch prompt&apos;s payload, read once and spent twice with nothing rebuilt or re-escaped by the runner; the cost scales with the work (about 300 bytes per bare block against 5 KB per annotated one, in a section that ran to 16 KB earlier in this milestone) where a whole-file read scales with what the sweep skips and moves a deterministic test back into prose against the Read access decision that puts every less-than-whole consumer on `list`, and the long listing saves the `locate` calls only by adding a columnar output shape and handing back un-escaped, blockless text the orchestrator would re-escape into a block for the prompt — the prompt&apos;s need for the verbatim block is what breaks that tie, and letting `locate` take several ids as `strip` does closes the call-count gap; the bare `list` stays unfiltered because `derive-tasks`&apos; precondition and the review pass need every block.</recommendation>
</open-question>
<open-question id="Fragment child order">
  <question>When a fragment passed to `embed` orders its children differently from the fixed alternatives, applied-principles, depends-on, recommendation sequence, does validation reject it or does the serializer reorder the children on write?</question>
  <alternative id="Reject out-of-sequence children">
    `embed` adds a sequence test to its validation: after extraction and parse, a fragment&apos;s top-level children must run every `&lt;alternative&gt;`, then `&lt;applied-principle&gt;`, then `&lt;depends-on/&gt;`, then the one `&lt;recommendation&gt;` (and inside each alternative the what-it-is text, `&lt;advantage&gt;`, `&lt;drawback&gt;`), and a child out of that sequence fails the call with one `Error:` line naming the misplaced element, the document unchanged and the orchestrator&apos;s single repair attempt carrying that reason, while the serializer writes the tree in the order it was parsed.
    <advantage>The fragment&apos;s shape stays exactly the agent&apos;s step-3 contract with the tool as its enforcer — the serializer remains a pure renderer of tree order, so `write(read(x)) == x` is the whole format test and no write ever reinterprets a fragment — and a misordering reaches the agent as an aimed repair reason like every other structural miss.</advantage>
    <drawback>It adds a test the seven-test gate never had — the gate checked only the two boundary positions, and none of the 176 blocks embedded across the 26 annotation sweeps in this repository&apos;s history (55 carrying an optional middle kind, 589 alternatives) arrived out of sequence at either level — so it spends a repair round-trip, the corrective message plus a multi-kilobyte re-emission of the fragment, on a return that already carries every element the block needs, polices a middle no consumer reads positionally, and is partial by construction: the one misordering that loses an element, a child trailing `&lt;/recommendation&gt;`, is discarded by extraction before validation runs.</drawback>
  </alternative>
  <alternative id="Canonical order on write">
    Validation checks only what the gate&apos;s successor tests check — no wrapper or `&lt;question&gt;` line, at least one `&lt;alternative&gt;`, exactly one `&lt;recommendation&gt;` whose option names an alternative, every `&lt;depends-on/&gt;` resolving, no element of an unknown kind — and the serializer, which already re-renders every block into the fixed layout, writes each block&apos;s children grouped by kind in the fixed sequence (alternatives in their returned relative order, then applied-principles, then depends-on, then the recommendation, and inside each alternative the text, `&lt;advantage&gt;`, `&lt;drawback&gt;`), so a fragment&apos;s child order, like its indentation and escaping, stops being load-bearing.
    <advantage>A complete but misordered fragment embeds in one call with nothing lost — grouping by kind keeps every element and the alternatives&apos; relative order — and the on-disk sequence becomes a guarantee of the writer, exactly where the canonical-format decision already put the intra-alternative layout and every other rendering property, so the sequence rule lives once and the same tree writes the same bytes whatever the input looked like.</advantage>
    <drawback>The agent&apos;s step-3 instruction and the tool&apos;s tolerance state the order rule at two strengths — a reader of the agent file assumes it is enforced when the tool only normalizes — and a misordering is never reported, so drift in the agent&apos;s child order would go unnoticed; normalization also reaches only the extracted region, so a child trailing `&lt;/recommendation&gt;` is still lost to extraction, guarded by the agent&apos;s own second self-check test alone.</drawback>
  </alternative>
  <alternative id="Preserve returned order">
    Validation ignores order and the serializer writes each block&apos;s children in the order the fragment supplied them — today&apos;s behaviour, where the gate tests only the boundary positions and the orchestrator embeds the accepted region in the order returned.
    <advantage>The least tool code — no sequence test and no grouping by kind — and no consumer breaks, since every locate, lift, strip, reconcile, and capture read keys on an element&apos;s tag rather than its position.</advantage>
    <drawback>It forfeits the canonical form the serialization decision just bought: two blocks with the same content could differ in bytes, the file whose sole writer is the tool would carry a sequence the tool neither checks nor fixes, the &quot;child order is always alternatives, applied-principles, depends-on, recommendation&quot; invariant would rest on the agent&apos;s discipline alone, and git diffs and capture&apos;s removed-line reconstruction would see whatever order each sweep happened to return.</drawback>
  </alternative>
  <recommendation option="Canonical order on write">The sequence exists for the reader and the diff, not for any consumer — the two positions the pipeline needs, an alternative first and the recommendation last, are extraction&apos;s own anchors, the seven-test gate the validation replaces never tested the middle, and every locate, lift, strip, reconcile, and capture read keys on an element&apos;s tag, never its position — so child order is a rendering property, and the canonical-format decision already made the serializer the owner of every rendering property of the fragment, the intra-alternative layout included, which makes grouping children by kind in the fixed sequence the same normalization one level up, stated once in the writer and guaranteed on every write; grouping loses nothing (every element survives and the alternatives keep their relative order), so it belongs with re-indentation and re-escaping rather than with the loud failures this milestone reserves for a silently wrong outcome such as a mistyped `--option`, and a rejection would spend a corrective message plus a multi-kilobyte re-emission of the fragment on a return that already carries every element the block needs, against the goal&apos;s efficiency aim; the tie against rejecting breaks on the record and the reach of the check — none of the 176 blocks embedded across 26 annotation sweeps arrived out of sequence, and the one misordering that does lose an element, a child trailing `&lt;/recommendation&gt;`, is discarded by extraction before any validation could see it, so a sequence test would police a failure mode with no occurrence while missing the one that matters, whose only guard under any option is the agent&apos;s own second self-check test; preserving the returned order falls out because it forfeits the canonical form the tool exists to guarantee.</recommendation>
</open-question>
<open-question id="CI test job placement">
  <question>Do the two pytest runs join the existing drift-gate job, form a second job in that workflow, or live in a new workflow file, given that the README&apos;s ci badge reads the drift-gate workflow&apos;s status?</question>
  <alternative id="Steps in the existing check job">
    The two pytest `run:` lines join the single `check` job of `drift-gate.yml` as steps after `uv run scripts/build_hosts.py --check`, sharing its one checkout and one uv setup, so the workflow stays one job of three steps and the `ci` badge, whose URL names the file, reads a status that covers the drift gate and both test runs.
    <advantage>The smallest CI change on offer — two `run:` lines, no new job, and the two SHA-pinned action lines stay the only two bumped by hand — on the one-job shape both existing workflows have, the shape the CI-versions decision already gave the two pytest runs (steps of one job), and the literal &quot;CI step beside the drift gate&quot; the test-suite-location decision names; every reference in the badge URL, CONTRIBUTING, and CLAUDE.md stands, and the tests run in the checkout and synced uv environment the drift gate just used, so `uv run pytest` installs nothing more.</advantage>
    <drawback>Under default step semantics a red drift gate skips the two test steps — as a red pinned run already skips the floor run in the recorded one-job arrangement — so a contributor who pushed a stale `hosts/` learns of a test failure one push later, a pull request shows one `check` status whose failed step is a click away rather than named in the list, and the `drift-gate` file name comes to name the first of three steps rather than the whole; `if: ${{ !cancelled() }}` on the test steps removes the masking at one conditional per step.</drawback>
  </alternative>
  <alternative id="Second job in drift-gate.yml">
    `drift-gate.yml` gains a second job (`test`, say) beside `check`, with its own pinned checkout and setup-uv steps and the two pytest runs, so the two jobs run in parallel and report as two status lines on a pull request while the workflow-level conclusion the `ci` badge reads turns red when either fails.
    <advantage>The drift signal and the test signal are independent by construction — neither job&apos;s failure skips the other, no conditional is written, and each job&apos;s name tells the pull-request checks list which remedy applies, rebuild `hosts/` or fix the tool — while the badge stays honest at its unchanged URL.</advantage>
    <drawback>A second copy of the two SHA-pinned action lines in the same file, bumped by hand — four where milestone 24 accepted two, and a half-bumped file running two versions of one action fails nothing — plus a second runner&apos;s startup for a suite that runs in seconds, the first two-job workflow in a repository whose two workflows are one job each, and the same narrowing of the `drift-gate` name to one job of two.</drawback>
  </alternative>
  <alternative id="New workflow file">
    The pytest runs live in a new `.github/workflows/tests.yml` (or the like) with its own `push`/`pull_request` trigger, `permissions: contents: read`, and pinned checkout and setup-uv steps, leaving `drift-gate.yml` byte-for-byte as milestone 24 fixed it.
    <advantage>Every name stays exact — `drift-gate.yml` runs only the drift gate and the new file only the tests — in the file-per-concern layout GitHub repositories conventionally use, and nothing milestone 24 recorded about the drift-gate workflow&apos;s trigger, pins, or badge changes.</advantage>
    <drawback>The `ci` badge reads `drift-gate.yml` alone, so a red test suite on `main` leaves it green unless the README&apos;s row grows a fourth badge and relabels `ci` to the drift gate it then reports; the trigger, permissions, and both pinned actions are copied into a second file (four hand-bumped lines across two files, the pattern the traffic-workflow copies already need a grep for), CLAUDE.md&apos;s &quot;one of the two workflows&quot; becomes three, and a file of its own invites the per-workflow `paths` filter milestone 24 rejected as a hand-maintained copy of the build&apos;s input set.</drawback>
  </alternative>
  <recommendation option="Steps in the existing check job">The badge clause rules out only the new file — GitHub marks a workflow run failed when any of its jobs or steps fails, so either in-workflow placement keeps the `ci` badge reading every check on `main` at its unchanged URL — and between those two the step form wins on what this repository already counts: two `run:` lines and no new job against a second hand-bumped copy of the SHA pins in the same file, a second runner for a suite that runs in seconds, and the repository&apos;s first multi-job workflow, all bought for a separate status line and an independence the recorded one-job arrangement of the two pytest runs already forgoes between its pinned and floor runs; the masking a second job would remove costs one extra push in the case CONTRIBUTING already tells a contributor to rule out locally, with `if: ${{ !cancelled() }}` on the test steps as the one-line-per-step remedy to take if it ever bites, and the `drift-gate` file name stays as it is because the badge URL, CONTRIBUTING, and CLAUDE.md anchor to it, GitHub keys the run history to the path, and the label a visitor reads is already `ci`.</recommendation>
</open-question>
