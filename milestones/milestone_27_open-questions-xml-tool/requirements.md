# Milestone 27: Open Questions XML Tool

## Goal

Split the `## Open questions` section out of `requirements.md` into a sibling `<MILESTONE_DIR>/open_questions.xml` — a well-formed XML document under one root element, created empty by `define-milestone-goal`, whose "no `<open-question>` child remains" state is the convergence verdict and the `derive-tasks` precondition — and ship one stdlib-only Python tool inside the plugin (rendered into every host tree, invoked via `{{PLUGIN_ROOT}}`, and the file's sole writer) that owns every deterministic operation over that document: list, locate, add, lift, remove, embed with fragment validation replacing the seven-test acceptance gate, `<depends-on>` reconciliation in exact-id mode with transitive strip, dependent-strip on prune, and the answer sweep's dependency-graph walk. Rewire every skill, agent, and shared procedure that touches open questions from the `awk`/`sed`/`grep` boundary-line idiom onto tool invocations so that runtime prose keeps only judgment (authoring text, significance ranking, judgment-mode reconciliation for literal answers) and the entity-escaping, indentation, and whole-block-replacement contracts become the tool's internals — the aim being maximal token efficiency — while `capture-milestone-principle-updates` keeps its diff-line reconstruction, repointed at the new file. Give the tool a pytest suite run under `uv` in CI beside the drift gate, document Python 3.9+ as a prerequisite checked once by `init-milestone-base-workflow`, sync `CLAUDE.md`, `README.md`, and `docs/`, and provide no migration of or compatibility with pre-split milestones.

## Relevant starting state

### The `<open-question>` block and where it lives

Open questions are raw XML blocks under a single `## Open questions` section of `<MILESTONE_DIR>/requirements.md`, beside the four prose sections. `define-milestone-goal`'s template seeds `## Goal`, `## Relevant starting state`, `## Decisions`, and `## Out of Scope` only; `review-milestone-requirements` creates the section the first time it authors a block, so on disk it sits last, after `## Out of Scope`, and its empty heading survives once every block is answered. Every one of the 27 milestone directories holds exactly `requirements.md`, `TASKS_TODO.md`, and `TASKS_DONE.md`; 24 of the `requirements.md` files carry the heading and none carries a live block, so the annotated shape exists only in git history (for example the removed lines of `1cdddec`, `Recommendation-answer: Distribution badge form`). An authored block is three lines — a single-line `<open-question id="Short Title">` opening tag at the base column, one `<question>` child at a 2-space indent, `</open-question>` — and the recommend sweep grows it in a fixed child order: `<alternative id="…">` (what-it-is text, then `<advantage>` and `<drawback>`), `<applied-principle>`, self-closing `<depends-on question="…" option="…"/>`, and one `<recommendation option="…">`. Attribute values and element text are escaped with the five predefined entities, ids are compared case-folded after un-escaping, and no file declares a root element, an XML prolog, or a schema.

### The boundary-line CLI idiom and its runtime restatements

No runtime file invokes an executable today: `core/` holds Markdown only (`find core -type f ! -name '*.md'` returns nothing). Every deterministic operation over the question set is instead prose telling the runner to drive `awk`/`sed`/`grep` over the `<open-question …>`/`</open-question>` boundary lines and never `xmllint`, restated in each consumer: `core/shared/answer-procedure.md` (locate by `id="([^"]*)"`, re-query after the fold, line-range removal, dependent reconciliation with transitive strip), `core/shared/answer-with-recommendation-procedure.md` (locate and lift `<recommendation>`), `answer-open-question-with-alternative` (locate and lift an `<alternative>`), `discuss-open-question` (locate, then read the whole document), `recommend-all-open-questions` (gather once, the seven-test acceptance gate over the returned region with its eight reason strings and one-slot repair template, embed by whole-block-replacement `Edit` at 2-space indents), `answer-all-open-questions-with-recommendation` (gather in document order, drop unresolvable edges, depth walk with origin promotion, per-question re-check that also lifts the commit body), `review-milestone-requirements` (author, prune, dedup, transitive dependent strip), and `derive-tasks` (its precondition is a scan for any `<open-question` line, and its stop message lists the Short Titles). The five-entity un-escaping rule (`&amp;` last) is spelled out in six of those files. The `recommend-open-question` agent renders the child elements itself, indents them, escapes them, and self-checks the two boundary shape tests; the `answer-open-question-with-recommendation` agent stages `<MILESTONE_DIR>/requirements.md` path-scoped and returns bare `DONE`/`FAILED`.

### Indirect readers of the question set

`core/shared/recommend-procedure.md` reads `requirements.md` for the sibling blocks' embedded recommendations and the principle store `milestones/answer_decision_principles.md` in place. `ask-in-milestone-context` lists "any remaining open questions" among what it reads from `requirements.md`, `modify-milestone-goal` reports which open questions a goal change settles, opens, or moots, and `submit-task` and `discuss-new-task` only point at `/discuss-open-question`. `finish-current-milestone` and `goto-next-milestone` never read a question, and `goto-next-milestone` detects a defined milestone by its directory alone.

### Commit and provenance contracts on the question path

`core/shared/commit-procedure.md` takes an explicit PATHS set (guarded by `git status --porcelain -- <PATHS>`, staged by name, never `git add -A`), and every question skill passes `<MILESTONE_DIR>/requirements.md` as its sole path, under `Requirements-review:`, `Recommendation-annotation:`, `Manual-answer:`, `Recommendation-answer:`, and `Alternative-answer:` subjects. `capture-milestone-principle-updates` walks `git log -E --grep='^(Manual-answer|Alternative-answer|Recommendation-answer): ' -- milestones/<milestone_id>/requirements.md`, reads each commit with `git show <hash> --format='%s%n%n%b' -- milestones/<milestone_id>/requirements.md`, and reconstructs the answered block from the diff's removed lines between `-<open-question id="…"` and `-</open-question>` while reading the recorded decision from the added lines; it also treats pre-block-form blockquote answers (`> **Deferred — …:**`) as the no-recommendation case.

### The host build and what a host tree can carry

`scripts/build_hosts.py` walks every file under `core/`, applies `{{PLUGIN_ROOT}}` substitution and `prose_drop_patterns` to every UTF-8 text file whatever its extension, and lands each top-level `core/` directory where the host's `[layout]` maps it — both definitions map exactly `skills`, `agents`, `shared`, a file at the top of `core/` is a `BuildError`, and a layout that does not name every top-level directory fails validation. Its checks run over every rendered file: `unfilled-placeholder` fails on any `{{` not preceded by `$` (so a Python f-string's `{{` would fail), `host-name-in-core` fails on `claude` or `antigravity` as a whole word anywhere in `core/`, `dangling-plugin-root` requires every `<plugin_root>/<path>` (path characters `[A-Za-z0-9_./-]`) to name a file in the rendered tree, and the frontmatter and description checks apply to `skills/<name>/SKILL.md` and `agents/<name>.md` only. The plugin-root literals are `${CLAUDE_PLUGIN_ROOT}` and `.agents/plugins/cairn`, rendered into runtime prose as `${CLAUDE_PLUGIN_ROOT}/shared/answer-procedure.md` and `.agents/plugins/cairn/shared/answer-procedure.md`. `hosts/claude/` (37 files) and `hosts/antigravity/` (36 files) pass `--check` at version 1.5.1; the release skill stages `hosts/` whole, and `CLAUDE.md` enumerates the tree's contents as skills, agents, shared, plus exactly four non-plugin files.

### Python tooling, CI, and the test surface

`pyproject.toml` declares `cairn-tooling` with `requires-python = ">=3.11"` (the build imports `tomllib`), `pyyaml` as its one dependency, and `[tool.uv] package = false`; `.python-version` pins 3.13 and `uv.lock` holds only `cairn-tooling` and `pyyaml`. No `tests/` directory, no `pytest` reference, and no test runner exist anywhere in the repository — the build's validation gate is the only automated check. `.github/workflows/drift-gate.yml` is one job on unfiltered `push` and `pull_request` with `permissions: contents: read`, `actions/checkout@3d3c42e…` (v7.0.1) and `astral-sh/setup-uv@bec219d…` (v10.1.0) pinned by SHA, and the single step `uv run scripts/build_hosts.py --check`; `CONTRIBUTING.md`'s Development section documents those `uv run` commands as the whole build loop. On this machine `uv run` resolves to CPython 3.13.5 under uv 0.11.14; no Python 3.9 interpreter is installed, though uv lists `cpython-3.9.25` and `3.11.15` as downloadable.

### Prerequisites and the bootstrap skill

No document names a runtime prerequisite for the installed plugin: `README.md`'s `## Installation` gives the Claude Code marketplace commands, the Antigravity `curl … | tar -xz` extraction into `.agents/plugins/cairn`, and the bootstrap step, and both distribution README templates under `scripts/hosts/<host>/README.md` repeat the same sections; `uv` appears only in maintainer text. `init-milestone-base-workflow` probes just three things — `milestones/`, `milestones/README.md` with a `Current milestone:` line, and a `## Milestone Workflow` section in `CLAUDE.md` — creates what is missing, stops when all three exist, commits nothing, and runs no environment check; its `milestones/README.md` template describes `requirements.md` as "goal, relevant starting state, decisions, open questions", the line this repository's own `milestones/README.md` carries.

### Documentation surfaces that state the current design

`CLAUDE.md` (mirrored by the `AGENTS.md` symlink) mentions `open-question` 33 times and `boundary-line` 7 times: the "Milestone file structure" section says each milestone contains exactly three files, and the review, answer-procedure, recommend-sweep, answer-sweep, alternative-answer, capture, and dispatched-agent-return invariants each spell out the CLI idiom, the seven-test gate, or the whole-block-replacement embed. `README.md` repeats the three-file list under `## How it works` and, under `## Design principles`, claim 11's line that blocks are "found by their boundary lines, so every answer, cascade, and prune is a deterministic line-range edit"; `docs/design-claims.md` claim 11 gives that as its **Design** with a golden-file-diff **Metric**; `docs/workflow.md`'s "Iterating milestone requirements" paragraph states the query-where-it-pays convention (`awk`/`sed`/`grep`, never `xmllint`); and `docs/skill-reference.md` carries the entries for the eight question skills and agents that describe the same mechanics.

## Decisions

## Out of Scope

## Open questions

<open-question id="Root element shape">
  <question>What is the root element of `open_questions.xml` — its tag name, whether the document opens with an XML declaration, and whether the root carries attributes such as the milestone id?</question>
  <alternative id="Bare open-questions root">
    The root is `&lt;open-questions&gt;` with no XML declaration and no attributes: the file `define-milestone-goal` creates is that one empty root, and every `&lt;open-question&gt;` block sits one level inside it exactly as it stood under the old heading.
    <advantage>Nothing but the wrapper the goal requires — the XML spec reads an undeclared document as UTF-8, stdlib ElementTree parses it and (for UTF-8 output) writes it with no declaration by default, the tag keeps the `&lt;open-question&gt;` / `open_questions.xml` naming family, and both a declaration and root attributes remain available later as additive changes no ElementTree reader would break on.</advantage>
    <drawback>The document is self-describing only by its path — `file(1)` reports it as plain text rather than XML because there is no `&lt;?xml` magic, and there is no in-band milestone id — so a copy seen outside its directory says nothing about which milestone it belongs to.</drawback>
  </alternative>
  <alternative id="Declared open-questions root">
    The same `&lt;open-questions&gt;` root preceded by the conventional `&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;` first line, still with no attributes.
    <advantage>Reads as a textbook `.xml` file — the encoding is stated rather than defaulted, and editors, `file(1)`, and any non-XML tooling that identifies XML by the `&lt;?xml` magic recognize it at once.</advantage>
    <drawback>One fixed line that no consumer reads — the tool, the recommend agent&apos;s grounding, and capture&apos;s diff walk all key on `&lt;open-question` boundary lines — and stdlib ElementTree&apos;s own `xml_declaration=True` emits the single-quoted `&lt;?xml version=&apos;1.0&apos; encoding=&apos;utf-8&apos;?&gt;`, so the double-quoted header is a hand-written line the tool must keep byte-stable itself.</drawback>
  </alternative>
  <alternative id="Milestone-attributed root">
    The `&lt;open-questions&gt;` root carries the milestone id as an attribute — `&lt;open-questions milestone=&quot;milestone_&lt;NN&gt;_&lt;slug&gt;&quot;&gt;` — written by `define-milestone-goal` and optionally cross-checked by the tool against the directory it reads from.
    <advantage>The document identifies its milestone in-band, so a block seen out of context — a pasted file, a `git show` hunk — is attributable, and the tool can refuse a file copied into the wrong milestone directory.</advantage>
    <drawback>A second copy of the directory name that nothing in the pipeline reads — every runner resolves `&lt;MILESTONE_DIR&gt;` from the `milestones/README.md` pointer and capture&apos;s commit walk is already path-scoped — and the cross-check adds a failure mode with no real-world trigger under the goal&apos;s no-migration stance.</drawback>
  </alternative>
  <alternative id="Short questions root">
    A bare `&lt;questions&gt;` root with no declaration and no attributes, dropping the `open` qualifier the file name already carries.
    <advantage>Shortest tag, and `open` is not repeated three times across `open_questions.xml`, the root, and each block.</advantage>
    <drawback>`question` is already the text element inside every block, so the root and that element would differ only by a plural — an easy misread and a `grep &apos;&lt;question&apos;` hazard — and it breaks the `&lt;open-question&gt;` naming family the file name and the block tag share.</drawback>
  </alternative>
  <applied-principle>Drop-vs-replace by ambiguity</applied-principle>
  <applied-principle>Prefer domain-neutral terms</applied-principle>
  <recommendation option="Bare open-questions root">The root exists only to make the lifted section one well-formed document, so name it for the family it wraps and add nothing — an undeclared document is UTF-8 by the XML spec and stdlib ElementTree reads and writes it that way by default, the directory already names the milestone, and a declaration or attribute can be added later as an additive change without breaking any reader.</recommendation>
</open-question>
<open-question id="Empty document creation">
  <question>Does `define-milestone-goal` create the empty `open_questions.xml` by invoking a tool subcommand, keeping the tool the file&apos;s only writer, or by writing a fixed template itself?</question>
  <alternative id="Tool create subcommand">
    `define-milestone-goal` writes its three Markdown files as today and creates `open_questions.xml` through one invocation of the tool&apos;s `create` subcommand with the new milestone directory — a silent mutator that writes the serializer&apos;s own empty document (one empty root element), refuses an existing file with the document untouched, and whose output joins the skill&apos;s path-scoped commit as a fourth path.
    <advantage>The document on disk is the tool&apos;s output from its first byte, so the goal&apos;s sole-writer clause holds with no exception to state anywhere, and the empty form — open pair or self-closing root, trailing newline — is defined once, in the serializer whose every later write re-renders it: a converged document is byte-identical to a fresh one, one pytest case pins that form beside `write(read(x)) == x`, and the milestone-directory argument lets the tool write a milestone the pointer does not yet name, which is exactly this skill&apos;s case; it is also how the plugin&apos;s other sole writer already behaves — `capture-milestone-principle-updates` creates the principle store itself when it is absent.</advantage>
    <drawback>`define-milestone-goal`, today a pure template writer, gains the pipeline&apos;s earliest executable dependency — the maintainer&apos;s reserve-an-id run on `main` included — and a failure point among its own file writes that it must order around (the tool call first, creating the directory, or a missing interpreter leaves a half-scaffold the next run&apos;s numbering scan counts as taken), all for a subcommand with one caller and one call per milestone.</drawback>
  </alternative>
  <alternative id="Fixed template in skill">
    `define-milestone-goal` writes `open_questions.xml` itself from a fourth fixed template beside its three Markdown ones — one empty root element — and never runs the tool, whose first write is then the first `add` under `review-milestone-requirements`.
    <advantage>Nothing to build or run: the skill stays a template writer with no interpreter in its path, the file is born exactly as its three siblings are (one file write from one template block a few lines long), and milestone definition — which the maintainer also runs on `main` merely to reserve an id — keeps working on a machine with no Python at all.</advantage>
    <drawback>A second writer of the file, so the goal&apos;s &quot;sole writer&quot; clause carries a caveat everywhere it is stated, and the empty document&apos;s exact bytes exist twice — as prose in the skill and as code in the serializer — with nothing checking they agree: the build gate reads no template and no pytest case reaches the prose copy, so a divergence (self-closing root against open pair, a trailing newline) surfaces as root-line churn in the first `Requirements-review:` diff or as a converged document that no longer matches a fresh one; and the interpreter dependency it spares the skill is only deferred to the next skill in the pipeline.</drawback>
  </alternative>
  <alternative id="Tool scaffolds the milestone">
    The tool gains a scaffold subcommand that creates the whole milestone directory — `requirements.md` with the goal text read from stdin, the two TASKS files, and the empty `open_questions.xml` — and `define-milestone-goal` reduces to numbering, slugging, and that one call.
    <advantage>Every file a milestone is born with comes from one deterministic, pytest-covered writer, so a scaffold can never be half-written or inconsistently formatted, and the skill&apos;s prose shrinks to its one judgment (the slug) and one invocation.</advantage>
    <drawback>It widens the tool from &quot;every deterministic operation over that document&quot; to a writer of three Markdown files it never reads back and that the rest of the pipeline edits by hand, moving `define-milestone-goal`&apos;s templates out of the skill that owns them into Python string literals with a test surface of their own — a tool-scope change the goal does not ask for, carrying the same definition-time interpreter dependency as the `create` subcommand, bought for three files a single file write each already creates deterministically.</drawback>
  </alternative>
  <depends-on question="Tool file resolution" option="Milestone directory argument"/>
  <depends-on question="Canonical serialization format" option="Canonical writer, five entities"/>
  <depends-on question="Escape hatch under sole writer" option="Strip subcommand"/>
  <recommendation option="Tool create subcommand">The goal makes the tool the file&apos;s sole writer and the owner of its on-disk format, so the empty document is the serializer&apos;s to define and to write: one `create` call keeps that form in exactly one place, pinned by a test, with a converged document byte-identical to a fresh one and the sole-writer guarantee exception-free from the first byte, as the escape-hatch answer already keeps it at the other end of a block&apos;s life; the skill pays one invocation and one ordering rule (the tool call first, so a missing interpreter leaves nothing behind), while the interpreter dependency a template would spare it is only deferred to the next skill in the pipeline — which is what settles it against a template that is smaller to write but a second, untested copy of the format; scaffolding the whole milestone through the tool falls out on scope, since the goal binds the tool to this one document.</recommendation>
</open-question>
<open-question id="Tool directory placement">
  <question>Does the Python tool live in a new top-level `core/` directory that every host `[layout]` maps, such as `tools/`, or under the existing `shared/` directory beside the procedures?</question>
  <alternative id="Shared directory">
    The tool is one file under the existing `core/shared/`, rendered beside the procedures that invoke it, with both hosts&apos; `[layout]` tables untouched.
    <advantage>No layout, definition, or enumeration change anywhere: every `{{PLUGIN_ROOT}}` reference in the runtime keeps pointing into the one directory it points into today, and the tool sits next to `answer-procedure.md` and its siblings, the very files that invoke it, under the existing rule that what two or more runners depend on lives in `shared/`.</advantage>
    <drawback>`shared/` is defined everywhere it is described — `CLAUDE.md`, the docs, the build docstring — as the execution-neutral procedures a runner reads and follows (`core/shared/*.md`), and a program the runner executes is a different artifact kind with different rules (stdlib-only, a 3.9 floor, a pytest suite, the build&apos;s `unfilled-placeholder` hazard for `{{` in an f-string), so every rule about the directory gains a by-file-extension exception, and a test layout beside the tool would land as `shared/tests/` among the procedures.</drawback>
  </alternative>
  <alternative id="New tools directory">
    The tool lives in a new top-level `core/tools/` directory, and both host definitions add `tools = &quot;tools&quot;` to their `[layout]` so it renders to `{{PLUGIN_ROOT}}/tools/` on every host.
    <advantage>One top-level directory per artifact kind, matching `skills/`, `agents/`, and `shared/`: the executable gets rules and a documented home of its own without being carved out of the procedure rules by extension, the `[layout]` mechanism exists for exactly this addition and the build refuses a definition that omits it (so the edit cannot be half-done on one host), and a test layout beside the tool has a clean, `exclude`-able home whichever way the test-suite question settles.</advantage>
    <drawback>A fourth top-level directory to carry everywhere the tree is enumerated — both `settings.toml` `[layout]` tables, the `hosts/&lt;host&gt;/` contents list in `CLAUDE.md`, and the docs — so `{{PLUGIN_ROOT}}` references now point into two directories instead of one, and the name shares a word with the host&apos;s own built-in tools, though no host reserves a `tools/` plugin component.</drawback>
  </alternative>
  <alternative id="Root scripts copy">
    The tool lives outside `core/` in the repository-root `scripts/` beside the build tooling, and `build_hosts.py` copies it into every host tree the way it already copies the root `LICENSE`.
    <advantage>All of the repository&apos;s Python stays under one directory and one `uv`/`pyproject.toml` toolchain with the build scripts, so a single test runner covers both, and the tool never passes through `{{PLUGIN_ROOT}}` substitution or `prose_drop_patterns` it has no use for.</advantage>
    <drawback>Breaks the contract that the runtime layer is authored once under `core/` and every host tree is a pure render of it — the build gains a second runtime input and a one-off copy rule, and `CLAUDE.md` must caveat what `core/` is — while not even settling the question, since the copy rule must still choose the landing directory in the host tree; it also parks a 3.9+ runtime tool under a `pyproject.toml` that requires 3.11 for the build.</drawback>
  </alternative>
  <applied-principle>Name by distinctive function</applied-principle>
  <applied-principle>Prefer domain-neutral terms</applied-principle>
  <recommendation option="New tools directory">A program the runner executes is a different artifact kind from a procedure it follows, and the plugin already keeps one top-level directory per kind, so a fourth one keeps the `core/shared/*.md` procedure rules exact, gives the tool and any test layout beside it a home with rules of its own, and costs only one build-validated line in each host `[layout]`; `tools` rather than `bin` or `scripts` because it names what the runner does with the contents, reads as plain English, and overloads no directory the repository already reserves for maintainer build tooling.</recommendation>
</open-question>
<open-question id="Interpreter command name">
  <question>Which command runs the tool at every invocation site and in the bootstrap check — `python3`, `python`, or a probe that falls back between the two?</question>
  <alternative id="Fixed python3">
    Every invocation site and the bootstrap check run the tool as `python3 {{PLUGIN_ROOT}}/&lt;tool path&gt; …`, and the check probes `python3` alone.
    <advantage>`python3` is the one command PEP 394 guarantees for a Python 3 interpreter and the one a stock macOS or Debian-family shell actually has — on this very machine `/usr/bin/python3` exists and `/usr/bin/python` does not — so the runtime prose carries one short fixed command at every site with nothing restated.</advantage>
    <drawback>Not universal: a python.org installer on Windows provides `python.exe` and the `py` launcher but no `python3.exe` (only the Store build adds one), so under such an install every invocation fails and the bootstrap check reports that no Python 3.9+ exists when one does, with no hint that it answers to the other name.</drawback>
  </alternative>
  <alternative id="Fixed python">
    Every invocation site and the bootstrap check use the bare `python` command.
    <advantage>The name every Windows install and every activated virtual environment or conda environment expose, and the shortest spelling.</advantage>
    <drawback>Absent from the shells the coding agent most often runs in — macOS ships no `python` since 12.3 (`/usr/bin/python` does not exist on this machine) and Debian-family systems provide it only through the optional `python-is-python3` package — and on a legacy system it can still be Python 2, so the fixed name fails or misfires exactly where `python3` succeeds.</drawback>
  </alternative>
  <alternative id="Per-site probe">
    Every invocation site and the bootstrap check carry a fallback expression such as `$(command -v python3 || command -v python)` that picks whichever name is present at run time.
    <advantage>Works unchanged wherever either name resolves to a 3.9+ interpreter, asking nothing of the user on any platform.</advantage>
    <drawback>Restates one environment fact at every invocation site across the ten-odd runtime files the goal rewires — the very per-site restatement the goal folds into the tool, against its token-efficiency aim — adds a shell evaluation to every call, and a silent fallback can land on an interpreter the bootstrap never validated (a Python 2 `python`, or a virtual-environment `python` beside a system `python3`), so the once-checked prerequisite and the actual invocations can disagree.</drawback>
  </alternative>
  <alternative id="python3 with bootstrap probe">
    Every invocation site runs the tool as `python3 …`, while the one-time bootstrap check executes `python3` to read its version and, only when that fails, probes `python`, so its message can say the interpreter exists under the other name and must be exposed as `python3`.
    <advantage>The per-site probe&apos;s coverage at zero per-site cost — the runtime prose keeps one fixed deterministic command, the second name is consulted exactly once per project, the failure message names the precise remedy instead of a false no-interpreter verdict, and executing the command rather than merely locating it means a present-but-unrunnable shim (this machine&apos;s license-gated `/usr/bin/python3`) fails the check honestly.</advantage>
    <drawback>A user whose only interpreter answers to `python` (a python.org install on Windows) must do one manual step before the tool runs, and whether the check stops or merely warns in that case is the still-open `Prerequisite check failure mode` question&apos;s call.</drawback>
  </alternative>
  <recommendation option="python3 with bootstrap probe">`python3` is the name PEP 394 guarantees and the one the stock macOS and Debian-family shells the agent usually runs in actually have, so fixing it at every site keeps the runtime prose to one short deterministic command with nothing restated; the platforms where only `python` exists are exactly what the once-per-project bootstrap check is for, and probing the second name there — asking the user to expose it as `python3` — buys the per-site probe&apos;s coverage at no per-site cost, which is what breaks the tie against a bare fixed `python3`.</recommendation>
</open-question>
<open-question id="Tool file resolution">
  <question>Does every tool invocation take the `open_questions.xml` path or the milestone directory as an argument the caller resolves, or does the tool resolve the current milestone from the `milestones/README.md` pointer itself?</question>
  <alternative id="Milestone directory argument">
    Every invocation takes the already-resolved `&lt;MILESTONE_DIR&gt;` as an argument and the tool joins it with the one file name it owns, so the caller resolves the milestone exactly as it does today and the tool never reads `milestones/README.md`.
    <advantage>Matches how every layer below a skill already works — `commit-procedure.md` takes resolved PATHS, `answer-procedure.md` a resolved SHORT TITLE, the recommend agent an already-resolved `&lt;MILESTONE_DIR&gt;` it is told never to resolve itself — using the one token every question skill holds after its `get-current-milestone.md` step; it also serves `define-milestone-goal`, which by design writes a milestone the pointer does not name (the maintainer defines while it reads `none`), and a pytest case then needs nothing but a `tmp_path`.</advantage>
    <drawback>The tool is milestone-layout-aware — `open_questions.xml` is a constant baked into it, so a test or one-off use cannot point it at an arbitrarily named file — and every call site spends a few tokens restating a directory the prose has already resolved.</drawback>
  </alternative>
  <alternative id="Document path argument">
    Every invocation takes the explicit `open_questions.xml` path, making the tool a layout-agnostic XML document tool that knows nothing about milestones.
    <advantage>The most general contract — any document of the right shape, anywhere, is a valid target, so fixtures and ad-hoc runs need no directory convention and a future consumer outside `milestones/` costs nothing.</advantage>
    <drawback>Spreads the file name across every invocation site in the runtime prose instead of one tool-internal constant — a longer line per call against the goal&apos;s token-efficiency aim, and a rename that touches every skill — for generality no consumer needs, since the goal binds the tool to `&lt;MILESTONE_DIR&gt;/open_questions.xml` and to creating it by name.</drawback>
  </alternative>
  <alternative id="Tool resolves the pointer">
    The tool takes no location argument — it finds `milestones/README.md` under the working directory, parses the `Current milestone:` line itself, and operates on that milestone&apos;s document.
    <advantage>The shortest possible invocation, with no location for any runtime file to restate at any call site.</advantage>
    <drawback>Duplicates `get-current-milestone.md` as Python that must track the snippet&apos;s prefix, backtick, and `none` rules (a second pointer parser free to drift from the prose every other runner follows), hard-codes a `milestones/README.md` lookup relative to the working directory where an argument could carry an absolute path, needs a fixture pointer file plus a chdir in every test, and cannot serve `define-milestone-goal`, whose milestone is by design not the current one — so however the empty-document creation question settles, the tool could not be that file&apos;s writer.</drawback>
  </alternative>
  <alternative id="Pointer default with override">
    The tool resolves the pointer by default and accepts an optional `--milestone-dir` flag that bypasses it, for creation, tests, and any milestone the pointer does not name.
    <advantage>Keeps the argument-free common case while still reaching a non-current milestone.</advantage>
    <drawback>Carries every cost of the pointer-resolving form — the second parser, the working-directory assumption — plus a second code path and two ways to name the target, and the override is what every writer that matters ends up using (creation, every test) while the default saves only a few tokens at sites that already hold the directory.</drawback>
  </alternative>
  <recommendation option="Milestone directory argument">The tool sits below the skills exactly where the shared procedures do, and that layer takes resolved inputs from its caller — every question skill already holds `&lt;MILESTONE_DIR&gt;` from `get-current-milestone.md`, `define-milestone-goal` must write a milestone the pointer does not name, and a pytest case then needs only a `tmp_path` — so passing the directory keeps one pointer parser in the plugin and one file name inside the tool; the directory rather than the document path because the goal already binds the tool to `&lt;MILESTONE_DIR&gt;/open_questions.xml` and its creation, so the shorter argument the prose already carries wins.</recommendation>
</open-question>
<open-question id="Tool output contract">
  <question>What is the tool&apos;s output and error contract — what each operation prints on success (bare ids, whole blocks, lifted answer text) and how a failure is signalled (exit status and message form)?</question>
  <alternative id="Plain lines and exit status">
    Every read prints only the un-escaped value the runner needs, one item per line — set queries (list, the dependency walk) print bare ids in document or walk order, locate prints the block verbatim as the file holds it, lift prints the recombined answer text (&quot;&lt;option&gt; — &lt;rationale&gt;&quot; or &quot;&lt;id&gt; — &lt;what-it-is&gt;&quot;) — while every mutator (create, add, embed, remove, reconcile) prints nothing on success; every failure is exactly one `Error: &lt;reason&gt;` line on stderr with exit status 1 (a malformed invocation keeps argparse&apos;s native usage message and exit 2), a non-zero exit guarantees the document is byte-for-byte unchanged, no traceback ever reaches the runner, and an empty set is empty stdout with exit 0 — emptiness is data, never a failure.
    <advantage>The cheapest possible round trip for an LLM runner on every call — an id, a block, or an answer line is pasted straight into the next prompt, commit subject, or `## Decisions` entry with no decoding — and the one failure form serves every consumer as it stands: embed&apos;s reason line fills the repair template&apos;s single slot verbatim, a lift that exits non-zero is the answer sweep&apos;s re-check-and-skip in one call, a not-found is the answer path&apos;s clean stop (the available ids one `list` call away), and `&amp;&amp;`, CI, and a pytest `subprocess.run` assertion all read the same zero/non-zero exit; it is also the convention `build_hosts.py` and `set_version.py` already follow.</advantage>
    <drawback>A result that needs two fields per item (an id with its question text, an option with its rationale) has no envelope, so it is either a line-syntax convention or a second call, and the runner learns of a failure only through the shell tool&apos;s report of the exit status and stderr — the mandatory reason line is what keeps a failure from ever being silent, but no in-band token confirms success.</drawback>
  </alternative>
  <alternative id="JSON envelope">
    Every operation prints one JSON object on stdout — `{&quot;ok&quot;: true, …}` with named fields (`ids`, `block`, `answer`, `option`, `stripped`) on success and `{&quot;ok&quot;: false, &quot;error&quot;: &quot;&lt;reason&gt;&quot;}` on failure — with the exit status mirroring `ok`.
    <advantage>One self-describing shape for every operation, success and failure alike, that carries multi-field results (an id with its question text, an option with its rationale, the dependents a reconcile stripped) without any line syntax, extends by adding a key, and asserts in pytest as a dict comparison.</advantage>
    <drawback>No program ever parses the output — the consumer is an LLM reading a console — so every value it uses comes back JSON-quoted and escaped (`\&quot;`, `\\`, a `—` em dash under the stdlib default) inside braces and keys it must decode and re-type on every call, the locate block becomes XML inside a JSON string, and the per-call overhead lands on the very token budget the goal exists to cut.</drawback>
  </alternative>
  <alternative id="Narrative confirmations">
    Reads print their values and every mutator prints a one-line human-readable account of what it did (`Removed &quot;X&quot;; stripped 2 dependents: Y, Z`, `Embedded 4 alternatives into &quot;X&quot;`), with failures as prose sentences — the reporting style `build_hosts.py` uses for its own runs.
    <advantage>The runner sees exactly what each mutation did without a `git diff`, which helps a user watching a run and a developer debugging the tool.</advantage>
    <drawback>It reinstates at the tool layer the re-narration the terse-reporting rule retired at the skill layer — the committed diff is the durable record, a stripped dependent gets no advisory by design, and a runner handed a narrative tends to relay it — while spending tokens on every mutating call for information nothing acts on.</drawback>
  </alternative>
  <alternative id="Last-line verdict token">
    Every operation ends its stdout with a verdict line — `DONE` or `FAILED: &lt;reason&gt;` — mirroring the plugin&apos;s dispatched-agent return protocol, with any result lines above it and the exit status left as a secondary signal.
    <advantage>One status convention across everything an orchestrator judges, agent returns and tool calls alike, readable even where a host&apos;s shell tool reports exit codes poorly.</advantage>
    <drawback>Payload and status share one stream, so every read ends in a token the runner must separate from the block or answer text it wants (and a lifted rationale can itself contain the token), it duplicates the exit status and stderr that every shell, `&amp;&amp;` chain, CI step, and pytest already understand, and it copies a protocol that exists only because an agent has no exit status — a process has one.</drawback>
  </alternative>
  <recommendation option="Plain lines and exit status">The runner is an LLM reading a console, so the contract that costs it the fewest tokens per call and the least decoding is bare un-escaped values on stdout and nothing at all from a mutator, with failure carried by the two signals every process already has — a single `Error: &lt;reason&gt;` line on stderr and a non-zero exit that guarantees the document is untouched; that reason line fills the repair template&apos;s slot verbatim, a failing lift is the answer sweep&apos;s re-check and skip in one call, an empty set stays exit 0 so no consumer ever mistakes a converged document for an error, and the still-open Embed extraction ownership and Reconciliation command placement questions change which checks and mutations exist, never the form of what is printed — which is what breaks the tie against a verdict token that would only duplicate the exit status a process already has.</recommendation>
</open-question>
<open-question id="Fragment input channel">
  <question>How do `add` and `embed` receive multi-line text — the question text and the agent&apos;s returned fragment — as a command-line argument, on stdin, or from a file path?</question>
  <alternative id="Text on stdin">
    `add` and `embed` keep their short identifiers (the Short Title among them) as ordinary arguments and read their one free-text body — the question text, the returned fragment — from standard input, which every runtime invocation site supplies as a quoted-delimiter heredoc (`&lt;&lt;&apos;EOF&apos; … EOF`); the tool reads the byte stream, decodes it as UTF-8 itself, and refuses a terminal stdin with an error.
    <advantage>A quoted heredoc is the one shell construct that carries a multi-line body byte-for-byte with nothing expanded — verified on this machine: backticks, `$(…)`, `${…}`, double quotes, and em dashes all arrive untouched and nothing runs — so the runner pastes the text it already holds (the agent&apos;s return, its own question prose) between two delimiter lines with no per-call escaping, the whole mutation stays one shell call, it is the idiom the coding agent&apos;s shell tool already relies on for multi-line content, `subprocess.run(input=…)` covers the pytest case, and `&lt; file` redirection (or a pipe from a here-string in a shell without heredocs) gives any saved file the same entry at no code cost.</advantage>
    <drawback>The body is out of band from the argument line, so the tool must own three small things — decode `sys.stdin.buffer` as UTF-8 rather than trust the locale-dependent text stream (every rationale carries an em dash), refuse a terminal stdin so a call that forgot its heredoc fails instead of blocking, and read exactly one body per call — and the heredoc idiom the runtime prose states is POSIX-shell-specific even though the channel is not.</drawback>
  </alternative>
  <alternative id="Text as argument">
    The body is one more positional argument on the command line — `add … &quot;&lt;Short Title&gt;&quot; &quot;&lt;question text&gt;&quot;`, `embed … &quot;&lt;Short Title&gt;&quot; &quot;&lt;fragment&gt;&quot;` — quoted by the runner.
    <advantage>The plainest CLI shape — one line with every input visible in the command, a missing body caught by argparse&apos;s usage error for free, nothing that can block on a terminal — and size is no obstacle: the largest annotated block in this milestone is 7 KB against a 1 MiB argument space here and a 128 KiB per-argument cap on Linux, and the answer sweep already passes one-line lifted rationales this way as `git commit -m &quot;&lt;body&gt;&quot;`, where all 20 such bodies carrying backticks in this repository&apos;s history arrived intact.</advantage>
    <drawback>The runner must re-quote the body on every call, and both quoting styles fail on characters these texts routinely carry — backticks stand on 78 lines of the fragments embedded here (a command substitution inside double quotes, as is the `${CLAUDE_PLUGIN_ROOT}` one fragment cites), while the question prose `add` receives is unescaped (the goal makes entity-escaping the tool&apos;s internal) and carries apostrophes that end a single-quoted string, as would one slipped `&amp;apos;` in a 25-line fragment — so every call spends exactly the byte-level transformation the goal moves into the tool, and a miss surfaces as a shell error or a silently altered body instead of reaching the tool&apos;s validation and its aimed repair.</drawback>
  </alternative>
  <alternative id="Text from file path">
    The runner writes the body to a file with its file-writing tool and passes that path as an argument (`embed … &quot;&lt;Short Title&gt;&quot; &lt;path&gt;`); the tool reads the file.
    <advantage>The body never passes through a shell at all — the file-writing tool stores it verbatim whatever shell the host runs — and a rejected fragment stays on disk to inspect or resubmit after the repair.</advantage>
    <drawback>Two tool calls per mutation instead of one (write, then run) on the sweep&apos;s hottest path, plus a temporary file the runner must name, place outside the working tree or watch appear untracked beside the sole writer&apos;s document, and remove afterwards — all restated at every site — manufactured only to cross a process boundary that stdin already crosses, since neither the agent&apos;s return nor the review&apos;s question text exists on disk to begin with.</drawback>
  </alternative>
  <alternative id="File path or stdin">
    The body comes from a `--from &lt;path&gt;` argument where `-` selects standard input — the `git commit -F -` and `gh --body-file -` convention — so runtime sites use the heredoc while tests or hand use point at a saved file.
    <advantage>Both channels behind one familiar flag, with a saved fragment reusable by path and the heredoc kept for the runtime sites.</advantage>
    <drawback>Two code paths and two spellings for one input, where `&lt; file` redirection already puts any file on stdin for free — so the path branch has no consumer the shell does not already serve, and the runtime prose must still fix one idiom per site (or the runner varies), one more thing to state and test for nothing gained.</drawback>
  </alternative>
  <recommendation option="Text on stdin">The body is the one input that is prose or XML rather than an identifier, so it should travel by the channel that carries bytes untouched: a quoted-delimiter heredoc passes a 25-line fragment full of backticks, `&amp;apos;`, and `${…}` verbatim with nothing expanded, in one shell call and with no per-call escaping by the runner — the same idiom the coding agent&apos;s shell tool already relies on for multi-line content — while `&lt; file` and `subprocess.run(input=…)` give the file and pytest cases the same entry, so a second channel would be code without a consumer; the tool pays only for decoding stdin as UTF-8 and refusing a terminal, and the channel is indifferent to how the still-open Embed extraction ownership question settles, because the agent&apos;s whole message travels exactly as the extracted region does.</recommendation>
</open-question>
<open-question id="Embed extraction ownership">
  <question>Does `embed` accept the recommend agent&apos;s whole final message and extract the `&lt;alternative&gt;`…`&lt;/recommendation&gt;` region itself, or does the orchestrator still extract that region and pass only the fragment for validation?</question>
  <alternative id="Tool extracts from whole message">
    The orchestrator keeps only the last-line verdict — a return whose last non-whitespace line begins `FAILED:` is skipped unrepaired, as at every dispatch site — and otherwise pipes the agent&apos;s whole final message to `embed` on stdin, and the tool slices the region from the first `&lt;alternative` line through the last `&lt;/recommendation&gt;` line before it parses and validates the fragment, a slice that is the identity on a clean return.
    <advantage>Extraction is a deterministic line-range operation keyed on the fragment&apos;s own boundary tokens — absent from the goal&apos;s list of what runtime prose keeps (authoring text, significance ranking, judgment-mode reconciliation) — and on the milestone-19 evidence (eight of ten returns prefixed a grounding summary) it fires on most returns, so in the tool it is a short slice before the parse `embed` performs anyway, needs no mode because a clean fragment slices to itself, is pytest-covered (a summary above, a code fence around, a remark below, a missing boundary line), and makes every repair reason, the two extraction misses included, one `Error:` line from one place.</advantage>
    <drawback>The stdin body is the whole message, so the orchestrator re-emits the surrounding prose it would otherwise drop — the grounding summary most returns carry — and `embed`&apos;s contract widens from &quot;a fragment&quot; to &quot;any text containing one&quot;, so the document tool&apos;s two extraction reasons speak of a message rather than a document, and a hand or test call that passes stray text around the elements has it silently discarded rather than rejected.</drawback>
  </alternative>
  <alternative id="Orchestrator extracts fragment">
    The orchestrator keeps the last-line verdict and the extraction exactly as today — it locates the first `&lt;alternative` line and the last `&lt;/recommendation&gt;` line in the return by hand and pipes only that region to `embed` — and the tool validates the fragment strictly, rejecting any text outside the elements.
    <advantage>`embed` stays a pure document operation — a fragment in, a block out — whose input is exactly what a hand user or a pytest case supplies, whose every error concerns the fragment&apos;s own structure, and whose stdin body is the smallest possible; nothing about agent messages enters the tool.</advantage>
    <drawback>A deterministic line-range operation — the very kind the goal moves into the tool — stays in the orchestrator&apos;s runtime prose, applied by an LLM on every return, with its rule and its two failure reasons restated there while every other reason comes from the tool, so the repair slot has two sources; a mis-cut (a dropped or duplicated boundary line) is invisible because the tool validates whatever it is handed; and the recovery path the evidence shows firing on most returns is the one part of the sweep&apos;s judging the pytest suite cannot cover.</drawback>
  </alternative>
  <alternative id="Tool judges whole return">
    The orchestrator pipes the raw return to `embed` with no inspection at all, and the tool runs the whole judging pipeline — the last-line `FAILED:` verdict first, then the slice, then validation — signalling an explicit agent failure distinctly (a third exit status, or a reason line the orchestrator reads) so the orchestrator skips it without spending the repair.
    <advantage>The orchestrator&apos;s prose for a return collapses to exit-status branching — embedded, skip, or repair once — with no reading of the return&apos;s shape by the LLM, and the never-repair-an-explicit-failure rule is enforced by code rather than by prose.</advantage>
    <drawback>It moves the last-line `FAILED:` token — an agent-return protocol the two tool-less dispatch sites (`complete-all-tasks` and the answer sweep) keep in orchestrator prose, so it stops being one convention across the three sites — into a document tool that must then know the dispatch contract, and it needs a second failure class the settled output contract has no form for: a third exit status beside 1 and 2, or an `Error:` line the orchestrator must parse to choose between skip and repair, all for a check that is one glance at one line.</drawback>
  </alternative>
  <depends-on question="Tool output contract" option="Plain lines and exit status"/>
  <depends-on question="Fragment input channel" option="Text on stdin"/>
  <recommendation option="Tool extracts from whole message">Extraction is a deterministic line-range operation keyed on the fragment&apos;s own boundary tokens and, on the milestone-19 evidence, the common case rather than the exception, so it belongs in the tool — a short slice before the parse `embed` performs anyway, the identity on a clean return, pytest-covered, and the source of every repair reason as one `Error:` line — while the last-line `FAILED:` verdict stays with the orchestrator because it is agent protocol shared with the two tool-less dispatch sites and its outcome (skip, never repair) is a control decision only the orchestrator takes, which is what breaks the tie against handing the tool the whole judging pipeline; the surrounding prose the orchestrator re-emits is a paragraph against a multi-kilobyte fragment, far cheaper than a line-range rule kept in prose.</recommendation>
</open-question>
<open-question id="Escape hatch under sole writer">
  <question>With the tool as the file&apos;s only writer, how does a user clear a stale recommendation to force its regeneration — a `strip` subcommand or a documented hand-edit exception?</question>
  <alternative id="Strip subcommand">
    The tool gains a `strip` subcommand that takes one or more Short Titles beside the milestone location every other subcommand takes and, for each named block, deletes every child between its `&lt;question&gt;` element and its `&lt;/open-question&gt;` line — the `&lt;alternative&gt;`, `&lt;applied-principle&gt;`, `&lt;depends-on&gt;`, and `&lt;recommendation&gt;` children — leaving the wrapper and `&lt;question&gt;` intact, touching no dependent and no `&lt;depends-on&gt;` tag that names the block, rejecting an unknown id with the document unchanged, and doing nothing on a block already bare; the user runs it and re-runs `/recommend-all-open-questions`, whose skip rule regenerates the now recommendation-less block.
    <advantage>The sole-writer guarantee holds with no exception — the document on disk is always the tool&apos;s own output — and the operation is the per-block primitive the tool already needs for the transitive strip in `&lt;depends-on&gt;` reconciliation and the dependent-strip on prune, so exposing it costs an argparse entry, one pytest case, and one sentence in the sweep skill; the caller names an id instead of locating lines by eye in an entity-escaped document (for the coding agent acting on a user&apos;s request, one short command in place of an edit whose old text is the block&apos;s whole multi-kilobyte child set), a mistyped id fails loudly, and a half-deleted block the tool would refuse at every later call cannot arise.</advantage>
    <drawback>A mutator no skill invokes — its only caller is the user, or the coding agent on the user&apos;s behalf, outside any skill — so unlike every other subcommand its invocation is carried by no runtime prose: the caller must know the tool&apos;s installed path (on the host whose plugin root is a variable substituted at skill load, the installed tool must first be found) and the milestone directory, all for an escape hatch that none of the 299 commits touching a `requirements.md` in this repository has ever exercised; and the strip lands uncommitted, riding into the next `Recommendation-annotation:` commit exactly as the hand-clear does today.</drawback>
  </alternative>
  <alternative id="Documented hand-edit exception">
    The tool stays the only writer any skill or agent uses, and the sweep skill&apos;s escape-hatch sentence becomes the one documented exception: the user opens `open_questions.xml` and deletes every line between the block&apos;s `&lt;question&gt;` line and its `&lt;/open-question&gt;` line, which leaves a well-formed document the tool parses and re-renders on its next write — the reading of &quot;sole writer&quot; the plugin already gives the principle store, which `capture-milestone-principle-updates` alone writes yet the user may hand-edit for an urgent fix.
    <advantage>Nothing to build, test, or document beyond rewording the sentence the sweep skill already carries and its echoes in the docs; the exception has a precedent in the principle store; and a hand-edit is the one path that still works when the interpreter or the tool itself is unavailable.</advantage>
    <drawback>The exception lands on the one file whose escaping and indentation the goal makes tool-internal, so the user — or the coding agent acting for them — locates the lines by eye instead of naming an id (for the agent, an edit whose old text is the whole multi-kilobyte child set, the very token cost the goal exists to cut); a deletion one line too long leaves a document the tool refuses at every later call with nothing in the tool able to repair it, one line too short leaves the `&lt;recommendation&gt;` in place so the sweep silently skips the block; and &quot;the tool is the file&apos;s sole writer&quot; must carry a caveat everywhere it is stated.</drawback>
  </alternative>
  <alternative id="Strip behind a skill">
    The `strip` subcommand of the first option plus a user-facing skill that wraps it — `strip-open-question-recommendation &lt;Short Title&gt;`, say — which resolves `&lt;MILESTONE_DIR&gt;`, invokes the tool, and commits the cleared block path-scoped under its own function-derived subject, the plugin&apos;s ordinary way to expose a file-changing user action.
    <advantage>The user types a slash command with a Short Title and nothing else — no tool path, no milestone directory, the same on both hosts — and the clear is recorded as its own provenance-marked commit under the skill-layer commit rule instead of riding uncommitted into the next annotation commit.</advantage>
    <drawback>A whole skill — SKILL.md, frontmatter with a 25-word description, a commit subject, a `docs/skill-reference.md` entry, a `CLAUDE.md` pipeline line, a rebuild of both host trees — for an escape hatch never yet exercised, existing solely to run one tool call because the sweep stays argument-free by design; and it splits one regeneration into two commits, the strip and then the sweep&apos;s annotation, where the annotation diff alone already records the replacement.</drawback>
  </alternative>
  <recommendation option="Strip subcommand">The goal gives the tool every deterministic edit to the document and a strip is one it already performs internally — the per-block primitive behind the transitive strip in reconciliation and the dependent-strip on prune — so exposing it is an argparse entry and a test, and in return the user gets an id-validated one-line call in place of an eye-located multi-kilobyte edit on the one file whose escaping and indentation the goal makes tool-internal, the on-disk document stays the tool&apos;s own output without a caveat, and the half-deleted block the tool would refuse at every later call cannot arise; one primitive serving the reconciliation walk transitively and the user one block at a time is one behaviour, not two, and dependents stay untouched exactly as the stale-tags-left-alone rule already says; a skill around it is an additive step to take only if the hatch, unexercised in every commit to date, ever becomes routine — which is what breaks the tie against building one now.</recommendation>
</open-question>
<open-question id="Reconciliation command placement">
  <question>Does `remove` run the exact-id `&lt;depends-on&gt;` reconciliation and transitive strip itself when given the recorded option, or is reconciliation a separate subcommand the answer path invokes after the answered block and any mooted siblings are removed?</question>
  <alternative id="Remove reconciles inline">
    `remove` takes an optional `--option &lt;recorded option&gt;` and, after deleting the named block, reconciles that block&apos;s dependents in the same write: with the option, a surviving block whose `&lt;depends-on&gt;` names the removed id with an equal un-escaped, case-folded option loses that one tag and every other dependent of the removed id is stripped; without it — a mooted sibling, a review prune or dedup — every dependent is stripped; both transitively over the dependents of stripped blocks, before the single write.
    <advantage>Every removal the plugin performs — the answered block, a mooted sibling, a review prune, a review dedup — is followed by reconciliation and none ever skips it, so fusing the two makes &quot;no removal leaves a tag naming an absent block&quot; hold by construction: one call and one write per removed block at every site, the document never on disk with the block gone and its tags dangling, and one pytest post-condition checkable after every `remove`; and because a strip is monotone and subsumes the tag drop, reconciling one removed block per call in any order yields exactly the set-at-once result the current step 6 specifies, so the answered block and each mooted sibling go one call at a time with nothing restated.</advantage>
    <drawback>`remove` stops being a primitive — a caller wanting a bare deletion has none — and the bare form&apos;s default is strip-all, so the literal-answer path, whose runner judges some dependents as still agreeing, needs `remove` to take per-dependent verdicts or a way to withhold reconciliation and apply primitives afterwards (the still-open Judgment-mode verdict application question&apos;s call), while the tool&apos;s most consequential side effect, clearing siblings&apos; embedded analysis, rides behind a command named for something else.</drawback>
  </alternative>
  <alternative id="Separate reconcile subcommand">
    `remove` deletes only the named block, and a separate `reconcile &lt;removed id&gt;… [--option &lt;recorded option&gt;]` subcommand — invoked once per removed block, or once over the whole removed set after the answered block and its mooted siblings are gone — queries the surviving `&lt;depends-on&gt;` tags naming those ids and applies the tag drop or strip with the transitive strip: the current step 6 as a command of its own.
    <advantage>Two single-responsibility primitives that mirror the procedure&apos;s present order (remove, cascade, then reconcile), and one command that is the natural home for both reconciliation modes — `--option` for exact-id, a per-dependent verdict argument for the literal path — so the presence-discriminated two-mode contract RECORDED OPTION carries today lands in one place instead of being bolted onto `remove`.</advantage>
    <drawback>Every removal site — the answered block, each mooted sibling, every review prune and dedup — becomes two calls with the removed id restated in the second, the document sits on disk between them with the block gone and its tags dangling, and that second call is a step no caller ever wants to skip yet can forget: nothing detects the dangling tag, the answer sweep drops its edge as unresolvable, and the dependent&apos;s stale recommendation is recorded as an origin — the wrong decision the strip-on-doubt rule exists to prevent.</drawback>
  </alternative>
  <alternative id="Dangling-tag sweep on every write">
    `remove` keeps the `--option` handling for the agreeing tag drop, and beyond that reconciliation is a document post-condition rather than an operation: every tool write — remove, add, embed, whatever else the tool comes to offer — ends by stripping, transitively, each surviving block whose `&lt;depends-on&gt;` names an id no longer in the document, so no write can leave a dangling tag whatever removed the target.
    <advantage>The strongest guarantee — the invariant holds after any write, a hand-deleted block included — and pytest asserts one post-condition on every golden case instead of one per removal path.</advantage>
    <drawback>Only `remove` can know the recorded option, so the sweep never replaces the `--option` handling and only adds a second mechanism that fires solely on a state the sole-writer design forbids — a hand-deleted block — and when it does fire the strip lands in whatever write came next, an `add` under `Requirements-review:` or an `embed` under `Recommendation-annotation:`, where the diff cannot attribute it to the removal that caused it.</drawback>
  </alternative>
  <recommendation option="Remove reconciles inline">Every removal the plugin performs is followed by reconciliation and none ever skips it, and a removal with no recorded option is exactly the strip-every-dependent case under strip-on-doubt, so an optional `--option` on `remove` covers the answered block, a mooted sibling, a prune, and a dedup with one call and one write each and makes &quot;no removal leaves a dangling tag&quot; hold by construction; reconciling one removed block per call in any order reproduces the set-at-once outcome of the current step 6 because a strip is monotone and subsumes the tag drop, and the tie against a separate `reconcile` breaks on its second call buying nothing any caller wants while adding a forgettable step whose omission is silent and lets the answer sweep record a dependent&apos;s stale recommendation; the literal path&apos;s judged-agreeing dependents are the one thing left to route, which the still-open Judgment-mode verdict application question settles on top of this strip-all default.</recommendation>
</open-question>
<open-question id="Judgment-mode verdict application">
  <question>How does the literal-answer path, which judges each dependent&apos;s assumed option in prose, apply its per-dependent outcomes through the tool — per-dependent strip and tag-removal primitives, or one reconcile command that takes the verdicts?</question>
  <alternative id="Verdicts on remove">
    `remove` gains a repeatable `--agree &lt;dependent id&gt;` flag, mutually exclusive with `--option`: the literal path judges each dependent of the answered block in prose, names every agreeing one, and the tool drops that dependent&apos;s tag naming the removed block while stripping every other dependent transitively, in the one write the reconciling `remove` already performs.
    <advantage>The judgment keeps exactly the shape the procedure states — one verdict per dependent, held against that dependent&apos;s own assumed option — and the tool validates every named id as a real dependent of the removed block, so a mistyped verdict fails loudly with the document untouched, a forgotten one lands on the strip-all default, and the answered block, each mooted sibling, and every review prune still go one call and one write each.</advantage>
    <drawback>A second reconciliation surface on `remove` — a repeatable flag, its exclusivity with `--option`, its validation, and a second way to supply the agree set to test — for verdicts the runner can form only after enumerating the answered block&apos;s dependents and their assumed options across every sibling block, which is either a new two-field query (a `dependents` subcommand printing an id and an option per line) or a scan of the whole file for `&lt;depends-on question=&quot;…&quot;&gt;` lines, the boundary-line idiom the goal retires, re-grown in prose.</drawback>
  </alternative>
  <alternative id="Per-dependent primitives">
    The tool offers two single-purpose mutators — `untag &lt;dependent id&gt; &lt;target id&gt;`, deleting one `&lt;depends-on&gt;` line, and `strip &lt;id&gt;`, clearing a block&apos;s embedded children — and the literal path applies its verdicts around the removal: it untags each dependent it judges agreeing, then calls the bare `remove`, whose strip-all default settles every dependent still tagged.
    <advantage>One verdict maps to one readable call, `remove` carries nothing beyond the `--option` already settled, and the primitives are general — `strip` is the operation the still-open escape-hatch question may want, and `untag` lets a user correct one stale declaration through the sole writer without clearing a whole block.</advantage>
    <drawback>N+1 calls per answer with the dependents enumerated by the runner, and a window between the untags and the removal in which an agreeing dependent stands with its dependency undeclared beside a still-present target — an interrupted run leaves a block whose stale analysis a later, different answer to that target can no longer strip, the kept-stale-rationale the strip-on-doubt rule exists to prevent — while the escape-hatch strip must leave dependents alone where the reconciling strip is transitive, so the shared `strip` is two behaviours under one name.</drawback>
  </alternative>
  <alternative id="Judged alternative as option">
    The tool gains nothing: the literal path&apos;s judgment collapses to one verdict per answer — whether ANSWER plainly settles on one of the answered block&apos;s own `&lt;alternative id&gt;` values — and the procedure passes that id as `remove … --option`, so the exact-id engine untags the dependents that assumed it and strips the rest; an answer that settles on no listed alternative (or a hand-cleared target with none) passes nothing, which is the bare `remove` and its strip-all default.
    <advantage>One engine, one flag, and one write serve every answer path; the judgment is made against a closed set the runner already holds — the `locate`d block it is about to remove — instead of by scanning siblings; a literal answer reconciles identically to a lifted answer with the same option on the same document; and because a dependent&apos;s tag records an option rather than a dependent-specific claim, one verdict per option is exactly as fine-grained as per-dependent verdicts under strip-on-doubt, while the tool can reject an `--option` naming none of the removed block&apos;s alternatives so a mistyped judgment fails loudly rather than stripping silently.</advantage>
    <drawback>The procedure&apos;s judgment mode changes shape — from &quot;does the answer leave this dependent&apos;s assumed option standing&quot; to &quot;which listed alternative does the answer settle on&quot; — so an answer that agrees in substance but matches no listed id (a refinement, a partial answer, or a target regenerated with renamed alternatives after the tags were embedded) strips every dependent, a re-dispatch each; and the flag carries a runner-judged id on the literal path and a caller-lifted one on the other two, a distinction the tool cannot see, so the core&apos;s judgment-mode contract must be restated as judging one settled id from the block&apos;s alternatives — a judgment, not the anchor parse the invariant forbids, but a line an editor must keep drawn.</drawback>
  </alternative>
  <depends-on question="Reconciliation command placement" option="Remove reconciles inline"/>
  <recommendation option="Judged alternative as option">A dependent&apos;s tag records an option, not a claim about that dependent, so every per-dependent verdict the literal path could form is the one verdict of whether the answer settles on that option — exactly the comparison `remove --option` already makes — and routing the judgment through the flag the reconciling `remove` already carries gives one engine and one write for every answer path, keeps the judgment in prose against the closed set of alternatives the runner already holds, makes a literal answer reconcile exactly as a lifted one would on the same document, and needs no dependent enumeration, no new flag, and no multi-call window; the one thing per-dependent verdicts could add — keeping a dependent whose assumed option the answer only arguably preserves — is precisely the doubt case strip-on-doubt says to strip, which is what settles it against `--agree`.</recommendation>
</open-question>
<open-question id="Capture decision source">
  <question>With its commit walk repointed at `open_questions.xml`, does `capture-milestone-principle-updates` read the recorded `## Decisions` entry from the same commit&apos;s `requirements.md` hunk, or from the commit body alone?</question>
  <alternative id="Both hunks, one show">
    The per-commit `git show` names both files — `-- &lt;MILESTONE_DIR&gt;/open_questions.xml &lt;MILESTONE_DIR&gt;/requirements.md`, one command and no extra process — so the removed lines of the `open_questions.xml` hunk still feed the block reconstruction while the added lines of the `requirements.md` hunk fill `recorded_decision` exactly as today; the walk&apos;s log filter stays on `open_questions.xml` alone.
    <advantage>The smallest change the goal&apos;s &quot;keeps its diff-line reconstruction, repointed&quot; clause admits — the per-commit record, the `Manual-answer:` agreement test&apos;s &quot;the body or the added `## Decisions` entry&quot; fallback, and the fresh-option best guess stay word-for-word and one pathspec is added to one command — and the split makes the two reads cleaner than they are now, every removed line in the XML hunk and every added line in the Markdown hunk; the decision is read from the one place the recording core writes it by contract (the fold of ANSWER into `## Decisions`), which on the deliberated `Manual-answer:` path — whose body is by contract the deliberation&apos;s reasoning, not the answer text — is the only record of the decision itself, the input that sets that commit&apos;s `agrees`/`overrides` class and with it the reinforcement shield on the store entries it cited; the one-commit-=-one-answer invariant guarantees the two hunks always belong to the same answer.</advantage>
    <drawback>Every commit&apos;s read spans two files whose roles the prose must assign (removed lines from one, added lines from the other) and costs about a fifth more bytes than the removed block alone — measured at 0.20 across the last two milestones&apos; answer commits — paid on the 89 % of commits (`Recommendation-answer:`, `Alternative-answer:`) whose classification never consults the field, and the added lines may hold several entries when the cascade folded a mooted sibling&apos;s constraint too, leaving the runner to pick the answered question&apos;s entry among them as it does today.</drawback>
  </alternative>
  <alternative id="Body alone">
    Capture reads only the `open_questions.xml` hunk and the commit message: the `recorded_decision` field leaves the per-commit record, the `Manual-answer:` agreement test matches the recorded option against the body alone, and the fresh-option best guess reads the body&apos;s statement of the decision.
    <advantage>One path everywhere — log and show name the same file, the record loses a field, the added lines with their possible cascade entries are never read — and the body is already the decision by contract on two of the three provenances (`&lt;option&gt; — &lt;rationale&gt;`, `&lt;id&gt; — &lt;what-it-is&gt;`) and on the cold manual path (the literal answer text), so the per-commit read shrinks by about a fifth with no loss on those commits; in practice 21 of the 22 `Manual-answer:` bodies in this repository open by stating the decision outright.</advantage>
    <drawback>On the deliberated `Manual-answer:` path the body is defined as the conversation&apos;s reasoning, not the typed answer, which lands only in the `## Decisions` fold — so the one provenance whose `agrees`/`overrides` class must be judged from prose loses its by-contract decision statement and rests on a convention (the reasoning happens to name the choice) that one of the 22 bodies already bends by stating the decision only as a combination of two alternatives named in passing; a class misjudged there wrongly grants or withholds the reinforcement shield that keeps a store entry from being pruned or narrowed — the highest-value artifact in the loop — to save roughly a fifth of a read the removed block dominates anyway.</drawback>
  </alternative>
  <alternative id="Decision-first manual body">
    Body alone as above, but `answer-open-question`&apos;s body rule changes so a deliberated body opens with the answer text as typed and carries the reasoning after it, making the commit message a sufficient decision record on every provenance by contract.
    <advantage>Capture&apos;s single-file read becomes safe by construction rather than by convention, and every answer commit&apos;s message states what was decided and why in one place — the self-describing git log the commit convention already aims at.</advantage>
    <drawback>Widens the milestone into a second skill&apos;s commit contract to serve a reader that already has the same commit&apos;s diff in hand, duplicates the folded decision into every deliberated manual body against the rule that the committed diff is the durable record, must be stated so the bare-cold-answer test still reads a decision-plus-reasoning body as deliberated, and buys nothing the two-pathspec show does not already give.</drawback>
  </alternative>
  <alternative id="Decisions hunk for manual answers only">
    The show names `requirements.md` as a second pathspec only when the subject&apos;s marker is `Manual-answer:` — the one provenance whose agreement test and best guess consume `recorded_decision` — and reads the `open_questions.xml` hunk alone for `Recommendation-answer:` and `Alternative-answer:` commits, where agreement holds by construction or the option is the body text before its first ` — `.
    <advantage>Keeps the by-contract decision statement exactly where it is consumed and drops the roughly one-fifth overhead on the 89 % of commits that never use it, at the cost of one conditional the runner can apply from the subject it has already split.</advantage>
    <drawback>Two show shapes and a record whose `recorded_decision` field is filled for one provenance and absent for two — a per-provenance rule of the kind the diff read has so far avoided, its scoping sentence being deliberately element-agnostic and uniform — for a saving that is a paragraph per commit against the multi-kilobyte block every commit&apos;s read spends anyway.</drawback>
  </alternative>
  <recommendation option="Both hunks, one show">The recording core writes the decision into `## Decisions` by contract and the one-commit-=-one-answer invariant carries that fold beside the block removal in the same commit, so reading it from the same commit&apos;s `requirements.md` hunk keeps the per-commit record, the `Manual-answer:` agreement test, and the fresh-option best guess unchanged for one added pathspec on one command — and it is the only source that still holds the decision itself on the deliberated manual path, whose body is reasoning by contract and whose `agrees`/`overrides` class sets the reinforcement shield; against the body-alone forms that weighs a wrongly shielded or unshielded store entry against a fifth of a read the removed block dominates, and against the provenance-conditional read one uniform show wins because the saving is a paragraph per commit and the diff read has kept every rule it carries uniform.</recommendation>
</open-question>
<open-question id="Direct XML reads">
  <question>May read-only consumers that need the whole question set — the recommend agent&apos;s grounding, `discuss-open-question`, `ask-in-milestone-context`, `modify-milestone-goal` — read `open_questions.xml` directly, or must every read go through the tool?</question>
  <alternative id="Direct whole reads">
    Read-only consumers that need the set whole — the recommend agent&apos;s grounding, `discuss-open-question`&apos;s context step, `ask-in-milestone-context`, and `modify-milestone-goal`&apos;s impact analysis — read `&lt;MILESTONE_DIR&gt;/open_questions.xml` directly with the file-reading tool, exactly as they read `requirements.md` beside it, while every deterministic operation (list, locate, lift, the dependency walk) and every write stays a tool invocation: the tool is the file&apos;s sole writer, not its sole reader.
    <advantage>A whole read that feeds judgment is neither a deterministic operation nor a write, so it is exactly what the goal leaves in prose — each site is one file-read call with a bare path and nothing about the format restated, the runner sees the verbatim escaped XML the recommend agent must itself render, and because the sole writer re-renders the document canonically on every write, the bytes on disk are precisely what any tool read-back would print, so a round trip through it could add only a process.</advantage>
    <drawback>Two access paths to one document, so each read site must say which applies — the file whole for reasoning, the tool for any locate, list, or lift — and a runner that greps the raw file for one block by id re-grows the boundary-line idiom the goal retires; and a whole read of an annotated set spends the tokens of every embedded alternative even at the two sites (`ask-in-milestone-context`, `modify-milestone-goal`) whose judgment turns on ids and question text alone.</drawback>
  </alternative>
  <alternative id="Tool-only reads">
    The tool is the document&apos;s sole interface, reads included: it gains a whole-set subcommand (a `show` that prints the document, or `list` extended to print every block) and every consumer, the four read-only ones included, invokes it instead of the file-reading tool, so no runtime file ever names `open_questions.xml` as something to read.
    <advantage>One access path and one invocation idiom at every site: the on-disk form is the tool&apos;s internal alone, a malformed or hand-edited file is caught by the parser on every read rather than only at the next write, and no runner can drift into raw-file grepping because no runtime file reads the raw file at all.</advantage>
    <drawback>With the tool re-rendering the document canonically on every write, a whole-set read prints the same bytes a file read returns, so the round trip buys a Python process and a longer command line per read and nothing else; it forbids the file-reading tool, with its offset and limit, at the one document those same consumers read beside `requirements.md`, which they still read directly; and the sole-reader rule cannot hold anyway, since `git diff`, capture&apos;s removed-line reconstruction, and the user all read the raw file.</drawback>
  </alternative>
  <alternative id="Tool digest projection">
    Reads go through the tool but as a token-lean projection rather than the document: a subcommand prints, per block, the un-escaped id and question text plus the recommended option and its rationale when present, and the read-only consumers use it instead of reading the file, falling back to `locate` for any one full block.
    <advantage>The cheapest read for the consumers whose judgment needs only ids and question text — `modify-milestone-goal`&apos;s impact analysis and `ask-in-milestone-context`&apos;s what-is-still-open — while keeping one access path.</advantage>
    <drawback>The two consumers that most need the set whole cannot use it — the recommend agent must name a sibling&apos;s exact `&lt;alternative id&gt;` in a `&lt;depends-on&gt;` and weigh that sibling&apos;s drawbacks, and `discuss-open-question`&apos;s disclosure duty needs the same — so they fall to one `locate` per sibling or a direct read after all; a third output shape with its own line syntax must be specified, tested, and kept aligned with the block schema; and un-escaped prose is a form the agent must not copy from, since it renders escaped XML.</drawback>
  </alternative>
  <depends-on question="Canonical serialization format" option="Canonical writer, five entities"/>
  <recommendation option="Direct whole reads">The goal makes the tool the sole writer and the owner of every deterministic operation, and a whole read that feeds judgment is neither, so it stays where the goal leaves judgment — in prose, as one file-read call with a bare path — and because every write is the tool&apos;s canonical re-render, the file&apos;s bytes are exactly what a tool read-back would print, so routing the read through the tool could add only a process and a longer command line; the consumers that need less than the whole set already have the goal&apos;s `list`, and the one rule each site carries — a whole read is for reasoning only, every locate, list, or lift goes through the tool — is what breaks the tie against a digest subcommand that the two readers needing embedded alternative ids could not use.</recommendation>
</open-question>
<open-question id="Canonical serialization format">
  <question>What on-disk formatting does the tool guarantee when it writes `open_questions.xml` — one element per line at a fixed indent per level, and which entity escapes — so that git diffs and capture&apos;s removed-line reconstruction stay line-oriented?</question>
  <alternative id="Canonical writer, five entities">
    The tool re-renders the whole document from its parsed tree on every write through its own small serializer: one element per line at exactly 2 spaces per depth (the root at column 0, each `&lt;open-question&gt;` at 2, its `&lt;question&gt;`, `&lt;alternative&gt;`, `&lt;applied-principle&gt;`, `&lt;depends-on …/&gt;`, and `&lt;recommendation&gt;` children at 4, and inside an alternative the what-it-is text on its own line above `&lt;advantage&gt;` and `&lt;drawback&gt;` at 6), every text value folded to one line (whitespace runs and newlines collapsed to a single space, ends trimmed), self-closing tags written `/&gt;` with no space, and all five predefined entities (`&amp;amp;`, `&amp;lt;`, `&amp;gt;`, `&amp;quot;`, `&amp;apos;`) substituted in element text and attribute values alike.
    <advantage>Byte-determinism by construction — the same tree always writes the same bytes whatever the input looked like (the agent&apos;s fragment, a hand-edit, an older write), so a diff shows exactly the elements that changed and every child stays one greppable line for capture&apos;s removed-line reconstruction; the agent&apos;s own indentation and escaping stop being load-bearing because `embed` parses the fragment and re-renders it; the five-entity rule is the single convention every consumer already reverses (`&amp;amp;` last) and one `xml.sax.saxutils.escape` call with the two extra entities produces it for text and attributes alike; and `write(read(x)) == x` on a golden file is the whole format test.</advantage>
    <drawback>A serializer of the tool&apos;s own — a short walk over a fixed element set, but code to own and test — instead of stdlib output; every apostrophe in prose is stored as `&amp;apos;`, so the raw file and its diffs are noisier to a human than XML requires; and the one-line fold means no text value can ever carry a line break, a constraint the current one-line convention never needed but a hard one.</drawback>
  </alternative>
  <alternative id="Canonical writer, minimal escapes">
    The same serializer and layout, escaping only what XML requires — `&amp;amp;` and `&amp;lt;` in text (`&amp;gt;` too, as ElementTree does) plus `&amp;quot;` inside attribute values — so an apostrophe is never escaped and a double quote is escaped only inside attributes.
    <advantage>The raw file and every diff line read as prose — a possessive stays `root&apos;s`, a quoted term stays `&quot;tools&quot;` — at no parser cost, since ElementTree un-escapes whichever form it meets and capture&apos;s five-way reversal is a no-op on entities that never occur.</advantage>
    <drawback>Two escape rules where one would do (text and attribute values differ), the on-disk form then diverges from the five-entity rule every runtime file and the agent&apos;s fragment rule state, so a raw-line reader that never parses must know the writer&apos;s policy to know what a bare `&apos;` or `&quot;` in a line means; and the readability gain lands where it is least needed, because the raw file&apos;s readers are the tool, the recommend agent, and `git diff`, none of which trips on `&amp;apos;`.</drawback>
  </alternative>
  <alternative id="Stock ElementTree pretty-print">
    The tool parses with `xml.etree.ElementTree`, mutates the tree, and writes with `ET.indent(space=&quot;  &quot;)` followed by `ElementTree.write`, accepting whatever layout and escapes the stdlib emits.
    <advantage>No serializer code at all — parse, `indent`, write, every call present since Python 3.9 (`ET.indent` arrived in 3.9, exactly the documented floor) with well-formedness guaranteed by the library.</advantage>
    <drawback>It cannot produce the block shape: `ET.indent` leaves mixed content alone, so an `&lt;alternative&gt;` whose what-it-is text precedes its children renders as `&lt;alternative id=&quot;A&quot;&gt;what it is&lt;advantage&gt;…&lt;/advantage&gt;` on one line with `&lt;drawback&gt;` on the next, and re-reading an already-indented file drifts the first child&apos;s indent (verified on CPython 3.13.5) — the one-element-per-line guarantee fails on exactly the element capture reads as &quot;the element&apos;s own text before its first child&quot;; its escape set is a third convention (`&amp;gt;` escaped, `&apos;` and text `&quot;` not) and self-closing tags gain a space (`&lt;depends-on … /&gt;`); the only cures are a custom writer, which is Canonical writer, five entities, or moving the what-it-is text into a leaf child, which changes the block schema the goal fixes and every reader of it.</drawback>
  </alternative>
  <alternative id="Line splice, no re-render">
    The tool never re-serializes: it treats the file as lines, locates blocks by their boundary lines, validates each fragment it is handed by parsing it in memory, and splices or deletes line ranges byte-for-byte, so the file&apos;s formatting is whatever its writers supplied — checked, never normalized.
    <advantage>Untouched blocks never change a byte, so a diff is exactly the spliced range and nothing else, a hand-edit outside the target block survives verbatim, and it is the most direct port of the boundary-line prose into Python.</advantage>
    <drawback>It guarantees no format at all — the layout and escapes are the union of what each writer passed (the agent&apos;s fragment indentation, a hand-edit, a differently indented `add`), so &quot;fixed indent per level&quot; can only be enforced by rejecting every deviating fragment (with a repair round each time) or left inconsistent; and every operation is line arithmetic over a document the tool also parses, two representations that must agree — the fragility the goal moves into one tool to retire, relocated rather than removed.</drawback>
  </alternative>
  <depends-on question="Root element shape" option="Bare open-questions root"/>
  <recommendation option="Canonical writer, five entities">The guarantee the question asks for exists only if the writer normalizes, and the stdlib pretty-printer cannot render the mixed-content `&lt;alternative&gt;` one element per line, so the tool must own a small fixed-shape serializer in any case; owning it makes every write a full canonical re-render — same tree, same bytes, whatever the agent, a hand-edit, or an older write supplied — with every child one greppable line at 2 spaces per depth under the bare root and no column-0 exception for the blocks (every raw-line reader keys on the tag, not the column), and the five-entity rule wins the tie against minimal escaping because it is one rule for text and attributes, the convention every consumer already reverses, and the raw file&apos;s readers (the tool, the agent, `git diff`) pay nothing for it.</recommendation>
</open-question>
<open-question id="Test suite location">
  <question>Where does the pytest suite live — in a repository-root `tests/` directory outside `core/` that imports the tool, or beside the tool inside `core/` and excluded from the host render?</question>
  <alternative id="Root tests directory">
    The suite lives in a repository-root `tests/` directory beside `scripts/` and `pyproject.toml` — one `tests/test_*.py` module per concern plus its golden `open_questions.xml` fixtures — importing the tool from `core/tools/` through a `pythonpath = [&quot;core/tools&quot;]` line in `[tool.pytest.ini_options]` (with `testpaths = [&quot;tests&quot;]` and a pytest `dev` dependency group beside it), so `uv run pytest` at the root, locally and as the CI step beside the drift gate, runs it with no path argument.
    <advantage>`core/` stays exactly what every document and every build check says it is — the runtime layer, rendered whole into each host tree — so no `exclude` entry, no &quot;pure build of `core/`&quot; caveat, and no rule written for runtime prose reaches the tests: `host-name-in-core` scans every file under `core/` whether excluded or not, and 43 of the 197 answer commits in this repository remove block lines that name a host, so realistic golden fixtures lifted from history are legal here and illegal under `core/`; it is the conventional `pyproject.toml` + `tests/` pair that contributors, editors, and pytest&apos;s own default discovery expect, and the one home a later test of `build_hosts.py` or `set_version.py` could join.</advantage>
    <drawback>The tool and its tests sit in different top-level directories, so a reader of `core/tools/` sees no test beside the code and the import path is a root-config line rather than adjacency — though adjacency buys nothing here: pytest inserts the test file&apos;s own directory on `sys.path`, so a `tests/` subdirectory beside the tool needs the very same `pythonpath` line (verified), and only a test file in the tool&apos;s own directory imports it unconfigured.</drawback>
  </alternative>
  <alternative id="Beside the tool, excluded">
    The suite lives under `core/tools/tests/` beside the tool (a top-level `core/tests/` would need its own `[layout]` entry only to be excluded again) and both host definitions add `exclude = [&quot;tools/tests&quot;]` so the render skips it, with pytest configured at the root `pyproject.toml` exactly as above and `testpaths` and `pythonpath` pointing into `core/tools/`.
    <advantage>Code and tests are adjacent in one directory, the `exclude` key exists for exactly this and is empty on both hosts today, and the tool&apos;s whole footprint — program, tests, fixtures — moves or is deleted as one directory.</advantage>
    <drawback>`core/` gains its first non-runtime content, so &quot;every host tree is a pure build of `core/`&quot; and &quot;`core/` holds only what the runner needs&quot; each carry a caveat, the exclusion is restated once per host with no glob support (a definition that omits it ships the suite, and nothing but a reader of the tree notices), and the tests inherit rules written for runtime prose that exclusion does not switch off: `host-name-in-core` reads the source tree, so a golden fixture may never mention a host — ruling out the 43 historical blocks that do, the most realistic fixture source there is — while the import path is the same root-config line as for a root `tests/`.</drawback>
  </alternative>
  <alternative id="Beside the tool, shipped">
    The suite lives under `core/tools/tests/` and is not excluded, so it renders into every host tree like the tool itself and a user can run it against the installed interpreter.
    <advantage>No exclusion bookkeeping in either host definition, the pure-render contract holds with no caveat, and the shipped suite doubles as a user-side self-check of the Python 3.9+ prerequisite on the very interpreter the tool will run under.</advantage>
    <drawback>pytest is no user prerequisite — Python 3.9+ alone is — so the suite is inert cargo in every install; every test file and fixture then passes through `{{PLUGIN_ROOT}}` substitution and the full render gate (`unfilled-placeholder` forbids a bare `{{` in an f-string or a dict literal, `foreign-plugin-root` forbids a fixture mentioning the other host&apos;s plugin root, `host-name-in-core` still applies), each host tree grows by the whole suite and its golden files, and `CLAUDE.md`&apos;s enumeration of the tree gains a directory the runner never loads.</drawback>
  </alternative>
  <depends-on question="Tool directory placement" option="New tools directory"/>
  <recommendation option="Root tests directory">`core/` is by contract the runtime layer and nothing else, and every rule the build enforces over it — the host-name scan that reads excluded files too, the layout that must map each top-level directory, the render checks — is written for runtime prose, so a test corpus under it inherits those rules for nothing, while adjacency buys no import simplicity (a `tests/` subdirectory needs the same `pythonpath` line as a root one); the suite&apos;s configuration already lives at the root `pyproject.toml`, making a root `tests/` the conventional pair and the one home a build-script test could later join, and the tie against an excluded in-core suite breaks on golden fixtures: 43 of this repository&apos;s 197 answer commits remove block lines that name a host, and only outside `core/` may such a block copied from history serve as a fixture as it stands; the location is indifferent to how the still-open Tool test Python versions question settles, since `uv run` reads the root project from the working directory whichever directory holds the tests.</recommendation>
</open-question>
<open-question id="Tool test Python versions">
  <question>Which Python versions does CI run the tool&apos;s pytest suite under — a matrix reaching down to the documented 3.9 floor, or only the 3.13 the repository pins — given that `pyproject.toml` requires 3.11 for the build tooling?</question>
  <alternative id="Pinned interpreter only">
    One CI step, `uv run pytest`, in project mode — the suite runs under the interpreter `.python-version` pins (3.13 today) against the locked `dev` group, with no matrix and no second interpreter.
    <advantage>The cheapest and most uniform shape: the same project-mode `uv run` form as the drift gate&apos;s step, one interpreter download, one locked pytest, and nothing in the workflow that a pin bump or a floor change would have to track.</advantage>
    <drawback>The 3.9 floor the README documents and the bootstrap check enforces is then a promise nothing checks — the tool is written and tested on 3.13, where a `match` statement, an `X | None` annotation, or 3.12 f-string quoting passes every test and first fails as a `SyntaxError` at a user&apos;s stock 3.9.6 (the `python3` Apple&apos;s command-line tools install) on the first `add`, after the bootstrap check has already told them their interpreter is fine.</drawback>
  </alternative>
  <alternative id="Floor and pin">
    Two runs of the suite in the pytest job: the pinned one as above (`uv run pytest`, project mode, the interpreter from `.python-version`), and one under the documented floor as `uv run --no-project --python 3.9 --with pytest pytest`, which bypasses the build tooling&apos;s `requires-python = &quot;&gt;=3.11&quot;` (the stdlib-only tool needs nothing from that project) and resolves the last pytest that supports 3.9.
    <advantage>The floor becomes a tested fact for one extra step and one interpreter download — verified on this machine: `uv run --no-project --python 3.10 --with pytest` runs while project mode refuses any interpreter below 3.11, and a 3.9 target resolves pytest 8.4.2 against 9.1.1 on 3.13 — with `pyproject.toml`, `uv.lock`, and the build scripts untouched; the pinned run carries no version literal (it follows `.python-version`, so a pin bump is one file), and because stdlib availability is monotone across minors, the two bracketing runs catch every feature too new for the floor and every one removed by the pin, whether the suite imports the tool or drives it through `sys.executable`.</advantage>
    <drawback>Two pytest majors are in play (8.4.2 under 3.9, 9.1.1 under 3.13 today), so the suite must stay on the API both share; the floor run resolves its pytest ad hoc outside the lock on every CI run; the literal `3.9` gains a third home beside the README prerequisite and the bootstrap check; and 3.9, past end-of-life since October 2025, stays fetchable only while uv&apos;s managed builds keep shipping it (3.9.25 is listed today).</drawback>
  </alternative>
  <alternative id="Every minor from the floor">
    A matrix job over every minor from the floor to the pin — 3.9, 3.10, 3.11, 3.12, 3.13 — each leg running the suite with the same `--no-project` invocation (uniform because 3.9 and 3.10 cannot run in project mode) and a version literal per leg.
    <advantage>The only option that observes each supported minor directly, so a behaviour that differs in exactly one intermediate release cannot slip through, and every pull request shows one green check per supported version.</advantage>
    <drawback>Five runners and five interpreter downloads on every push and pull request for a stdlib-only tool whose interior legs can fail only on a version-specific stdlib bug — anything present at 3.9 and still present at 3.13 was present in between — while the pin is restated as a literal that must be bumped by hand beside `.python-version` and the matrix table grows another row each October.</drawback>
  </alternative>
  <alternative id="Lower the project floor">
    Set `requires-python = &quot;&gt;=3.9&quot;` in `pyproject.toml`, add `tomli; python_version &lt; &quot;3.11&quot;` as a dependency with an import fallback in `build_hosts.py` (its one 3.11-only import is `tomllib`), re-lock, and run every leg — floor to pin — in project mode with `uv run --python &lt;version&gt; pytest`.
    <advantage>One invocation shape for CI and local runs alike, a declared project floor that matches the plugin&apos;s documented floor, and a single lock governing pytest on every interpreter (uv&apos;s universal lock forks so that 3.9 resolves pytest 8.4 and 3.10 and later resolve 9).</advantage>
    <drawback>It rewrites the build tooling — a second dependency beside `pyyaml` and an import shim — for an interpreter that tooling never runs under (the drift gate, the release skill, and `.python-version` all sit on 3.13), so the scripts&apos; new floor is itself a claim nothing checks unless the drift gate gains legs too, and `uv.lock` grows fork markers across 3.9–3.13, all to buy what `--no-project` already gives the stdlib-only tool&apos;s suite with the project untouched.</drawback>
  </alternative>
  <recommendation option="Floor and pin">The 3.9 floor is a promise the README makes and the bootstrap check enforces, so CI must run the suite under it or the promise is checked by nothing — and the tool is written on the pinned 3.13, exactly where a `match` or an `X | None` slips in unnoticed and first fails at a user&apos;s stock 3.9.6 after bootstrap has passed them; the pinned run is the project-mode `uv run pytest` the locked `dev` group already serves, and the floor run costs one `uv run --no-project --python 3.9 --with pytest pytest` step that leaves `pyproject.toml`, the lock, and the build scripts untouched (verified: `--no-project` bypasses the `&gt;=3.11` requirement and resolves the last pytest supporting 3.9); two bracketing runs catch every too-new-for-the-floor and removed-by-the-pin slip because stdlib availability is monotone across minors, which is what breaks the tie against a five-leg matrix whose interior runners could fail only on a version-specific stdlib bug, and lowering the project floor falls out because it rewrites build tooling for an interpreter it never runs under.</recommendation>
</open-question>
<open-question id="Prerequisite check failure mode">
  <question>When `init-milestone-base-workflow` finds no Python 3.9+ interpreter, does it stop the bootstrap or warn and continue creating the scaffold?</question>
  <alternative id="Stop before any write">
    The interpreter check is the skill&apos;s first step, ahead of the state detection: it executes `python3` to read its version (probing `python` only to name the remedy, per the interpreter-command answer) and, when no 3.9+ interpreter answers, stops with a full explanatory message — what it looked for, what it found, the install or expose-as-`python3` remedy, and that a re-run completes the bootstrap — having created nothing, so the scaffold is written only once the check passes.
    <advantage>The one check the goal places in the pipeline actually enforces the prerequisite: every later skill, `define-milestone-goal`&apos;s `create` call first, may assume the interpreter exists and carries no check of its own, the gap surfaces once, at setup, with the aimed remedy instead of a raw `command not found` mid-skill, and because the skill is idempotent and non-committing the stop costs exactly one re-run after the install with no partial state — the clean-stop-and-point idiom `goto-next-milestone`, `derive-tasks`, and the answer skills already use for an unmet precondition; running it first also gives a re-run on an already-bootstrapped project (every earlier adopter, this repository included) the same check.</advantage>
    <drawback>A scaffold that needs no Python to write is withheld for a prerequisite the scaffold itself never uses, so a user who wants to commit the scaffold now and install the interpreter later, or whose interpreter the probe cannot see (a `py`-launcher-only PATH, an unactivated virtual environment), is blocked until they act — every miss of the check is a hard stop rather than a warning.</drawback>
  </alternative>
  <alternative id="Warn and continue">
    The check runs, but a miss only adds a warning to the step-5 report: the scaffold is created exactly as today, and the report names the missing prerequisite and its remedy, leaving the user to install before `/define-milestone-goal`.
    <advantage>The bootstrap never blocks — its own writes need no interpreter, its purely additive contract is untouched, a false miss by the check costs the user nothing but a line to ignore, and the scaffold can be committed from a machine that will never run the tool.</advantage>
    <drawback>The plugin&apos;s only check of the prerequisite becomes advisory, so enforcement falls to the first tool invocation failing raw — `python3: command not found`, or a `SyntaxError` under an old interpreter — inside `define-milestone-goal`, the skill the empty-document answer deliberately keeps check-free and had to give an ordering rule so a missing interpreter leaves nothing behind; and the warning sits in a success report, the easiest place to miss it, so the user meets the gap twice, once softly and once as a shell error, instead of once.</drawback>
  </alternative>
  <alternative id="Notice and confirm">
    On a miss the skill prints a one-line notice naming the missing interpreter and asks once whether to proceed without it — the guard shape `capture-milestone-principle-updates` uses for a repeat capture or a dirty store — creating the scaffold on proceed and stopping otherwise.
    <advantage>The user decides with the fact in front of them: a deliberate proceed covers the scaffold-now-install-later case and the check&apos;s blind spots, while stop stays the default on the ordinary path.</advantage>
    <drawback>It offers a proceed into a state in which nothing after the bootstrap can run, so the choice is between a stop and an unusable scaffold one re-run would recreate in seconds — an interactive branch, with prompt wording and a second success path to state, spent on a case the plugin&apos;s notice-and-confirm precedent reserves for a proceed that is genuinely useful (a repeat capture, a hand-edited store worth keeping); and a proceed converts the once-checked prerequisite into the same raw failure at `define-milestone-goal` as warn-and-continue, now with the user&apos;s consent on record.</drawback>
  </alternative>
  <depends-on question="Interpreter command name" option="python3 with bootstrap probe"/>
  <depends-on question="Empty document creation" option="Tool create subcommand"/>
  <recommendation option="Stop before any write">The scaffold exists only to enable the skills that follow, and with the tool creating the empty document the very next one, `define-milestone-goal`, invokes it, so a scaffold written without an interpreter is unusable from its first step and the goal&apos;s checked-once clause holds only if the one check enforces — a warning would move the failure to a raw shell error inside a skill kept check-free by design; the stop costs one re-run of an idempotent, non-committing skill and nothing else, its message carries the remedy the probe identified, and it is the clean-stop-and-point idiom every unmet precondition in the plugin already uses, which is what settles it against a notice-and-confirm whose proceed could only yield a scaffold nothing can use.</recommendation>
</open-question>
