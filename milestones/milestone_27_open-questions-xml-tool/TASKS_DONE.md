# TASKS DONE

## Tool Module Serializer And Create Subcommand

Create the stdlib-only `core/tools/open_questions.py` with its argparse frame (every subcommand takes the resolved `<MILESTONE_DIR>` and joins it with `open_questions.xml`), the parser and fixed-shape canonical serializer (bare `<open-questions>` root with no declaration, one element per line at 2 spaces per depth, children grouped by kind in the fixed order, text values folded to one line, `/>` self-closing tags, all five predefined entities in text and attributes), the output and error contract (bare un-escaped values on stdout, silent mutators, one `Error: <reason>` line on stderr with exit 1 and the document byte-for-byte unchanged, argparse's own exit 2, empty set as empty stdout with exit 0), and the `create` subcommand that writes the serializer's empty document and refuses an existing file. In the same change add `tools = "tools"` to both host `[layout]` tables so `uv run scripts/build_hosts.py` renders the tool into every host tree byte-identical to the source, and scaffold the root `tests/` suite — pytest in a `dev` dependency group, `pythonpath = ["core/tools"]` and `testpaths = ["tests"]` under `[tool.pytest.ini_options]`, golden `open_questions.xml` fixtures — so `uv run pytest` passes a `write(read(x)) == x` round-trip test plus `create` and contract cases, and the build's `--check`, `unfilled-placeholder`, and `host-name-in-core` gates pass over the new source.

**Verified:**

- `core/tools/open_questions.py` exists, imports only the standard library (`argparse`, `os`, `stat`, `sys`, `tempfile`, `xml.etree.ElementTree`, `dataclasses`, `typing`), and its 52-case suite passes under both `uv run pytest` (Python 3.13.5, pytest 9.1.1) and `uv run --no-project --python 3.9 --with pytest pytest` (Python 3.9.25, pytest 8.4.2); a plain `grep '{{'` over the source finds nothing and no `claude`/`antigravity` word appears in `core/tools/`.
- Argparse frame: `add_subparsers(required=True)` with every subcommand registered through one `add_subcommand` helper that adds the `MILESTONE_DIR` positional, joined with `open_questions.xml` by `document_path`; `python3 core/tools/open_questions.py` with no subcommand, an unknown subcommand, or a missing `MILESTONE_DIR` exits 2 with argparse's own usage on stderr and empty stdout (`tests/test_contract.py`).
- Canonical serializer: `render_document(parse_document(x)) == x` holds for all four golden fixtures under `tests/fixtures/` (`empty`, `bare`, `entities`, and `annotated`, whose first block is the historical "Escape hatch under sole writer" block from commit `eb6049c` shifted to column 2); the empty document renders as `<open-questions/>\n`; a non-canonical input with an XML declaration, 4-space indents, misordered children, multi-line text, `<depends-on ... />` with a space, and raw quotes renders to the fixed shape (root at 0, block at 2, children at 4, alternative body at 6, children grouped `<question>`, `<alternative>`, `<applied-principle>`, `<depends-on>`, `<recommendation>`, text folded to one line, `/>` with no space, all five entities in text and attributes) and rendering is idempotent (`tests/test_format.py`).
- Output and error contract: a successful `create` prints nothing on stdout or stderr and exits 0; an existing document, an unparseable one, a non-UTF-8 one, or an operating-system failure (a file where the directory should be) each yield exactly one `Error: <reason>` line on stderr, exit 1, empty stdout, no `Traceback`, and the target bytes unchanged; `save_document` writes through a same-directory temporary file renamed over the target and preserves its mode (`tests/test_contract.py`, `tests/test_create.py`).
- `create` writes the serializer's empty document byte-identical to `tests/fixtures/empty/open_questions.xml`, creates a missing milestone directory (including parents) so nothing else is left behind, refuses an existing file with `Error: <path> already exists` and the file untouched, and its output parses back to `Document()` (`tests/test_create.py`).
- `tools = "tools"` appended to the `[layout]` table of both `scripts/hosts/claude/settings.toml` and `scripts/hosts/antigravity/settings.toml`; `uv run scripts/build_hosts.py` builds hosts/antigravity/ (37 files) and hosts/claude/ (38 files), `cmp` shows `hosts/claude/tools/open_questions.py` and `hosts/antigravity/tools/open_questions.py` byte-identical to `core/tools/open_questions.py`, and `uv run scripts/build_hosts.py --check` passes (which runs the `unfilled-placeholder` and `host-name-in-core` gates over the new source).
- Root `tests/` scaffold: `pyproject.toml` carries `[dependency-groups] dev = ["pytest>=8.4"]` and `[tool.pytest.ini_options]` with `pythonpath = ["core/tools"]` and `testpaths = ["tests"]`; `uv lock` recorded pytest 9.1.1 in `uv.lock` with the `cairn-tooling` version block `set_version.py` reads unchanged; `tests/conftest.py` sets `sys.dont_write_bytecode` so importing the tool leaves no `__pycache__` under `core/` (confirmed by `find core -name __pycache__` returning nothing after both runs), and `.gitignore` gained `__pycache__/` and `.pytest_cache/`.

---

## Tool List Locate And Lift Subcommands

Add the tool's read subcommands: `list` printing bare ids in document order with a `--unannotated` filter for blocks carrying no `<recommendation>` element, `locate` taking one or more Short Titles and printing each block verbatim as the file holds it, and `lift` printing the recombined answer text — `<option> — <rationale>` from the block's `<recommendation option="…">` or `<id> — <what-it-is>` from a named `<alternative id="…">` with the `<advantage>`/`<drawback>` children excluded — with ids compared un-escaped and case-folded and a missing block, missing recommendation, or unmatched alternative id failing as one `Error:` line. These are what the rewired skills call in place of the `awk`/`sed`/`grep` idiom, and the work is verified by pytest cases covering each subcommand, its empty-set behaviour, and its error paths.

**Verified:**

- `list MILESTONE_DIR` prints the un-escaped `id` of every `<open-question>` block, one per line in document order (the `annotated` fixture yields `Escape hatch under sole writer`, `Root element form`, `Fixture bare block`; the `entities` fixture yields `Ampersand & angle <brackets>, "quotes", 'apostrophes'`), exit 0; the `empty` fixture prints nothing with exit 0 (`tests/test_list.py`).
- `list --unannotated` (flag before or after `MILESTONE_DIR`) prints in document order only the ids of blocks with no `<recommendation>` element: `Fixture bare block` alone from `annotated`, both ids from `bare`, and nothing with exit 0 from the fully annotated `entities` fixture and from `empty` (`tests/test_list.py`).
- `locate MILESTONE_DIR SHORT_TITLE...` takes one or more Short Titles (`nargs="+"`; none is argparse's exit 2) and prints each named block verbatim — for all six fixture blocks the stdout equals the raw line slice of the golden file from its column-2 `<open-question id="…">` line through its `</open-question>` line, entities and indentation intact — consecutively in the order named and once per block even when a title is repeated; an unknown title, alone or among known ones, fails with empty stdout as `Error: no <open-question> block has the id "…"; the document holds "…", "…"` (or `the document holds no blocks` on the `empty` fixture) (`tests/test_locate.py`).
- `lift MILESTONE_DIR SHORT_TITLE` prints exactly one line `<option> — <rationale>` (spaced em dash) from the block's `<recommendation option="…">`; `lift … --alternative ALTERNATIVE_ID` prints `<id> — <what-it-is>` using the stored alternative id and only its what-it-is text, with the `<advantage>`/`<drawback>` children excluded; both values are printed un-escaped (`A & B — One rule for text & attributes: <, >, ", '.` from the `entities` fixture), and an empty rationale or what-it-is text prints the bare option/id with no dash (`tests/test_lift.py`).
- Every id comparison — Short Title against block `id`, `--alternative` against `<alternative id>` — is made on the un-escaped, case-folded values through `id_key`: `ROOT ELEMENT FORM` and `OPEN and close PAIR` match, the un-escaped `ampersand & ANGLE <brackets>, …` matches the entity-bearing block while its `&amp;`-escaped spelling does not (`tests/test_locate.py`, `tests/test_lift.py`).
- Error paths each fail as exactly one `Error: <reason>` line on stderr, exit 1, empty stdout, no traceback, document bytes unchanged: no block with the Short Title (naming the held ids), `<open-question id="…"> carries no <recommendation> element`, `<open-question id="…"> carries no <alternative> elements`, `<open-question id="…"> has no <alternative> with the id "…"; its alternatives are "…", "…"`, a missing `open_questions.xml` (`… does not exist`), and a malformed document (`not well-formed XML`); `--alternative` without a value and `lift` without a Short Title are argparse's exit 2 (`tests/test_list.py`, `tests/test_locate.py`, `tests/test_lift.py`).
- The three subcommands are registered through the existing `add_subcommand` helper (each takes `MILESTONE_DIR` first), appear in `--help` (`tests/test_contract.py`), and are described in the module docstring with the id-comparison rule; the lookups `find_question`, `find_questions`, and `find_alternative` are module-level helpers for the later mutators; the tool adds no import (stdlib only), `grep '{{'` over `core/tools/open_questions.py` finds nothing, and no `claude`/`antigravity` word appears under `core/tools/`.
- The suite grew from 52 to 104 cases and passes under both `uv run pytest` (Python 3.13.5, pytest 9.1.1) and `uv run --no-project --python 3.9 --with pytest pytest` (Python 3.9.25, pytest 8.4.2), leaving no `__pycache__` under `core/`.
- `uv run scripts/build_hosts.py` rebuilt hosts/antigravity/ (37 files) and hosts/claude/ (38 files), `cmp` shows both `hosts/<host>/tools/open_questions.py` byte-identical to `core/tools/open_questions.py`, and `uv run scripts/build_hosts.py --check` passes.

---

## Tool Add And Strip Subcommands

Add `add`, which takes the Short Title as an argument, reads the question text from `sys.stdin.buffer` as UTF-8 (refusing a terminal stdin and reading exactly one body per call), and appends a bare block with only its `<question>` child in canonical form, and `strip`, which takes one or more Short Titles and deletes every child between each block's `<question>` element and its `</open-question>` line while touching no dependent and no `<depends-on>` tag naming the block, rejecting an unknown id with the document unchanged and doing nothing on an already-bare block. `add` is the review pass's authoring write and `strip` is the user's hand-clear replacement for a stale recommendation, and both are verified by pytest cases including the stdin-refusal and duplicate-id or unknown-id errors.

**Verified:**

- `add MILESTONE_DIR SHORT_TITLE` reads the question text as the one body on standard input (`read_body` reads `sys.stdin.buffer` to end of file once and decodes it as UTF-8), appends a bare block — the `<open-question id="…">` wrapper and its one `<question>` child — after every existing block, and writes the whole document in canonical form with the id and text whitespace-folded to one line and the five entities escaped; success prints nothing and exits 0, and two `add` calls on the `empty` fixture reproduce `tests/fixtures/bare/open_questions.xml` byte-for-byte (`tests/test_add.py`).
- `add` refuses a terminal stdin without blocking — a pseudo-terminal on fd 0 in a subprocess and an `isatty()`-true stdin in-process each fail as `Error: the question text must be piped on standard input (as a quoted heredoc or a redirected file), not typed at a terminal`, exit 1, empty stdout, document unchanged, the terminal never read — and a closed stdin, an empty or whitespace-only body, and a non-UTF-8 body each fail as one `Error:` line with the document unchanged (`tests/test_add.py`).
- `add` rejects a duplicate id — a Short Title matching an existing block's id un-escaped, whitespace-folded, and case-folded — as `Error: an <open-question> block with the id "…" already exists`, exit 1, document byte-for-byte unchanged; an empty Short Title fails as `Error: the Short Title is empty`; a missing `open_questions.xml` fails with `does not exist` and `add` creates nothing; a malformed document fails as one `not well-formed XML` line and is left as it was (`tests/test_add.py`).
- `strip MILESTONE_DIR SHORT_TITLE...` takes one or more Short Titles (`nargs="+"`; none is argparse's exit 2) and, for each named block, deletes every `<alternative>`, `<applied-principle>`, `<depends-on>`, and `<recommendation>` child, leaving the wrapper and `<question>` intact as the canonical three-line bare block; success is silent with exit 0, several blocks are stripped in one call, a block named twice is stripped once, and ids match un-escaped and case-folded (`tests/test_strip.py`).
- `strip` touches no dependent and no `<depends-on>` tag naming the stripped block: after stripping `Escape hatch under sole writer` in the `annotated` fixture, `Root element form` still carries `<depends-on question="Escape hatch under sole writer" option="Strip subcommand"/>` and every other child, and every line of the document outside the stripped block is byte-identical to the fixture (`tests/test_strip.py`).
- `strip` of an unknown id, alone or among known ones, fails as `Error: no <open-question> block has the id "…"; the document holds …` (or `the document holds no blocks` on `empty`), exit 1, empty stdout, with the document byte-for-byte unchanged and no partial strip of the known ids; a missing document fails with `does not exist` and a malformed one as one `not well-formed XML` line, each left as it was (`tests/test_strip.py`).
- `strip` on an already-bare block does nothing: exit 0, silent, the document byte-for-byte unchanged and not even rewritten (same inode and mtime), and a bare block named beside an annotated one leaves only the annotated one stripped (`tests/test_strip.py`).
- The strip is one module-level per-block primitive, `strip_question(question)` (with `is_bare(question)` beside it), that returns whether it deleted anything and that `cmd_strip` applies per named block, saving only when something changed; `add` and `strip` are registered through the existing `add_subcommand` helper, appear in `--help` (`tests/test_contract.py`), and are described in the module docstring together with the stdin-channel rule.
- The tool stays stdlib-only (no new import), `grep '{{'` over `core/tools/open_questions.py` finds nothing, and no `claude`/`antigravity` word appears under `core/tools/`; the suite grew from 104 to 153 cases and passes under both `uv run pytest` (Python 3.13.5, pytest 9.1.1) and `uv run --no-project --python 3.9 --with pytest pytest` (Python 3.9.25, pytest 8.4.2), leaving no `__pycache__` under `core/`.
- `uv run scripts/build_hosts.py` rebuilt hosts/antigravity/ (37 files) and hosts/claude/ (38 files), `cmp` shows both `hosts/<host>/tools/open_questions.py` byte-identical to `core/tools/open_questions.py`, and `uv run scripts/build_hosts.py --check` passes.

---

## Tool Remove With Dependent Reconciliation

Add `remove`, which deletes the named block and, before its single write, reconciles that block's dependents: with `--option <recorded option>` a surviving block whose `<depends-on>` names the removed id with an equal un-escaped case-folded option loses only that tag while every other dependent of the removed id is stripped, and without the flag every dependent is stripped — both transitively over the dependents of stripped blocks, using the same per-block strip primitive. The tool must reject an `--option` naming none of the removed block's own `<alternative id>` values with the document unchanged, and pytest cases cover the tag-drop, strip-all, transitive, cycle, and rejected-option outcomes so no removal can leave a dangling tag.

**Verified:**

- `remove MILESTONE_DIR SHORT_TITLE [--option RECORDED_OPTION]` is registered through the existing `add_subcommand` helper, listed in `--help` (`tests/test_contract.py`) and the module docstring, deletes the one named block (id matched un-escaped and case-folded by `find_question`), and on success prints nothing on stdout or stderr with exit 0; removing the last block leaves `<open-questions/>\n`, every block that is neither the removed one nor one of its transitive dependents is byte-identical afterward, and the written document stays canonical (`tests/test_remove.py`).
- Tag-drop: with `--option` equal (un-escaped, case-folded) to the option of a surviving block's `<depends-on>` naming the removed id, that block loses only that tag — its `<alternative>`, `<applied-principle>`, tags naming other blocks, and `<recommendation>` all stand — and is not stripped; on the `annotated` fixture `remove "Escape hatch under sole writer" --option "strip SUBCOMMAND"` leaves `Root element form` as the fixture's block minus its one `<depends-on>` line (`tests/test_remove.py`).
- Strip-all: without `--option`, every surviving block whose `<depends-on question>` names the removed id is stripped by `strip_question` to its wrapper and `<question>`; with `--option`, every dependent whose tag option differs, or that carries both an agreeing and a differing tag naming the removed block, is likewise stripped, and a block whose tags name only other blocks is untouched (`tests/test_remove.py`).
- Transitive: every block whose `<depends-on>` names a block stripped in the pass is stripped in turn (a four-deep chain, a diamond joined once) until no tag names a removed or stripped block; an untagged agreeing dependent propagates nothing, a dependent untagged on the removed block but naming a stripped sibling is still stripped; a cycle among the dependents, a cycle containing the removed block, and a self-dependent block each terminate with every reachable member stripped once, and a cycle unreachable from the removed block is byte-identical afterward (`tests/test_remove.py`).
- Rejected option: `--option` naming none of the removed block's own `<alternative id>` values fails as `Error: --option "…" names none of the <alternative> ids of <open-question id="…">, which are …`, and `--option` on a block with no alternatives as `Error: --option "…" was given, but <open-question id="…"> carries no <alternative> elements` — each one stderr line, exit 1, empty stdout, the document byte-for-byte unchanged and nothing stripped even with dependents present; the escaped form of an entity-bearing id is rejected while its un-escaped, case-folded form is accepted; an unknown Short Title (with or without `--option`), an empty document, a missing document, and a malformed document fail the same way, and zero or two Short Titles are argparse's exit 2 (`tests/test_remove.py`).
- Single write: the removal and the reconciliation are applied on the parsed tree by the module-level `remove_question(document, question, recorded_option=None)` (over `dependents_of` and the existing `strip_question` primitive, returning the stripped ids in order) and `save_document` is called exactly once per successful call and never on a rejected option (monkeypatched count via `main`); no temporary file is left behind (`tests/test_remove.py`).
- No dangling tag: after every successful `remove` on the `annotated` fixture (six argument sets) and after every removal with no option, `A`, and `B` over a seven-block dependency web, no surviving block carries a `<depends-on question>` naming the removed id or any block stripped in that pass, and the re-rendered document parses back to the same tree (`tests/test_remove.py`).
- `tests/test_remove.py` covers the tag-drop, strip-all, transitive, cycle, and rejected-option outcomes (52 cases, 47 test functions); the suite grew from 153 to 205 cases and passes under both `uv run pytest` (Python 3.13.5, pytest 9.1.1) and `uv run --no-project --python 3.9 --with pytest pytest` (Python 3.9.25, pytest 8.4.2), leaving no `__pycache__` under `core/`.
- The tool stays stdlib-only (no new import), `grep '{{'` over `core/tools/open_questions.py` finds nothing, and no `claude`/`antigravity` word appears under `core/tools/`; `uv run scripts/build_hosts.py` rebuilt hosts/antigravity/ (37 files) and hosts/claude/ (38 files), `cmp` shows both `hosts/<host>/tools/open_questions.py` byte-identical to `core/tools/open_questions.py`, and `uv run scripts/build_hosts.py --check` passes.

---

## Tool Embed With Extraction And Validation

Add `embed`, which takes the Short Title, reads the recommend agent's whole final message from stdin, slices from the first `<alternative` line through the last `</recommendation>` line (the identity on a clean return), parses the fragment, validates it — no wrapper or `<question>` line, at least one `<alternative>`, exactly one `<recommendation>` whose option names one of the fragment's alternatives, every `<depends-on question="…" option="…"/>` resolving one hop against a still-present block that carries embedded children and one of its `<alternative id>` values, no element of an unknown kind, and never child order — and re-renders the block with the children grouped by kind. Each miss, the two extraction misses included, is one `Error:` line that the orchestrator's repair template quotes verbatim, and pytest cases cover clean, prose-wrapped, misordered, and each failing fragment.

**Verified:**

- `embed MILESTONE_DIR SHORT_TITLE` is registered through the existing `add_subcommand` helper, listed in `--help` (`tests/test_contract.py`) and the module docstring, and reads the recommend agent's whole final message as its one body through `read_body` (`sys.stdin.buffer` to end of file once, decoded as UTF-8); a pseudo-terminal stdin is refused without blocking as `Error: the recommend agent's message must be piped on standard input (as a quoted heredoc or a redirected file), not typed at a terminal`, a closed stdin and a non-UTF-8 body each fail as one `Error:` line, a missing Short Title is argparse's exit 2, and a body full of backticks, `${VAR}`, `$(cmd)`, and a literal `&amp;apos;` travels untouched (`tests/test_embed.py`).
- Extraction (`extract_fragment`) slices from the first line holding `<alternative` through the last line holding `</recommendation>` and discards everything outside — the identity on a clean return (asserted directly), a grounding summary above and a closing remark below discarded to the same bytes a clean embed writes, a returned `<open-question>` wrapper and `<question>` around the children discarded, CRLF line ends accepted, and a child placed above the first `<alternative` line or below the last `</recommendation>` line discarded as the format decision accepts; a message with no `<alternative` line (empty, whitespace, prose-only, or `&lt;alternative` escaped) fails as `Error: no <alternative> line to extract from`, one with no `</recommendation>` line (missing, unterminated, or self-closing recommendation) as `Error: no </recommendation> line to extract to`, and a `</recommendation>` line above the first `<alternative` line as `Error: the last </recommendation> line precedes the first <alternative> line` (`tests/test_embed.py`).
- Validation (`parse_fragment`, sharing the block parser's child walk through the extracted `_parse_children`) refuses, each as one `Error:` line: any `<open-question>`, `</open-question>`, `<question>`, `<question/>`, or `</question>` line inside the fragment (a `<questionable>` tag and `&lt;question&gt;` text are not mistaken for one); text preceding `<alternative>` on the opening line, trailing `</recommendation>` on the closing line, or sitting between the elements; a fragment that is not well-formed XML, reported with the expat reason and the line counted from the fragment's own first line (`mismatched tag at line 3, column 2`, `not well-formed (invalid token) at line 1, column 26`); no `<alternative>` element; no `<recommendation>` element; two `<recommendation>` elements; a `<recommendation>` option naming none of the fragment's `<alternative>` ids (listing them; compared un-escaped and case-folded, so `a &amp; b` names `A &amp; B`); an element of an unknown kind at the top level, inside an alternative, inside an `<applied-principle>` or `<recommendation>`, and text inside a `<depends-on>`; and a repeated alternative id, a missing or empty `id`, a missing `option`, a missing `<depends-on>` attribute, or an unknown attribute. Child order is never checked (`tests/test_embed.py`).
- Every `<depends-on question="…" option="…"/>` resolves one hop (`check_dependencies`) against a block of the live document other than the one being embedded that carries a `<recommendation>`, and its option against one of that block's `<alternative id>` values, both un-escaped and case-folded (`root element FORM`, the entities fixture's id upper-cased with `a &amp; b`); the target's own tags are not followed (a target whose `<depends-on>` names an absent block still resolves) and a cycle is tolerated; a question naming an absent block or a bare sibling fails as `Error: the <depends-on question="…"/> names no block that carries a <recommendation>; the annotated blocks are "…", "…"` (or `; no other block carries one`), a self-reference fails the same way, and an option matching none of the target's alternatives fails as `Error: the <depends-on question="…" option="…"/> names none of that block's <alternative> ids, which are "…", "…"` (`tests/test_embed.py`).
- On success (`embed_fragment` then one `save_document`) the fragment's children become the block's children and the whole document is re-rendered canonically — alternatives in their returned relative order, then applied-principles, depends-on tags, and the recommendation, and inside each alternative the what-it-is text, advantages, drawbacks — so a complete but misordered fragment (children shuffled at both levels) embeds with nothing lost, tabs, 4-space indents, multi-line text, CDATA, raw quotes, and `<depends-on … />` with a space normalize to the fixed shape with all five entities, every other block is byte-identical, nothing is printed, exit is 0, and the block then leaves `list --unannotated` and lifts as `Only — Nothing else applies.`; `strip` followed by `embed` of a block's own fixture children reproduces `tests/fixtures/annotated/open_questions.xml` byte-for-byte for both annotated blocks (`tests/test_embed.py`).
- The block and document guards each fail as one `Error:` line, exit 1, empty stdout, no traceback, document byte-for-byte unchanged and never written (`save_document` monkeypatch-counted at exactly one call on success and zero on a refusal, no temporary file left): a block already carrying a `<recommendation>` (`Error: <open-question id="…"> already carries a <recommendation> element; strip it first to embed a new one`, matched case-folded), an unknown Short Title (naming the held ids, or `the document holds no blocks`), a missing `open_questions.xml` (`does not exist`, nothing created), and a malformed document (`not well-formed XML`) (`tests/test_embed.py`).
- Every miss is exactly one `Error: <reason>` line on stderr naming the failed check and never echoing the offending prose (a body with `SECRET PROSE` lines yields only `Error: the fragment carries text between its elements`), so the line can fill the orchestrator's repair template verbatim.
- `tests/test_embed.py` covers the clean, prose-wrapped, and misordered returns and each failing fragment in 75 cases; the suite grew from 205 to 280 cases and passes under both `uv run pytest` (Python 3.13.5, pytest 9.1.1) and `uv run --no-project --python 3.9 --with pytest pytest` (Python 3.9.25, pytest 8.4.2), leaving no `__pycache__` under `core/`.
- The tool stays stdlib-only (its two new imports are `re` and `xml.parsers.expat`), `grep '{{'` over `core/tools/open_questions.py` finds nothing, and no `claude`/`antigravity` word appears under `core/tools/`; `uv run scripts/build_hosts.py` rebuilt hosts/antigravity/ (37 files) and hosts/claude/ (38 files), `cmp` shows both `hosts/<host>/tools/open_questions.py` byte-identical to `core/tools/open_questions.py`, and `uv run scripts/build_hosts.py --check` passes.

---

## Tool Dependency Walk Subcommand

Add the answer sweep's walk subcommand, which gathers every block carrying a `<recommendation>` in document order, builds edges from each `<depends-on question="…">` value to the gathered block whose id matches (dropping an edge whose target is absent or un-annotated), and prints bare ids in dispatch order — origins first, then every block whose every resolvable edge is placed, same-depth ties in document order, a stranded set broken by promoting its document-order-first member to an origin — with an empty document or no annotated block printing nothing at exit 0. The orchestrator replaces its prose graph walk with this one call, and pytest cases cover a linear chain, a diamond, dropped edges, and a cycle.

**Verified:**

- `walk MILESTONE_DIR` is registered through the existing `add_subcommand` helper, listed in `--help` (`tests/test_contract.py`) and the module docstring, takes no other argument (an extra positional and a missing `MILESTONE_DIR` are argparse's exit 2), only reads the document — after a `walk` the file's bytes, inode, and mtime are unchanged and no other file is left in the directory — and on success prints the bare un-escaped `id` of each block one per line on stdout with exit 0 and empty stderr (`Ampersand & angle <brackets>, "quotes", 'apostrophes'` from the `entities` fixture) (`tests/test_walk.py`).
- It gathers exactly the blocks carrying a `<recommendation>` element in document order — the complement of `list --unannotated`, asserted against the bare `list` over a mixed document — so recommendation-less blocks are never printed (the `annotated` fixture yields `Escape hatch under sole writer` then `Root element form` and never `Fixture bare block`; a bare block carrying a `<depends-on>` tag is neither printed nor an edge target), and every gathered block is printed exactly once over a diamond with a dropped edge, a cycle with a tail, self-edges, and a mixed web (`tests/test_walk.py`).
- Edges run from each gathered block's `<depends-on question="…">` values to the gathered block whose `id` matches through `id_key` (un-escaped and case-folded: `question="first &amp; &lt;FIRST&gt;"` on disk resolves to the block `First & <first>`); the `option` value is never read (a tag whose option names no alternative of its target is still an edge); an edge whose target is absent from the document (`Gone`; the `entities` fixture's `Some "other" question & more`) or present without a `<recommendation>` is dropped, leaving the block an origin, keeping its other resolvable edges, and never pulling the un-annotated target into the output (`tests/test_walk.py`).
- Dispatch order: origins first in document order (`C, A, B` independent stay `C, A, B`); then repeatedly every unplaced block whose every resolvable edge names an already-placed block, in document order among themselves — a linear chain written `C→B→A` prints `A, B, C` and a five-block chain places one block per depth; a diamond written `D(B,C), C(A), B(A), A` prints `A, C, B, D` (the branch tie broken by document order, not edge order) and a block waiting on a depth-0 and a depth-2 target is placed at depth 3; a stranded remainder is broken by promoting its document-order-first block to an origin, repeated whenever the walk strands again — `A↔B` prints `A, B` or `B, A` by document order, a dependent tail follows the cycle, `C(B), A(B), B(A)` prints `C, A, B` (the literal document-order-first rule), origins `O, P` precede a promotion into `A↔B`, a three-block cycle and two disjoint cycles each terminate with every member once, and a self-edge is a cycle of one; over an acyclic web every target precedes its dependents (`tests/test_walk.py`).
- The `empty` fixture and the `bare` fixture (no annotated block) each print nothing with exit 0 and empty stderr; a missing `open_questions.xml` fails as `Error: <path>/open_questions.xml does not exist` with nothing created, and a malformed document as one `not well-formed XML` line — each exit 1, empty stdout, one stderr line, no `Traceback` (`tests/test_walk.py`); `main(["walk", …])` in-process prints the two annotated fixture ids and returns 0.
- `core/skills/answer-all-open-questions-with-recommendation/SKILL.md` step 1 (retitled "Gather the recommendation-bearing questions in dispatch order with one call") runs `python3 {{PLUGIN_ROOT}}/tools/open_questions.py walk <MILESTONE_DIR>` and takes its printed lines as the dispatch order; the prose gather-and-graph-walk is gone (a grep for `xmllint`, `awk`, `Origins first`, `Next depth`, `Stranded set`, `Drop every edge`, and `attribute-name-anchored` finds nothing in the file) while the empty-output stop, the `Error:`-line stop, and the walk-once/no-outer-re-gather-loop rule stay; step 2a's now-unanchored "the same line-oriented boundary-line CLI" lost only the word "same"; the frontmatter is unchanged (18-word description); the reference renders as `${CLAUDE_PLUGIN_ROOT}/tools/open_questions.py` and `.agents/plugins/cairn/tools/open_questions.py` and resolves to the rendered tool in both host trees under the build's `dangling-plugin-root` check.
- `tests/test_walk.py` (36 test functions, 39 cases, sectioned the gather / a linear chain / a diamond / dropped edges / a cycle / totality / the sweep's use / the contract) covers a linear chain, a diamond, dropped edges (absent target; un-annotated target), and a cycle, plus the empty and no-annotated-block cases and a `remove --option` followed by `walk` showing stripped dependents leave the order and untagged ones stay; the suite grew from 280 to 319 cases and passes under both `uv run pytest` (Python 3.13.5, pytest 9.1.1) and `uv run --no-project --python 3.9 --with pytest pytest` (Python 3.9.25, pytest 8.4.2), leaving no `__pycache__` under `core/`.
- The tool stays stdlib-only (no new import; `walk_order` is a module-level function over `id_key` and the parsed `Document`), `grep '{{'` over `core/tools/open_questions.py` finds nothing, and no `claude`/`antigravity` word appears under `core/tools/`; `uv run scripts/build_hosts.py` rebuilt hosts/antigravity/ (37 files) and hosts/claude/ (38 files), `cmp` shows both `hosts/<host>/tools/open_questions.py` byte-identical to `core/tools/open_questions.py`, and `uv run scripts/build_hosts.py --check` passes.

---

## Run Pytest Suite In CI

Extend the single `check` job of `.github/workflows/drift-gate.yml` with two steps after `uv run scripts/build_hosts.py --check`: the pinned run `uv run pytest` and the floor run `uv run --no-project --python 3.9 --with pytest pytest`, leaving the two SHA-pinned action lines, the job count, and the file name unchanged so the `ci` badge covers the drift gate and both runs. The suite must stay on the pytest API shared by the 3.9 and 3.13 interpreters, and the change is verified by both commands passing locally.

**Verified:**

- `.github/workflows/drift-gate.yml` keeps exactly one job, `check`, whose `steps` list gained exactly two new steps after `- run: uv run scripts/build_hosts.py --check`, in this order: `- run: uv run pytest` (the pinned run, carrying no version literal) then `- run: uv run --no-project --python 3.9 --with pytest pytest` (the floor run) — read back from the file; `git diff --numstat` shows 2 lines added, 0 removed.
- The two SHA-pinned action lines — `- uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1` and `- uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4 # v10.1.0` — are byte-identical to `HEAD` (`diff` over the `uses:` lines is empty), and neither pytest step carries an `if:` (the `!cancelled()` remedy stays reserved).
- The file name `.github/workflows/drift-gate.yml` and its `name: drift-gate` are unchanged and no second workflow file was added (`.github/workflows/` still holds exactly `drift-gate.yml` and `traffic-badges.yml`), so the README `ci` badge URL reads one status covering the drift gate and both test runs; `on: [push, pull_request]`, `permissions: contents: read`, and `runs-on: ubuntu-latest` are unchanged.
- The edited workflow parses under `yaml.safe_load` as one job with five steps — two `uses` and three `run` (`uv run scripts/build_hosts.py --check`, `uv run pytest`, `uv run --no-project --python 3.9 --with pytest pytest`).
- The pinned run `uv run pytest` passes locally under the `.python-version` interpreter (CPython 3.13.5) against the locked `dev` group's pytest 9.1.1: 319 passed, exit 0.
- The floor run `uv run --no-project --python 3.9 --with pytest pytest` passes locally under CPython 3.9.25 with the last pytest supporting 3.9 (pytest 8.4.2): 319 passed, exit 0 — the suite stays on the API both pytest majors share.
- `pyproject.toml`, `uv.lock`, `.python-version`, `scripts/`, `core/`, and `tests/` are untouched — `git status --short` lists `.github/workflows/drift-gate.yml` as the only changed path beyond the two task-list files.
- The project's done check `uv run scripts/build_hosts.py --check` still passes (the workflow file lies outside every rendered host tree).

---

## Bootstrap Python Prerequisite Check

Make `init-milestone-base-workflow` run an interpreter check first, ahead of its state detection: execute `python3` to read its version, probe `python` only when that fails, and when no Python 3.9+ interpreter answers stop before any write with a full message naming what it looked for, what it found, the install or expose-as-`python3` remedy, and that a re-run completes the bootstrap. Its `milestones/README.md` template must describe the four milestone files (`open_questions.xml` beside the three Markdown files), and the skill is verified by reading the rendered host trees and passing `uv run scripts/build_hosts.py --check` after a rebuild.

**Verified:**

- `core/skills/init-milestone-base-workflow/SKILL.md` opens its workflow with a new `### 1. Check the Python prerequisite` step placed ahead of the state detection, which became `### 2. Detect existing state`; the four later steps were renumbered 3–6 with no other change, and the skill's opening paragraph states the check as a whole-skill rule (it stops before writing anything when no Python 3.9 or later interpreter answers as `python3`).
- The check step tells the runner to execute `python3 --version` first and to run `python --version` only when `python3` is not found or reports a version below 3.9, with `python`'s result deciding nothing about passing; a numeric minor-by-minor comparison note keeps `3.10` and `3.13` later than `3.9` and `3.8` not.
- On failure the step says **stop before any write** — create and edit nothing — and prints one full message stating, in order: what it looked for (a Python 3.9+ interpreter as `python3`, failing that as `python`), what it found (not found, or the exact version, for each name), the remedy (when `python` reported 3.9+, it exists under the other name and must be exposed as `python3`; otherwise install Python 3.9+ so `python3` resolves to it), and that re-running `/init-milestone-base-workflow` completes the bootstrap with nothing to undo.
- The check runs on every invocation — the step says so explicitly, and because it precedes step 2 a re-run on an already-bootstrapped project gets the same check before the already-initialized stop.
- The `milestones/README.md` template lists four files in order — `requirements.md` (goal, relevant starting state, decisions, out of scope), `open_questions.xml` (the open questions, one `<open-question>` block each under a single `<open-questions>` root, written only by the plugin's open-question tool), `TASKS_TODO.md`, `TASKS_DONE.md` — so `requirements.md` no longer claims "open questions"; the skill's `## Milestone Workflow` template for `CLAUDE.md` names the same four files, keeping the scaffold it writes self-consistent.
- The runtime file names no host (a whole-word `claude`/`antigravity` grep over the rendered skill matches nothing beyond the `CLAUDE.md` filename) and its one `{{PLUGIN_ROOT}}/tools/open_questions.py` reference renders as `${CLAUDE_PLUGIN_ROOT}/tools/open_questions.py` and `.agents/plugins/cairn/tools/open_questions.py`, each naming an existing `hosts/<host>/tools/open_questions.py`; the frontmatter is untouched (21-word unquoted description, no colon or semicolon).
- `uv run scripts/build_hosts.py` rebuilt hosts/antigravity/ (37 files) and hosts/claude/ (38 files); `git status --porcelain` shows exactly the core skill and its two rendered copies changed; both `hosts/claude/skills/init-milestone-base-workflow/SKILL.md` and `hosts/antigravity/skills/init-milestone-base-workflow/SKILL.md` carry the check step, the message parts, and the four-file templates, no `{{` remains in either, and `uv run scripts/build_hosts.py --check` passes.

---
