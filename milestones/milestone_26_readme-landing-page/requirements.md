# Milestone 26: README landing page

## Goal

Turn the root `README.md` from a 6,500-word reference document into a landing page: drop the banner so the first screen is the release/CI/license badges and the adoption block, a generic host-neutral one-liner, a single simplified mermaid diagram of one milestone loop (define → review → recommend → answer → derive → complete, a stand-in until a later milestone records the demo GIF), and per-host installation, followed by a host-neutral "Why Cairn?" and a short "Design principles" block built from selected design claims. Move the phase narrative with its six mermaid diagrams, the skill reference, the commit conventions, and the answer-principle-learning loop into a new `docs/` directory, promote `temp/cairn-design-claims.md` into it as a committed page the principles block links to, and update every cross-reference in `CLAUDE.md` and `CONTRIBUTING.md` that named those README sections. Rework the two distribution README templates under `scripts/hosts/*/README.md` to the same shape — one-liner, that repository's adoption badges, installation, and the redirect to the root repository for contributors — and rebuild `hosts/` so `build_hosts.py --check` passes.

## Relevant starting state

### Root `README.md`

`README.md` is 6,498 words in ten `##` sections, weighted toward reference: `## Skill reference` is 4,312 words (25 `###` entries — 23 skills plus the two agents), `## Workflow pipeline` 1,107 (six mermaid `flowchart TD` diagrams, one per phase, chained by shared amber state nodes `D0`–`D5`, each under an identical `%%{init: …}%%` theme block that contains no `{{`), `## How skills commit` 329, `## Installation` 210, `## Why Cairn?` 147, `## Adoption` 110, `## How it works` 96, `## Contributing` 72, `## Self-dogfooding` 58, `## License` 6. The file opens with a `<p align="center">` banner (`.github/assets/readme/cairn-banner.png`, 4.7 MB, `width="100%"`) above a second centered paragraph of three shields.io badges (latest release, drift-gate CI on `main`, MIT), then `# Cairn`, the tagline "Mark the path from idea to shipped. / Milestone-driven development for any kind of work.", and `## Why Cairn?`, whose second paragraph reads "Cairn gives Claude Code a structured, repeatable process…". The one internal anchor is `[Skill reference](#skill-reference)` in the pipeline intro. The pipeline's "Iterating milestone requirements" paragraph already carries the boundary-line-CLI / query-where-it-pays rationale, and a `### The answer-principle-learning loop` subsection (three bullets: teaching flow, recommendation advisory, correction loop) sits inside the skill reference between `review-milestone-requirements` and `discuss-open-question`. Two skill-reference entries are known stale, recorded as follow-ups by milestone 15: `goto-next-milestone <number> <title>` (the skill is argument-free and creates nothing) and `finish-current-milestone`'s trailing "run `/goto-next-milestone` after".

### Cross-references to README sections

No `docs/` directory exists and nothing links to one. `CLAUDE.md` names the README in its repository-layout entry (line 41: "authoritative workflow documentation and skill reference, with a `## Adoption` section…"), in the **Skill Frontmatter** invariant (material cut from frontmatter descriptions has "`README.md`'s `## Skill reference` and this file" as its homes), and in the `## Development` prose ("the README's `ci` badge reports `main`'s pushed HEAD"; the `traffic-data` branch holds what "the README's `## Adoption` table reads"). `CONTRIBUTING.md` line 38 links `[Installation](README.md#installation)`; its `## Development` section is the build-loop text milestone 24 moved out of the README. `.github/PULL_REQUEST_TEMPLATE.md`, the three issue forms, and the release skill reference `milestones/README.md` and the rendered distribution README templates only, never a root-README section.

### Distribution README templates and the host build

`scripts/hosts/claude/README.md` (402 words) and `scripts/hosts/antigravity/README.md` (381 words) share one shape: `# Cairn for <Host>`, an opening paragraph (the Claude one says the plugin "gives Claude Code a milestone-driven development workflow", the Antigravity one "gives your coding agent…"), a **Generated — do not edit** blockquote that names the `traffic-data` branch, says issues are disabled, and redirects problems and proposals to `uHappyLogic/cairn`, then `## Installation` (that host's install steps plus `### Bootstrap your project`), `## Source` (the only `{{VERSION}}` slot, twice on one line), and `## License`. Each definition directory also carries a slot-free `CONTRIBUTING.md` pointer that already routes bug reports, pull requests, vulnerabilities, and questions to the root repository. `scripts/build_hosts.py` renders every non-`settings.toml` file in a definition directory to the same relative path in `hosts/<host>/`, and its `unfilled-placeholder` check (`(?<!\$)\{\{`) runs over every rendered file, README included, so any `{{` a template gains outside a `${{` expression fails the build; the committed `hosts/<host>/README.md` files are pure renders and `--check` diffs them byte-for-byte. Both distribution repositories carry their own `traffic-data` branch with `views-unique.svg` and `clones-unique.svg`, already linked from the root README's `## Adoption` table, so per-repository badges exist for each template. A template change reaches `hosts/` on rebuild but reaches the distribution repositories only through `/release-plugin`.

### Design-claims source

`temp/cairn-design-claims.md` is 1,288 words and untracked — `temp/` is in `.gitignore`. It lists 19 claims in six groups (Decisions 1–6, Work 7–9, Records 10–12, Efficiency 13–14, Robustness 15–16, Engineering 17–19), each a one-line claim with a **Design** paragraph and a **Metric** line, written in ASD-STE100 Simplified Technical English, closing with a note that claims 17–19 concern the plugin code rather than a user project. The three novelties the goal names map to claim 11 (machine-readable records, boundary-line CLI), claims 2 and 12 (one answer = one commit with a provenance subject; path-scoped staging), and claim 5 (advice improves per milestone via the principle store).

### Hosts and assets

Exactly two hosts exist — `scripts/hosts/claude/` and `scripts/hosts/antigravity/` — and the bug issue form's host dropdown mirrors that directory; the string "Codex" appears nowhere in the repository. `.github/assets/readme/` holds only `cairn-banner.png`, referenced solely by `README.md` line 2. The repository contains no terminal-recording tooling, tape script, sample project, or GIF — the demo the goal defers has no existing footing.

## Decisions

### Root README first screen

The adoption block moves to the first screen as the existing three-row pipe table — six badges, each row the linked owner/repo name, each badge linking to its repository's `traffic-data` branch — with the `## Adoption` heading dropped; the table stays because a badge's own label names only its metric, so the row label is what says which repository it counts. Its caption shrinks from three sentences to one sentence of about two lines that keeps the four facts a reader of the displayed numbers needs: counts are cumulative since 2026-09-04, unique counts are summed per day so a returning visitor counts again, the badge workflow's own fetch adds one unique clone per repository per day, and the monorepo's clones include its CI checkouts. No badge strip and no partial promotion of the monorepo's pair: the whole three-repository block sits on the first screen, since the distribution rows, not the monorepo's, are the counts an install moves.

## Out of Scope

## Open questions

<open-question id="One-liner wording">
  <question>What does the generic host-neutral one-liner say, and does it replace the existing two-line tagline or sit beside it?</question>
  <alternative id="One-liner as subtitle">
    Keep the bold &quot;Mark the path from idea to shipped.&quot; as the lead under # Cairn and put the host-neutral one-liner in the subtitle&apos;s seat in place of &quot;Milestone-driven development for any kind of work.&quot; — &quot;Milestone-driven development for your coding agent — any kind of work, one milestone at a time.&quot; — the same sentence the two distribution templates carry under their own # Cairn for &lt;Host&gt; titles.
    <advantage>The one-liner supplies the two things the current subtitle omits — whom Cairn is for, named host-neutrally, and the one-milestone-at-a-time motif — without adding a line: the two source lines carry no trailing hard break, so GitHub already renders them as one paragraph, and the recorded brand line stays as the bold lead at zero first-screen cost while, once the banner goes, it is the only carrier left of the trail-marker metaphor behind the name.</advantage>
    <drawback>The first words a visitor reads stay a metaphor rather than a description, and &quot;shipped&quot; — the one product-flavored word milestone 12&apos;s work-type sweep left in the tagline — sits on the first screen beside a sentence claiming any kind of work.</drawback>
  </alternative>
  <alternative id="One-liner alone">
    Drop both tagline lines and set the single sentence alone under # Cairn — &quot;Milestone-driven development for your coding agent — any kind of work, one milestone at a time.&quot; — identical in the root README and both distribution templates.
    <advantage>The plainest possible first screen, one text kept in step across three READMEs, and no brand line left to defend or to neutralize.</advantage>
    <drawback>It reverses milestone 1&apos;s recorded tagline decision for no space gain — the bold lead costs no line on GitHub — and, with the banner dropped in the same milestone, leaves # Cairn with nothing on the page that hints at the cairn trail-marker metaphor behind the name.</drawback>
  </alternative>
  <alternative id="Loop sentence alone">
    Drop both tagline lines and use the distribution templates&apos; existing opening sentence, made host-neutral, as the one-liner: &quot;Cairn gives your coding agent a milestone-driven development workflow — clarify the goal, resolve every open question, derive an ordered task list, complete the tasks, and close out the milestone before moving on — for any kind of work.&quot;
    <advantage>Nothing new is authored — the Antigravity template already carries this sentence almost word for word — and a visitor learns the whole shape of the workflow before reaching the diagram.</advantage>
    <drawback>Forty words is a paragraph, not a one-liner, and its steps (clarify, resolve, derive, complete, close out) sit one screen above a diagram whose six steps are named differently (define → review → recommend → answer → derive → complete), so the first screen states the loop twice in two vocabularies.</drawback>
  </alternative>
  <alternative id="Additive third line">
    Keep both existing tagline lines unchanged and add the host-neutral one-liner as a new paragraph beneath them, ahead of the loop diagram.
    <advantage>Two recorded decisions — milestone 1&apos;s tagline and milestone 12&apos;s neutralized subtitle — stay byte-for-byte intact, and the one-liner is authored once for the distribution templates without touching the root title block.</advantage>
    <drawback>The subtitle and the one-liner then say the same thing twice (milestone-driven development, any kind of work), so the first screen spends an extra paragraph restating itself on exactly the space the milestone reclaims for the diagram and the per-host install.</drawback>
  </alternative>
  <applied-principle>Prefer domain-neutral terms</applied-principle>
  <recommendation option="One-liner as subtitle">The subtitle seat is where the one-liner belongs — it is that line&apos;s host-neutral successor, adding whom Cairn is for and the one-milestone-at-a-time motif in wording that reads across any kind of work — and the bold tagline stays because it renders inline at no first-screen cost and, with the banner gone, is the last visible trace of the metaphor behind the name; the same sentence goes verbatim into both distribution templates.</recommendation>
</open-question>
<open-question id="Loop diagram shape">
  <question>What does the simplified first-screen mermaid diagram show beyond the six named loop steps — the review-and-answer iteration, the milestone close, neither — and does it keep the theme block the six phase diagrams share?</question>
  <alternative id="Six steps only">
    The six verbs — define, review, recommend, answer, derive, complete — chained by plain arrows and nothing else: no return edge, no finish step, no state node.
    <advantage>It is the goal&apos;s parenthesis drawn literally and the smallest thing that can sit between the subtitle and the install commands — six short labels in one chain, read in a glance with nothing to misread.</advantage>
    <drawback>It draws a checklist, not a loop: nothing shows that review, recommend and answer repeat until no question is open before any task is derived, which is the one fact the six words cannot say and the mechanism the design-principles block below then has to explain in prose.</drawback>
  </alternative>
  <alternative id="Iteration back-edge">
    The six-verb chain plus one dashed return edge from answer to review carrying the convergence condition (until no open questions remain) — the same dashed repeat-until-satisfied edge the docs&apos; requirements diagram draws — with no finish step and no milestone-return edge.
    <advantage>One short arc adds exactly the fact that makes the picture a loop — requirements iterate to convergence before derive — and that iteration is what every novelty below rests on (machine-readable question blocks, one answer = one commit, the principle store), so the summary points at the thing the docs diagrams then expand.</advantage>
    <drawback>The chain still ends at complete, so the diagram itself never shows a milestone being closed and the next one starting; that half of the cycle is carried only by the words above it.</drawback>
  </alternative>
  <alternative id="Closed milestone cycle">
    The six-verb chain extended by a seventh step, finish, with one dashed return edge from finish back to define labelled next milestone — the docs&apos; D5 loop compressed into the row — and no iteration edge.
    <advantage>The diagram becomes a literal loop and shows where a milestone ends, the seam that &quot;one milestone at a time&quot; names, using the same next-milestone edge a reader meets again in the docs.</advantage>
    <drawback>The return arc spans the whole chain — the widest element in the diagram — to draw what the subtitle already says in words, adds a seventh step beyond the six the goal names, and leaves the requirements iteration, the part that separates Cairn from a task runner, invisible.</drawback>
  </alternative>
  <alternative id="Both loops">
    The six-verb chain with the finish step and both dashed edges — answer back to review for the iteration and finish back to define for the next milestone.
    <advantage>The complete honest shape: the inner requirements loop and the outer milestone cycle are exactly the two dashed loops the six phase diagrams carry, so nothing a reader later finds in the docs is missing from the summary.</advantage>
    <drawback>Seven nodes with two dashed return arcs around the same row is the busiest of the four and stops reading at a glance — the arcs nest and their labels crowd — and the outer arc exists to draw what the subtitle already states.</drawback>
  </alternative>
  <depends-on question="One-liner wording" option="One-liner as subtitle"/>
  <recommendation option="Iteration back-edge">The requirements iteration is the one fact the six verbs cannot convey and the mechanism the design claims rest on, and a single short arc buys it, while the milestone cycle is already stated in words by the subtitle&apos;s &quot;one milestone at a time&quot; and would cost a seventh node and a chain-wide arc to draw; the shared theme block stays because it has no real rival — invisible when rendered, it keeps the summary in the same font, line color and curve style as the six diagrams it expands into, its wrappingWidth setting is what keeps the return edge&apos;s label on one line, and it contains no `{{`, so it would pass the host build&apos;s placeholder check should a distribution template ever carry the diagram.</recommendation>
</open-question>
<open-question id="Docs page layout">
  <question>How are the moved sections — the phase narrative with its six diagrams, the skill reference, the commit conventions, and the answer-principle-learning loop — split across files under docs/, and what are those pages named?</question>
  <alternative id="One page per section">
    Four pages, one per moved section — docs/workflow.md for the six phase sections and their diagrams, docs/skill-reference.md for the per-skill and per-agent entries, docs/commit-conventions.md for How skills commit, and docs/answer-principle-learning.md for the loop — beside the promoted design-claims page.
    <advantage>Every section gets a file with a single subject and a guessable name, so each external consumer (the CLAUDE.md skill-reference pointer, the CONTRIBUTING.md pipeline link, a README design-principles entry) links to a whole file rather than an anchor inside a longer page.</advantage>
    <drawback>Two of the four pages are stubs of 329 and 571 words that each lean on the phase narrative for context — the commit page on the orchestrators and answer skills, the loop page on the requirements phase and capture — so they need re-orientation prose or read cold, and five pages of cross-links are kept in step for about 5,700 words of content.</drawback>
  </alternative>
  <alternative id="Narrative and reference pages">
    Two pages — docs/workflow.md carrying the six phase sections and diagrams followed by How skills commit and The answer-principle-learning loop as sibling sections (the loop promoted out of the reference), and docs/skill-reference.md carrying the per-skill and per-agent entries — beside the promoted design-claims page.
    <advantage>Matches the two ways the content is read — one page runs end to end as run, record, learn (the phase prose already names capture three times, and the commit section&apos;s provenance subjects are what capture reads) and the other is scanned as a lookup — and keeping every heading&apos;s text means the existing anchors survive as path-prefixed links (docs/workflow.md#how-skills-commit, docs/skill-reference.md#review-milestone-requirements), so re-pointing CLAUDE.md, CONTRIBUTING.md, and the pipeline intro is a path swap.</advantage>
    <drawback>The workflow page runs to about 2,000 words plus six diagrams, and the loop leaves its seat beside the review-milestone-requirements entry, so a reader of the reference page needs a link out to reach it.</drawback>
  </alternative>
  <alternative id="Single docs page">
    One page — docs/workflow.md — holding all four sections in their current README order.
    <advantage>A straight cut that preserves every existing anchor on one file, so every cross-reference becomes the same path prefix and nothing inside the moved text is re-linked.</advantage>
    <drawback>It re-creates the 5,700-word wall the milestone exists to break up under a different path, with the skill lookup buried below the walkthrough and one page name stretched to cover everything.</drawback>
  </alternative>
  <alternative id="Concepts page split">
    Three pages — docs/workflow.md with only the six phase sections and diagrams, docs/skill-reference.md, and a third page such as docs/records-and-learning.md combining How skills commit with the answer-principle-learning loop as the what-git-records-and-what-the-project-learns page.
    <advantage>Keeps the workflow page a pure walkthrough that the README loop diagram expands into, and gives the README design-principles block one page to link for the record and learning novelties.</advantage>
    <drawback>The third page&apos;s subject is a compound whose only honest name is a conjunction, and both of its sections lean on the phase narrative for context, so it reads as two appendices split off from the page that explains them.</drawback>
  </alternative>
  <applied-principle>Name by distinctive function</applied-principle>
  <recommendation option="Narrative and reference pages">The split follows how the content is read — one narrative page that runs as run, record, learn and one lookup page — under two names that carry the repository&apos;s established terms (workflow, skill reference) rather than a generic guide or reference label, and keeping every heading text turns the cross-reference update into a path swap.</recommendation>
</open-question>
<open-question id="Design-claims page fidelity">
  <question>Is the design-claims file promoted into docs/ verbatim, keeping its Simplified Technical English style, its Metric lines, and its closing note on the plugin-code claims, or edited for a reader of the repository?</question>
  <alternative id="Verbatim promotion">
    The file is copied into docs/ exactly as it stands — title, self-describing intro, the 19 claims in Simplified Technical English with their Design paragraphs and Metric lines, and the closing benchmark-versus-showcase note — and only its path changes.
    <advantage>Nothing is authored, so nothing can drift and the whole job is one copy: the 19 numbered claims and their Metric lines — the seed of the eval suite the repository&apos;s improvement plan calls for — reach git exactly as written, and the controlled register keeps each claim to one testable reading a future suite can implement one case per claim.</advantage>
    <drawback>A README visitor who follows the design-principles link lands on a note written for its author: the intro announces its own writing standard, the Metric lines name golden-file, judge-score and kill-and-resume tests that no evals directory runs, the closing line weighs &quot;a benchmark&quot; against &quot;a showcase&quot; the reader has never heard of, and claim 12&apos;s &quot;the work tree is clean after each skill&quot; — contradicted by the non-committing bootstrap skill and by claim 15&apos;s own partial-work-stays rule — goes in unreviewed.</drawback>
  </alternative>
  <alternative id="Frame edit, claims kept">
    The 19 claims keep their wording, grouping, Design paragraphs and Metric lines in Simplified Technical English, and only what addresses the wrong reader changes: the self-describing intro becomes one orienting paragraph for a repository reader (what the list is, why it is written in a controlled register, that each Metric names the test that would check the claim, and that no test suite in the repository runs them yet), the closing note keeps its reader-facing half — claims 17–19 concern the plugin&apos;s own code, not a user&apos;s project — and drops the benchmark-versus-showcase aside, and a sentence the live plugin contradicts is corrected in the same register.
    <advantage>About a hundred words of change stop the page reading as a working note while the claim set a future eval suite would implement stays intact and numbered, so the README design-principles block can cite or quote claims by number however Design principles selection settles, and the page states plainly what is measured today (nothing) rather than leaving the Metric lines to imply results.</advantage>
    <drawback>Two registers share one page — a plain-prose frame over controlled-English claims — so every later edit must choose which voice it writes in, and the Metric lines still name tests that nothing in the repository runs, leaving the orienting sentence to carry the honesty on its own.</drawback>
  </alternative>
  <alternative id="Prose rewrite">
    The claims are rewritten in the README&apos;s ordinary voice as a design-principles page — each claim with its design paragraph, the Metric lines and the Simplified Technical English constraint dropped, the closing note gone, and the file retitled to match — so docs/ speaks in one register throughout.
    <advantage>One voice across README and docs/: the page is the natural long form of the README design-principles block (same shape, nineteen entries instead of a few), so a reader moves from block to page without a register change and nothing on the page names a test that does not exist.</advantage>
    <drawback>It throws away the one thing that makes the file a claims list rather than marketing — a test per claim — just as the repository is planning the eval suite those metrics were written for, and rewriting 1,300 words of carefully bounded sentences (&quot;one repair, then a skip, never a run stop&quot;) into prose is exactly where a claim quietly widens past what the plugin does, with no test to catch it.</drawback>
  </alternative>
  <recommendation option="Frame edit, claims kept">The claims and their Metric lines already say what the plugin does and seed the planned eval suite, so they stay word-for-word in their register; only the frame addresses the wrong reader, and swapping the self-describing intro and the benchmark-versus-showcase aside for one orienting paragraph — plus correcting the one sentence the plugin contradicts — is the smallest edit that turns a working note into a documentation page, with the README block left to do the reader-facing paraphrase.</recommendation>
</open-question>
<open-question id="Design principles selection">
  <question>Which design claims does the README design-principles block present, how many, and in what form — the claim line alone, or the claim with one sentence of the design behind it?</question>
  <alternative id="Three novelties with design sentence">
    Three entries — the claims the starting state maps the goal&apos;s named novelties to: the records are machine-readable (claim 11), each decision has a record in git (claim 2, carrying claim 12&apos;s provenance subject), and advice gets better with each milestone (claim 5) — each the claim line verbatim as it heads the promoted page, so it matches the heading it links to, followed by one sentence of the design behind it in the README&apos;s own voice, and one closing link to the page for all nineteen.
    <advantage>The novelty is in the mechanism, not the claim — a boundary-line CLI over XML question blocks, one answer = one commit whose subject names how it was made, a principle store the recommender cites — and these three are the mechanisms the loop diagram and &quot;Why Cairn?&quot; leave invisible, so about a hundred words in three entries give the first screen the one thing it otherwise lacks, with the depth one link away and the loop narrative left to the section above whichever depth its rewrite takes.</advantage>
    <drawback>Two of six groups are represented, so a visitor who never follows the link sees nothing of the work phase (coverage matrix, late design, verified bullets) or the robustness rules, and each design sentence compresses a page paragraph of two to four sentences, so it is authored text kept in step with the page by hand.</drawback>
  </alternative>
  <alternative id="Claim lines only, one per group">
    Six bold claim lines, one from each of the page&apos;s six groups (for instance 1, 7, 11, 14, 16, 17), each the claim sentence verbatim with no design sentence under it, followed by the link to the page.
    <advantage>The shortest block that maps the whole design — six lines, about fifty words, nothing authored, since each line is a page heading and links to its numbered claim — and it reads as a principles list in the ordinary sense of the term.</advantage>
    <drawback>A claim line alone is a slogan any tool could print — &quot;Each decision has a record in git.&quot; heads a git-backed to-do app as well as Cairn — and everything the starting state records as the goal&apos;s novelties lives in the design paragraphs this form leaves out, while one line comes from the engineering group the page&apos;s own closing note says a showcase does not need.</drawback>
  </alternative>
  <alternative id="One per group with design sentence">
    Six claims, one per group, each the claim line plus one sentence of the design behind it, followed by the link — the three-entry form at twice the count.
    <advantage>It shows the design&apos;s whole shape — decisions, work, records, efficiency, robustness, engineering — so the block is a faithful summary of the page rather than a highlight reel, and every group on the page has its hook in the README.</advantage>
    <drawback>Six entries with mechanism sentences run to about two hundred words, the length of the present &quot;Why Cairn?&quot; section, and half of them — smallest necessary context, stop and continue without loss, one source of truth behind a drift gate — describe how the plugin is built rather than what a user gets, so the &quot;short block&quot; the goal names becomes a fourth prose section a visitor scrolls past.</drawback>
  </alternative>
  <alternative id="Loop-order user-facing set">
    Five claims in the order the loop diagram runs — Cairn finds the open questions before the work starts (1), the person decides (3), each decision has a record in git (2), advice gets better with each milestone (5), each requirement becomes a task with proof (7) — each with one design sentence, followed by the link.
    <advantage>Each principle lands on a step the visitor has just read in the diagram — review, answer, the answer commit, capture, derive and complete — and the block keeps the decisions-first premise rather than only the record-keeping.</advantage>
    <drawback>Three of the five restate what &quot;Why Cairn?&quot; says in prose — resolve every open question, every decision is recorded, every requirement maps to a task — so the first screen says the same things twice in two forms at about a hundred and seventy words, and the selection rule drops the machine-readable-records claim, the first novelty the starting state records, because a data format is not a step in the loop.</drawback>
  </alternative>
  <depends-on question="Design-claims page fidelity" option="Frame edit, claims kept"/>
  <recommendation option="Three novelties with design sentence">The block exists to show an evaluating engineer what is novel and the novelty is in the mechanism, so the design sentence is not optional and the selection is the three mechanisms nothing else on the first screen can show — machine-readable question blocks, one answer = one commit with a provenance subject, and the principle store — while the loop itself stays with the diagram and &quot;Why Cairn?&quot;; three entries of about a hundred words keep the block short, and the promoted page, keeping all nineteen numbered claims in their own wording with design paragraphs and metrics, holds the depth one link away and gives each verbatim claim line a heading to match.</recommendation>
</open-question>
<open-question id="Why Cairn rewrite depth">
  <question>Does the &quot;Why Cairn?&quot; section change only its host-naming sentence, or is it rewritten around the design-claims narrative, and does it stay a section separate from the design-principles block?</question>
  <alternative id="Host sentence only">
    The section keeps its three paragraphs and its own `## Why Cairn?` heading between installation and the design-principles block, and the one edit is &quot;Cairn gives Claude Code a structured, repeatable process&quot; becoming &quot;Cairn gives your coding agent a structured, repeatable process&quot; — the phrase the Antigravity template and the new subtitle already use.
    <advantage>That sentence is the section&apos;s only host-specific item — `CLAUDE.md` is the plugin&apos;s own convention on both hosts, named by the Antigravity skills too — and the rest is prose milestone 12 already swept for work-type neutrality and whose environment sentence milestone 15 verified against the live skills, so the goal&apos;s host-neutral requirement is met with a two-word swap that authors nothing and keeps the section&apos;s three jobs: the failure modes, the user&apos;s-eye telling of the loop, and how the workflow adapts through `CLAUDE.md`.</advantage>
    <drawback>The text was written as the first prose under the title, and in its new seat a quarter of its 147 words restate what the visitor has just passed — &quot;one milestone at a time&quot; and &quot;any kind of project&quot; echo the subtitle, and the clarify-resolve-derive-complete-close-out sentence retells the diagram&apos;s six verbs in a second vocabulary — while nothing in it says what is distinctive about Cairn, which the block below then carries alone.</drawback>
  </alternative>
  <alternative id="Trimmed for its new seat">
    The section stays separate and keeps its subject, but is re-cut for a seat below the diagram and install: the host sentence goes neutral, the loop-narration sentence and the two subtitle echoes (&quot;one milestone at a time&quot;, &quot;It works with any kind of project&quot;) are cut, and the failure-modes paragraph, the decisions-recorded-and-requirements-mapped promise, and the `CLAUDE.md` environment sentence stay, at about a hundred words.
    <advantage>Every sentence left does work nothing above it does — the failure modes Cairn exists to prevent, the promise that decisions are recorded and requirements map to tasks, and the mechanism by which it adapts to any kind of work — so a visitor who scrolls past install meets no third telling of the loop and no second telling of the subtitle.</advantage>
    <drawback>It removes the only user&apos;s-eye prose telling of the loop on the page — the diagram&apos;s six bare verbs are skill-flavoured names, and the clarify-resolve-derive-complete-close-out sentence is what a reader who skips diagrams needs before docs/workflow.md — which the principles-selection recommendation leaves to the diagram and this section, and it replaces twice-verified text with authored sentences for a saving of some fifty words below the fold.</drawback>
  </alternative>
  <alternative id="Rewritten around the claims">
    The section stays separate but its three paragraphs are replaced by a pitch that follows the claims page&apos;s arc in the README&apos;s voice — Cairn finds the open questions before the work starts, the person decides and the machine advises, each decision has a record in git, each requirement becomes a task with proof, and advice gets better with each milestone — host-neutral, at about the present length.
    <advantage>The section then says what is distinctive about Cairn rather than what any structured workflow tool would claim, and the pitch, the principles block, and the claims page share one narrative, so a reader moves from section to block to page without changing story.</advantage>
    <drawback>It puts two adjacent sections retelling the same claims in two forms — prose, then the block&apos;s three claim lines with mechanism sentences — the very restatement the principles-selection analysis priced as a cost in the other direction, drops the failure-modes paragraph that is the only text on the page saying why, and turns 147 verified words into authored text kept in step with the claims page by hand.</drawback>
  </alternative>
  <alternative id="Merged with the principles block">
    One `## Why Cairn?` section carries both — the problem and process paragraphs, trimmed, followed by the three claim entries and the link to the claims page — with no separate `## Design principles` heading.
    <advantage>The heading&apos;s question gets one continuous answer — what goes wrong, what Cairn does, and the three mechanisms that make it different — in a single section a visitor reads top to bottom, with one heading fewer on a page the milestone exists to shorten.</advantage>
    <drawback>The goal names a host-neutral &quot;Why Cairn?&quot; and a short &quot;Design principles&quot; block as two things, the principles-selection analysis rests on a distinct block whose verbatim claim lines match numbered headings on the claims page and are kept in step with it while the pitch is stable prose, a `## Design principles` heading is the landmark a reader scanning the page&apos;s headings looks for, and the merged section runs past two hundred words — the longest prose on the landing page.</drawback>
  </alternative>
  <depends-on question="Design principles selection" option="Three novelties with design sentence"/>
  <depends-on question="One-liner wording" option="One-liner as subtitle"/>
  <recommendation option="Host sentence only">The goal names host-neutrality as this section&apos;s one change and gives the design-claims narrative to the principles block beneath it, so the section keeps its job — the failure modes, the user&apos;s-eye telling of the loop, and how the workflow adapts through `CLAUDE.md` — in text two sweeps already verified against the live plugin, and the only edit is the host name becoming &quot;your coding agent&quot;, the phrase the subtitle and the Antigravity template already carry; it stays its own section because the block below is a linked, claim-numbered list kept in step with the claims page while the pitch is stable prose, and the restatement it carries below the fold is a prose complement to six bare verbs, not a first-screen cost worth authored text.</recommendation>
</open-question>
<open-question id="Install section scope">
  <question>Does the first-screen installation section keep the bootstrap steps and the marketplace-migration paragraph, or do those move below the fold so only the per-host install commands remain on the first screen?</question>
  <alternative id="Whole section stays">
    The `## Installation` section keeps its milestone-23 shape on the first screen — the Claude Code subsection with its install block, the migration paragraph with its second block, the Antigravity subsection with its pinning sentence, and the shared `### Bootstrap your project` — word for word as the two distribution templates carry it.
    <advantage>Nothing is authored or relocated: an installer reads one contiguous section that takes them from marketplace add to a bootstrapped project, and every subsection stays byte for byte identical to its distribution template exactly as milestone 23 recorded, so the hand-aligned duplicate stays a straight copy.</advantage>
    <drawback>At 210 words and four code blocks it is the longest first-screen element after the diagram, and 64 of those words plus a whole block — the migration paragraph — address only installs made from `uHappyLogic/cairn` before 1.5.0 shipped on 2026-09-17, a reader who by definition is not the visitor a landing page is for; the goal names per-host installation as the first-screen content, and the bootstrap subsection&apos;s `/init` sentence enumerates the same four environment items `Why Cairn?` names a hundred words below.</drawback>
  </alternative>
  <alternative id="Commands only up top">
    The first-screen section keeps only the two per-host subsections — each its intro sentence and command block, the Antigravity one with its pinning sentence — and both the bootstrap steps and the migration paragraph move to a section of their own below the design-principles block, wherever the below-the-fold order settles.
    <advantage>The first screen&apos;s install area drops to about 95 words and two blocks — exactly the per-host installation the goal names — and each relocated item lands where its reader is: the bootstrap steps beside the below-the-fold prose about what the scaffold holds, the migration note out of every new visitor&apos;s path.</advantage>
    <drawback>`/init-milestone-base-workflow` must run before any other workflow skill — `/goto-next-milestone` reads the pointer file only it creates — so parting the mandatory second step from the install commands by some 250 words of pitch invites a visitor to install, skip the scaffold, and hit a missing-pointer stop one skill later, and the templates, which have no fold to move anything below, either keep the bootstrap steps adjacent and break the identical-subsection property or split for no reason of their own.</drawback>
  </alternative>
  <alternative id="Bootstrap stays, migration moves">
    The per-host subsections and the shared `### Bootstrap your project` stay on the first screen, and only the migration paragraph with its second command block leaves, becoming a short section of its own below the fold — placed wherever the below-the-fold order settles — reached from one bold pointer line in the Claude Code subsection, with the cairn-claude template split the same way so its install subsection stays word for word identical to the root&apos;s.
    <advantage>It removes the one item written for a reader who has already installed — 64 words and a block, thirty percent of the section — while keeping the step every new installer must run next to the commands it follows, so the first screen still carries a complete install-to-bootstrap path at about 160 words and three blocks, the switch commands stay documented while the monorepo marketplace goes on serving pinned installs, and the template identity survives as an identical install subsection plus an identical note elsewhere on each page.</advantage>
    <drawback>The README gains a one-topic section of sixty-odd words and a pointer line that must stay in step with it, its seat among the below-the-fold sections is a decision this question cannot settle, and the bootstrap subsection&apos;s `/init` sentence still repeats the four environment items `Why Cairn?` names beneath it unless that section&apos;s rewrite drops them.</drawback>
  </alternative>
  <alternative id="Migration note dropped">
    The migration paragraph and its block are deleted from the root README and the cairn-claude template rather than moved, and the per-host subsections plus the bootstrap subsection stay on the first screen as they are.
    <advantage>Milestone 23 recorded the note as optional by construction — the monorepo marketplace keeps serving `./hosts/claude`, so a pinned install keeps working and updating — and the section sheds the same thirty percent with nothing relocated, nothing new to keep in step, no pointer line, and every subsection still identical to its template.</advantage>
    <drawback>It removes the only place the switch commands exist — the 1.5.0 changelog names the note but not the commands — three days after the recommended source changed, so every install made before 2026-09-17 stays on the monorepo marketplace unless its owner works out the remove-add-install sequence alone, and it reverses a decision recorded four days ago on no new fact beyond first-screen space.</drawback>
  </alternative>
  <recommendation option="Bootstrap stays, migration moves">The two items differ in kind — the bootstrap step is mandatory before any other skill and belongs beside the commands it follows, while the migration paragraph, thirty percent of the section, is written for someone who has already installed — so demoting only the latter gives the first screen the per-host installation the goal names without splitting the install path; it beats deleting the note because the monorepo marketplace still serves every pre-1.5.0 install and nothing else records the switch commands, and the note can go in a later release once that audience has switched.</recommendation>
</open-question>
<open-question id="Below-the-fold sections">
  <question>Which existing root README sections stay below the fold — how it works, contributing, self-dogfooding, license — and in what order after the design-principles block?</question>
  <alternative id="Current order kept">
    All four stay in the order they hold today once the moved sections leave — `## How it works` (its three-file paragraph kept, closed by one new sentence linking docs/workflow.md and docs/skill-reference.md, since the pipeline intro&apos;s only anchor leaves with the pipeline), `## Contributing`, `## Self-dogfooding`, `## License` — with the relocated migration note as a short `##` section of its own between Self-dogfooding and License.
    <advantage>Nothing is reordered: the tool-level explainer still precedes the project tail milestone 24 shaped and milestone 25 ordered (join, showcase, license), and the only authored text below the principles block is the one docs-pointer sentence, so every section keeps a seat a recorded decision already justified.</advantage>
    <drawback>Contributing&apos;s five routing lines sit between the on-disk model and the one paragraph that shows a live instance of it, so a reader who takes &quot;look at milestones/&quot; on trust has to pass the issue-form and security links to reach it, and the page ends showcase, note, license rather than the contributing-then-license close a README reader expects.</drawback>
  </alternative>
  <alternative id="Showcase after the model">
    All four stay, with Self-dogfooding moved up one seat — `## How it works` (the same paragraph and closing docs-pointer sentence), `## Self-dogfooding`, `## Contributing`, `## License` — and the migration note seated as its own short `##` section between Contributing and License; no section text changes beyond the docs sentence.
    <advantage>The tail reads in the order its readers use it: the on-disk model is followed at once by the repository that runs it — the &quot;look at milestones/&quot; paragraph landing right after the paragraph that says what milestones/ holds — while Contributing, which milestone 24 placed as a hygiene surface a visitor scans for by heading rather than reads in sequence, closes the page in the conventional contributing-then-license position; the four sections become contiguous for the first time — some 5,700 words of pipeline, commit conventions and skill reference stood between How it works and the tail when milestone 25 fixed its order — so the swap answers an adjacency that order never weighed.</advantage>
    <drawback>It revises the join-showcase-license sequence milestone 25 recorded, for a gain only the reader who scrolls past the principles block linearly ever sees, and the temporary migration note wedged before License interrupts the contributing-then-license close until a later release retires it.</drawback>
  </alternative>
  <alternative id="Model and showcase merged">
    `## How it works` absorbs Self-dogfooding: one section carries the three-file paragraph, the docs-pointer sentence, and the this-repository-runs-it paragraph as its close, so the page runs How it works, Contributing, the migration note, License, with one heading fewer.
    <advantage>The model and its live instance read as one continuous section with a single home for the docs links, and the page below the principles block drops to three headings plus the note.</advantage>
    <drawback>It saves a heading and no words while taking away the landmark milestone 1 recorded the showcase for — a scanning reader no longer sees that this repository dogfoods its own workflow — and it files a claim about this repository under a heading that describes the tool, the subject split milestone 25 kept when it refused to put the adoption table under Self-dogfooding.</drawback>
  </alternative>
  <alternative id="Documentation links instead">
    `## How it works` is dropped and a `## Documentation` section of three links — the workflow walkthrough, the skill reference, the design-claims page — takes its seat, followed by Contributing, Self-dogfooding, the migration note, and License.
    <advantage>The fewest words below the principles block — a link list in place of an explainer of under a hundred words — and every page under docs/ is reachable from one place a reader finds by heading.</advantage>
    <drawback>The three-file paragraph is the only text on the landing page saying what Cairn creates in a project — the scaffold the bootstrap subsection above and the Self-dogfooding paragraph below both point at — so dropping it sends a visitor into a 2,000-word walkthrough for a fact three bullets carry, and the claims link repeats the one the principles block already ends with.</drawback>
  </alternative>
  <depends-on question="Docs page layout" option="Narrative and reference pages"/>
  <depends-on question="Install section scope" option="Bootstrap stays, migration moves"/>
  <recommendation option="Showcase after the model">All four sections earn their seat — the model, its live instance, the routing surface, the license — and the milestone makes them contiguous for the first time, which is the fact milestone 25&apos;s join-then-showcase order never weighed; Self-dogfooding is read in flow and belongs right after the model it instantiates, Contributing is found by heading and belongs where a README reader expects it, last before License, and the docs-pointer sentence closes How it works because it is the last tool-level section on the page; the migration note goes between Contributing and License as an appendix reached from its pointer line, out of every scrolling reader&apos;s path and removable later without moving anything else.</recommendation>
</open-question>
<open-question id="Distribution README depth">
  <question>Beyond the one-liner, adoption badges, installation, and contributor redirect, do the distribution README templates also carry the loop diagram, the &quot;Why Cairn?&quot; text, or the design-principles block?</question>
  <alternative id="Four items only">
    The templates carry exactly what the goal enumerates — the one-liner under `# Cairn for &lt;Host&gt;`, that repository&apos;s two adoption badges, the host&apos;s installation section, and the generated-do-not-edit note that redirects contributors — over the existing source-pointer and license tail, with no diagram, no &quot;Why Cairn?&quot; and no principles block; the link to `uHappyLogic/cairn` the opening sentence already carries is where a reader goes for the pitch.
    <advantage>It reads the goal&apos;s em-dash list as the shape it names and keeps the distribution README what milestone 23 defined it to be — the install page of the recommended source — so the only text kept in step with the root README by hand stays the install subsection that decision accepted plus a one-sentence subtitle, on two pages that reach their repositories only through a release and that nothing in the build checks against the root README.</advantage>
    <drawback>With the one-liner taking over from the forty-word clarify-resolve-derive-complete-close-out sentence the templates open with today, a visitor arriving from search sees one sentence and two command blocks and learns the loop&apos;s shape only by following the root link, and the README that travels inside every Claude Code plugin cache and every Antigravity `.agents/plugins/cairn/` extraction says nothing about how the skills beside it fit together.</drawback>
  </alternative>
  <alternative id="Diagram added">
    The four items plus the root README&apos;s loop diagram, pasted verbatim as a fenced mermaid block between the one-liner and `## Installation` in both templates, with neither prose section copied.
    <advantage>It is the one element that copies cleanly — a self-contained block of some fifteen lines, identical on both hosts because its labels are skill names, rendered by GitHub in any repository, and free of `{{` so it passes the build&apos;s unfilled-placeholder check — and it gives back at a glance the workflow shape the templates&apos; forty-word opening sentence described, on the page that travels inside every installed plugin tree.</advantage>
    <drawback>The goal calls the diagram a stand-in until a later milestone records the demo GIF, so the copy is made to be replaced at three sites instead of one — or left standing in the distribution repositories once the root README shows a GIF the templates could pull only by absolute URL from the root repository — and it sits between the subtitle and the install commands that are the page&apos;s purpose, on repositories whose unique-views badges read 4 each against the monorepo&apos;s 22.</drawback>
  </alternative>
  <alternative id="Diagram and Why Cairn">
    The four items plus both self-contained elements — the diagram and the &quot;Why Cairn?&quot; section, its host sentence in the phrase each template already uses — leaving out only the principles block, whose claim lines are kept in step with a docs/ page that exists in the root repository alone.
    <advantage>Each template then stands on its own as what, why and how — a reader holding only the installed tree or only the distribution repository never needs the root README to understand what the skills beside it do — and the copy rule is mechanical: everything that needs no link into another repository copies, and the one element that does stays home.</advantage>
    <drawback>Some 150 words of prose become a third hand-kept copy on two pages nothing checks against the root README, and the copy is release-lagged — a &quot;Why Cairn?&quot; edit on `main` reaches the distribution repositories only with the next `/release-plugin`, so the pitch reads two ways across the three READMEs between releases — while the section&apos;s failure-modes paragraph addresses someone deciding whether to adopt Cairn, not the reader of an install page.</drawback>
  </alternative>
  <alternative id="Full landing page">
    The templates mirror the root landing page whole — one-liner, badges, diagram, installation, &quot;Why Cairn?&quot; and the design-principles block with its link rewritten as an absolute URL into `uHappyLogic/cairn`&apos;s `docs/` — above the note, the source pointer and the license.
    <advantage>Every repository a visitor can land on carries the complete pitch, so the recommended install source is a landing page in the full sense rather than an install page that points elsewhere.</advantage>
    <drawback>It turns the one hand-aligned duplicate milestone 23 accepted into four across three files, the principles block cannot even be a verbatim copy — its link changes form and its claim lines already track the claims page by hand, so the templates would track a copy of a copy — and the whole mirror reaches the distribution repositories only at release time, for pages read by four unique visitors each since 2026-09-04.</drawback>
  </alternative>
  <depends-on question="One-liner wording" option="One-liner as subtitle"/>
  <recommendation option="Four items only">The goal&apos;s list is the shape: milestone 23 made the distribution README the install page of the recommended source and accepted exactly one hand-aligned duplicate for the installer&apos;s self-sufficiency, and none of the three elements serves that reader — the diagram is a stand-in the demo GIF will retire, and the two prose sections are pitch kept in step by hand and release-lagged on pages with four unique views each — so the templates carry the four named items and the root link in their opening sentence carries the reader to the pitch; should the maintainer want the workflow drawn on the install page anyway, the diagram is the only element worth copying, since it alone copies verbatim, build-clean and link-free.</recommendation>
</open-question>
<open-question id="Distribution badge form">
  <question>Where do a distribution repository&apos;s own unique-views and unique-clones badges sit in its README — in the top badge row or in an adoption section — and do they carry the caption about the workflow&apos;s own daily clone?</question>
  <alternative id="Row with caption line">
    The two badges form a centred `&lt;p align=&quot;center&quot;&gt;` strip above `# Cairn for &lt;Host&gt;` — the root&apos;s header-row idiom, each badge linking to this repository&apos;s `traffic-data` branch as milestone 25 fixed — with a one-sentence caption as the paragraph beneath them: cumulative since 2026-09-04, each day&apos;s uniques summed, and the clone count including the badge workflow&apos;s own daily fetch as one unique clone a day — the clauses the root&apos;s caption keeps for every row, minus the monorepo-only CI-checkouts one.
    <advantage>It is the root&apos;s first screen transposed to one repository — badges, the sentence that qualifies them, then the title — so the floor is disclosed beside the number it inflates, on the page where that floor grows fastest relative to the count (one unique clone a day against the 18 and 26 the two clones badges read today), in a sentence the root already carries.</advantage>
    <drawback>The first prose on an install page becomes two lines about what the numbers do not mean, set above the h1 where the root&apos;s status strip is wordless, and the page then describes the same branch twice — the caption above the title and the note&apos;s `traffic-data` sentence below — with the caption a third copy of a root sentence, kept in step by hand on templates that reach their repositories only at release.</drawback>
  </alternative>
  <alternative id="Row with note clause">
    The same centred two-badge strip above the title with no caption, and the disclosure folded into the generated note&apos;s `traffic-data` sentence — the one that already says the workflow runs only in this repository and writes its data file and badge SVGs there daily &quot;for the adoption table in the root repository&apos;s README&quot;, and has to be reworded once those SVGs serve this page too — as one added clause: the workflow&apos;s own daily fetch of that branch is one unique clone a day in the clones badge.
    <advantage>The first screen stays what the four-item shape makes it — badges, title, one-liner, install commands — with the badges in the root&apos;s wordless strip idiom, needing no repository label because the page is about one repository, and every fact about the workflow (that it runs only here, what it writes, and that its fetch is itself a clone) sits in the one paragraph a consumer already reads about it, in a sentence this milestone rewords anyway.</advantage>
    <drawback>The floor is disclosed away from the number it qualifies — past the title, the one-liner, and wherever the four-item shape seats the note — so a visitor who reads the clones badge at the top and leaves never meets it, and the note, already three sentences of exceptions, grows a fourth clause.</drawback>
  </alternative>
  <alternative id="Adoption section">
    The badges keep the form milestone 25 gave the root — a `## Adoption` section holding the two linked badges and the caption paragraph — seated between the one-liner and `## Installation`, where the goal&apos;s list places the adoption badges, with no strip above the title.
    <advantage>A captioned measurement under a heading a reader finds by name, the very argument milestone 25 recorded for keeping caveated numbers out of the wordless status strip, in plain Markdown throughout — no HTML block enters templates that today carry none.</advantage>
    <drawback>A `##` heading over two badges and one sentence is the heaviest frame on the page for its smallest element, it pushes the install commands — the page&apos;s purpose — below a section written for an evaluating reader, and it gives a distribution page the heading the goal&apos;s first-screen shape leaves the root no room for, so the &quot;same shape&quot; the goal names is not the same.</drawback>
  </alternative>
  <alternative id="Bare row">
    The centred two-badge strip above the title and nothing more — no caption and no clause in the note — on the reasoning that the root README&apos;s adoption caption already discloses the floor for every repository and each badge links to the `traffic-data` branch whose `totals.json` holds the per-day counts.
    <advantage>The smallest change — two linked images in a strip — with no caveat prose on an install page and nothing new kept in step between the root&apos;s caption and the templates.</advantage>
    <drawback>It shows a number whose floor compounds daily with no disclosure on the page that displays it — a distribution repository&apos;s clones badge gains one unique clone a day from the workflow&apos;s own fetch, some thirty a month against the 18 and 26 read today, so &quot;unique clones&quot; reads as people when a growing share is the bot — the misreading milestone 25 chose to disclose rather than accept, and the root&apos;s caption is another repository away from a visitor who lands here from an install command or a search result.</drawback>
  </alternative>
  <depends-on question="Distribution README depth" option="Four items only"/>
  <recommendation option="Row with note clause">A distribution README is the install page of one repository, so its two badges go in a centred strip above the title — the root&apos;s header-row idiom, where they need neither a repository label nor a table — and the floor in their clones count is a fact about the traffic workflow, that its own fetch is a clone, which belongs in the generated note&apos;s sentence about that workflow and its branch, a sentence this milestone must reword anyway once the SVGs stop serving only the root&apos;s table; what separates it from the caption line is cost — the clause edits text already kept in step across the two templates, while a caption adds a third copy of a root sentence above the h1 of a page whose first screen exists for the install commands — and the start date and unique-sum clauses stay with the root&apos;s caption, the one place three repositories&apos; counts are compared.</recommendation>
</open-question>
<open-question id="Stale skill entries">
  <question>Are the two known stale skill-reference entries — the goto-next-milestone arguments and the finish-current-milestone next-step sentence — corrected as the reference moves into docs/, or left for a later milestone?</question>
  <alternative id="Corrected in the move">
    As the skill reference moves to its docs/ page, the two entries are rewritten to match the live skills — the `goto-next-milestone` heading loses `&lt;number&gt; &lt;title&gt;` and its body says the skill activates an already-defined milestone (scans `milestones/` for a defined-but-not-yet-active directory, points `milestones/README.md` at it, creates nothing), and `finish-current-milestone`&apos;s closing sentence names `/define-milestone-goal` and then `/goto-next-milestone` as what runs next — with the wording drawn from the two skills&apos; own opening paragraphs and the `CLAUDE.md` pipeline map.
    <advantage>This is the first milestone since 15 whose scope includes rewriting the reference&apos;s home, so it closes a follow-up carried through ten milestones for about sixty words that already exist elsewhere: the new page then never tells a reader to pass arguments the argument-free skill rejects or to skip the `/define-milestone-goal` step its zero-candidate stop demands, and nothing in the repository links to either entry&apos;s anchor, so the heading change breaks no link.</advantage>
    <drawback>The relocation is no longer a pure move — a reviewer diffing the docs/ page against the README section it came from meets two edited entries among twenty-five — and the fix rides on the move being content work rather than on a goal clause, where milestones 15 and 16 both declined the same edit as outside their scope.</drawback>
  </alternative>
  <alternative id="Whole reference re-verified">
    The move doubles as a verification pass: every one of the 25 entries is checked against its live `SKILL.md` or agent file and the `CLAUDE.md` map, and whatever is stale — the two known entries and anything else found — is corrected on the docs/ page, with a ledger of what was checked.
    <advantage>The page starts life verified end to end rather than carrying two fixes and an assumption that the other twenty-three entries still hold after ten milestones of change.</advantage>
    <drawback>It is milestone 15&apos;s README-verification task over again — a 4,312-word, 25-entry ledger against 25 runtime files, a whole task with fifty **Verified:** bullets — grafted onto a landing-page milestone whose goal never names it, for a low expected yield: that pass cleared every other entry, the milestones since have synced their own README changes in eleven commits, and a spot-check for retired vocabulary finds nothing else stale.</drawback>
  </alternative>
  <alternative id="Left for a later milestone">
    The reference moves verbatim, the two entries included, and the follow-up milestone 15 recorded is carried forward once more, to be fixed by a milestone whose goal names it.
    <advantage>The move stays a pure relocation — the docs/ page checks against the README section by a plain diff — and it keeps the discipline milestones 15 and 16 applied, editing only what the milestone&apos;s own scope makes false.</advantage>
    <drawback>No goal will ever name a two-entry fix, so &quot;later&quot; has had no owner for ten milestones and gets none here, while the page this milestone creates to be read opens on the two skills that bracket every milestone with a claim false since before milestone 1 was defined — give `goto-next-milestone` a number and a title it rejects — and a next-step sentence that skips the `/define-milestone-goal` step, for exactly the landing-page visitor who takes the reference at face value.</drawback>
  </alternative>
  <recommendation option="Corrected in the move">The reference&apos;s home is rewritten by this milestone, which is the one time the two entries are in scope by construction rather than by a sweep&apos;s falsification rule, and the fix is sixty words already written in the skills&apos; own opening paragraphs and the `CLAUDE.md` map — so leaving them ships a fresh docs page that is wrong about the two skills every milestone starts and ends with, while a full re-verification is a task-sized audit whose only known findings are these two.</recommendation>
</open-question>
<open-question id="Banner file disposal">
  <question>Is the banner image file deleted from the repository once the README no longer references it, or kept for another use?</question>
  <alternative id="Deleted with the reference">
    The task that drops the `&lt;p align=&quot;center&quot;&gt;` banner paragraph from `README.md` also removes `.github/assets/readme/cairn-banner.png` in the same commit, taking the now-empty `.github/assets/` directory with it, so no commit leaves the reference gone and the file stranded.
    <advantage>The tree keeps no asset nothing references — the file&apos;s only consumer is `README.md` line 2, the distribution trees never carry it because the release publishes the `hosts/&lt;host&gt;` trees alone, and `.github/` returns to exactly the issue forms, pull-request template and two workflows — while the image loses nothing: both banner blobs (milestone 1&apos;s 4.17 MB webp and this 4.7 MB PNG, some 8.9 MB of the 37 MB object store) are permanent history in every clone whichever way this settles, so `git show 06f4aac:.github/assets/readme/cairn-banner.png` recovers it byte for byte the day a use appears.</advantage>
    <drawback>The one visual carrier of the trail-marker metaphor behind the name leaves the visible tree, so a later use — a social preview, a header for the demo-GIF milestone — has to know to dig it out of history or revert the deletion, and a 4.7 MB smaller checkout is all a clone gains, since deleting a committed blob shrinks no clone.</drawback>
  </alternative>
  <alternative id="Kept in place, unreferenced">
    `README.md` drops its reference and the file stays at its path untouched, as it would by default if the task edited only the README.
    <advantage>The milestone touches nothing outside the README for this item, and the image stays one path away for whichever later use claims it, with no revert and no history dig.</advantage>
    <drawback>It keeps an orphan — a 4.7 MB binary under a directory named for the README that the README no longer uses, which every reader of the tree finds and traces to nothing — for a use the question cannot name: the repository&apos;s social preview is an upload through its settings page at 1280×640 under 1 MB, never read from a path, and the demo GIF the goal defers is a different file; and keeping it saves no clone size either, because both banner blobs are already permanent history.</drawback>
  </alternative>
  <alternative id="Repurposed as the social preview">
    The PNG stays in the tree as the source of a 1280×640 crop uploaded through the repository&apos;s Settings → Social preview — today `usesCustomOpenGraphImage` is false, so a shared link renders GitHub&apos;s generated card — so the banner&apos;s art still meets a visitor on every link to the repository.
    <advantage>The banner keeps a job — every OpenGraph card for the repository shows it — and the tree holds the source the upload was derived from.</advantage>
    <drawback>It adds a repository-settings task the goal never names, done by hand in the web UI because no API sets the social preview, from a crop and re-encode the tree file is not the byte-for-byte source of — 3168×976 at 4.7 MB against a 2:1 upload under 1 MB — so the tree keeps a 4.7 MB file whose only consumer is an off-repository setting nothing in the build or release checks, and whose real source is equally the blob already in history.</drawback>
  </alternative>
  <recommendation option="Deleted with the reference">The file&apos;s only consumer is the README line the goal removes, the distribution trees never carry it, and git history already holds it permanently — deletion reclaims nothing from a clone and loses nothing recoverable — so the tree should not keep an orphan binary under a README-named directory for a use nobody has named; the social preview, if ever wanted, is a resized upload through repository settings that history can source as well as the tree, and the demo GIF the goal defers is a different asset that starts the assets directory fresh; remove the file in the same task as the README edit so no commit leaves the reference gone and the file stranded.</recommendation>
</open-question>
