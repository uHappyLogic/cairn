# TASKS DONE

## Move Workflow And Skill Reference Into Docs

Create `docs/workflow.md` carrying the README's six `## Workflow pipeline` phase sections with their mermaid diagrams followed by `How skills commit` and `The answer-principle-learning loop` as sibling sections, and `docs/skill-reference.md` carrying the 25 per-skill and per-agent entries, removing all of it from `README.md` while keeping every heading's text so existing anchors survive as path-prefixed links; as the reference moves, correct the two stale entries — the `goto-next-milestone` heading loses its `<number> <title>` arguments and its body says the skill activates an already-defined milestone and creates nothing, and `finish-current-milestone`'s closing sentence names `/define-milestone-goal` and then `/goto-next-milestone` as what runs next. Re-point the pipeline intro's `Skill reference` anchor and `CONTRIBUTING.md`'s `Workflow pipeline` link to the new pages in the same change. Verified when the two pages hold the moved sections word for word apart from the two corrections, `README.md` no longer contains them, and no link in the repository still targets a moved README anchor.

**Verified:**

- `docs/workflow.md` exists and holds, word for word against `git show HEAD:README.md` lines 81–240, the `## Workflow pipeline` heading and intro, the six `###` phase sections with their six mermaid diagrams, and `## How skills commit` with both paragraphs, followed by `## The answer-principle-learning loop` (heading text kept, promoted from `###` to `##` as a sibling section) with its paragraph and three bullets from lines 266–270; a `diff` against the committed README shows only the `Skill reference` anchor re-point and that one heading-level change.
- `docs/skill-reference.md` exists and holds all 23 `###` per-skill and per-agent entries the README carried (21 skills plus the `answer-open-question-with-recommendation` and `recommend-open-question` agents — the starting state's "25" was a miscount; the `complete-task` agent never had an entry) word for word with the loop subsection lifted out; a `diff` against the committed README shows only the two corrections and the `How skills commit` link re-point.
- The `goto-next-milestone` heading reads ``### `goto-next-milestone` `` with no `<number> <title>` arguments, and its body says the skill activates an already-defined milestone — scans `milestones/` for a defined-but-not-yet-active directory and updates `milestones/README.md` to point to it — and creates no files or directories.
- `finish-current-milestone`'s closing sentence reads "run `/define-milestone-goal` to define the next milestone, then `/goto-next-milestone` to activate it".
- `README.md` no longer contains `## Workflow pipeline`, any `###` phase section, any mermaid fence, `## How skills commit`, `## Skill reference`, any skill entry, or the loop subsection (each grep returns 0); `git diff README.md` is 265 deletions and 0 insertions, leaving `# Cairn`, Why Cairn?, Installation and its three subsections, How it works, Adoption, Contributing, Self-dogfooding, and License intact.
- Every moved heading's text is kept except `goto-next-milestone`'s (whose old anchor nothing in the repository references), so the existing anchors survive as path-prefixed links — `docs/workflow.md#workflow-pipeline`, `docs/workflow.md#how-skills-commit`, `docs/workflow.md#the-answer-principle-learning-loop`, `docs/skill-reference.md#skill-reference`, `docs/skill-reference.md#review-milestone-requirements`.
- The pipeline intro's `[Skill reference](#skill-reference)` now reads `[Skill reference](skill-reference.md)` in `docs/workflow.md`; the `answer-open-question` entry's `[How skills commit](#how-skills-commit)` now reads `workflow.md#how-skills-commit` in `docs/skill-reference.md`; a reader of the reference page reaches the loop by the link out in its one-line intro.
- `CONTRIBUTING.md` line 13 links `[Workflow pipeline](docs/workflow.md#workflow-pipeline)` (attributed to "the docs'" rather than "the README's"), and its untouched `README.md#installation` link targets a section that did not move.
- A repository-wide grep (excluding `.git`, `temp/`, and the historical `milestones/milestone_*` records, where the old anchors survive only as backticked prose inside starting-state and Verified text) finds no link to a moved README anchor, and a script resolving every relative link in the four touched files against existing files and headings reports 0 unresolved.
- `docs/workflow.md` carries six mermaid fences with six byte-identical `%%{init: …}%%` theme blocks, neither docs page contains `{{` outside a `${{` expression, and `uv run scripts/build_hosts.py --check` exits 0 ("Check passed").

---

## Promote Design Claims Into Docs Page

Commit `temp/cairn-design-claims.md` as `docs/design-claims.md` with its 19 claims, six-group order, **Design** paragraphs, and **Metric** lines kept word for word in Simplified Technical English, changing only the frame: the intro becomes one orienting paragraph for a repository reader (what the list is, why it uses a controlled register, that each Metric names the test that would check the claim, and that no test suite in the repository runs them yet), the closing note keeps only the half saying claims 17–19 concern the plugin's own code, and claim 12's sentence "the work tree is clean after each skill" is corrected in the same register to match the non-committing bootstrap skill and the partial-work-stays rule. Verified when the committed page diffs from the source only in those three places and each claim line still heads its own entry so the README block can link to it.

**Verified:**

- `docs/design-claims.md` exists beside `docs/workflow.md` and `docs/skill-reference.md`, derived from `temp/cairn-design-claims.md` by three exact replacements with everything else copied byte-for-byte.
- All 19 `### N. …` claim headings are byte-identical to the source and in the same six-group order under the same six `##` group headings (Decisions, Work, Records, Efficiency, Robustness, Engineering) — a `diff` of the two files' heading lines is empty; counts are 6 groups, 19 claims, 19 **Design** lines, 19 **Metric** lines.
- Every **Design** paragraph and **Metric** line is word-for-word identical to the source except claim 12's Design, where the one sentence "The work tree is clean after each skill." became "The work tree is clean after each skill, with two exceptions. The bootstrap skill does not commit and leaves its changes staged for the user. A failed run leaves its partial work in the tree for the next run." in the same short-sentence STE register; claim 12's heading, its other Design sentences, and its Metric line are unchanged, and the two exceptions match the live plugin (`init-milestone-base-workflow` is non-committing and leaves changes staged; the completion procedure's carry-out step continues from uncommitted partial work).
- The intro is one paragraph (no blank line inside it) addressed to a repository reader that states what the list is (19 design claims in six groups, each with its design and one metric), why it uses a controlled register (ASD-STE100 Simplified Technical English, so each claim reads the same to a reader and to a test author), that each **Metric** line names the test that would check its claim, and that no test suite in the repository runs those tests yet.
- The closing note after the `---` is the single sentence "Claims 17 to 19 are about the plugin code, not a user project."; the benchmark-versus-showcase sentence is dropped.
- `diff -u temp/cairn-design-claims.md docs/design-claims.md` shows exactly 3 hunks — the intro, claim 12's Design paragraph, and the closing note — and nothing else.
- Each claim line still heads its own entry as a `###` heading, so the README block can link to it; claims 2, 5, and 11 head lines 20, 45, and 100 as `### 2. Each decision has a record in git.`, `### 5. Advice gets better with each milestone.`, and `### 11. The records are machine-readable.`.
- `grep -P '(?<!\$)\{\{'` finds nothing in the page, and `uv run scripts/build_hosts.py --check` exits 0 ("Check passed").

---

## Rebuild README First Screen Header

Rework the top of `README.md` so the first screen opens with the three shields.io badges followed by the adoption block — the existing three-row pipe table with its `## Adoption` heading dropped, its caption cut to one sentence of about two lines that keeps the four facts (cumulative since 2026-09-04, unique counts summed per day, the badge workflow's own fetch adds one unique clone per repository per day, the monorepo's clones include CI checkouts) — placed above `# Cairn`, then the title with its bold "Mark the path from idea to shipped." lead and the subtitle replaced verbatim by "Milestone-driven development for your coding agent — any kind of work, one milestone at a time." Delete the `<p align="center">` banner paragraph together with `.github/assets/readme/cairn-banner.png` and the then-empty `.github/assets/` directory in the same commit, and remove the old `## Adoption` section from its place near the foot of the file. Verified when the rendered page opens on badges, table, and title with no banner, the file has one adoption table and no `## Adoption` heading, and nothing in the repository references the deleted image.

**Verified:**

- `README.md` no longer contains the `<p align="center">` banner paragraph — `grep -c 'cairn-banner\|alt="Cairn banner"'` returns 0 — and its first non-blank line (line 1) opens the three shields.io badges block (release, CI, MIT), which `diff` shows byte-identical to lines 4–14 of `HEAD:README.md`.
- Directly under the badge block (one blank line after `</p>`) and above `# Cairn` (line 23) sits the adoption block: the three-row pipe table at lines 13–17, `diff`-identical to lines 83–87 of `HEAD:README.md` (header `repository | unique views | unique clones`, rows for `uHappyLogic/cairn`, `cairn-claude`, `cairn-antigravity`, each label linked to its repository, each of the six badges a raw `traffic-data` SVG image linked to that branch page), with no `## Adoption` heading anywhere above it.
- The caption (line 19) is one sentence of 234 characters with a single terminal period — "Cumulative since 2026-09-04, with unique counts summed per day (a returning visitor counts again); the badge workflow's own daily fetch adds one unique clone per repository per day, and the monorepo's clones also include CI checkouts." — carrying all four facts: cumulative since 2026-09-04, unique counts summed per day so a returning visitor counts again, the workflow's own daily fetch adds one unique clone per repository per day, and the monorepo's clones include CI checkouts.
- The `---` rule stays immediately above the title block: `# Cairn`, `**Mark the path from idea to shipped.**` on its own line, then the exact line "Milestone-driven development for your coding agent — any kind of work, one milestone at a time." (1 match) in place of "Milestone-driven development for any kind of work." (0 matches), with `## Why Cairn?` following at line 28 unchanged.
- The old `## Adoption` section near the foot is gone: `## Adoption` occurs 0 times, `| repository |` occurs exactly once, and a `diff` of everything from `## Why Cairn?` onward against `HEAD:README.md` shows only the ten-line `## Adoption` section removed — the `##` order Why Cairn? → Installation → How it works → Contributing → Self-dogfooding → License is otherwise unchanged (the tail reorder belongs to a later task); `git diff --numstat` is 9 insertions and 14 deletions on `README.md`.
- `.github/assets/readme/cairn-banner.png` is deleted (`git status` shows ` D`) with its removal staged alongside the README edit so both land in one commit, and `.github/assets/` no longer exists, leaving `.github/` with exactly `ISSUE_TEMPLATE/`, `PULL_REQUEST_TEMPLATE.md`, and `workflows/`.
- Nothing in the repository references the deleted image: a grep for `cairn-banner` and `.github/assets` excluding `.git`, `temp/`, and `milestones/` returns no match, and inside `milestones/` the path never appears inside a link or `src=` — it survives only as prose in milestone 24's and 26's starting-state, decision, and task text.
- GitHub's markdown API (`gh api markdown`, gfm mode) renders the page as the badge `<p align="center">`, then `<table>` (four `<tr>`, six `traffic-data` badge images), then the caption `<p>`, then `<hr>`, then `<h1>Cairn</h1>`, with no banner `<img>` anywhere and the `<h2>` sequence Why Cairn?, Installation, How it works, Contributing, Self-dogfooding, License.
- `uv run scripts/build_hosts.py --check` exits 0 ("Check passed … at version 1.5.1") and `grep -P '(?<!\$)\{\{' README.md` finds nothing.

---
