# TASKS DONE

## Move Workflow And Skill Reference Into Docs

Create `docs/workflow.md` carrying the README's six `## Workflow pipeline` phase sections with their mermaid diagrams followed by `How skills commit` and `The answer-principle-learning loop` as sibling sections, and `docs/skill-reference.md` carrying the 25 per-skill and per-agent entries, removing all of it from `README.md` while keeping every heading's text so existing anchors survive as path-prefixed links; as the reference moves, correct the two stale entries — the `goto-next-milestone` heading loses its `<number> <title>` arguments and its body says the skill activates an already-defined milestone and creates nothing, and `finish-current-milestone`'s closing sentence names `/define-milestone-goal` and then `/goto-next-milestone` as what runs next. Re-point the pipeline intro's `Skill reference` anchor and `CONTRIBUTING.md`'s `Workflow pipeline` link to the new pages in the same change. Verified when the two pages hold the moved sections word for word apart from the two corrections, `README.md` no longer contains them, and no link in the repository still targets a moved README anchor.

**Verified:**

- `docs/workflow.md` exists and holds, word for word against `git show HEAD:README.md` lines 81–240, the `## Workflow pipeline` heading and intro, the six `###` phase sections with their six mermaid diagrams, and `## How skills commit` with both paragraphs, followed by `## The answer-principle-learning loop` (heading text kept, promoted from `###` to `##` as a sibling section) with its paragraph and three bullets from lines 266–270; a `diff` against the committed README shows only the `Skill reference` anchor re-point and that one heading-level change.
- `docs/skill-reference.md` exists and holds all 23 `###` per-skill and per-agent entries the README carried (21 skills plus the `answer-open-question-with-recommendation` and `recommend-open-question` agents — the starting state's "25" was a miscount; the `complete-task` agent never had an entry) word for word with the loop subsection lifted out; a `diff` against the committed README shows only the two corrections and the `How skills commit` link re-point.
- The `goto-next-milestone` heading reads ``### `goto-next-milestone` `` with no `<number> <title>` arguments, and its body says the skill activates an already-defined milestone — scans `milestones/` for a defined-but-not-yet-active directory and updates `milestones/README.md` to point to it — and creates no files or directories.
- `finish-current-milestone`'s closing sentence reads "run `/define-milestone-goal` to define the next milestone, then `/goto-next-milestone` to activate it".
- `README.md` no longer contains `## Workflow pipeline`, any `###` phase section, any mermaid fence, `## How skills commit`, `## Skill reference`, any skill entry, or the loop subsection (each grep returns 0); `git diff README.md` is 265 deletions and 0 insertions, leaving `# Cairn`, Why Cairn?, Installation and its three subsections, How it works, Adoption, Contributing, Self-dogfooding, and License intact.
- Every moved heading's text is kept except `goto-next-milestone`'s (whose old anchor nothing in the repository references), so the existing anchors survive as path-prefixed links — `docs/workflow.md#workflow-pipeline`, `docs/workflow.md#how-skills-commit`, `docs/workflow.md#the-answer-principle-learning-loop`, `docs/skill-reference.md#skill-reference`, `docs/skill-reference.md#review-milestone-requirements`.
- The pipeline intro's `[Skill reference](#skill-reference)` now reads `[Skill reference](skill-reference.md)` in `docs/workflow.md`; the `answer-open-question` entry's `[How skills commit](#how-skills-commit)` now reads `workflow.md#how-skills-commit` in `docs/skill-reference.md`; a reader of the reference page reaches the loop by the link out in its one-line intro.
- `CONTRIBUTING.md` line 13 links `[Workflow pipeline](docs/workflow.md#workflow-pipeline)` (attributed to "the docs'" rather than "the README's"), and its untouched `README.md#installation` link targets a section that did not move.
- A repository-wide grep (excluding `.git`, `temp/`, and the historical `milestones/milestone_*` records, where the old anchors survive only as backticked prose inside starting-state and Verified text) finds no link to a moved README anchor, and a script resolving every relative link in the four touched files against existing files and headings reports 0 unresolved.
- `docs/workflow.md` carries six mermaid fences with six byte-identical `%%{init: …}%%` theme blocks, neither docs page contains `{{` outside a `${{` expression, and `uv run scripts/build_hosts.py --check` exits 0 ("Check passed").

---

## Promote Design Claims Into Docs Page

Commit `temp/cairn-design-claims.md` as `docs/design-claims.md` with its 19 claims, six-group order, **Design** paragraphs, and **Metric** lines kept word for word in Simplified Technical English, changing only the frame: the intro becomes one orienting paragraph for a repository reader (what the list is, why it uses a controlled register, that each Metric names the test that would check the claim, and that no test suite in the repository runs them yet), the closing note keeps only the half saying claims 17–19 concern the plugin's own code, and claim 12's sentence "the work tree is clean after each skill" is corrected in the same register to match the non-committing bootstrap skill and the partial-work-stays rule. Verified when the committed page diffs from the source only in those three places and each claim line still heads its own entry so the README block can link to it.

**Verified:**

- `docs/design-claims.md` exists beside `docs/workflow.md` and `docs/skill-reference.md`, derived from `temp/cairn-design-claims.md` by three exact replacements with everything else copied byte-for-byte.
- All 19 `### N. …` claim headings are byte-identical to the source and in the same six-group order under the same six `##` group headings (Decisions, Work, Records, Efficiency, Robustness, Engineering) — a `diff` of the two files' heading lines is empty; counts are 6 groups, 19 claims, 19 **Design** lines, 19 **Metric** lines.
- Every **Design** paragraph and **Metric** line is word-for-word identical to the source except claim 12's Design, where the one sentence "The work tree is clean after each skill." became "The work tree is clean after each skill, with two exceptions. The bootstrap skill does not commit and leaves its changes staged for the user. A failed run leaves its partial work in the tree for the next run." in the same short-sentence STE register; claim 12's heading, its other Design sentences, and its Metric line are unchanged, and the two exceptions match the live plugin (`init-milestone-base-workflow` is non-committing and leaves changes staged; the completion procedure's carry-out step continues from uncommitted partial work).
- The intro is one paragraph (no blank line inside it) addressed to a repository reader that states what the list is (19 design claims in six groups, each with its design and one metric), why it uses a controlled register (ASD-STE100 Simplified Technical English, so each claim reads the same to a reader and to a test author), that each **Metric** line names the test that would check its claim, and that no test suite in the repository runs those tests yet.
- The closing note after the `---` is the single sentence "Claims 17 to 19 are about the plugin code, not a user project."; the benchmark-versus-showcase sentence is dropped.
- `diff -u temp/cairn-design-claims.md docs/design-claims.md` shows exactly 3 hunks — the intro, claim 12's Design paragraph, and the closing note — and nothing else.
- Each claim line still heads its own entry as a `###` heading, so the README block can link to it; claims 2, 5, and 11 head lines 20, 45, and 100 as `### 2. Each decision has a record in git.`, `### 5. Advice gets better with each milestone.`, and `### 11. The records are machine-readable.`.
- `grep -P '(?<!\$)\{\{'` finds nothing in the page, and `uv run scripts/build_hosts.py --check` exits 0 ("Check passed").

---

## Rebuild README First Screen Header

Rework the top of `README.md` so the first screen opens with the three shields.io badges followed by the adoption block — the existing three-row pipe table with its `## Adoption` heading dropped, its caption cut to one sentence of about two lines that keeps the four facts (cumulative since 2026-09-04, unique counts summed per day, the badge workflow's own fetch adds one unique clone per repository per day, the monorepo's clones include CI checkouts) — placed above `# Cairn`, then the title with its bold "Mark the path from idea to shipped." lead and the subtitle replaced verbatim by "Milestone-driven development for your coding agent — any kind of work, one milestone at a time." Delete the `<p align="center">` banner paragraph together with `.github/assets/readme/cairn-banner.png` and the then-empty `.github/assets/` directory in the same commit, and remove the old `## Adoption` section from its place near the foot of the file. Verified when the rendered page opens on badges, table, and title with no banner, the file has one adoption table and no `## Adoption` heading, and nothing in the repository references the deleted image.

**Verified:**

- `README.md` no longer contains the `<p align="center">` banner paragraph — `grep -c 'cairn-banner\|alt="Cairn banner"'` returns 0 — and its first non-blank line (line 1) opens the three shields.io badges block (release, CI, MIT), which `diff` shows byte-identical to lines 4–14 of `HEAD:README.md`.
- Directly under the badge block (one blank line after `</p>`) and above `# Cairn` (line 23) sits the adoption block: the three-row pipe table at lines 13–17, `diff`-identical to lines 83–87 of `HEAD:README.md` (header `repository | unique views | unique clones`, rows for `uHappyLogic/cairn`, `cairn-claude`, `cairn-antigravity`, each label linked to its repository, each of the six badges a raw `traffic-data` SVG image linked to that branch page), with no `## Adoption` heading anywhere above it.
- The caption (line 19) is one sentence of 234 characters with a single terminal period — "Cumulative since 2026-09-04, with unique counts summed per day (a returning visitor counts again); the badge workflow's own daily fetch adds one unique clone per repository per day, and the monorepo's clones also include CI checkouts." — carrying all four facts: cumulative since 2026-09-04, unique counts summed per day so a returning visitor counts again, the workflow's own daily fetch adds one unique clone per repository per day, and the monorepo's clones include CI checkouts.
- The `---` rule stays immediately above the title block: `# Cairn`, `**Mark the path from idea to shipped.**` on its own line, then the exact line "Milestone-driven development for your coding agent — any kind of work, one milestone at a time." (1 match) in place of "Milestone-driven development for any kind of work." (0 matches), with `## Why Cairn?` following at line 28 unchanged.
- The old `## Adoption` section near the foot is gone: `## Adoption` occurs 0 times, `| repository |` occurs exactly once, and a `diff` of everything from `## Why Cairn?` onward against `HEAD:README.md` shows only the ten-line `## Adoption` section removed — the `##` order Why Cairn? → Installation → How it works → Contributing → Self-dogfooding → License is otherwise unchanged (the tail reorder belongs to a later task); `git diff --numstat` is 9 insertions and 14 deletions on `README.md`.
- `.github/assets/readme/cairn-banner.png` is deleted (`git status` shows ` D`) with its removal staged alongside the README edit so both land in one commit, and `.github/assets/` no longer exists, leaving `.github/` with exactly `ISSUE_TEMPLATE/`, `PULL_REQUEST_TEMPLATE.md`, and `workflows/`.
- Nothing in the repository references the deleted image: a grep for `cairn-banner` and `.github/assets` excluding `.git`, `temp/`, and `milestones/` returns no match, and inside `milestones/` the path never appears inside a link or `src=` — it survives only as prose in milestone 24's and 26's starting-state, decision, and task text.
- GitHub's markdown API (`gh api markdown`, gfm mode) renders the page as the badge `<p align="center">`, then `<table>` (four `<tr>`, six `traffic-data` badge images), then the caption `<p>`, then `<hr>`, then `<h1>Cairn</h1>`, with no banner `<img>` anywhere and the `<h2>` sequence Why Cairn?, Installation, How it works, Contributing, Self-dogfooding, License.
- `uv run scripts/build_hosts.py --check` exits 0 ("Check passed … at version 1.5.1") and `grep -P '(?<!\$)\{\{' README.md` finds nothing.

---

## Add Simplified Milestone Loop Diagram

Insert one mermaid `flowchart` directly under the title block of `README.md`, before `## Installation`, drawing the six verbs define, review, recommend, answer, derive, complete chained by plain arrows plus one dashed return edge from answer back to review labelled with the convergence condition (until no open questions remain), with no finish step and no milestone-return edge, under the same `%%{init: …}%%` theme block the six phase diagrams in `docs/workflow.md` carry so the wrappingWidth keeps the return label on one line. Verified when the diagram renders on GitHub with exactly those six nodes and seven edges, its theme block is byte-identical to the phase diagrams', and the file gains no `{{` sequence.

**Verified:**

- `README.md` carries exactly one mermaid fence (one ```` ```mermaid ```` line), at lines 28–47, inserted directly under the title block — one blank line after the one-liner at line 26 and one blank line before `## Why Cairn?` at line 49 — so it sits before `## Installation` (line 57); `git diff --numstat` is 21 insertions and 0 deletions on `README.md`, and nothing else in the file changed (the `## Why Cairn?` seat is the reorder task's, whose own verification places it after Installation).
- The fence's first line is the `%%{init: …}%%` theme block, byte-identical to the six phase diagrams' theme line in `docs/workflow.md` — md5 `c21c74ddf202de57bb26dc0a9fba6417` for the README line and for the docs' six lines alike.
- The diagram is a `flowchart LR` declaring exactly six nodes — `define`, `review`, `recommend`, `answer`, `derive`, `complete` — and no finish node; its three `classDef` lines (the init, req, and auto colours copied from the phase diagrams, so each verb carries the colour of the phase it expands into) add no node.
- The six verbs are chained by five plain `-->` arrows in the order define → review → recommend → answer → derive → complete, plus exactly one dashed `-.->` edge from `answer` back to `review` labelled `until no open questions remain`; no edge leaves `complete` and none returns to `define` (no milestone-return edge). That is six edges — five chain plus one return — exactly the set the decision enumerates; the brief's "seven" over-counts its own enumeration by one (six verbs chained give five arrows, not six), as the docs task's "25 entries" was a miscount.
- The diagram renders on GitHub: `gh api markdown` (gfm, context `uHappyLogic/cairn`) emits `<h1>Cairn</h1>`, then the `<pre lang="mermaid" aria-label="Raw mermaid code">` element GitHub hands its client-side renderer with the diagram text intact (arrows entity-escaped, the label and `-.->` preserved), then `<h2>Why Cairn?</h2>` and `<h2>Installation</h2>`; and real mermaid (mermaid.ink) renders the fence body to a 991×129 SVG/PNG with six node labels, six edge paths (`L_define_review`, `L_review_recommend`, `L_recommend_answer`, `L_answer_derive`, `L_derive_complete`, `L_answer_review`), the return edge in the `edge-pattern-dotted` class, and the label "until no open questions remain" as one unwrapped line.
- `README.md` gains no `{{` sequence: `grep -c '{{' README.md` is 0 and `grep -P '(?<!\$)\{\{'` finds nothing; `uv run scripts/build_hosts.py --check` exits 0 ("Check passed … at version 1.5.1").

---

## Demote Migration Note And Reorder Tail Sections

Move the "Already installed from `uHappyLogic/cairn`?" marketplace-migration paragraph and its second command block out of `## Installation` into a short `##` section of its own placed between `## Contributing` and `## License`, leaving one bold pointer line to it in the `### Claude Code` subsection, so Installation keeps only the two per-host subsections and `### Bootstrap your project`; order the remaining tail as `## How it works`, `## Self-dogfooding`, `## Contributing`, the migration section, `## License`, and add one closing sentence to `## How it works` linking `docs/workflow.md` and `docs/skill-reference.md`, changing no other section text. Verified when the section order reads Installation, Why Cairn, How it works, Self-dogfooding, Contributing, migration, License, the Claude Code subsection matches the root's install commands plus the pointer line, and the migration commands appear once, below the fold.

**Verified:**

- `## Installation` holds exactly three `###` subsections — `### Claude Code`, `### Antigravity`, `### Bootstrap your project` — and the "Already installed from `uHappyLogic/cairn`?" paragraph and its `/plugin marketplace remove cairn` command block no longer appear anywhere inside it (both greps over the section return 0).
- `### Claude Code` is the intro sentence "In Claude Code, add the `cairn-claude` marketplace and install the plugin from it:" and the two-line install block, byte-identical (`diff` empty) to lines 61–66 of `HEAD:README.md` — the same install commands the Claude template mirrors — followed by exactly one bold pointer line, "**Already installed from `uHappyLogic/cairn`?** That install keeps working — see [Migrating from the old marketplace](#migrating-from-the-old-marketplace) to switch to this source.", and nothing else.
- The migration paragraph and its three-line command block sit verbatim (`diff`-identical to lines 68–75 of `HEAD:README.md`) under a new short section `## Migrating from the old marketplace` at line 116, between `## Contributing` (line 107) and `## License` (line 126); `/plugin marketplace remove cairn` occurs exactly once in the file, at line 121, below the fold.
- `grep '^## ' README.md` reads exactly Installation, Why Cairn?, How it works, Self-dogfooding, Contributing, Migrating from the old marketplace, License — `## Why Cairn?` (heading and three paragraphs byte-identical to HEAD) now sits after `## Installation`, the seat the two earlier tasks left to this one, and `## Self-dogfooding` moved up one seat ahead of `## Contributing`.
- `## How it works` keeps its three-file paragraph and `milestones/README.md` paragraph byte-identical to lines 99–105 of `HEAD:README.md` and gains one closing sentence as its final paragraph — "How the skills move a milestone through those files is described in [docs/workflow.md](docs/workflow.md), and what each skill does is in [docs/skill-reference.md](docs/skill-reference.md)." — whose two relative links resolve to existing files.
- No other section text changed: the header, adoption table, caption, title block, and diagram (lines 1–48), the Antigravity and Bootstrap subsections, Why Cairn?, Self-dogfooding, Contributing, the moved migration paragraph and block, and License are each `diff`-identical to their `HEAD:README.md` ranges; the only lines absent from HEAD are the pointer line, the closing sentence, and the new `##` heading, and `git diff --numstat` is 23 insertions and 17 deletions (128 lines, up from 122 by those three lines and their blank lines).
- `uv run scripts/build_hosts.py --check` exits 0 ("Check passed … at version 1.5.1"), `grep -P '(?<!\$)\{\{' README.md` finds nothing, GitHub's markdown API (gfm, context `uHappyLogic/cairn`) renders the `<h2>` sequence Installation, Why Cairn?, How it works, Self-dogfooding, Contributing, Migrating from the old marketplace, License with the pointer's `href="#migrating-from-the-old-marketplace"` equal to the slug GitHub derives from the heading text, and a script resolving every relative link in `README.md` against existing files and headings reports 0 unresolved.

---

## Neutralize Why Cairn And Add Design Principles

Change the one host sentence in `## Why Cairn?` from "Cairn gives Claude Code a structured, repeatable process" to "Cairn gives your coding agent a structured, repeatable process", keeping its heading and three paragraphs otherwise intact, and add a short `## Design principles` section directly beneath it presenting three entries — claim 11 (records are machine-readable), claim 2 carrying claim 12's provenance subject (each decision has a record in git), and claim 5 (advice gets better with each milestone) — each the claim line verbatim as it heads `docs/design-claims.md` followed by one sentence of the design behind it in the README's voice, closing with one link to that page for all nineteen. Verified when the block is about a hundred words, each verbatim claim line matches a heading on the claims page, and `README.md` no longer names Claude Code as the host outside the Installation section.

**Verified:**

- `## Why Cairn?`'s second paragraph now opens "Cairn gives your coding agent a structured, repeatable process" (1 hit) and "Cairn gives Claude Code" no longer appears (0 hits); the section's heading and its three paragraphs are otherwise unchanged — `git diff README.md` shows that one substituted line as its only deletion.
- `## Design principles` (line 91) is the next `##` heading after `## Why Cairn?` (line 83) and sits ahead of `## How it works` (line 99).
- The section presents exactly three entries in the order claim 11, claim 2, claim 5, each opening with its claim line verbatim — "The records are machine-readable.", "Each decision has a record in git.", "Advice gets better with each milestone." — and each of those lines grep-matches a `###` heading in `docs/design-claims.md` (lines 100, 20, and 45).
- Each claim line is followed by exactly one sentence of its design in the README's voice (sentence count 1 per entry), and the claim-2 sentence carries claim 12's provenance subject: "one path-scoped commit whose subject marks how it was made — manual, recommendation, or alternative".
- The block closes with one page-level link to `docs/design-claims.md` for all nineteen claims; that link and the three per-entry heading links (`#11-the-records-are-machine-readable`, `#2-each-decision-has-a-record-in-git`, `#5-advice-gets-better-with-each-milestone`) each resolve to an existing file and a heading slug on that page.
- The block, from its `## Design principles` heading through the line before `## How it works`, is 118 words by `wc -w` (114 with markup stripped), within the about-a-hundred bar.
- `grep -n "Claude Code" README.md` hits only lines 51 and 53, both inside `## Installation` (lines 49–82); no line outside that section names Claude Code as the host.
- `git status --short` lists only `README.md`, and `git diff --stat` is 9 insertions and 1 deletion — the substituted sentence plus the eight inserted block lines — with no other section text changed.
- `uv run scripts/build_hosts.py --check` exits 0 ("Check passed … at version 1.5.1").

---

## Rework Distribution README Templates And Rebuild Hosts

Rework `scripts/hosts/claude/README.md` and `scripts/hosts/antigravity/README.md` to the same shape: a centred two-badge strip of that repository's `traffic-data` unique-views and unique-clones badges above `# Cairn for <Host>` with no caption and no heading, the one-liner "Milestone-driven development for your coding agent — any kind of work, one milestone at a time." verbatim under the title, the generated-do-not-edit note with its `traffic-data` sentence reworded to say the SVGs now serve this page too and gaining one clause that the workflow's own daily fetch counts as one unique clone a day in the clones badge, the host's installation section (the Claude one split exactly as the root's, its install subsection word for word identical and the migration note in its own section reached by the same pointer line), over the existing `## Source` and `## License` tail, with no loop diagram, no "Why Cairn?" text, and no design-principles block; then run `uv run scripts/build_hosts.py` and commit the rebuilt `hosts/` trees with the templates. Verified when `uv run scripts/build_hosts.py --check` exits zero and both rendered `hosts/<host>/README.md` files carry exactly those items with no `{{` left outside a `${{` expression.

**Verified:**

- Both templates, and both rendered `hosts/<host>/README.md` files, open at line 1 with a `<p align="center">` strip (closing `</p>` at line 8) holding exactly two badge images — `alt="unique views"` on `raw.githubusercontent.com/uHappyLogic/cairn-<host>/traffic-data/views-unique.svg` and `alt="unique clones"` on `…/clones-unique.svg`, the only two raw refs in the file — each wrapped in a link to `https://github.com/uHappyLogic/cairn-<host>/tree/traffic-data` (2 such hrefs), with only a blank line 9 between the strip and `# Cairn for <Host>` at line 10, no `## Adoption` heading (0 hits), and no caption (`Cumulative since` 0 hits); GitHub's markdown API (gfm, context `uHappyLogic/cairn-claude`) renders one `<p align="center">` whose two `<img>` alts are `unique views`, `unique clones` ahead of `<h1>Cairn for Claude Code</h1>`.
- Line 12 of each rendered file — the first paragraph under the title — is exactly "Milestone-driven development for your coding agent — any kind of work, one milestone at a time." (1 whole-line match each), the same line as root `README.md` line 26; the opening paragraph that follows keeps its `[Cairn](https://github.com/uHappyLogic/cairn)` root link and its what-this-repository-carries sentence, with the forty-word "clarify a goal, resolve every open question, derive an ordered task list, complete the tasks, and close out the milestone before moving on" clause (the "Why Cairn?" sentence the one-liner takes over from) removed.
- The generated note's `traffic-data` sentence now reads that the workflow "writes its per-day data file and badge SVGs there daily for the two badges above this page's title as well as for the adoption table in the root repository's README, and its own daily fetch of that branch counts as one unique clone a day in the clones badge" (1 hit each; old wording "daily for the adoption table" 0 hits); its first sentence (rendered from `uHappyLogic/cairn`, overwritten by the next release) and third sentence (issues disabled, redirect to `uHappyLogic/cairn`) are unchanged (1 exact hit each), and a `diff` of the blockquote line between the two templates is empty.
- `hosts/claude/README.md`'s `## Installation` holds exactly `### Claude Code` and `### Bootstrap your project`; its `### Claude Code` subsection (11 lines: intro sentence, the two-command block, and the bold pointer line to `#migrating-from-the-old-marketplace`) is `diff`-identical to the root `README.md`'s, and the migration paragraph plus its three-command block sit in their own `## Migrating from the old marketplace` section (10 lines, `diff`-identical to the root's) between `## Installation` and `## Source`, with the pointer line occurring once and `/plugin marketplace remove cairn` once, at line 46; the `### Bootstrap your project` subsection is `diff`-identical to the root's on both hosts.
- `hosts/antigravity/README.md`'s `## Installation` (`### Antigravity` + `### Bootstrap your project`) is `diff`-identical to the HEAD template's Installation section and its `### Antigravity` subsection `diff`-identical to the root's; the file mentions no migration (0 hits).
- The `## Source` (with its three `{{VERSION}}` slots) and `## License` sections of both templates are `diff`-identical to `HEAD`, and each file's `##` order is Installation → (Claude only: Migrating from the old marketplace →) Source → License, which GitHub's renderer reports as the `<h2>` sequence Installation, Migrating from the old marketplace, Source, License.
- Neither rendered file contains `mermaid`, `flowchart`, `Why Cairn`, any "Why Cairn?" sentence (`predictable ways`, `structured, repeatable`, `clarify a goal, resolve every open question`), `Design principles`, or `design-claims` (0 hits each).
- `uv run scripts/build_hosts.py` exited 0 ("Built hosts/antigravity/ (36 files), hosts/claude/ (37 files) … at version 1.5.1"), rewriting only `hosts/claude/README.md` and `hosts/antigravity/README.md`; `uv run scripts/build_hosts.py --check` then exited 0 ("Check passed … at version 1.5.1").
- In both rendered files `grep -P '(?<!\$)\{\{'` finds nothing, `{{VERSION}}` occurs 0 times and the literal `1.5.1` 3 times, and each rendered file is byte-identical to its template with `{{VERSION}}` substituted; `git status --short` lists exactly the two templates and the two rendered READMEs (74 insertions, 22 deletions).

---
