# Milestone 25: Traffic Adoption Badges

## Goal

Publish adoption evidence in README.md as a table of albertoarena/github-traffic-badge badges — one row per repository (uHappyLogic/cairn, cairn-claude, cairn-antigravity), two columns (unique views, unique clones), and a caption stating the counts are cumulative from the workflows' first run and that the monorepo's clones include CI checkouts. Each repository runs a daily scheduled workflow (the action pinned to a full commit SHA, one step per metric writing a distinct SVG to the traffic-data branch): the monorepo's under .github/workflows/, each distribution repository's rendered from a scripts/hosts/<host>/ template into its hosts/<host>/ tree so releases publish it. The milestone stores the fine-grained PAT at temp/PAT as the TRAFFIC_TOKEN secret on all three repositories with gh secret set, seeds every traffic-data branch once via workflow_dispatch before the README references its badges, and verifies that the action's daily push keeps the distribution repositories' cron workflows from GitHub's 60-day inactivity disable.

## Relevant starting state

### README badge block

`README.md` opens with the banner image and one centred `<p>` of three shields.io badges — release (`github/v/release`), `ci` (reading `drift-gate.yml`'s status on `main`), and license — all `style=flat`, each wrapped in a link. The rest of the file is plain Markdown under `# Cairn` (`## Why Cairn?`, `## Installation`, `## How it works`, `## Workflow pipeline`, `## How skills commit`, `## Skill reference`, `## Contributing`, `## Self-dogfooding`, `## License`); it contains no Markdown table and no mention of traffic, adoption, or usage figures anywhere. The monorepo README is hand-authored; only the distribution READMEs are built from templates.

### Monorepo GitHub Actions

`.github/workflows/drift-gate.yml` is the only workflow: `on: [push, pull_request]` with no branch or path filter, `permissions: contents: read`, and its two actions pinned to full commit SHAs each followed by a `# vX.Y.Z` comment bumped by hand (no Dependabot); every run performs an `actions/checkout`. Repository settings: Actions enabled, all actions allowed, `sha_pinning_required` off, default `GITHUB_TOKEN` permissions read-only. The repository has a single branch, `main`, and no repository secrets.

### Distribution repositories

`uHappyLogic/cairn-claude` and `uHappyLogic/cairn-antigravity` each hold exactly one commit — the root commit `Release: 1.5.0` of 2026-09-17, tagged `1.5.0` — on their only branch `main`, with no workflows, no secrets, and issues, Discussions, wiki, and projects disabled; Actions is enabled on both with all actions allowed and read-only default token permissions. They change only when `/release-plugin` publishes: `git commit-tree` over the monorepo's `HEAD:hosts/<host>` tree object, pushed over SSH (`git@github.com:uHappyLogic/cairn-<host>.git`) atomically to `main` and the version tag, so between releases they receive no pushes at all. Nothing in the project records whether a workflow's own pushes count as repository activity for GitHub's 60-day scheduled-workflow disable.

### Host build and templates

Every file under `scripts/hosts/<host>/` other than `settings.toml` is a template rendered to the same relative path in `hosts/<host>/` (the walk is recursive and hidden directories render — `scripts/hosts/claude/.claude-plugin/` already does), substituting only the `{{VERSION}}` and `{{NAME}}` slots; `prose_drop_patterns`, `strip_frontmatter_keys`, and `exclude` apply to `core/` files only, and there is no per-template escape or opt-out. The validation gate's `unfilled-placeholder` check fails the whole build on any line containing `{{` in any rendered text file, templates included, so a workflow file using GitHub's `${{ … }}` expression syntax cannot pass the build as it stands. `--check` compares the render byte-for-byte with the committed trees and runs in CI on every push and pull request. Whatever the tree carries reaches consumers whole: the release publishes it verbatim, Claude Code marketplace installs clone the distribution repository, and the Antigravity install extracts the full `main` tarball into `.agents/plugins/cairn/`.

### The github-traffic-badge action

`albertoarena/github-traffic-badge` is a composite action (MIT, zero runtime dependencies, Node 20+, run with `node` on the runner) whose latest release is `v1.1.4` (2026-06-17) at commit `56f6f3e0ed586f14440561758b197ca57a38f480`, which the `v1` tag also points at; the default branch carries later unreleased commits (HEAD `63470f9`, 2026-07-05). Inputs: `token` (required — a PAT; the default `GITHUB_TOKEN` gets 403 on the Traffic API), `metric` (`views` | `clones` | `views-unique` | `clones-unique`), `label`, `color`, `style`, `output` (default `badge.svg`), `branch` (default `traffic-data`), `commit-message`, `base`, `abbreviated`, `lowercase`, `font-size`; outputs `total` and `badge-path`. Each run re-clones the data branch (orphan on first run) into a fresh temp directory over HTTPS using the same `token`, fetches both `/traffic/views` and `/traffic/clones` (the API's 14-day window), upserts both into one shared date-keyed `totals.json`, renders the one requested metric to `<output>`, then commits as `github-actions[bot]` and pushes with that token — so multiple steps with distinct `output` names share one `totals.json` and each step sees the previous step's push. A unique-metric total is the sum of per-day `uniques` over every day the branch has recorded, beginning with the 14 days the first run captured. The badge URL form is `https://raw.githubusercontent.com/<owner>/<repo>/traffic-data/<output>`.

### Token, secrets, and current traffic

`temp/` is gitignored; `temp/PAT` holds a 93-byte fine-grained token (`github_pat_` prefix, no trailing newline) that authenticates as `uHappyLogic`, for which the API returns no `github-authentication-token-expiration` header. It reads `/traffic/views` and `/traffic/clones` on all three repositories (HTTP 200), but a dry-run HTTPS push with it is refused on all three (`403`, permission denied), so as stored it lacks the contents-write access the action's push step uses. `gh` is logged in as `uHappyLogic` (scopes `gist`, `read:org`, `repo`; git protocol ssh), and no `TRAFFIC_TOKEN` — or any other — secret exists on any of the three repositories. The Traffic API's current 14-day figures: `cairn` 56 views / 9 unique, 398 clones / 112 unique; `cairn-claude` 4 / 2 views, 11 / 9 clones; `cairn-antigravity` 8 / 2 views, 6 / 4 clones — no data older than 14 days is retrievable.

## Decisions

### Host build and templates

The validation gate's `unfilled-placeholder` check tests for a `{{` not immediately preceded by `$` (the one-character negative lookbehind `(?<!\$)\{\{`) instead of any `{{`, so GitHub Actions `${{ … }}` expressions pass in any rendered file of any host while every slot of the plugin's own bare `{{UPPER}}` token family — `{{VERSION}}`, `{{NAME}}`, `{{PLUGIN_ROOT}}` — still fails the build. The gate stays one uniform check over every file: no path-scoped exemption, template escape sequence, or exempt-templates definition key is added, nothing in any `settings.toml` or template changes, and a workflow template under `scripts/hosts/<host>/` is the literal file GitHub will run (valid YAML, lintable, copy-pasteable). The build script's docstring and the CLAUDE.md clauses stating no `{{` anywhere are reworded to no `{{` outside a `${{` expression.

### Token and secrets

The stored fine-grained token is extended in place rather than replaced: on its GitHub settings page, Contents: Read and write is added to its repository permissions for the three repositories, keeping everything already verified — user-owned, scoped to exactly `uHappyLogic/cairn`, `cairn-claude`, and `cairn-antigravity`, Administration: read, no expiration — so the 93-byte value at `temp/PAT` stays as it is and is the value stored as `TRAFFIC_TOKEN`. A second HTTPS push dry-run against all three repositories confirming the grant is the proof, so the secret the milestone stores is exactly the token whose access was verified; no replacement token is minted.

Once `TRAFFIC_TOKEN` is set on all three repositories, `temp/PAT` is deleted rather than kept: the task that runs `gh secret set` confirms with `gh secret list` that each repository carries the secret, then removes the file (verified by `test ! -e temp/PAT`) and records the deletion in its `TASKS_DONE.md` entry, since `temp/` is gitignored and nothing in git would otherwise record it. From then on the token exists only in GitHub's write-only secret store, and its recovery path is the Regenerate token button on the token's own settings page — a fresh value set on all three repositories together — so no local copy, keychain item, or other second store is kept for a re-set nothing in the milestone anticipates.

## Out of Scope

## Open questions

<open-question id="Distribution seeding sequence">
  <question>Each distribution repository receives its workflow only when a release publishes hosts/&lt;host&gt;/, yet every traffic-data branch must be seeded before README.md references its badge: does the milestone cut a release to publish the workflows (and at which version), push the workflows to the distribution repositories once by hand, or hold the README table until after the next regular release?</question>
  <alternative id="Mid-milestone patch release">
    Commit every piece that changes hosts/ first — the two workflow templates, the build-gate change they need, any distribution README rewording — rebuild, and have the maintainer run /release-plugin 1.5.1 between tasks: with no Milestone-finish since 1.5.0 the skill takes its empty-range path (commit-derived notes behind one explicit confirmation), publishes each hosts/&lt;host&gt;/ tree as a Release: 1.5.1 commit, and the milestone then continues with the secrets, one workflow_dispatch per repository, the README table, and the inactivity evidence.
    <advantage>The workflow reaches each distribution repository by the only route its contract allows, so every claim about those repositories — never hand-edited, main advances only by release snapshots, the tree built from the tag its README names — stays true; the milestone delivers its whole goal with the seeding verified inside it; the release skill already carries this path by name (a deliberate version-only patch release, with 1.0.1 as the precedent for tooling-only content); and the distribution workflows begin their daily pushes — the clock the inactivity question needs — days before a post-finish release could start them.</advantage>
    <drawback>The first release cut inside a milestone (every tag from 0.9.4 on names a milestone finish or the Release commit right after one), a maintainer act with two confirmations no dispatched task can give, so the task list splits around it; and it splits the record: consumers see a 1.5.1 that changes nothing they can use, its notes are a commit-derived Changes since 1.5.0 section rather than a milestone section, and the milestone&apos;s own section lands in whichever release follows the finish — one that ships a host tree differing from 1.5.1 only in its version slots unless it rides with the next milestone.</drawback>
  </alternative>
  <alternative id="One-off hand push">
    Push the current hosts/&lt;host&gt;/ tree to each distribution main once outside any release — reusing the release plumbing (git commit-tree over HEAD:hosts/&lt;host&gt; with distribution main as parent, pushed to refs/heads/main alone, no tag) so the tree is genuine build output — then dispatch each workflow and commit the table, leaving the next regular release to parent on that commit.
    <advantage>The cheapest route to a seeded branch: no version bump for a change no consumer can act on, no second set of release notes, no maintainer gate in the task list (a dispatched task can run the push over the same SSH remote the release uses), and the next release is undisturbed, since its publish step fetches distribution main as the parent and never inspects what that commit carries.</advantage>
    <drawback>It makes the distribution repositories misdescribe themselves for as long as it stands: their README and the monorepo Installation section say main advances only by release snapshots and the main archive is always the latest release, CLAUDE.md says they are never hand-edited, and the pushed tree&apos;s own README says it was built from release tag 1.5.0 while carrying a file 1.5.0 never had — yet both install paths fetch main, so every install made before the next release carries a tree no tag names; the wording question would have to carve out the exception, and the one-off is the precedent that a release is not the only way in.</drawback>
  </alternative>
  <alternative id="Hold until finish release">
    Land everything the monorepo owns now — the monorepo workflow, seeded at once since it runs from main the moment it is committed, both templates, the secrets — and hold the README table, or at least its two distribution rows, until the release that follows the milestone finish has published the workflows and each has been dispatched once, adding those rows as follow-up work outside the milestone.
    <advantage>No extra release and no hand push: the cadence stays one release per finish, the release notes stay whole, and every claim the distribution repositories make about themselves stays exactly true throughout.</advantage>
    <drawback>The milestone cannot deliver its own goal — the three-row table, the seeding of every branch, and the verification that the daily push keeps the distribution workflows alive all wait on a release that by definition comes after the finish — so the rows become untracked post-milestone work or a second milestone for two table rows, the seed-before-reference rule holds only if someone remembers the follow-up, and the 60-day evidence clock starts later than it needs to.</drawback>
  </alternative>
  <recommendation option="Mid-milestone patch release">The distribution repositories have exactly one documented way to advance and the release skill already provides it for a range with no finished milestone, so a 1.5.1 cut once every hosts/-changing task has landed seeds all three branches and lands the table inside the milestone without falsifying a single claim the distribution repositories or the monorepo README make; the patch number is right because nothing in the plugin runtime changes for a consumer, matching the 1.0.1 precedent and the skill&apos;s own name for that path; holding the table is the runner-up, and what breaks the tie is that it moves the goal&apos;s core deliverable — three seeded rows — outside the milestone that exists to deliver it, while the hand push saves a version bump at the price of every install carrying a tree no tag names.</recommendation>
</open-question>
<open-question id="Workflow copy duplication">
  <question>With the monorepo workflow hand-authored under .github/workflows/ and each distribution workflow rendered from its own scripts/hosts/&lt;host&gt;/ template, the same workflow exists in three copies: is that duplication accepted, or does the build gain a shared template rendered into every host tree?</question>
  <alternative id="Accept three copies">
    The monorepo workflow is hand-authored beside drift-gate.yml under .github/workflows/ and each definition directory carries its own scripts/hosts/&lt;host&gt;/.github/workflows/ template, the three kept in step by hand exactly as the two CONTRIBUTING.md pointers and the README boilerplate already are.
    <advantage>Nothing changes in the build, its docstring, either settings.toml, CONTRIBUTING.md, or CLAUDE.md — the one template rule (every file beside settings.toml renders to the same relative path) already carries it, each definition directory stays the complete picture of what its host ships beyond core/ and LICENSE, and every copy is free to differ where it must: the distribution copies carry the inert-copy header naming their own repository while the monorepo copy carries none, and a per-repository cron offset is an ordinary edit.</advantage>
    <drawback>Three ~20-line files share one body that nothing verifies — a hand-bumped action SHA, a badge parameter, or the permissions block must be edited three times, --check compares each template only with its own rendered tree, and a copy missed in an edit silently keeps the old pin until someone greps for github-traffic-badge@ across all three.</drawback>
  </alternative>
  <alternative id="Shared template source">
    The build gains a host-neutral template source — a new directory such as scripts/templates/ rendered into every host tree alongside the per-host templates, or, with no Python at all, a core/ directory mapped by [layout] to .github — from which both distribution workflows render, while the monorepo copy stays hand-authored under .github/workflows/.
    <advantage>One file serves every distribution repository — an action SHA bump or a parameter change is edited once for the hosts, the render&apos;s existing same-path collision check already refuses a per-host template that would shadow it, and a future host gets its workflow for free.</advantage>
    <drawback>A third kind of source (neither core/ runtime nor per-host template) for a single ~20-line file: a new location that scripts/hosts/ discovery would otherwise load as a host, widened wording in the build docstring, both settings.toml headers, CONTRIBUTING.md, the two CLAUDE.md contract sentences, and the goal&apos;s own clause placing each distribution workflow under scripts/hosts/&lt;host&gt;/ — or, on the core/ route, repository plumbing misfiled as plugin runtime content that the host-name guard, {{PLUGIN_ROOT}} substitution, and prose_drop_patterns all process; either way the copies fall from three to two, since the monorepo copy is still written by hand, and with no {{HOST}} slot the shared file cannot name the repository its inert-copy header is meant to name.</drawback>
  </alternative>
  <alternative id="Monorepo workflow copied">
    The build copies the monorepo&apos;s own .github/workflows/ traffic workflow verbatim into every host tree the way it already copies the root LICENSE, so one hand-authored file is the workflow all three repositories run.
    <advantage>Zero duplication by construction — the action reads its repository from the run context, so one body genuinely fits all three, the LICENSE copy is an exact precedent, and a SHA bump is a single edit that --check then forces into the host trees.</advantage>
    <drawback>It hard-codes a .github/workflows/ file name in Python that today names no path a host tree must carry, makes the monorepo&apos;s live CI configuration a build input whose every edit fails --check until hosts/ is rebuilt, gives a host no exclude for a non-core file, and forces byte-identical copies — the distribution copies cannot carry the inert-copy header naming their repository without the monorepo copy carrying the same false claim, and no per-repository cron offset is possible.</drawback>
  </alternative>
  <depends-on question="Workflow in installed trees" option="Accept inert file"/>
  <recommendation option="Accept three copies">The duplicated unit is a ~20-line file whose three copies are meant to differ at the edges — the distribution copies carry the inert-copy header naming their own repository and the monorepo copy, beside drift-gate.yml, carries none — and the build already treats exactly this shape (the two 10-line CONTRIBUTING.md pointers, the identical README boilerplate) as one template per definition directory with no shared source, so a shared template would add a third kind of source to the build and five documents to remove one copy, since the monorepo&apos;s workflow stays hand-authored under .github/workflows/ whatever is decided; the runner-up is the shared template source, and what breaks the tie is that it halves the duplication rather than removing it — the residual pair still needs the same lockstep edit, so the mechanism buys half the benefit at the full conceptual cost — while the copied-monorepo-file route that would remove it entirely forecloses the per-copy header and hard-codes a GitHub path the build otherwise never names.</recommendation>
</open-question>
<open-question id="Workflow name and cron time">
  <question>What file name, workflow name, and daily cron time (UTC) does the traffic workflow use, and do the three repositories run at the same time or staggered?</question>
  <alternative id="Upstream quick start">
    The file is .github/workflows/github-traffic-badge.yml with name: Traffic Badge and cron 0 3 * * * (03:00 UTC), exactly as the action&apos;s README, its quick-start page, and its own repository workflow prescribe, with the same three lines in every repository.
    <advantage>It matches the upstream documentation line for line — a maintainer checking any copy against the action&apos;s quick start finds nothing to explain, and the file name tells a reader which action powers the badges before they open it.</advantage>
    <drawback>The names describe the mechanism rather than the function (the third-party action, in the singular, for a workflow that publishes two badges), break the monorepo&apos;s one precedent of a lowercase file stem reused as the workflow name (drift-gate.yml, name: drift-gate), and the minute sits on the top of the hour that GitHub&apos;s own documentation names as its high-load time, when scheduled runs are delayed and queued jobs can be dropped outright.</drawback>
  </alternative>
  <alternative id="Project names, shared minute">
    The file is .github/workflows/traffic-badges.yml with name: traffic-badges, mirroring drift-gate.yml, and the one line cron: 17 3 * * * (daily at 03:17 UTC, commented as such) is identical in the monorepo workflow and both distribution templates.
    <advantage>The stem names what the workflow publishes and pairs with the traffic-data branch it writes, it follows the monorepo&apos;s existing name-equals-stem convention and stays a stable URL component (the README already builds its ci link and status badge from drift-gate.yml, and the table-links question may want a runs link the same way), the hour is the action&apos;s documented one shifted seventeen minutes off the boundary GitHub says to avoid — landing at 05:17 on the maintainer&apos;s CEST clock, before the day starts — and one line to remember means the three copies differ in nothing this question controls.</advantage>
    <drawback>It departs from the action&apos;s quick start, so a reader comparing a copy with the upstream README must map two names and a minute, and the three runs queue in the same minute, so a scheduling delay at 03:17 UTC affects all three rows the same morning.</drawback>
  </alternative>
  <alternative id="Project names, staggered">
    The same names, but each repository gets its own minute — the monorepo at 17 3 * * *, cairn-claude at 27 3 * * *, cairn-antigravity at 37 3 * * * — so no two runs start together.
    <advantage>No two of the account&apos;s runs share a start minute, so a transient delay or drop in one slot cannot stall all three badges the same morning, and each repository&apos;s traffic-data commits carry a distinct time of day.</advantage>
    <drawback>It isolates runs that share nothing — each pushes its own repository&apos;s traffic-data branch, and the token&apos;s 5,000-requests-per-hour budget dwarfs the four Traffic API calls a two-step run makes — at the price of a second deliberate difference between three ~20-line files the duplication question keeps in step by hand, where a missed edit already carries a stale pin silently.</drawback>
  </alternative>
  <alternative id="Just past midnight">
    The project names with cron 7 0 * * * (00:07 UTC), the first off-hour minute after the Traffic API&apos;s UTC day boundary, identical in all three copies.
    <advantage>The run lands minutes after the day the API reports has closed, so the badge shows yesterday complete as early as any schedule can and the run date matches the last full day in totals.json.</advantage>
    <drawback>The minutes after 00:00 UTC are where the backlog of every daily-at-midnight workflow is still draining, so a 00:07 slot inherits the delay the documentation warns of at the top of the hour, and the freshness it buys is invisible: the action upserts each of the 14 days it fetches on every run, so a day captured at 00:07 and a day captured at 03:17 are the same number on a cumulative badge by the next morning.</drawback>
  </alternative>
  <applied-principle>Name by distinctive function</applied-principle>
  <depends-on question="Workflow copy duplication" option="Accept three copies"/>
  <recommendation option="Project names, shared minute">traffic-badges.yml with name: traffic-badges follows the monorepo&apos;s one workflow (drift-gate.yml, name: drift-gate — a lowercase stem naming the function, reused as the URL component the README links from) and names the two badges the workflow publishes rather than the action that renders them; 17 3 * * * keeps the action&apos;s documented 03:00 UTC hour but leaves the top-of-the-hour minute GitHub documents as delay- and drop-prone, arriving at 05:17 CEST before the day starts; and the same line in all three copies, because the runs share nothing — each pushes its own traffic-data branch and the token&apos;s hourly budget dwarfs four API calls — so staggering would add a second hand-kept per-copy difference for nothing; the upstream-verbatim form is the runner-up, and what breaks the tie is that its names describe the mechanism and its minute is the one GitHub tells you to avoid.</recommendation>
</open-question>
<open-question id="Workflow permissions block">
  <question>The action pushes with the PAT rather than GITHUB_TOKEN, yet its README prescribes permissions contents: write: does the traffic workflow declare contents: read as drift-gate does, or contents: write as the action documents?</question>
  <alternative id="Contents read">
    The traffic workflow opens with the top-level block drift-gate.yml carries — permissions: contents: read — in each of its copies.
    <advantage>The block is byte-identical to the one workflow the monorepo already runs and to the read-only default token setting all three repositories carry, so every workflow in the project shows one familiar least-privilege form, and a step added later that checks the repository out (what drift-gate&apos;s read serves) would already be covered.</advantage>
    <drawback>The read is never exercised: the pinned v1.1.4 action builds its remote from the token input (https://x-access-token:${TOKEN}@github.com/&lt;owner&gt;/&lt;repo&gt;.git) for its ls-remote, fetch, and push, reads the Traffic API with that same PAT, and the runner fetches the composite action itself, so the job token does nothing — the block repeats a neighbour&apos;s grant instead of stating this workflow&apos;s need, the same copying that left the write in the action&apos;s README, and on a public repository it is indistinguishable in effect from granting nothing.</drawback>
  </alternative>
  <alternative id="Contents write">
    The traffic workflow declares permissions: contents: write with the README&apos;s comment that it is required to push the badge, as the action&apos;s quick start prescribes at both the pinned release and the default-branch HEAD.
    <advantage>It matches the upstream quick start line for line, so a maintainer checking the file against the action&apos;s documentation finds nothing to explain, and it would keep working were a later pin bump to bring back the action&apos;s first form, whose token input defaulted to github.token and so pushed with the job token.</advantage>
    <drawback>It grants the job token a write nothing uses: the README line dates from the action&apos;s first day (2026-06-02), when token defaulted to github.token, and survived unchanged the same-day fix that made a PAT required because GITHUB_TOKEN gets 403 on the Traffic API — src/index.js reads INPUT_TOKEN alone — so the file would carry a false comment, lift the job token above the read-only default all three repositories carry, and hand a third-party action a second write credential beside the PAT it already holds, in the one workflow each public distribution repository shows its readers and against drift-gate&apos;s posture, for no function.</drawback>
  </alternative>
  <alternative id="Empty permissions">
    The traffic workflow declares permissions: {} — GitHub&apos;s documented syntax for disabling every scope of the job token — followed by a trailing comment that the job token is unused because the action reads traffic and pushes with TRAFFIC_TOKEN.
    <advantage>It states the fact — this job uses its GITHUB_TOKEN for nothing — so the tightest grant is also the accurate one, the file itself tells a reader that TRAFFIC_TOKEN is the workflow&apos;s only credential, and it is the workflow-level form GitHub documents and hardening linters recommend (zizmor&apos;s excessive-permissions audit: permissions: {} at the workflow level, job-level grants only where used).</advantage>
    <drawback>It is a form neither drift-gate.yml nor the action&apos;s README uses, so the monorepo&apos;s two workflows carry different blocks and a reader who has not met {} needs the comment to see it is deliberate, and a step added later that does use the job token — a checkout of the repository, say — needs the block widened first, where contents: read would already have covered it.</drawback>
  </alternative>
  <recommendation option="Empty permissions">The job token does nothing in this workflow — the pinned action reads the Traffic API and runs its ls-remote, fetch, and push over an x-access-token remote built from the token input, index.js reads INPUT_TOKEN alone, and the job never checks out its own repository — so contents: write is a leftover from the action&apos;s first day, when token defaulted to github.token, kept verbatim through the same-day fix that made a PAT required, and contents: read would repeat drift-gate&apos;s grant for a checkout this workflow never performs; permissions: {} with a one-line comment is the block that says what is true, in GitHub&apos;s own documented form; contents: read is the runner-up, identical in effect on a public repository, and what breaks the tie is that a permissions block should state what the job&apos;s token needs rather than copy a neighbour&apos;s — the very copying that left the unneeded write in the action&apos;s README.</recommendation>
</open-question>
<open-question id="Badge rendering parameters">
  <question>What label text, color, style, and output SVG file names do the two badges per repository use, given that the table columns already name the metrics and the existing README badges use style=flat?</question>
  <alternative id="Metric labels, README blue">
    Both steps write every rendering input explicitly and identically in all three workflow copies: label unique views / unique clones (typed lowercase, as the header badges type release and ci), color 3b82f6 on both (the hex the README already uses for its License badge), style flat written out rather than inherited from the action&apos;s default, and output views-unique.svg / clones-unique.svg so each file name is the step&apos;s own metric value plus .svg.
    <advantage>Every SVG says what its number is wherever it is seen — at the raw URL that serves it into the README, in the traffic-data branch listing, on a hotlink, and in its own aria-label (unique views: 9) — its file name is the one token the workflow&apos;s metric line, the README URL, and a grep all share, and the action&apos;s flat geometry (20 px, radius 3, #555 label, Verdana 11) is exactly the shields flat the header badges use, so in the README&apos;s own blue the six cells read as one informational family beside them.</advantage>
    <drawback>Each cell repeats its column header — unique views stands in the header and in the three badges under it, four times per column — and in one colour the two columns are told apart by text alone, with the label making up most of each badge&apos;s width; a value-only badge would avoid both, but the action cannot render one, since a blank label falls back to Repo views.</drawback>
  </alternative>
  <alternative id="Upstream two-badge example">
    Take the action&apos;s documented multiple-badges example as it stands, with only the metric lines switched to views-unique and clones-unique: labels Views and Clones, named colours blue and orange, style left to the default, and output views.svg / clones.svg.
    <advantage>It matches the action&apos;s own documentation line for line, so a maintainer checking a step against the docs finds nothing to explain, and the one-word-shorter labels give the narrowest badges with a colour per column to tell them apart at a glance.</advantage>
    <drawback>Labels and file names name the two metrics these badges do not show — views.svg labelled Views carries the unique count, six-fold smaller than the views the monorepo&apos;s current 14 days report (9 against 56) — so any badge seen outside the table reads as a total it is not, the natural names are spent if a total-views badge is ever added, the Title-case labels sit beside the header&apos;s lowercase release and ci, and blue (#007ec6) and orange (#fe7d37) are off the README&apos;s 22c55e / 3b82f6 palette.</drawback>
  </alternative>
  <alternative id="Repository labels, column colours">
    The badge label names the row instead of the column — cairn, cairn-claude, cairn-antigravity, hard-coded per copy or ${{ github.repository }} for identical copies at the cost of the uHappyLogic/ prefix in every badge — and the metric is carried by the column header plus a colour per column (views 3b82f6, clones 22c55e), with style flat and output views-unique.svg / clones-unique.svg.
    <advantage>Nothing in a cell repeats its column header, and the badge names the one thing the column cannot — which repository it counts — so a badge lifted out of the table still says whose traffic it shows, and the README&apos;s two accent hexes get one column each.</advantage>
    <drawback>The two badges on a row differ only in colour and number, so outside the table nothing says whether cairn 112 counts views or clones — the aria-label becomes cairn: 112, and colour carries nothing to a screen reader or to a reader who has not learned the mapping — while the row label the table-markup sibling settles already names the repository, so the repetition moves from column to row rather than disappearing, the green is the README&apos;s release colour, and a hard-coded name makes the label a per-repository difference between the three workflow copies.</drawback>
  </alternative>
  <alternative id="GitHub Insights vocabulary">
    The same explicit set as the metric-label option — 3b82f6, style flat, views-unique.svg / clones-unique.svg — but with the labels unique visitors and unique cloners, the words GitHub&apos;s Insights Traffic page and the action&apos;s own views-unique example use for the uniques figure.
    <advantage>The label says what the number counts — distinct people per day, summed — in the words a maintainer sees on the very page they would check the badge against, where unique views reads as if a view could be unique.</advantage>
    <drawback>The goal fixes the column names as unique views and unique clones, so every cell would name one number with two nouns, header against badge, and either the caption-precision sibling&apos;s wording has to bridge them or the goal&apos;s column names change.</drawback>
  </alternative>
  <applied-principle>Name by distinctive function</applied-principle>
  <recommendation option="Metric labels, README blue">A badge is served as its own file at its own URL, so its label must say what its number is, and the words are already chosen — the goal&apos;s column names — so unique views / unique clones typed lowercase like release and ci, with each file named by the step&apos;s own metric value, give the label and the name of every badge its distinctive content in the vocabulary the table and the action already use; style flat is the action&apos;s default and the very shields geometry the header uses, written out so a pin bump cannot change the look silently, and 3b82f6 is the README&apos;s own informational blue, one colour because the label already tells the columns apart; the GitHub-vocabulary form is the runner-up, and what breaks the tie is that the goal has already fixed the nouns, so one pair per cell beats a truer pair that disagrees with its own header.</recommendation>
</open-question>
<open-question id="Adoption table placement">
  <question>Where does the adoption table go in README.md — inside the header badge block, in a new section (and where in the section order), or beside Self-dogfooding?</question>
  <alternative id="Header badge block">
    A centred HTML table of the six badges with its caption inserted into the header region — after the badge paragraph and before the horizontal rule, above # Cairn — so it renders with the banner and the release, ci, and license badges before the title.
    <advantage>The most visible slot on the page: the six counts sit on the first screen beside the three status badges of the same flat kind, with nothing to scroll for, and the header stays the one place the README shows live figures.</advantage>
    <drawback>The header is a wordless strip of three one-line badges about this repository&apos;s release state, and the table is three repositories&apos; counts under a caption of caveats (cumulative from first run, CI checkouts, whatever further clauses the caption sibling adds) — prose that would stand above the project&apos;s own title and tagline, pushing # Cairn and the pitch further down on every screen; a Markdown table can neither be centred nor nested in that HTML, so the markup sibling is forced to an HTML table; and today&apos;s figures (9 unique viewers on the monorepo, 2 on each distribution repository) would lead the page for months.</drawback>
  </alternative>
  <alternative id="Section after Why Cairn">
    A new ## Adoption section holding the table and caption between ## Why Cairn? and ## Installation, so the evidence follows the pitch and precedes the install commands.
    <advantage>Prominent without touching the header: the second heading on the page and the first thing after the pitch, so a skim of the top reads claim then evidence, and as an ordinary Markdown section it leaves the markup sibling free.</advantage>
    <drawback>It puts a caveated measurement table between the two things a first visit is for — the pitch and the install command — so every reader who came to install scrolls past three repositories&apos; cumulative counts and their fine print, and the top of the file, which today runs pitch, install, how it works, gains project-level meta ahead of the product it describes.</drawback>
  </alternative>
  <alternative id="Section before Contributing">
    A new ## Adoption section holding the table and caption after ## Skill reference and before ## Contributing, opening the project-level tail (Contributing, Self-dogfooding, License) that follows the tool documentation.
    <advantage>The table lands where its genre already lives — beside the sections about the repositories rather than the tool — under a heading named for exactly what a reader looking for usage evidence scans GitHub&apos;s outline for, with room for the caption&apos;s caveats as plain Markdown, while the page&apos;s opening (banner, three status badges, title, pitch, install) stays as it is.</advantage>
    <drawback>It sits below roughly a hundred lines of skill reference, so a visitor who reads only the top never meets the evidence unless they look for the heading, and the reach the milestone exists to publish is near the end of the page rather than at the top.</drawback>
  </alternative>
  <alternative id="Under Self-dogfooding">
    The table and caption appended to the existing ## Self-dogfooding section after its paragraph, with one bridging sentence and no new heading.
    <advantage>No new section: the paragraph already frames this repository as its own first user, so counts placed under it read as use beyond the maintainer, and the table sits in the tail where its fine print belongs.</advantage>
    <drawback>Self-dogfooding is about this repository running Cairn&apos;s own milestone workflow on itself, and the table is unrelated to that — two of its three rows are the distribution repositories, built trees with no milestones of their own — so the heading stops describing its section, nothing in GitHub&apos;s outline names the evidence, and a reader looking for adoption figures would not look there.</drawback>
  </alternative>
  <recommendation option="Section before Contributing">The table is a captioned measurement, not a status badge — three repositories&apos; cumulative counts under a caption whose caveats the goal already fixes and siblings may extend — which is project-level evidence of the kind the README&apos;s tail already holds and that a reader seeks by heading, whereas the header is a wordless strip of three one-line badges above the title and the top of the file is pitch then install; so a ## Adoption section opening the tail, after ## Skill reference and before ## Contributing (reach, then the invitation to join, then the repository&apos;s own use of the workflow, then the license), gives the evidence a findable name and its fine print a home without leading the page with today&apos;s single-digit distribution rows; the section after Why Cairn? is the runner-up, and what breaks the tie is that it would put a caveated table between the pitch and the install command, the two things every first visit is for.</recommendation>
</open-question>
<open-question id="Table markup and links">
  <question>Is the adoption table a Markdown table or HTML like the header badge block, are the row labels full owner/repo names or short names, and do the row labels or badges link anywhere (the repository, its traffic-data branch, the workflow runs)?</question>
  <alternative id="Markdown, full names, data links">
    A GitHub-flavoured pipe table under the section heading — header row repository | unique views | unique clones, one row per repository whose label is the full uHappyLogic/cairn, uHappyLogic/cairn-claude, uHappyLogic/cairn-antigravity as a Markdown link to that repository, each badge the Markdown image at its raw traffic-data URL with its column name as alt text and wrapped in a link to that repository&apos;s traffic-data branch page, and the caption as the paragraph after the table.
    <advantage>It is the body&apos;s own idiom — every section below the horizontal rule is plain Markdown with Markdown links, and the caption stays a paragraph that can carry backticks and links as the caption siblings add clauses — every badge on the page then links to what it reports on exactly as the three header badges do, the traffic-data branch being the one public view of the number (GitHub shows Insights → Traffic only to accounts with push access, while the branch page lists the per-day totals.json every count is summed from and the daily github-actions[bot] commits), and the labels are the form GitHub and every repository link in the distribution READMEs write a repository in.</advantage>
    <drawback>Nine links in a three-by-three grid, six of them to a branch listing of two SVGs and a JSON file that few visitors will open; the owner is repeated three times for three repositories that share it; and a Markdown table can be neither centred nor bound to a caption element, so it sits left-aligned with its caption as the following paragraph.</drawback>
  </alternative>
  <alternative id="Markdown, short names, no links">
    The same pipe table with row labels cairn, cairn-claude, cairn-antigravity in code font, as the README&apos;s Installation prose and the goal&apos;s own list already name the distribution repositories, no link on any label, and each badge the bare Markdown image line the action&apos;s README prescribes.
    <advantage>The fewest characters that meet the goal: each cell is the one line the action documents, the labels are names the page already uses bare, and the table carries no URL beyond the six badge sources, so nothing in it can go stale.</advantage>
    <drawback>The six adoption badges become the only badges on the page that go nowhere while the header&apos;s three each link to what they report on, the number&apos;s single public source — totals.json on traffic-data — is reachable only by a reader who knows the branch exists, and bare cairn under # Cairn names the plugin and the cairn marketplace as readily as the repository, so the one row whose clones the caption singles out for CI checkouts is the one whose label is ambiguous.</drawback>
  </alternative>
  <alternative id="HTML table like the header">
    A &lt;table&gt; written in the header block&apos;s idiom — a &lt;th&gt; header row, an &lt;a href&gt;&lt;img alt src/&gt;&lt;/a&gt; pair per badge cell exactly like the release, ci, and license badges, a &lt;caption&gt; element for the caption, and align=&quot;center&quot; on the table — with the same labels and link targets as the Markdown form.
    <advantage>One markup for every badge on the page (the cells are the header&apos;s own &lt;a&gt;&lt;img&gt; pairs), a caption element bound to its table, and the centring and per-cell align attributes GitHub&apos;s sanitizer keeps — the only form that could sit inside the header block, had the placement sibling chosen it.</advantage>
    <drawback>It would be the one HTML block in a body that is plain Markdown everywhere below the horizontal rule, some thirty lines of tags for a three-by-three grid; GitHub renders no Markdown inside an HTML block, so the caption&apos;s caveats — with whatever backticks and links the caption siblings add — must be hand-written as HTML too; and the sanitizer strips style, so the tags buy only align and caption, neither of which a table under a Markdown heading needs.</drawback>
  </alternative>
  <alternative id="Badges to workflow runs">
    The Markdown table with linked full names, but each badge links to its repository&apos;s traffic workflow runs page (github.com/uHappyLogic/&lt;repo&gt;/actions/workflows/&lt;file&gt;.yml), as the header&apos;s ci badge links to drift-gate&apos;s runs on main.
    <advantage>It copies the page&apos;s closest precedent — the ci badge is the one workflow-fed badge, and it links to its workflow — and the runs page shows the daily schedule-triggered runs, the very activity the inactivity-evidence sibling reads as proof the workflow stays alive.</advantage>
    <drawback>A run shows that a badge was regenerated, not what its number is made of — the per-day counts live in totals.json on traffic-data, which the runs page never links — so the click lands on CI logs the visitor did not ask for; the URL hard-codes the workflow file name the name-and-cron sibling settles, so a rename breaks six links; and a delayed or failed scheduled run shows red beside a badge whose number is still right.</drawback>
  </alternative>
  <applied-principle>Drop-vs-replace by ambiguity</applied-principle>
  <depends-on question="Adoption table placement" option="Section before Contributing"/>
  <recommendation option="Markdown, full names, data links">The placement sibling puts the table in a Markdown section where nothing needs centring and GFM cells take the image-in-link form the header badges already use, so a pipe table with the caption as its following paragraph is the body&apos;s own idiom; the labels are the full owner/repo because that is how GitHub and the distribution READMEs write a repository and because bare cairn under # Cairn names the plugin as readily as the repository, so the qualifier cannot be dropped for the one row the caption singles out; and each label links to its repository while each badge links to that repository&apos;s traffic-data branch — the only public view of the number, holding the per-day totals.json and the daily bot commits where Insights → Traffic is visible to push access alone — so every badge on the page links to what it reports on; the short-name unlinked form is the runner-up, and what breaks the tie is that it would make the six adoption badges the only badges on the page that go nowhere while the number&apos;s one public source stays unreachable.</recommendation>
</open-question>
<open-question id="Caption precision">
  <question>Beyond the two facts the goal fixes, does the caption also state that counts include the 14 days the first run captured, that unique counts sum per-day uniques, and that each row starts from the first run of its own workflow (the distribution rows later than the monorepo row)?</question>
  <alternative id="Goal facts only">
    The caption states exactly what the goal fixes — counts cumulative from the workflows&apos; first run, the monorepo&apos;s clones include CI checkouts — plus whatever clone clause the self-clone sibling settles on, and says nothing about the 14-day capture, how a unique count is formed, or when each row starts.
    <advantage>Nothing beyond the goal to author or keep true, the shortest fine print under six badges, and the action author&apos;s own README shows its badge with no caption at all, so the upstream precedent is zero explanation.</advantage>
    <drawback>The one word the table repeats in every header and badge — unique — is left to mean what a reader assumes, distinct people, while the badge is a per-day sum: the monorepo&apos;s last 14 days give 18 summed daily uniques where GitHub&apos;s own distinct figure is 9, a returning visitor counts once per day without bound, and a clone clause stating one unique clone per repository per day reads as nonsense to a reader who does not know that.</drawback>
  </alternative>
  <alternative id="Unique clause only">
    The caption adds one clause — a unique count is the sum of each day&apos;s unique visitors or cloners, so a visitor returning on another day counts again — and leaves the start as the goal words it, with the 14-day capture and each row&apos;s first day left to the totals.json on the traffic-data branch every badge links to.
    <advantage>One clause corrects the one misreading the table invites, the goal&apos;s own wording carries the rest unchanged, and the per-day record is one click away for a reader who wants the exact days.</advantage>
    <drawback>From the workflows&apos; first run stays thirteen days wrong — the first run captured the 14 days before it — and names no day at all, so the caption cannot tell a visitor whether the period is a month or a year without that click.</drawback>
  </alternative>
  <alternative id="All three clauses">
    The caption states everything the question lists — that the record begins with the 14 days the first run captured, that a unique count sums per-day uniques, and that each row runs from its own workflow&apos;s first run, the distribution rows starting later than the monorepo row — beside the goal&apos;s two facts and the clone clause.
    <advantage>Every reading of every number is stated in the caption itself; nothing about the counts is left to a click or to a reader&apos;s assumption.</advantage>
    <drawback>Six clauses of fine print under six badges, two of which describe constants a reader cannot use without a date — a 13-day head start whose share of the total fades with every day, and a gap between rows that is whatever the task order makes it, likely a day or none since all three seeds wait on the same secrets — while each row from its own first run restates what the goal&apos;s plural workflows already says.</drawback>
  </alternative>
  <alternative id="Unique clause, dated start">
    The caption adds the unique clause and writes the start as a date — cumulative since the earliest day in each traffic-data branch&apos;s totals.json, named as the first day the workflows&apos; first run captured, one date when the three seeds share it and a second in parentheses for the distribution rows when they do not — in place of a clause about the 14-day capture and a clause about per-row starts.
    <advantage>The start becomes a fact a reader can use — a date says at a glance whether the period is a month or a year — and is exactly right rather than thirteen days off, while the two remaining facts the question lists collapse into that date (a second one only when the rows differ), so the caption gains a date instead of two clauses.</advantage>
    <drawback>A hand-typed date is the one constant in the caption the README author must read off the seeded branch after the fact, that the goal&apos;s wording never asked for, and that a re-seeded traffic-data branch would silently falsify; and the caption is still three sentences of fine print under a badge table.</drawback>
  </alternative>
  <depends-on question="Self-clone inflation" option="Disclose in the caption"/>
  <depends-on question="Table markup and links" option="Markdown, full names, data links"/>
  <recommendation option="Unique clause, dated start">The caption exists to keep six numbers from being misread, and the table invites exactly two misreadings — that unique means distinct people, when the badge sums each day&apos;s uniques (the monorepo&apos;s last 14 days already give 18 against the 9 distinct viewers GitHub reports, a returning visitor counts once per day without bound, and the clone clause of one unique clone per repository per day only makes sense once that sum is stated) and that from the first run means the day the badges appeared, when the first run captured the 14 days before it — so one clause fixes the first, and writing the start as the earliest captured date fixes the second together with the per-row question, one date when the three seeds share a day and a second for the distribution rows when they do not, with the per-day totals.json one click away on the traffic-data branch every badge links to; the unique clause alone is the runner-up, and what breaks the tie is that from the first run names no day and is thirteen days wrong, while a date costs a few words.</recommendation>
</open-question>
<open-question id="Self-clone inflation">
  <question>Every step of the action fetches the repository over git by the same mechanism as actions/checkout, which the caption already treats as a counted clone, so the workflow inflates the clone counts of every repository daily — including the distribution repositories the caption presents as CI-free: is that accepted, mitigated, or disclosed in the caption?</question>
  <alternative id="Disclose in the caption">
    Keep the action, the two-step job, and the daily cron exactly as the goal fixes them, and widen the caption&apos;s one clone caveat from the monorepo row to every row: clone counts include the badge workflow&apos;s own daily fetch of the traffic-data branch (one unique clone and two clones per repository per day), and the monorepo&apos;s additionally include CI checkouts.
    <advantage>It costs one clause in a caption the milestone writes anyway and makes every number on the table honest at once: the inflation is a fixed, stated floor — one unique clone per row per day from the second run on — that a reader can net out against the same caption&apos;s from-first-run start date, no mechanism changes, and the goal&apos;s named action, pinned SHA, two-step job, and daily cadence all stand.</advantage>
    <drawback>The numbers themselves stay inflated, and by a constant that is a different share of each row: about a ninth of the monorepo&apos;s current eight unique cloners a day, but likely the majority of each distribution row, whose only recorded day shows 9 and 4 unique cloners against a floor of seven a week — so the distribution rows&apos; unique-clone badges will read mostly as the workflow counting itself, and the clause&apos;s exact wording joins whatever the caption-precision question settles.</drawback>
  </alternative>
  <alternative id="Accept without change">
    Leave the caption at the goal&apos;s two facts — cumulative from first run, the monorepo&apos;s clones include CI checkouts — and say nothing about the workflow&apos;s own fetch on any row.
    <advantage>Nothing to write or maintain, the caption stays exactly as the goal fixes it, and the inflation is the same absolute floor on every row, so the table stays as comparable across rows as any badge table is.</advantage>
    <drawback>The caption&apos;s monorepo-only clause becomes actively misleading: it tells the reader the distribution rows are clean when, on the action author&apos;s own repository, the action&apos;s fetch shows up as a never-zero floor of one clone and one unique a day even through seventeen straight zero-view days — so the distribution rows&apos; unique-clone counts would be presented as adoption while consisting mostly of the workflow counting itself.</drawback>
  </alternative>
  <alternative id="Weekly cadence">
    Keep the action and disclose nothing new, but run each workflow weekly instead of daily — the Traffic API&apos;s 14-day window still covers every day between runs — so the self-fetch adds one unique clone per row per week (about 52 a year) instead of per day.
    <advantage>A seven-fold reduction of the inflation from a single cron-line change, with the pinned action, the two-step job, and the push that keeps the scheduled workflow active all unchanged.</advantage>
    <drawback>It contradicts the goal&apos;s daily-scheduled-workflow clause and the daily line the name-and-time sibling recommends, so it needs a goal revision; it gives up the redundancy daily runs provide (each day is re-fetched on fourteen successive runs, so a scheduled run GitHub delays or drops costs nothing, while two missed weekly runs lose a week of data for good); the badges lag up to a week; and the floor it leaves — still comparable to a distribution row&apos;s real cloners — needs the same caption clause anyway.</drawback>
  </alternative>
  <alternative id="API-writing replacement">
    Replace the action&apos;s git fetch and push with REST writes — a fork or a bespoke script that reads the Traffic API and writes totals.json and the SVG through the Contents API on the traffic-data branch — so no step ever fetches the repository over git and nothing the workflow does registers as a clone.
    <advantage>It removes the self-inflation outright rather than describing it: the distribution rows&apos; clone counts become genuine (the monorepo&apos;s keep only the CI checkouts the caption already names), which the pinned action cannot deliver — it queries and fetches GITHUB_REPOSITORY only, its repos input is unimplemented, and its base input rejects a negative offset.</advantage>
    <drawback>It abandons the goal&apos;s named action and pinned SHA for code the project must own — a fork whose pin no longer tracks upstream&apos;s releases, or a script re-implementing the date-keyed merge, the SVG renderer, and an orphan-branch bootstrap the Contents API alone cannot do (it needs a parentless commit through the Git Data API first) — a goal revision and a maintenance liability for a cleaner number under a README badge.</drawback>
  </alternative>
  <recommendation option="Disclose in the caption">With the pinned action the fetch is unavoidable — it fetches and queries only the repository running it, has no API-write mode, and rejects a negative base — so every way to shrink the number abandons the goal&apos;s action or its daily cadence, while the inflation is a fixed, known quantity (one unique clone and two clones per row per day, proven by the never-zero one-clone-one-unique floor on the action author&apos;s own repository) that one caption clause states plainly and a reader can net out against the same caption&apos;s from-first-run start date; the weekly cadence is the runner-up, and what breaks the tie is that it trades the goal&apos;s daily clause and the redundancy that makes a dropped scheduled run harmless for a floor that still needs the same clause.</recommendation>
</open-question>
<open-question id="Inactivity disable evidence">
  <question>GitHub disables a scheduled workflow only after 60 idle days, so what evidence verifies within the milestone that the daily traffic-data push keeps the distribution workflows active — GitHub documentation, an API read-back of the workflow state after some days, or a follow-up check scheduled beyond the milestone?</question>
  <alternative id="Precedent read-back">
    The verifying task reads, with the public REST API (read-only gh api calls), the live state of the action author&apos;s own repository albertoarena/github-traffic-badge — where since the last human commit on 2026-07-05 the only pushes are the action&apos;s daily github-actions[bot] commits to traffic-data (each a PushEvent attributed to the token&apos;s owner), every run of its scheduled traffic workflow is schedule-triggered, and there is no issue, pull request, tag, or release — confirms that workflow still reads state active past day 60 (day 75 at the time of this read), then after each distribution repository&apos;s first scheduled run confirms the same signal on its own repository (pushed_at advanced and a public PushEvent on refs/heads/traffic-data), recording the dated observations in the decision and the task&apos;s Verified entry.
    <advantage>It is real evidence about the exact mechanism the milestone deploys — the same action, the same PAT-authenticated push to a non-default branch, the same public-repository rule, even seeded by one workflow_dispatch on 2026-06-03 as the milestone plans — obtainable today inside the milestone with three read-only API calls, already fifteen days past the 60-day mark with zero confounding activity, and the same-signal read on the distribution repositories closes the gap between the precedent and this deployment.</advantage>
    <drawback>It is evidence by precedent on another account&apos;s repository: GitHub could change the rule or what feeds it without notice, and the read is a one-time observation with nothing in the milestone that would notice a later disable beyond GitHub&apos;s own warning email and a badge that stops advancing.</drawback>
  </alternative>
  <alternative id="Documentation citation">
    The decision and the verifying task cite GitHub&apos;s documentation — in a public repository, scheduled workflows are automatically disabled when no repository activity has occurred in 60 days — and record the argument that a commit pushed daily to traffic-data is repository activity, with the documentation URL as the evidence.
    <advantage>The cheapest option and the only authoritative source, with no dependence on another repository&apos;s state or on the distribution workflows running inside the milestone.</advantage>
    <drawback>The documentation never defines repository activity — neither the schedule page nor the disabling page says whether a bot-authored push to a non-default branch counts — so the citation restates the rule the question already knows and proves nothing about the case it asks about; it is an argument, not evidence.</drawback>
  </alternative>
  <alternative id="Own-state read-back">
    After each distribution repository&apos;s first scheduled runs, the task reads that repository&apos;s workflow state and pushed_at through the API and records state active plus the advancing pushed_at as the evidence, without reference to any other repository.
    <advantage>The evidence concerns the actual repositories the goal names, produced by their own workflows and their own token, with no reliance on a third party&apos;s repository staying as it is.</advantage>
    <drawback>Within the milestone&apos;s days state active is trivially true — the rule fires on day 61, so a read on day 3 cannot tell a push that counts from one that does not — and an advancing pushed_at shows that a signal fired, not that GitHub&apos;s rule keys on it; the conclusive read on these repositories is only possible around the 60th day after their release push, outside the milestone.</drawback>
  </alternative>
  <alternative id="Deferred day-61 check">
    Record a dated follow-up — read back each distribution repository&apos;s workflow state through the API once 60 days have passed since the release push that landed its workflow, mid-to-late November 2026 for a release this month — in the milestone&apos;s finish summary or a later milestone, as the verification.
    <advantage>The only direct proof on the actual repositories that the rule was survived, with no inference from documentation or from another repository.</advantage>
    <drawback>It contradicts the goal&apos;s verifies-within-the-milestone clause, the plugin has no cross-milestone follow-up mechanism so the note is owned by nobody, and it adds nothing that GitHub&apos;s own warning email (sent about a week before an inactivity disable) and a badge that stops advancing would not already announce.</drawback>
  </alternative>
  <depends-on question="Distribution seeding sequence" option="Mid-milestone patch release"/>
  <recommendation option="Precedent read-back">The documentation states the rule but never defines the activity it keys on, and inside the milestone&apos;s days the distribution repositories&apos; own state is trivially active, so the one observation that settles the question now is a public repository already past day 60 on nothing but this action&apos;s daily push — the author&apos;s own, read today at day 75 with every run schedule-triggered, no human commit, issue, pull request, tag, or release since 2026-07-05, and its scheduled workflow still active — recorded together with the same-signal read on each distribution repository after its first scheduled run, which the mid-milestone release makes possible inside the milestone; the deferred day-61 check is the runner-up, and what breaks the tie is that it lies outside the milestone and would only re-observe what GitHub&apos;s warning email and a stalled badge already announce.</recommendation>
</open-question>
<open-question id="Workflow in installed trees">
  <question>A workflow file in hosts/&lt;host&gt;/ reaches every consumer install — the Antigravity tarball extracts it under .agents/plugins/cairn/.github/workflows/ and Claude Code clones it: is that inert file accepted in installed plugin trees, or kept out of what consumers receive?</question>
  <alternative id="Accept inert file">
    Ship the traffic workflow inside hosts/&lt;host&gt;/ exactly as the goal places it, so every Claude Code plugin cache and every Antigravity .agents/plugins/cairn/ extraction carries .github/workflows/ as inert cargo, with a leading comment in the file stating that it runs only in uHappyLogic/cairn-&lt;host&gt; and does nothing in an installed copy.
    <advantage>No new machinery anywhere — the byte-identical publish (git commit-tree over HEAD:hosts/&lt;host&gt;), the --check drift gate, both README install commands, and the distribution README claim that every file is published verbatim all stay true — and the file is inert by construction, since GitHub reads workflows only from a repository-root .github/workflows/ and a copy nested under .agents/plugins/cairn/ or a plugin cache can never run, just as the monorepo-sourced 1.0.0–1.4.0 Claude Code installs carried the whole repository (CLAUDE.md, milestones/, scripts/, a .venv) as inert cargo without consequence.</advantage>
    <drawback>A workflow that references secrets.TRAFFIC_TOKEN and a cron lands in every consumer workspace or cache, reads like something that runs to anyone who opens the tree (and is checked in alongside .agents/ by consumers who commit the plugin), and grows the host tree past its documented exactly-three-non-plugin-files contract, so the CLAUDE.md invariant and the distribution README wording (its own sibling question) must be amended to name the fourth.</drawback>
  </alternative>
  <alternative id="Install-time exclusion">
    Keep the workflow in the tree but add --exclude=&apos;*/.github&apos; to the documented Antigravity extraction command (bsdtar and GNU tar both honour it together with --strip-components, verified locally) so it never reaches a consumer workspace, while Claude Code, which copies the whole marketplace source into its plugin cache with no filter, keeps receiving it.
    <advantage>A one-flag change with no build or release impact keeps the only place the file is genuinely visible — the consumer&apos;s own workspace and, when .agents/ is committed, their repository — clean.</advantage>
    <drawback>Partial and leaky: it does nothing for Claude Code (the marketplace clone and the cache copy both carry the file), protects only consumers who run the documented one-liner verbatim (a tag-pinned extraction, a GUI unzip, or a re-extract without the flag brings the file back), and turns the README claim that the repository is received exactly as published into a documented exception maintained in two README templates.</drawback>
  </alternative>
  <alternative id="Plugin subdirectory layout">
    Restructure each distribution repository so the plugin tree sits under a subdirectory (say plugin/) with the workflow, README, and the Claude marketplace.json at the root, pointing the marketplace source at ./plugin so Claude Code copies only that directory into its cache, and rewriting the Antigravity command to extract only the plugin/ member.
    <advantage>The installed plugin tree on both hosts is a pure plugin tree by construction rather than by consumer discipline, and the non-plugin files (workflow, README, marketplace) gain a natural home outside it.</advantage>
    <drawback>The largest blast radius for a cosmetic gain: both host definitions&apos; [layout] values and template paths, both manifest locations, the monorepo root marketplace source (./hosts/claude), both README install commands, and the CLAUDE.md/README documentation all move; the old Antigravity one-liner silently produces a broken nested install for anyone still using it; and Claude Code still clones the whole repository as the marketplace, so the file is kept out of the cache only, never out of what consumers fetch.</drawback>
  </alternative>
  <recommendation option="Accept inert file">GitHub runs a scheduled workflow only from the default branch, and the default branch is exactly what both install paths fetch, so a per-repository traffic workflow is inseparable from what consumers receive; the only levers that remove it are an install-side filter that covers one host and only for consumers who type the documented command, or a layout restructure out of all proportion to a file that cannot run anywhere but the distribution repository itself — so accept it and make its inertness explicit in a header comment.</recommendation>
</open-question>
<open-question id="Distribution README wording">
  <question>The distribution README templates state that every file in the repository is rendered by the host build and that main advances only by release snapshots: are they amended to account for the action-written traffic-data branch and the workflow that writes it?</question>
  <alternative id="Leave unchanged">
    Keep both README templates byte-for-byte as they stand, reading the note&apos;s every file in this repository and the Antigravity line that cairn-antigravity advances only by release snapshots as statements about the plugin tree on main, and leaving the traffic workflow&apos;s own header comment as the only account of the traffic-data branch anywhere in the distribution repositories.
    <advantage>Nothing to edit, rebuild, or keep in step across the two templates, the two CONTRIBUTING pointers that carry the same sentence, and the root README&apos;s Installation section, and the note&apos;s one job — routing every edit and pull request to the root repository — holds exactly as before, since the branch is written by github-actions[bot] and never by hand.</advantage>
    <drawback>From the first seed on, the note&apos;s first sentence is false for the three files on traffic-data (rendered by no build, published by no release, and never overwritten by one) and the Antigravity line is false every day the bot pushes, in the very repositories the seeding sibling chose a patch release over a hand push specifically so that no claim they make about themselves would be falsified — and a visitor who opens the branch dropdown or follows a badge from the root README&apos;s table lands on daily bot commits that nothing in the repository explains.</drawback>
  </alternative>
  <alternative id="Scope to main">
    Make the smallest wording change that keeps every sentence true — the note reads Every file on main, this README included, is rendered … and the Antigravity line reads since main advances only by release snapshots (the same phrase corrected in the root README&apos;s Installation section, and the two CONTRIBUTING pointers scoped the same way) — with no mention of traffic-data or the workflow in either template.
    <advantage>Two words per claim restore literal truth in every template while the note stays the terse contributor-routing banner it is, describing no mechanism a plugin consumer never needs, and the scoping holds for any further non-main branch the repositories might ever gain.</advantage>
    <drawback>The repository now visibly has a second branch that its README pointedly excludes and never names, so a reader who opens the branch dropdown or follows a badge from the root README&apos;s adoption table to the traffic-data page finds daily github-actions[bot] commits and three data files with no account of what writes them or why, and the one explanation that exists — the workflow&apos;s header comment — is read only by someone who opens .github/workflows/ in the tree.</drawback>
  </alternative>
  <alternative id="Scope and name the branch">
    Scope the claims exactly as the previous option does and add one sentence to the note in both templates naming the exception it now carves out: the repository&apos;s only other branch, traffic-data, is not built from anything — the traffic workflow this tree carries, which runs only in this repository and never in an installed copy, writes its per-day data file and badge SVGs there daily for the adoption table in the root repository&apos;s README, and no release touches it (the CONTRIBUTING pointers take the scoping alone).
    <advantage>Every sentence stays true and the one thing in the repository that is not release output is named where the claim is qualified, with its writer, its purpose, and its irrelevance to installs — so the branch a visitor sees in the dropdown or reaches from a root-README badge, and the workflow every consumer finds in their extracted or cached tree, are both explained by the README that ships beside them, in one sentence.</advantage>
    <drawback>The note grows to three sentences and starts describing a data branch, a workflow, and a badge table in another repository to an audience that came to install a plugin; five files change in step by hand (two README templates, two CONTRIBUTING pointers, the root README) for phrase-level edits; the sentence must stay generic about file names or it couples the README to the badge-parameter and workflow-name decisions; and it describes a branch that exists only once the post-release workflow_dispatch has seeded it.</drawback>
  </alternative>
  <depends-on question="Distribution seeding sequence" option="Mid-milestone patch release"/>
  <depends-on question="Workflow in installed trees" option="Accept inert file"/>
  <depends-on question="Table markup and links" option="Markdown, full names, data links"/>
  <recommendation option="Scope and name the branch">The note is the one sentence in each distribution repository that claims everything there is build output, and from the seed on the three files on traffic-data are not — rendered by no build, published by no release — so the claim must be scoped, and the same scoping repairs the Antigravity line, which stays true of main only because the workflows arrive by the mid-milestone patch release rather than a hand push; the extra sentence earns its place because the branch is the single thing in the repository a visitor can see that the scoped note would otherwise leave unexplained — listed in the branch dropdown and the destination of every badge in the root README&apos;s adoption table — and because the workflow reaches every install as inert cargo, the README that travels beside it is where a consumer reads that it runs only here; scoping alone is the runner-up, and what breaks the tie is that it makes every sentence true while leaving the branch it now pointedly excludes unexplained by anything a visitor reads.</recommendation>
</open-question>
