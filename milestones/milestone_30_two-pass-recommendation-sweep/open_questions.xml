<open-questions>
  <open-question id="Alternatives pass commit subject and body">
    <question>/provide-alternatives-to-all-open-questions embeds and commits each return as it lands. What is its commit subject (for example Alternatives-annotation: &lt;Short Title&gt;), what if anything goes in the body given that lift prints only recommendation or single-alternative lines, and what is its one fixed terse success line?</question>
    <alternative id="Paired marker no body">
      Commit each embedded return under `Alternatives-annotation: &lt;Short Title&gt;` with no body at all — the subject-only form `commit-procedure.md` already provides for when a caller supplies no BODY — and report the run with the one fixed line `Alternatives embedded.`, the reporting invariant&apos;s own no-op line and still-skipped advisory being the only other output.
      <advantage>It answers all three halves from conventions already in the repo and invents nothing: the marker is `Recommendation-annotation:`&apos;s sibling differing in exactly the word that names its half, so the two passes read as the two halves of one annotation and the counterpart the recommendation sibling&apos;s drawback leaves unnamed is filled; the descriptor is the Short Title because the commit is per return, exactly as every per-item commit here carries its item&apos;s handle while every once-per-run one carries the milestone id; the body is omitted because this half has no one-line tool print to hold — `lift` prints a recommendation or a single named alternative and nothing else — and subject-only commits are ordinary here, `Question-ordering:` among them; and `Alternatives embedded.` is the existing success line with one noun changed, identifier-free as the rule requires. It also stays clear of capture&apos;s anchors by construction, since that grep is anchored and its `Alternative-answer` branch cannot match `Alternatives-annotation`.</advantage>
      <drawback>It leaves the one annotation family inconsistent in the log — the recommendation half carries its rationale in the body, this half carries nothing — so `git log` says which questions gained alternatives but never which alternatives, and reviewing a wide parallel run means opening one diff per commit; and the marker sits one letter from `Alternative-answer:`, so a human skim or a loosely written `Alternative-` grep can conflate an annotation with an answer.</drawback>
    </alternative>
    <alternative id="Lifted alternative lines">
      Same subject and success line, but give each commit a body of one `lift --alternative &quot;&lt;id&gt;&quot;` line per embedded alternative — &quot;`&lt;id&gt;` — `&lt;what-it-is&gt;`&quot; — the ids recovered after the embed from a `locate` of the block the call just wrote.
      <advantage>It holds the whole annotation family&apos;s bodies to the single rule the recommendation sibling&apos;s pick turns on — every body line is the tool&apos;s own print of text the same commit added, so a body cannot drift from its diff — and it puts each question&apos;s enumerated option ids in the log where a reviewer scanning a wide parallel run reads them without opening a diff.</advantage>
      <drawback>Nothing the pass already runs prints those ids, so the body costs a `locate` plus a prose read of the printed block to recover them and then one `lift` call per alternative, reintroducing into the busiest loop in the plugin exactly the orchestrator-side reading of a block&apos;s contents that this design has otherwise moved into the tool — and what it yields is an unbounded multi-line restatement of the very children the same commit&apos;s one-element-per-line canonical diff already shows.</drawback>
    </alternative>
    <alternative id="Tool print body">
      Keep the subject and the success line and give the tool a whole-block alternatives print — for example `lift --alternatives &quot;&lt;Short Title&gt;&quot;`, one &quot;`&lt;id&gt;` — `&lt;what-it-is&gt;`&quot; line per alternative — so this half&apos;s body is one tool call, as the recommendation half&apos;s is.
      <advantage>It recovers a tool-printed, drift-proof body in exactly one call per commit with no reading of the block by the pass at all, and the same print would serve any later consumer needing a block&apos;s option roster without a `locate`.</advantage>
      <drawback>It widens the tool — a new flag, its refusal strings and its own test coverage — beyond the three tool changes the goal names, and does so for a commit body alone, producing text that is a second copy of the diff it rides in, which is the thinnest reason in this repo to add a tool surface.</drawback>
    </alternative>
    <alternative id="Skill-derived marker">
      Derive the marker from the pass&apos;s own name rather than from the sweep it splits off — `Alternatives-provision: &lt;Short Title&gt;`, or `Alternatives-enumeration:` — with the body and the success line as in the first option.
      <advantage>It derives the marker straight from the skill&apos;s distinctive function the way the commit convention asks of a new marker, and it puts a whole word between this marker and `Alternative-answer:`, so no log skim or hand-written grep can conflate the two.</advantage>
      <drawback>It breaks the pairing the split&apos;s own vocabulary creates — two annotating passes whose markers differ in exactly the word naming their half — leaving `Recommendation-annotation:` reading as a leftover rather than one of a pair, and it buys separation from an answer marker no automated reader can confuse, since capture&apos;s grep is both-ends-anchored and names its three branches literally.</drawback>
    </alternative>
    <applied-principle>Name by distinctive function</applied-principle>
    <recommendation option="Paired marker no body">The split produces two annotating halves, so their markers should differ in exactly the word that names the half — `Alternatives-annotation: &lt;Short Title&gt;` per return beside `Recommendation-annotation: &lt;milestone_id&gt;` per run, the descriptor following the commit&apos;s granularity as it already does everywhere here — and the body is left empty because this half has no one-line tool print to put in it and every substitute is either a block read the tool was built to absorb or a restatement of the same commit&apos;s diff, while `Alternatives embedded.` is the existing success line with one noun changed; only if a reviewer routinely had to know a block&apos;s option ids from the log alone, without opening the diff, would adding the whole-block print be worth its tool surface.</recommendation>
  </open-question>
</open-questions>
