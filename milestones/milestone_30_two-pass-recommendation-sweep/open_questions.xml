<open-questions>
  <open-question id="Alternatives agent name">
    <question>The recommend-open-question agent becomes an alternatives-only agent dispatched by /provide-alternatives-to-all-open-questions and is renamed to match its role. What is its new name (for example provide-alternatives-to-open-question), given that the orchestrator addresses it by its namespaced registry name and five files reference the old one?</question>
  </open-question>
  <open-question id="Strip command forms">
    <question>The tool needs a partial strip that clears only the recommendation, depends-on, and applied-principle children while keeping the alternatives, and the full strip that clears every child still serves as the hatch for a stale alternative set. Is the partial strip a flag on the existing strip subcommand, a new subcommand, or the new default of strip with the full strip behind a flag?</question>
  </open-question>
  <open-question id="Embed shape selection">
    <question>The embed subcommand must accept two fragment shapes: alternatives only (from the parallel agent) and recommendation only, with its depends-on and applied-principle children (from the inline recommendation pass). Does the tool tell them apart by inspecting the fragment, or does the caller name the shape with an explicit flag, and what must the target block already hold for each shape (no alternatives yet for the first, alternatives but no recommendation for the second)?</question>
    <alternative id="Explicit shape flag">
      One `embed` subcommand whose caller names the shape with a required, mutually exclusive flag pair — `--alternatives` for the parallel pass (sliced between its own first and last `&lt;alternative&gt;` anchor lines, accepted only when the block carries no `&lt;alternative&gt;` yet, the full strip being the hatch) and `--recommendation` for the inline pass (accepted only when the block carries at least one `&lt;alternative&gt;` and no `&lt;recommendation&gt;` yet, the partial strip being the hatch, with the recommendation&apos;s `option` validated against the block&apos;s own alternative ids since the fragment carries none) — each shape writing only its own child kinds and sharing one validation core.
      <advantage>It matches the tool&apos;s established one-verb-with-a-mode-flag style (`list --unannotated`, `lift --alternative`, `remove --option`), keeps every call site self-describing about which pass it is, and lets each refusal name both what the pass asked for and what the block holds, while `--recommendation` never touching the `&lt;alternative&gt;` children makes the frozen alternative set a property of the tool rather than of caller discipline.</advantage>
      <drawback>It is the tool&apos;s first subcommand with a required argument, the flag restates information the document already determines so it must be kept true at two call sites and in the repair template, and it reads inconsistently if the sibling question &quot;Strip command forms&quot; settles on a second subcommand rather than a flag on `strip`.</drawback>
    </alternative>
    <alternative id="Fragment inference">
      `embed` keeps its single call form and decides the shape from the piped message — an `&lt;alternative&gt;` anchor line present means the alternatives shape, its absence with a `&lt;recommendation&gt;` line means the recommendation shape — then applies whichever precondition that shape implies.
      <advantage>Nothing new appears at either call site or in the shape-reminder repair template, so the two passes state what to pipe and never what mode to pipe it in.</advantage>
      <drawback>The slice anchors must be chosen before the parse, by substring tests over prose that these very agents write about `&lt;alternative&gt;` elements, and a return carrying both halves is indistinguishable from an intended one, so the recommendation pass could overwrite the frozen alternatives unless the tool infers a shape only to refuse it — the flag&apos;s job done blind — and every refusal names what the fragment lacks instead of what the pass expected.</drawback>
    </alternative>
    <alternative id="Block state selects">
      No flag and no fragment guessing: the target block&apos;s current children select the shape, because the two preconditions are mutually exclusive and exhaustive over the legal states — a block with no `&lt;alternative&gt;` accepts only an alternatives fragment, a block with alternatives and no `&lt;recommendation&gt;` only a recommendation fragment, and a block with both is refused until one of the strips runs.
      <advantage>The shape test and the precondition are one single test, so no flag can ever disagree with the document, nothing is inferred from prose, and both passes keep an identical one-line call form.</advantage>
      <drawback>The caller cannot state its intent, so a block whose alternatives an answer-time cascade cleared silently becomes an alternatives-shape target and the recommendation pass&apos;s return is refused for holding no `&lt;alternative&gt;` element — blaming the fragment when the fault is the missing alternative set — and any later third shape breaks the exhaustiveness the whole scheme rests on.</drawback>
    </alternative>
    <alternative id="Two embed subcommands">
      Retire the single `embed` for a pair, `embed-alternatives` and `embed-recommendation`, each with its own positional argument, help text, precondition, refusal strings, and test file.
      <advantage>The shape is fixed at the call site and can never be omitted, and each subcommand&apos;s argparse help documents exactly one contract end to end.</advantage>
      <drawback>It puts two names in front of one write path whose wrapper-line, well-formedness, stray-text and `&lt;depends-on&gt;` checks are shared, introduces the first two-word subcommand into a vocabulary of single verbs (create, list, locate, lift, add, strip, embed, remove, walk, sort), and re-points every documentation surface that names `embed` today.</drawback>
    </alternative>
    <recommendation option="Explicit shape flag">Two shapes of one write are exactly what a mode flag expresses in this tool&apos;s existing style, and naming the shape lets each refusal cite the pass&apos;s expectation alongside the block&apos;s state while the preconditions it enforces — no `&lt;alternative&gt;` yet for the alternatives shape, alternatives but no `&lt;recommendation&gt;` for the recommendation shape, whose option is checked against the block&apos;s own ids — are what keep the alternative set frozen between passes; only if the recommendation pass ever had to write into a block with no alternatives would block-state selection win instead.</recommendation>
  </open-question>
  <open-question id="Alternatives list filter">
    <question>The parallel alternatives pass needs the ids of the blocks that carry no alternative elements, which the existing list --unannotated (no recommendation element) does not express. What is the new list filter called, and does --unannotated keep its current meaning?</question>
  </open-question>
  <open-question id="Inline embed ordering">
    <question>The inline recommendation pass reasons over the whole question set at once but must still write each block with one embed call, and embed resolves every depends-on tag against a sibling that already carries a recommendation. Does the skill order its embed calls so every declared target is written before its dependents, or does the tool relax the one-hop check for this pass?</question>
  </open-question>
  <open-question id="Recommendation run commit subject and body">
    <question>The inline recommendation pass commits once per run with its reasoning in the commit body. What is the subject (for example Recommendation-annotation: &lt;milestone_id&gt;), and what shape does the body take: one line per question in the lifted option-and-rationale form, or free prose covering the whole set?</question>
  </open-question>
  <open-question id="Alternatives pass commit subject and body">
    <question>/provide-alternatives-to-all-open-questions embeds and commits each return as it lands. What is its commit subject (for example Alternatives-annotation: &lt;Short Title&gt;), what if anything goes in the body given that lift prints only recommendation or single-alternative lines, and what is its one fixed terse success line?</question>
  </open-question>
  <open-question id="Stale alternatives in recommendation pass">
    <question>Alternatives are frozen, but a sibling&apos;s recorded decision can make an embedded alternative infeasible before the recommendation pass runs. When the inline pass judges a block&apos;s alternative set stale or dead, does it skip that block and print an advisory naming the full strip as the remedy, recommend the best surviving option and note the dead one in the rationale, or stop the run?</question>
  </open-question>
  <open-question id="Parallel dispatch host fallback">
    <question>Runtime prose names capabilities, never hosts, and not every host can run several subagent dispatches at once. How does /provide-alternatives-to-all-open-questions key its dispatch on that capability, does it fall back to sequential dispatch where concurrency is unavailable, and is there a cap on dispatches in flight?</question>
  </open-question>
  <open-question id="Alternatives return repair">
    <question>The current sweep judges every agent return by last-line verdict, then embed, then exactly one repair (a same-session continuation where the host can continue one, otherwise one fresh re-dispatch). Does the parallel alternatives pass keep that whole pipeline per return, with the shape reminder rewritten for an alternatives-only fragment whose anchors are the first &lt;alternative line and the last &lt;/alternative&gt; line?</question>
  </open-question>
  <open-question id="Discuss with frozen alternatives">
    <question>/discuss-open-question must work on a block in any state and reuse embedded alternatives when present. In that case may it surface an option the embedded set lacks, and how does it present the embedded recommendation when the block carries one: as its own starting recommendation to defend or challenge, or as context beside a fresh one?</question>
  </open-question>
  <open-question id="Shared procedure split">
    <question>recommend-procedure.md currently produces alternatives and recommendation as one unit and is referenced by the recommend agent and /discuss-open-question. With the alternatives agent needing only the enumeration half, the inline recommendation pass only the pick half, and discuss both, is the shared core split into two shared files or kept as one file with two separately referenceable steps?</question>
    <alternative id="Two shared files">
      Split the core into two shared procedures — an enumeration file that the alternatives agent and discuss-open-question follow, and a pick file that the inline recommendation pass and discuss-open-question follow (the pick half can keep the existing recommend-procedure.md path to limit reference churn) — each self-contained with its own grounding step.
      <advantage>Each runner loads only the half it acts on, and each grounding step names only that half&apos;s inputs: the principle store and the sibling-dependency disclosure duty are pick-half concerns, while the alternatives agent, dispatched in parallel before any sibling carries a recommendation, has none to lean on — and each half has two runners, so both clear the plugin&apos;s shared-means-two-or-more-runners bar.</advantage>
      <drawback>The genuinely common grounding discipline (prefer the live project over memory, read requirements.md and open_questions.xml whole for reasoning only) is stated in both files and can drift; discuss-open-question must reference both in order and say when the pick half runs against already-embedded alternatives, wording that depends on how &quot;Discuss with frozen alternatives&quot; settles.</drawback>
    </alternative>
    <alternative id="One file, two steps">
      Keep recommend-procedure.md as one file and let each caller name the steps it follows — the alternatives agent grounding plus enumeration, the inline pass grounding plus the pick, discuss-open-question both.
      <advantage>One grounding step with nothing duplicated, the smallest edit to a file three runners already reference, and a reader still sees the whole analytical arc in one place.</advantage>
      <drawback>Every runner loads the half it must not act on — the alternatives agent would read a recommend-one step it is forbidden to perform, exactly the bleed the two-pass split exists to prevent — and the per-half divergences (principle store, disclosure duty, frozen alternatives) become conditionals inside shared steps, against the runtime rule that a file holds only what the runner acting on it needs.</drawback>
    </alternative>
    <alternative id="Grounding plus two halves">
      Three shared files: one grounding procedure both halves reference, plus an enumeration file and a pick file that each add only their own analytical step.
      <advantage>No grounding text is duplicated anywhere while each half still states only its own work.</advantage>
      <drawback>Two hops of reference and three files for what is a 72-line procedure today, each runner still loading two of them so the load-minimality gain is small, and a grounding step alone is too thin to carry the Inputs contract every shared procedure here declares.</drawback>
    </alternative>
    <recommendation option="Two shared files">Two passes run by different runners at different times are two units of work, and their grounding genuinely diverges — the principle store and the sibling-disclosure duty belong to the pick half alone — so the halves are two texts rather than one duplicated; were the two grounding steps to come out word-for-word identical, the one-file option would win instead.</recommendation>
  </open-question>
  <open-question id="Existing recommendations on re-run">
    <question>The inline recommendation pass reasons over the whole question set at once. On a re-run where some blocks already carry a recommendation, does it leave those untouched and recommend only on the blocks lacking one, as today&apos;s --unannotated idempotency does, or may it revise an existing recommendation in light of the whole set?</question>
  </open-question>
  <open-question id="Headless chain alternatives line">
    <question>docs/ways-of-using-cairn.md chains invoke /recommend-all-open-questions three times at opus with effort xhigh or max, and the recommendation pass is meant for a strong model. Where does the new /provide-alternatives-to-all-open-questions line go in each chain, and with which model and effort values, given every line must be a concrete invocation that ran?</question>
  </open-question>
</open-questions>
