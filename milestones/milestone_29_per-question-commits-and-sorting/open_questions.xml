<open-questions>
  <open-question id="Report line when only the sort commits">
    <question>When a run embeds nothing but the final sort reorders the document and commits, which console line does the sweep print — the terse success line, the distinct nothing-annotated no-op line, or a third line — given that the success line reads as if recommendations were embedded?</question>
    <alternative id="Success line as is">
      Keep step 5 keyed on whether the run committed anything: a reorder-only run passed the ordering commit&apos;s guard, so it is a success path and prints the unchanged fixed line `Recommendations embedded.`, leaving the Question-ordering commit to git as its only record.
      <advantage>Nothing in the report step or the terse-reporting invariant changes — one success line, one no-op line — and the selection rule stays the simplest possible: any commit prints the success line, no commit prints the no-op line.</advantage>
      <drawback>The line is false on exactly the runs it now covers: under an every-run sort an all-annotated document the answer path left out of walk order reorders with zero dispatches, and a strip-then-all-skipped run reorders while every dispatched question is listed as skipped in the advisory printed right under it — so the console says recommendations were embedded when none were, and a user reading that line as the cue to run the answer sweep is misled.</drawback>
    </alternative>
    <alternative id="No-op line reworded">
      Treat the ordering commit as housekeeping outside the sweep&apos;s reportable outcome: a run in which no embed wrote prints the distinct nothing-annotated line — reworded from &quot;nothing changed, nothing committed&quot; to &quot;no recommendation was embedded&quot; plus its usual reason — whether or not the sort committed, leaving the Question-ordering commit to git.
      <advantage>It reports the one decision-critical fact — there is nothing new to answer — with the line already reserved for it, and the reorder, being recorded in git, fails the git-absent half of the advisory test and so earns no console mention by the plugin&apos;s own rule.</advantage>
      <drawback>It makes a committing run print the no-op line: every current form of that line says nothing was committed while a Question-ordering commit just landed, the invariant defines the line by the dirty-own-path guard having fired while one guard passed, and the rewording needed to keep it true — key it on the embed guard, drop &quot;nothing committed&quot; — turns it into a third line in all but name while blurring the no-op concept every other committing skill shares.</drawback>
    </alternative>
    <alternative id="Third fixed line">
      Add a second fixed terse success line for the sweep, `Questions reordered.`, printed when no embed wrote but the sort&apos;s Question-ordering commit landed; step 5 then picks by a strict order — an embed wrote: `Recommendations embedded.`; else the sort committed: `Questions reordered.`; else the no-op line with its reason — with the still-skipped advisory alongside whichever line prints.
      <advantage>Every line stays true and identifier-free, the success line keeps meaning &quot;there are new recommendations to answer&quot; and the no-op line keeps meaning &quot;nothing was committed&quot;, the reorder line reads like its own commit marker so the console matches the git log, and an all-skipped-but-reordered run prints a line the skipped-question advisory beneath it does not contradict.</advantage>
      <drawback>The sweep becomes the one skill with two success lines, so the terse-reporting invariant&apos;s &quot;exactly one fixed terse status line&quot; and the docs entry need a carve-out and a stated selection rule, and the string is one more fixed line to keep in sync across CLAUDE.md, docs/skill-reference.md, and both host trees.</drawback>
    </alternative>
    <alternative id="Broadened success line">
      Replace `Recommendations embedded.` with one line true of any committing run — `Open questions swept.` or similar — so step 5 keeps exactly two outcomes, the success line whenever either commit landed and the no-op line when neither did.
      <advantage>The two-line report step and its invariant survive untouched, and the selection rule stays &quot;did anything commit&quot;, with no per-skill carve-out and no third string.</advantage>
      <drawback>The line stops naming what the run did — the one thing a fixed line is for, as `Tasks derived.` and `Recommendations recorded.` show — so the user must open the git log to learn whether there is anything to answer, and the common embed case gives up its accurate line to serve the rare reorder-only one.</drawback>
    </alternative>
    <recommendation option="Third fixed line">With the sort running on every run, a reorder-only run is a real commit that neither existing line describes truthfully, and a second fixed line chosen by what the run committed — embed over reorder over nothing — keeps every line accurate, keeps the success line the cue that there is something to answer, and lets the skipped advisory sit under a line it does not contradict.</recommendation>
  </open-question>
</open-questions>
