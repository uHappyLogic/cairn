<open-questions>
  <open-question id="Inline-runner allowance form">
    <question>When the CLAUDE.md whole-file allowance is reworded to name the inline runners, does it enumerate the skills by name (a list that must be maintained as skills change) or describe them as a class (for example, user-invoked skills running inline in the conversation) that any future inline runner falls under without an edit?</question>
    <recommendation option="Describe as a class">The other layer-scoped invariants already speak of classes such as every file-changing user-invoked skill, and a class sits cleanly opposite the new dispatched-agent invariant so a future inline runner falls on the right side without an edit, whereas a name list or an example list goes silently stale with no build step or test reading CLAUDE.md, and the exclusion form hands the allowance to any consumer that is not an agent by default.</recommendation>
    <alternative id="Describe as a class">
      The allowance describes its holders as a class without naming any skill. For example, it covers a user-invoked skill running inline in the conversation, and a shared procedure followed by one, when it needs the whole set to reason over. Any present or future inline runner falls under it without an edit.
      <advantage>It needs no upkeep and matches how CLAUDE.md already words its other layer-scoped invariants, such as &quot;every file-changing user-invoked skill commits exactly its own paths&quot;. It also lines up with the dispatched-agent invariant it now sits beside, one class allowed and the other constrained, so the pair splits cleanly along the inline/isolated line and a new skill falls on one side by construction.</advantage>
      <drawback>It is less concrete. A reader can no longer see which runners actually use the allowance, and a shared procedure with both kinds of consumer, like alternatives-procedure.md, sits on the line and needs the class wording to handle it explicitly. The drawback also depends on how the question on the shared procedure&apos;s sibling-scope wording settles.</drawback>
    </alternative>
    <alternative id="Enumerate by name">
      The reworded allowance lists the six inline runners that state the whole read today by name (review-milestone-requirements, recommend-all-open-questions, answer-all-open-questions-with-recommendation, discuss-open-question, ask-in-milestone-context, modify-milestone-goal). It also names the shared procedures they follow, alternatives-procedure.md for discuss-open-question and recommend-procedure.md, which are covered through their inline consumers.
      <advantage>It is exact and can be checked. A maintainer can grep each named SKILL.md for its whole-read sentence and confirm the invariant matches the runtime one-to-one. Nothing is left to judgment about whether a borderline consumer qualifies, and CLAUDE.md already keeps hand-maintained enumerations of this kind, such as the skill pipeline list.</advantage>
      <drawback>The list has to be kept up to date by hand, and nothing enforces it. No build step or test reads CLAUDE.md, so a skill that is added, renamed, or retired, or one that gains or drops the whole read, makes the list silently wrong. A future inline runner that relies on the allowance would seem to break the invariant until someone edits the list.</drawback>
    </alternative>
    <alternative id="Class with current instances">
      The allowance is stated as a class rule, as in the class option, followed by a parenthetical naming today&apos;s inline runners as examples (&quot;currently …&quot;). The class decides who holds the allowance, and the names are there only to help a reader find them.
      <advantage>It keeps both strengths. The class wording means a new inline runner never breaks the invariant, and the named examples let a maintainer find the runtime sites that state the allowance without searching the tree.</advantage>
      <drawback>The example list still goes out of date with nothing to catch it. It is harmless to the rule&apos;s meaning, but it misleads anyone who reads it as complete. It also adds exactly the kind of mirrored detail the &quot;rationale only in CLAUDE.md&quot; principle tries to keep out of the invariants.</drawback>
    </alternative>
    <alternative id="Define by exclusion">
      The allowance stays unscoped but gains an exception clause. Any consumer except a dispatched agent may read the file directly for reasoning only. The inline runners are named implicitly as everything outside the new dispatched-agent invariant&apos;s scope.
      <advantage>It is the smallest wording change. It adds one clause that points at the new dispatched-agent invariant instead of defining a second class. The two invariants cannot drift apart because one is defined as the other&apos;s complement, so no consumer falls in neither category.</advantage>
      <drawback>It does not literally name the inline runners, as the milestone goal&apos;s wording (&quot;reworded to name the inline runners&quot;) asks. Any new kind of consumer that is neither an inline skill nor a dispatched agent, such as a headless chain step or a script, gets the whole-read allowance by default and never has to justify it.</drawback>
    </alternative>
  </open-question>
  <open-question id="Invariant reach over complete-task">
    <question>The new dispatched-agent invariant covers both dispatched agents: does it constrain only what a dispatch prompt carries (what the orchestrator holds and the agent cannot fetch), or also forbid a dispatched agent any whole read of a tool-owned document, and if the latter, how does it leave the complete-task agent&apos;s whole read of TASKS_TODO.md (a file no tool owns) untouched?</question>
    <depends-on question="Inline-runner allowance form" option="Describe as a class"/>
    <recommendation option="Read rule beside allowance">The ban belongs next to the allowance it narrows, inside the invariant that already governs the question document, where its scope to that one document is fixed by where it sits, so the dispatched-agent invariant stays a prompt-content rule true of both agents without qualification and complete-task&apos;s whole read of TASKS_TODO.md is never in reach; a tool-owned clause is empty for complete-task today and must dodge the test fixtures, and the narrowest-slice rule shifts meaning whenever a tool gains a subcommand.</recommendation>
    <alternative id="Read rule beside allowance">
      The dispatched-agent invariant covers only what a prompt carries. The reading rule instead goes into the sole-writer invariant, right beside the reworded whole-file allowance: inline runners may read the question document whole, and dispatched agents reach it only through the tool. That makes one boundary sentence scoped to the tool&apos;s own document, so it never reaches `TASKS_TODO.md`.
      <advantage>The ban sits next to the allowance it narrows and inside the invariant that already governs `open_questions.xml`. Its scope to that one document is therefore clear from where it sits. The complete-task agent is never mentioned and cannot be caught by it, and the new dispatched-agent invariant stays true of both agents without qualification.</advantage>
      <drawback>The rules on what a dispatched agent receives and what it may read end up in two different invariants (three, counting the return contracts). An editor changing an agent has to find and reconcile all of them. How cleanly the ban fits depends on how the allowance itself is worded, which is decided separately.</drawback>
    </alternative>
    <alternative id="Prompt content only">
      The new invariant covers only what a dispatch prompt carries: the orchestrator passes what it already holds and the agent cannot fetch itself. It says nothing about what an agent reads. The alternatives agent&apos;s ban on reading `open_questions.xml` whole lives only in that agent&apos;s own file, and in CLAUDE.md it follows from the reworded whole-file allowance, which names only the inline runners and so leaves dispatched agents out.
      <advantage>The rule holds for both dispatched agents as written, and nothing changes for complete-task. Its prompt already carries only the task&apos;s `##` heading, and its whole reads of `TASKS_TODO.md`, `requirements.md`, and the pointer are never touched. There is no carve-out to write, and the invariant matches the Goal&apos;s first sentence exactly.</advantage>
      <drawback>The reading half of the milestone has no stated reason anywhere. Dispatched agents are excluded only because the reworded allowance leaves them off its list, which an editor can easily miss. So a later edit can bring back the alternatives agent&apos;s whole read (or add a new agent that does one), and no invariant visibly forbids it.</drawback>
    </alternative>
    <alternative id="Tool-owned reads clause">
      The invariant has two clauses. One limits what a dispatch prompt carries. The other says a dispatched agent reaches a tool-owned document (the milestone&apos;s question document) only through the tool, never by reading it whole. The second clause is keyed on the document&apos;s owner, so files no tool writes, such as `TASKS_TODO.md`, `TASKS_DONE.md`, `requirements.md`, and `milestones/README.md`, are outside it by construction, with no named exception.
      <advantage>Both halves of the milestone get their reason recorded in one agent-scoped place. The complete-task agent needs no carve-out: its `TASKS_TODO.md` read is outside the clause because no tool owns that file, and the clause would still apply if a tool later took ownership of some other file.</advantage>
      <drawback>For complete-task the reading clause is empty today, since the completion procedure never opens the question document. So the claim that the invariant covers both agents is partly nominal. The clause also has to name the milestone&apos;s question document, not the filename `open_questions.xml`. Otherwise a completion task in this repo that edits `tests/fixtures/*/open_questions.xml` would read as a violation.</drawback>
    </alternative>
    <alternative id="Narrowest-slice principle">
      The invariant covers the prompt and adds a general reading rule: a dispatched agent fetches the narrowest slice a tool offers and reads whole only what no tool can slice. `TASKS_TODO.md` and `requirements.md` are left alone because no tool slices them. The question document is covered because `locate` and the new list flag slice it.
      <advantage>It states the reason behind the whole milestone, keeping the agent&apos;s context small, as one general rule. It also extends to future tools without an edit: if a task-list tool later offers a per-task slice, the complete-task agent comes under the rule automatically.</advantage>
      <drawback>What the rule allows changes silently whenever a tool gains or loses a subcommand. So it cannot be checked against the text alone, and a new tool feature could quietly make an existing whole read in complete-task or the completion procedure a violation. It also pushes a reader to ask whether `requirements.md` should be sliced too, which the Goal explicitly keeps as a whole read.</drawback>
    </alternative>
  </open-question>
</open-questions>
