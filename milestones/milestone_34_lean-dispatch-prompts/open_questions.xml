<open-questions>
  <open-question id="Question-text list line shape">
    <question>What is the new list flag called, and what line shape does it print for each block — how are the id and the folded question text separated on one line so that an id containing spaces, punctuation, or the separator itself stays unambiguous to a reader?</question>
  </open-question>
  <open-question id="Question-text flag with filters">
    <question>Does the new question-text flag combine with the existing --without-alternatives and --without-recommendation filters (printing the filtered blocks with their text), or is it refused alongside them so the flag stays a single-purpose whole-set listing?</question>
    <alternative id="Compose with filters">
      The question-text flag changes only the line shape of `list`. The `--without-alternatives` and `--without-recommendation` filters decide which blocks print exactly as they do now, alone or together, so `list --without-alternatives` with the new flag prints each bare block&apos;s id beside its folded question text.
      <advantage>It keeps `list` one subcommand in which each option does one independent job. `cmd_list` changes in one spot (what it prints, not which blocks), no new refusal path is added to the error contract, and the three places the contract is stated only need the flag described as a shape switch. An inline runner that later wants the filtered set with its text can have it without another tool change.</advantage>
      <drawback>The only consumer the milestone&apos;s goal names, the dispatched agent&apos;s sibling scope, wants the whole unfiltered set. That makes the combined forms a surface with no current consumer that still needs its own tests in `tests/test_list.py`, both filters crossed with the flag, to stay covered.</drawback>
    </alternative>
    <alternative id="Refuse alongside filters">
      The question-text flag is only for listing the whole set. Passing it with either `--without-alternatives` or `--without-recommendation` is refused as a usage error that exits non-zero and prints nothing, while the two filters stay freely combinable with each other.
      <advantage>The flag&apos;s contract stays as narrow as its one purpose: every block&apos;s id and question text, for sibling scope. A caller can&apos;t ask for a filtered text listing that nobody designed or needs, so the flag can&apos;t gradually become a second way to gather filtered sets.</advantage>
      <drawback>It adds a refusal the tool doesn&apos;t have today. The rule is that the flag excludes both filters while the filters still combine with each other. A single argparse mutually exclusive group can&apos;t express that, so it needs a hand-written check in `cmd_list` that follows the error contract. It also needs refusal tests and a note in the docstring, the help text, and the error contract. If an inline runner later needs filtered text, the refusal has to be lifted in a further tool change.</drawback>
    </alternative>
  </open-question>
  <open-question id="Shared procedure sibling-scope wording">
    <question>The shared alternatives procedure&apos;s grounding step tells its reader to read open_questions.xml whole with the file-reading tool, and it is followed by both the dispatched agent (which must not) and the inline discuss-open-question skill (which may): does the shared step keep a runner-neutral wording that names the sibling scope without prescribing how it is obtained, leaving each consumer to state its own source, or does it carry both instructions?</question>
  </open-question>
  <open-question id="Sibling locate allowance">
    <question>With sibling scope supplied as one id-and-question line per block, may the dispatched agent still run locate on a sibling block (to see alternatives an earlier pass embedded on it) when it judges that bears on the boundary between two questions, or is its reading of the question document limited to its own block and the list print?</question>
    <alternative id="Own block and list only">
      The dispatched agent reads the question document only through two tool calls: one `locate` of its own Short Title, and one run of the new question-text `list` flag for sibling scope. It never runs `locate` on a sibling. The agent file states this limit once, as its only source of sibling scope.
      <advantage>This fits the Goal&apos;s rule that the agent fetches its sibling scope with the new list flag. The agent&apos;s context stays exactly the size the milestone is trying to reach. It also keeps the existing rule that siblings supply scope and nothing more: the agent never sees a sibling&apos;s `&lt;recommendation&gt;` or `&lt;depends-on&gt;` pick, so it cannot start treating that pick as settled. The behaviour is the same on every run, whether or not an earlier pass annotated a sibling.</advantage>
      <drawback>Sometimes a boundary can&apos;t be read from question text alone, for example when two questions are worded so they overlap and only a sibling&apos;s embedded options show where it actually ends. In that case the agent may list an option that belongs to the sibling, or leave out a real one. That weakens the set, and the set has to be complete now because it outlasts later decisions. How often this happens depends on how unambiguous the list print&apos;s id-and-question line turns out to be.</drawback>
    </alternative>
    <alternative id="Sibling locate at judgment">
      The agent may `locate` any sibling block whenever it judges that the sibling&apos;s embedded content bears on the boundary between the two questions. Everything the agent reads stays read-only and is used for reasoning only. Its default source of sibling scope is still the list print.
      <advantage>The agent gets the most accurate view of the boundary when it needs one. A sibling already annotated by an earlier alternatives pass (for example, before a review pass added this question) shows exactly which options it covers, so they aren&apos;t duplicated here. The cost falls only on the runs that need it, since in a single sweep most siblings are still bare and a `locate` would add nothing.</advantage>
      <drawback>The limit is left to prose judgment, so how much of the question document an agent reads varies from run to run and can&apos;t be checked mechanically. A located sibling also prints any `&lt;recommendation&gt;` and `&lt;depends-on&gt;` it carries, which invites exactly the assumption about how a sibling settles that the enumeration is told never to make. In a parallel sweep, a sibling another agent is embedding at that moment may appear bare or annotated depending on timing, so the same agent could produce different results on different runs.</drawback>
    </alternative>
    <alternative id="Alternatives-only sibling locate">
      The agent may `locate` a sibling only if that sibling already carries alternatives, meaning the `--without-alternatives` list doesn&apos;t print it. It reads only that sibling&apos;s `&lt;alternative&gt;` elements as scope and explicitly ignores any recommendation or dependency elements on the block.
      <advantage>The only thing a sibling locate can usefully add is embedded options that mark where the sibling ends, and this rule allows just that. The agent never reads a sibling&apos;s pick, so the rule that siblings are never assumed settled holds. Locates on bare siblings, which would only repeat the list print, are ruled out.</advantage>
      <drawback>The rule is more complicated. It needs an extra `list --without-alternatives` call, or a check that this depends on, and two conditions stated in the agent&apos;s prose. Ignoring the pick elements is still prose discipline: they are printed into the agent&apos;s context anyway, and nothing mechanical stops them from influencing it. The tool can&apos;t print alternatives alone, so this would need a new tool option, and the milestone has recorded no decision to add one.</drawback>
    </alternative>
  </open-question>
  <open-question id="Inline-runner allowance form">
    <question>When the CLAUDE.md whole-file allowance is reworded to name the inline runners, does it enumerate the skills by name (a list that must be maintained as skills change) or describe them as a class (for example, user-invoked skills running inline in the conversation) that any future inline runner falls under without an edit?</question>
  </open-question>
  <open-question id="Invariant reach over complete-task">
    <question>The new dispatched-agent invariant covers both dispatched agents: does it constrain only what a dispatch prompt carries (what the orchestrator holds and the agent cannot fetch), or also forbid a dispatched agent any whole read of a tool-owned document, and if the latter, how does it leave the complete-task agent&apos;s whole read of TASKS_TODO.md (a file no tool owns) untouched?</question>
  </open-question>
</open-questions>
