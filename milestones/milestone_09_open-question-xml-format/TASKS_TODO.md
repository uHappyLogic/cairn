# TASKS TODO

## Rewire Recommend Sweep Orchestrator To XML

Rewrite the `recommend-all-open-questions` skill — the sole document mutator of the recommend sweep, which gathers the open/deferred questions, dispatches the read-only `recommend-open-question` subagent per question, embeds each returned recommendation, and stages (never commits) — so it operates on the new `<open-question>` XML blocks instead of the one-line Markdown blockquote header and its `>`-run sub-block. This is the orchestrator half of the recommendation-render conversion; the paired `Render Recommendation As XML Sub-Elements` task produces the exact XML sub-elements this orchestrator embeds.

**Notes:**
- The edit to `skills/recommend-all-open-questions/SKILL.md` must be made through the `skill-creator:skill-creator` skill, not by editing the SKILL.md file directly.
- The XML contract, CLI idiom, and embedding mechanism are already decided — do not re-invent them or reach for `xmllint`/an XML processor. Read and follow these `requirements.md` `## Decisions` subsections: **CLI query tooling** (line-oriented `awk`/`sed`/`grep` keyed on the `<open-question …>` / `</open-question>` boundary lines) for the gather; **Question-block placement** (all blocks live under the single `## Open questions` section — this is exactly the "one bounded section the CLI slices deterministically" case the gather relies on); **Recommendation embedding mechanism** (embed by reading the target block and rewriting it whole via an exact-string structural Edit against the document the sweep already holds from its single gather pass — deliberately **not** splicing via the line-oriented CLI, which stays scoped to locate/extract/remove); **Block indentation** (2-space per nesting level for the inserted child elements); **Boundary-line tag contract** (extract `id`/`status` by attribute-name-anchored regex, attribute-order-independent); and **Open/Deferred encoding** (both types share one `<open-question …>` / `</open-question>` boundary-token pair distinguished by `status`, so the gather ignores type).
- The idempotency skip moves off the `> **Recommendation:**` anchor: a block already carries a recommendation when it **contains a `<recommendation>` element**. The stale-recommendation escape hatch becomes "delete that block's embedded sub-elements (the `<alternative>` / `<applied-principle>` / `<recommendation>` children), leaving the `<open-question>` wrapper and its `<question>` element intact, then re-run" — still no `refresh`/selectable mode, still argument-free.
- The orchestrator embeds exactly the sub-elements the `recommend-open-question` subagent returns (the paired render task's output: `<alternative>` … optional `<applied-principle>` … `<recommendation>`) — it never authors the `<open-question>` wrapper or `<question>` element, which the block-author path owns; it inserts the returned children inside the existing wrapper.
- All the sweep's simplifications and their rationale are **retained**, only re-vocabularized: the subagent dispatch protocol (one per surviving question, independent, parallelizable, prompt carrying Short Title + context), the no-gather-order / no-outer-re-gather / no-clean-working-tree simplifications (the question set never shrinks under an annotate-only sweep), the path-scoped stage-and-stop (`git add <MILESTONE_DIR>/requirements.md`, never `-A`, no commit), and the final report format all stay — with wording updated from the blockquote vocabulary (contiguous `>` run, unchanged one-line greppable header, empty-`>` separation) to the XML block vocabulary.

**Success:**
- The gather step (step 1) describes fetching the open/deferred set via the line-oriented boundary-line CLI — deterministic extraction of every `<open-question …>` block (its `id`, `status`, and question text) keyed on the `<open-question …>` / `</open-question>` boundary lines within the single `## Open questions` section — rather than reading for `> **Open question — …` / `> **Deferred — …` blockquote headers.
- The idempotency skip (step 2) keys on the presence of a `<recommendation>` element in the block, replacing the `> **Recommendation:**` anchor check.
- The escape hatch describes deleting the block's embedded sub-elements (`<alternative>` / `<applied-principle>` / `<recommendation>`), leaving the `<open-question>` wrapper and `<question>` element intact, then re-running — and the skill stays argument-free with no `refresh`/selectable mode.
- The embed step (step 4) describes locating the target `<open-question id="...">…</open-question>` in the document the sweep already holds and replacing it whole via an exact-string structural Edit, with the returned child elements inserted at 2-space indentation — and explicitly states this is **not** a line-oriented CLI splice.
- The post-embed block example matches the decided XML shape: an `<open-question id="..." status="...">` wrapper containing `<question>`, one or more `<alternative id="...">` (each with `<advantage>`/`<drawback>`), zero or more `<applied-principle>`, and a `<recommendation option="...">`.
- The subagent dispatch protocol is retained (one read-only `recommend-open-question` per surviving question, independent/parallelizable, prompt carrying Short Title + context), and the orchestrator remains the sole document mutator.
- The stage-and-stop semantics are unchanged: path-scoped `git add <MILESTONE_DIR>/requirements.md` (never `-A`), no commit, no clean-working-tree precondition, and the no-gather-order / no-re-gather rationale is preserved.
- No `>`-blockquote / contiguous-`>`-run / empty-`>`-separation / `> **Recommendation:**` anchor / one-line-greppable-header vocabulary remains anywhere in the skill (description, workflow, or rules).

---

## Rewire Discuss-Open-Question Locate To CLI

Rewrite the locate step (step 1) of the `discuss-open-question` skill — the interactive per-question deliberation skill that finds a named question, grounds in the live project, runs the shared `recommend-procedure` core inline, and adds its own "what would change your mind" layer — so it fetches the named question through the milestone's decided line-oriented CLI idiom instead of reading the whole file and matching a Markdown blockquote header. This is the deterministic-locate side of the query-where-it-pays convention for the interactive path; the skill's broader grounding stays whole-document reading, and the recommend-render / answer-path rewires are separate tasks.

**Notes:**
- The edit to `skills/discuss-open-question/SKILL.md` must be made through the `skill-creator:skill-creator` skill, not by editing the SKILL.md file directly.
- The XML contract and CLI idiom are already decided — do not re-invent them or reach for `xmllint`/an XML processor. Read and follow these `requirements.md` `## Decisions` subsections: **CLI query tooling** (line-oriented `awk`/`sed`/`grep` keyed on the `<open-question …>` / `</open-question>` boundary lines, never a real XML processor), **id match case-sensitivity** (case-fold both the queried Short Title and the block's `id` before comparing — preserving today's case-insensitive locate), **Boundary-line tag contract** (extract `id` by attribute-name-anchored regex like `id="([^"]*)"`, attribute-order-independent), **XML special-char escaping** (the stored `id` is entity-escaped, so handle the five predefined entities when comparing), **Open/Deferred encoding** (one `<open-question>` element with `status`, so open and deferred blocks share one boundary-token pair and status is irrelevant to the locate), and **Question-block placement** (all blocks live under the single `## Open questions` section — the one bounded region the CLI enumerates deterministically).
- The locate must pull the whole matched block as the question context the deliberation runs on — the `<question>` text plus any already-embedded `<alternative>` / `<applied-principle>` / `<recommendation>` sub-elements — not just the `id`, so the inline `shared/recommend-procedure.md` run has the full question to work from.
- This is a **deterministic-locate-only** change. The skill's step-2 grounding (reading `requirements.md` and the live source files that bear on the question, per the shared core) is reason-across work and stays whole-document reading — do not convert it to CLI. Everything downstream of the locate is untouched: the `shared/recommend-procedure.md` reference (never restated), the conversational framing and the "what would change your mind" layer, the edits-nothing rule, and the on-decision offers of `/answer-open-question` and `/modify-milestone-goal` (including the run-`/modify-milestone-goal`-first ordering when both apply).

**Success:**
- Step 1 (locate) describes a concrete CLI operation that finds the `<open-question>` block by case-folded `id` match, extracting the `id` by attribute-name-anchored regex (attribute-order-independent) and accounting for the stored `id` being entity-escaped when comparing, and pulls the whole matched block (its `<question>` text plus any embedded `<alternative>` / `<applied-principle>` / `<recommendation>` sub-elements) as the question context.
- Open and deferred blocks are located uniformly through the single `<open-question …>` / `</open-question>` boundary-token pair, with no type-specific branch.
- The mismatch path lists the available titles by CLI-enumerating the `id` of every `<open-question>` block in the `## Open questions` section, replacing the read-and-match-the-header error path.
- The step-2 grounding still reads `requirements.md` and the bearing source files whole (not via CLI), and the inline `shared/recommend-procedure.md` reference, the "what would change your mind" layer, the edits-nothing rule, and the `/answer-open-question` + `/modify-milestone-goal` on-decision offers (with the modify-first ordering) are unchanged.
- No `>`-blockquote / `Open question — …` / `Deferred — …` blockquote-header vocabulary remains anywhere in the skill (description, workflow, or rules); any residual block-description wording is the `<open-question>` XML vocabulary.

---

## Sync Answer Wrappers To XML Vocabulary

Sync the three thin wrappers over the reworked shared answer procedures — the `answer-open-question` skill (`skills/answer-open-question/SKILL.md`), the `answer-open-question-with-recommendation` skill (`skills/answer-open-question-with-recommendation/SKILL.md`), and the `answer-open-question-with-recommendation` agent (`agents/answer-open-question-with-recommendation.md`) — to the new `<open-question>` XML format. These wrappers delegate all locate/lift/record mechanics to `shared/answer-procedure.md` and `shared/answer-with-recommendation-procedure.md` (rewired to the CLI/XML idiom by the earlier `Rewire Answer-Procedure Locate And Remove To CLI` and `Rewire Recommendation-Lift To CLI XML Extract` tasks), so their own change is a vocabulary-and-contract sync of their prose, not a logic rewire. This completes the answer-path half of the Markdown→XML conversion.

**Notes:**
- All three edits **must be made through the `skill-creator:skill-creator` skill**, not by editing the `SKILL.md`/agent files directly — these are `skills/`+`agents/` files, so the milestone's skill-creator authoring constraint applies.
- The XML contract is already decided — do not re-invent it. Ground the reworded prose in these `requirements.md` `## Decisions` subsections: **Recommendation lift mapping** (the lifted answer is the `<recommendation>` element's `option` attribute recombined with its text as "`<option>` — `<rationale>`", `option` referencing the winning `<alternative id>`), **XML special-char escaping** (entities are reversed when the recommendation is lifted into `## Decisions` prose, so the commit-body content is clean un-escaped text), **Applied-principle placement** (`<applied-principle>` is a sibling of `<recommendation>`, so the lifted content stays provenance-free), **Open/Deferred encoding** (`Open question` / `Deferred` entries are one `<open-question>` element distinguished by `status="open"|"deferred"`), and **Boundary-line tag contract** / **id match case-sensitivity** (the block is located by case-folded `id`).
- Concrete vocabulary substitutions: a question's "blockquote run", "one-line header", and the `> **Recommendation:**` anchor become the `<open-question>` block and its `<recommendation option="...">` element; the with-recommendation skill's and agent's guard descriptions (clean-stop when no block matches the Short Title, or the matched block carries no recommendation) reference the **no-`<recommendation>`-element guard**; the commit-body wording that describes the lifted content still describes the "`<option>` — `<rationale>`" answer text, now derived from the `option` attribute plus element text with entities un-escaped; the with-recommendation skill's clean-stop-and-point message still directs the user to run `/recommend-all-open-questions` first or record a literal answer via `/answer-open-question`. In `answer-open-question`, any reference to the located `Open question` / `Deferred` entry reads as the `<open-question status="open|deferred">` block.
- `answer-open-question` carries almost no blockquote/anchor vocabulary already (it delegates locate/remove wholesale), so its sync is light — but it must still be reviewed for stale entry-noun phrasing and confirmed clean.
- Behavior that must **not** change (byte-level-equivalent in effect to before): `answer-open-question`'s first-`.` arg parsing, its retired-sentinel redirect guard (exact whole-string `record the recommendation` match after trim+lowercase, never a substring — no milestone resolution, no file I/O, no recording), its literal-answer path, and its `Manual-answer: <Short Title>` commit subject/rules; the with-recommendation pair's inline-vs-isolated split (the skill runs the shared procedure inline and **never** spawns the agent; the agent runs it in isolation); the agent's `DONE`/`FAILED` return protocol; both committers' path-scoped `git add <MILESTONE_DIR>/requirements.md` (never `-A`) staging, their `Recommendation-answer: <Short Title>` subject, and the commit-only-when-the-answer-was-actually-recorded condition.
- The shared procedures stay **referenced via `${CLAUDE_PLUGIN_ROOT}`, never restated** — do not fold any locate/lift/remove/fold/cascade step back into a wrapper.

**Success:**
- No stale blockquote/anchor vocabulary — `> **Recommendation:**`, "contiguous `>`-run" / "`>`-prefixed run", "one-line (greppable) header", empty-`>` separation — remains anywhere in any of the three files (description/front-matter, workflow, or rules).
- The `answer-open-question-with-recommendation` skill and agent describe the answered thing as an `<open-question>` block and its lifted recommendation as the `<recommendation option="...">` element; their guard prose references the no-`<recommendation>`-element (and no-matching-block) clean stop.
- Both committers' commit-body wording describes the lifted "`<option>` — `<rationale>`" content as derived from the `<recommendation>` element's `option` attribute plus text with entities un-escaped.
- The with-recommendation skill's clean-stop-and-point message still points to `/recommend-all-open-questions` and to a literal `/answer-open-question` answer.
- `answer-open-question`'s first-`.` parsing, the exact-whole-string `record the recommendation` redirect guard (no file I/O, records nothing), the literal-answer path, and the `Manual-answer:` subject/commit-condition rules are all unchanged in behavior.
- The with-recommendation pair's inline-vs-isolated split is intact (skill never spawns the agent; agent keeps its `DONE`/`FAILED` protocol), and both committers keep path-scoped staging, the `Recommendation-answer:` subject, and the commit-only-when-recorded condition.
- All three files still reference the shared procedures (`shared/answer-procedure.md` and, for the pair, `shared/answer-with-recommendation-procedure.md`) via `${CLAUDE_PLUGIN_ROOT}` and restate none of their steps.

---

## Rewire Answer Sweep Orchestrator To XML

Rewrite the `answer-all-open-questions-with-recommendation` skill — the batch sweep that gathers every recommendation-bearing open/deferred question and dispatches the file-editing `answer-open-question-with-recommendation` agent strictly sequentially (one commit per answer, the orchestrator never editing or committing itself) — so its gather (step 1) and per-dispatch live re-check (step 2a) operate on the new `<open-question>` XML blocks via the milestone's decided line-oriented CLI idiom instead of the one-line Markdown blockquote header and its `> **Recommendation:**` anchor. This is the answer-sweep orchestrator half of the Markdown→XML conversion; the paired `answer-open-question-with-recommendation` skill+agent (rewired by `Sync Answer Wrappers To XML Vocabulary`) are what this orchestrator dispatches, and the `shared/answer-with-recommendation-procedure.md` lift they run is rewired by `Rewire Recommendation-Lift To CLI XML Extract`.

**Notes:**
- The edit to `skills/answer-all-open-questions-with-recommendation/SKILL.md` must be made through the `skill-creator:skill-creator` skill, not by editing the SKILL.md file directly.
- The XML contract and CLI idiom are already decided — do not re-invent them or reach for `xmllint`/an XML processor. Read and follow these `requirements.md` `## Decisions` subsections: **CLI query tooling** (line-oriented `awk`/`sed`/`grep` keyed on the `<open-question …>` / `</open-question>` boundary lines, never a real XML processor) for both the gather and the re-check; **Question-block placement** (all blocks live under the single `## Open questions` section — the one bounded region the gather slices deterministically); **Boundary-line tag contract** (extract `id`/`status` by attribute-name-anchored regex like `id="([^"]*)"`, attribute-order-independent); and **Open/Deferred encoding** (both `Open question` and `Deferred` entries share one `<open-question …>` / `</open-question>` boundary-token pair distinguished by `status`, so the gather ignores type).
- Gather becomes: fetch every `<open-question>` block that **contains a `<recommendation>` element** — deterministic boundary-line CLI extraction of each block's `id` (and `status`) within the `## Open questions` section — replacing the "carries an embedded `> **Recommendation:**` anchor in its blockquote run" filter. Recommendation-less blocks (no `<recommendation>` element) are still **never gathered** — annotating them stays the recommend sweep's job (`/recommend-all-open-questions`).
- The step-2a live re-check becomes a cheap deterministic CLI check that the block with that `id` **still exists and still contains a `<recommendation>` element** (a prior answer's cascade may have removed it) — the deterministic-locate side of query-where-it-pays, replacing the re-read-and-match-the-anchor check. This is a locate/extract check only; it does not read the whole document.
- Orchestration semantics that must **not** change — retain them, only re-vocabularized from blockquote/anchor to the `<open-question>`/`<recommendation>` XML vocabulary: gather once; order loosely most-significant-first (cascade-parent-first proxy, a sequencing heuristic only that never decides *whether* a question is answered); walk the order exactly once with **no** outer re-gather loop; dispatch the agent **strictly sequentially, never in parallel** (every dispatch mutates the same `requirements.md`); the **agent** owns all mutation and its own path-scoped `Recommendation-answer: <Short Title>` commit while the **orchestrator never edits `requirements.md` and never commits**; **no** clean-working-tree precondition; stop-and-report on an agent `FAILED` or missing/ambiguous return with **no** fallback recording; and the final report format (how many recorded, point at the per-answer commits, one-sentence nothing-recorded case, no enumeration of untouched questions).
- The dispatch prompt still passes the question's **Short Title**; the agent's `DONE`/`FAILED` return protocol and the treat-ambiguous-as-`FAILED` rule are unchanged.

**Success:**
- The gather step (step 1) describes fetching the recommendation-bearing set via the line-oriented boundary-line CLI — deterministic extraction of every `<open-question …>` block that contains a `<recommendation>` element (with its `id`/`status`), keyed on the `<open-question …>` / `</open-question>` boundary lines within the single `## Open questions` section — replacing the "reads the whole file and gathers entries carrying a `> **Recommendation:**` anchor" filter.
- Recommendation-less blocks (no `<recommendation>` element) are still never gathered, and the "if none carries a recommendation, say so and stop" path is retained keyed on the `<recommendation>`-element presence.
- The step-2a re-check describes a cheap deterministic CLI check that the block with that `id` still exists and still contains a `<recommendation>` element (skip-and-continue when a cascade already removed it), replacing the re-read-and-match-the-`> **Recommendation:**`-anchor check.
- The block `id` used for both gather and re-check is extracted by attribute-name-anchored regex (attribute-order-independent), and open/deferred blocks are handled uniformly through the single `<open-question …>` / `</open-question>` boundary-token pair with no type-specific branch.
- The gather-once / order-loosely-most-significant-first / walk-once / no-outer-re-gather-loop rules read unchanged in effect.
- The strictly-sequential-never-parallel dispatch, the agent-owns-all-mutation-and-its-own-`Recommendation-answer:`-commit / orchestrator-never-edits-or-commits split, and the no-clean-working-tree-precondition rules read unchanged.
- The stop-and-report-on-`FAILED`-or-ambiguous-return behavior with no fallback recording, and the final report format, are unchanged.
- No `>`-blockquote / contiguous-`>`-run / `> **Recommendation:**` anchor / one-line-(greppable)-header vocabulary remains anywhere in the skill (description, workflow, or rules); residual block-description wording is the `<open-question>` / `<recommendation>` XML vocabulary.

---

## Sync CLAUDE.md Invariants To XML Format

Re-sync every `CLAUDE.md` passage that describes the old open-question representation to the new `<open-question>` XML block format and the query-where-it-pays convention, so the plugin's own per-skill/-procedure/-agent invariants match the skills as shipped by this milestone's earlier tasks. This is the documentation-sync half of the Markdown→XML conversion for `CLAUDE.md`; only format- and convention-describing passages change, and the architectural invariants themselves are preserved verbatim in meaning.

**Notes:**
- `CLAUDE.md` is a plain documentation file edited **directly** with `Edit` — the milestone's `skill-creator` authoring constraint applies only to `skills/`+`agents/` files, not to `CLAUDE.md`.
- The XML contract is already decided — do not re-invent it. Ground every rewrite in this milestone's `requirements.md` `## Goal` and `## Decisions` (which pin all contracts), and also verify each rewritten invariant against the **actually-updated** `skills/`/`shared/`/`agents/` files, since those land first (the earlier tasks in this milestone): the invariant prose must match the file it describes.
- Concrete stale claims to replace: the one-line greppable blockquote header and contiguous-`>`-run vocabulary (including `shared/answer-procedure.md`'s "removes the entire contiguous `>`-prefixed run" invariant); the empty-`>` internal-separation discipline; the `> **Recommendation:**` anchor and its last-line / lifted-verbatim contract; the `> **Applied principle:**` citation-line stacking; and the recommend-sweep's "embed beneath the unchanged one-line header" description.
- Their replacements describe: the `<open-question id status>` block under the single `## Open questions` section (boundary-line tag contract, id-first single-line opening tag, `status="open"|"deferred"` encoding, 2-space indentation, uniform entity-escaping); the `<alternative>` / `<advantage>` / `<drawback>` children; sibling `<applied-principle>` elements (provenance-free lift by construction); the `<recommendation option>` element and its "`<option>` — `<rationale>`" lift mapping with reverse entity-substitution; the line-oriented `awk`/`sed`/`grep` CLI (never `xmllint`) keyed on boundary lines with case-folded `id` matching; the whole-block-replacement embedding idiom; and the query-where-it-pays convention (CLI for deterministic locate/extract/remove; whole-document reads for cascade analysis and reconciliation).
- Scope discipline — do **not** touch the non-format architectural invariants: who commits vs. stages, who mutates, the inline-vs-isolated skill/agent splits, the single-source-of-truth shared-procedure references, the `Manual-answer:` / `Recommendation-answer:` capture-grep boundaries, and the orchestration-sequencing rules are all preserved verbatim in meaning. The out-of-scope notes (no migration; `migrate-workspace` untouched) stay as they are.

**Success:**
- `grep`ing `CLAUDE.md` for the old vocabulary — `> **Recommendation:**`, `> **Applied principle:**`, `` contiguous `>` ``, `blockquote`, `greppable header`, `` empty `>` `` — surfaces no stale format claims.
- Every invariant statement about locate / remove / lift / embed / authoring matches the corresponding updated `skills/`/`shared/`/`agents/` file (e.g. `answer-procedure` removal now describes deleting the `<open-question …>` … `</open-question>` block; the recommend sweep now describes whole-block-replacement embedding of `<alternative>`/`<applied-principle>`/`<recommendation>` children).
- The XML-format vocabulary is present where the old format vocabulary was: `<open-question>` block under `## Open questions`, `status="open"|"deferred"`, `<recommendation option>` with the "`<option>` — `<rationale>`" lift, sibling `<applied-principle>`, the boundary-line CLI idiom, and the query-where-it-pays convention.
- The non-format architectural invariants (who commits, who mutates, inline-vs-isolated splits, single-source-of-truth procedure references, capture-grep boundaries, orchestration sequencing) are unchanged in meaning, and the out-of-scope notes (no migration, `migrate-workspace` untouched) are unchanged.

---

## Sync README To XML Format

Re-sync every `README.md` passage that describes the old open-question representation to the new `<open-question>` XML block format and the query-where-it-pays convention, so the public, adoption-facing workflow documentation matches the skills as shipped by this milestone's earlier tasks. This is the documentation-sync half of the Markdown→XML conversion for `README.md`; only the format-describing passages and the *Iterating milestone requirements* Mermaid diagram change, and the overall workflow structure, other phase diagrams, and non-format skill descriptions stay as they are.

**Notes:**
- `README.md` is a plain documentation file edited **directly** with `Edit` — the milestone's `skill-creator` authoring constraint applies only to `skills/`+`agents/` files, not to `README.md`.
- The XML contract is already decided — do not re-invent it. Ground every rewrite in this milestone's `requirements.md` `## Goal` and `## Decisions` (which pin all contracts), and also verify each rewritten passage against the **actually-updated** `skills/`/`shared/`/`agents/` files, since those land first (the earlier tasks in this milestone): the README prose must match the files it describes.
- The known touchpoints, all of which today describe stale format details:
  - The **`### Iterating milestone requirements`** section (around line 102) — its prose (which describes the "embedded Alternatives + Recommendation block" annotate/lift flow) and its Mermaid diagram. The section and diagram must accurately depict the XML-block flow (author → recommend/annotate → lift/answer) as shipped; no stale blockquote/anchor vocabulary in the prose or the diagram edge labels.
  - The **`### The answer-principle-learning loop`** prose — its **Recommendation advisory** bullet describes the agent rendering each bearing principle as a `> **Applied principle:** <Short Title>` line above the `> **Recommendation:**` anchor.
  - The per-skill reference entries for the open-question-handling skills wherever they describe the format: **`review-milestone-requirements`** (convergence keys on `Open question` blocks remaining → `status="open"` blocks), **`recommend-all-open-questions`** (the "unchanged one-line question header / greppable on line 1" embed-beneath-the-header mechanism, the sub-block embedding, and the "skips any block already carrying a `> **Recommendation:**` anchor" idempotency), **`recommend-open-question` (subagent)** (its returned "recommendation sub-block" whose `> **Recommendation:** …` anchor is lifted), **`discuss-open-question`**, **`answer-open-question`**, **`answer-open-question-with-recommendation`** (lifts the `> **Recommendation:** <chosen option> — <rationale>` anchor embedded beneath the question's header), and **`answer-all-open-questions-with-recommendation`** (gathers questions "carrying an embedded recommendation").
- Replacements describe: the `<open-question id status>` XML block under the single `## Open questions` section (its `<question>` / `<alternative>` (with `<advantage>`/`<drawback>`) / `<applied-principle>` / `<recommendation>` sub-elements, `status="open"|"deferred"` encoding, boundary-line tag contract, entity-escaping); the `<recommendation option="...">` element and its "`<option>` — `<rationale>`" lift with reverse entity-substitution; sibling `<applied-principle>` elements (provenance-free lift by construction); the line-oriented `awk`/`sed`/`grep` CLI (never `xmllint`) keyed on the boundary lines with case-folded `id` matching for deterministic locate/extract/remove; the whole-block-replacement embedding idiom the recommend sweep uses; and the query-where-it-pays convention (CLI for deterministic locate/extract/remove versus whole-document reads for reasoning-across operations like cascade analysis and reconciliation). It should also convey the deliberate trade the goal states: question blocks become raw structured data that no longer renders as clean Markdown, while the `## Goal` / `## Relevant starting state` / `## Decisions` / `## Out of Scope` sections stay prose Markdown — traded for deterministic CLI queryability and future UI-parseability.
- Scope discipline — only the format-describing passages and the *Iterating milestone requirements* diagram change. The overall workflow structure, the other phase Mermaid diagrams (setup, milestone init, derivation/completion, follow-up, ending), and the non-format skill descriptions (who commits vs. stages, inline-vs-isolated splits, dispatch/sequencing, capture-grep provenance, the shared-procedure references) stay exactly as they are.

**Success:**
- `grep`ing `README.md` for the old vocabulary — `> **Recommendation:**`, `> **Applied principle:**`, "one-line (greppable) header", "blockquote", "sub-block", "greppable on line 1" — surfaces no stale format claims.
- The `### Iterating milestone requirements` section prose and its Mermaid diagram accurately depict the XML-block flow (author → recommend/annotate → lift/answer) as shipped by the earlier tasks, with no blockquote/anchor vocabulary in the prose or the diagram edge labels.
- The XML-format vocabulary is present where the old format vocabulary was: the `<open-question>` block under `## Open questions`, `status="open"|"deferred"`, the `<recommendation option="...">` element with the "`<option>` — `<rationale>`" lift, sibling `<applied-principle>` elements, the boundary-line CLI idiom (never `xmllint`), and the query-where-it-pays convention.
- The per-skill reference entries for `review-milestone-requirements`, `recommend-all-open-questions`, `recommend-open-question`, `discuss-open-question`, `answer-open-question`, `answer-open-question-with-recommendation`, and `answer-all-open-questions-with-recommendation`, and the answer-principle-learning-loop **Recommendation advisory** bullet, describe the XML block / `<recommendation>` element rather than the blockquote header and its anchor, and each matches the corresponding updated `skills/`/`shared/`/`agents/` file.
- The overall workflow structure, the other phase Mermaid diagrams, and the non-format skill descriptions are unchanged in meaning; the rest of the README is untouched.

---
